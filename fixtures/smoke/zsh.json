{
	"id": "smoke_zsh",
	"description": "Smoke test: load zsh and parse a simple snippet",
	"category": "smoke",
	"tags": ["smoke"],
	"skip": {
		"languages": ["wasm"],
		"reason": "Grammar fails to load under the wasm32 dynamic-loading build; tracked as a follow-up"
	},
	"input": {
		"source_code": "echo hello",
		"config": {
			"language": "zsh"
		}
	},
	"assertions": [
		{
			"type": "not_error"
		}
	]
}
