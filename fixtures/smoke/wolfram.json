{
  "id": "smoke_wolfram",
  "description": "Smoke test: load wolfram and parse a simple snippet",
  "category": "smoke",
  "tags": ["smoke"],
  "skip": {
    "languages": [],
    "reason": "tree-sitter-wolfram triggers free() invalid next size (heap corruption) under glibc when parsing trivial input — tracked separately; smoke skipped to unblock CI"
  },
  "input": {
    "source_code": "x",
    "config": {
      "language": "wolfram"
    }
  },
  "assertions": [
    {
      "type": "not_error"
    }
  ]
}
