// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7519d04898f1e5e9a82cc2d44c4a5ecc28ac94d6496f41a1851565c19b43abad
// To regenerate: alef generate
// To verify freshness: alef verify

import { describe, expect, it } from "vitest";
import { process, WasmProcessConfig } from "@xberg-io/tree-sitter-language-pack-wasm";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
  const contentEncoding = response.headers.get("content-encoding");
  let bytes = new Uint8Array(await response.arrayBuffer());

  // The runtime's own fetch (undici on Node, and the browser equivalent) decodes
  // `content-encoding` transparently and leaves the header in place, so by the time the
  // body reaches here it is usually already plain text. Decoding unconditionally on the
  // header alone therefore fails ("incorrect header check") against a server that really
  // did compress. Decode only when the payload still carries the encoding: gzip announces
  // itself with the 0x1f8b magic pair; brotli has no magic, so it is attempted and the
  // already-decoded case falls back to the bytes as received.
  if (contentEncoding === "gzip" && bytes.length >= 2 && bytes[0] === 0x1f && bytes[1] === 0x8b) {
    const { gunzipSync } = await import("zlib");
    bytes = new Uint8Array(gunzipSync(bytes));
  } else if (contentEncoding === "br") {
    const { brotliDecompressSync } = await import("zlib");
    try {
      bytes = new Uint8Array(brotliDecompressSync(bytes));
    } catch {
      // Already decoded by the runtime.
    }
  }

  const text = new TextDecoder().decode(bytes);
  return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}

describe("process", () => {
  it("c_function_process: Intel: C function with include", () => {
    const result = process(
      '#include <stdio.h>\n\nint main() {\n    printf("hello");\n    return 0;\n}\n',
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "c";
        return _u0;
      })(),
    );
    expect(result.language).toBe("c");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(6);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("config_all_python: Intel: process with all features enabled", () => {
    const result = process(
      '# A comment\ndef greet(name):\n    """Say hello."""\n    return f\'Hi {name}\'\n\nimport os\n',
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(result.imports.length).toBeGreaterThanOrEqual(1);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(6);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("config_minimal_python: Intel: process with minimal config - only metrics", () => {
    const result = process(
      "def hello():\n    pass\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(2);
  }, 30000);

  it("go_function_process: Intel: extract structure from Go function definition", () => {
    const result = process(
      'package main\n\nimport "fmt"\n\nfunc main() {\n\tfmt.Println("hello")\n}\n',
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "go";
        return _u0;
      })(),
    );
    expect(result.language).toBe("go");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(result.imports.length).toBeGreaterThanOrEqual(1);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(7);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("go_function_process_detail: Intel: extract structure from Go function definition", () => {
    const result = process(
      'package main\n\nimport "fmt"\n\nfunc main() {\n\tfmt.Println("hello")\n}\n',
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "go";
        return _u0;
      })(),
    );
    expect(result.language).toBe("go");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(result.imports.length).toBeGreaterThanOrEqual(1);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(7);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("java_class_process: Intel: Java class with methods and imports", () => {
    const result = process(
      'import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return "Hello " + name;\n    }\n}\n',
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "java";
        return _u0;
      })(),
    );
    expect(result.language).toBe("java");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Class"))).toBe(true);
    expect(result.imports.length).toBeGreaterThanOrEqual(1);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(7);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("java_package_declaration_process: Intel: Java package declaration emitted as Module so callers can build FQNs (#112)", () => {
    const result = process(
      'package com.example.widget;\n\npublic class Widget {\n    public String name() { return "w"; }\n}\n',
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "java";
        return _u0;
      })(),
    );
    expect(result.language).toBe("java");
    expect(result.structure.length).toBeGreaterThanOrEqual(2);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Module"))).toBe(true);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Class"))).toBe(true);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("javascript_multi_import_process: Intel: detect multiple imports and function in JavaScript", () => {
    const result = process(
      "import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "javascript";
        return _u0;
      })(),
    );
    expect(result.language).toBe("javascript");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(result.imports.length).toBeGreaterThanOrEqual(2);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(6);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("javascript_multi_import_process_detail: Intel: detect multiple imports and function in JavaScript", () => {
    const result = process(
      "import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "javascript";
        return _u0;
      })(),
    );
    expect(result.language).toBe("javascript");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(result.imports.length).toBeGreaterThanOrEqual(2);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(6);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("kotlin_package_class_process: Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112)", () => {
    const result = process(
      'package foo.bar\n\nclass Widget {\n    fun greet(): String = "hi"\n}\n',
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "kotlin";
        return _u0;
      })(),
    );
    expect(result.language).toBe("kotlin");
    expect(result.structure.length).toBeGreaterThanOrEqual(2);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Module"))).toBe(true);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Class"))).toBe(true);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("process_javascript_exports_count: JavaScript with multiple exports, verify export count", () => {
    const result = process(
      "export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "javascript";
        return _u0;
      })(),
    );
    expect(result.language).toBe("javascript");
    expect(result.exports.length).toBeGreaterThanOrEqual(2);
  }, 30000);

  it("process_javascript_exports_detail: JavaScript with exports, verify export count", () => {
    const result = process(
      "export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "javascript";
        return _u0;
      })(),
    );
    expect(result.language).toBe("javascript");
    expect(result.exports.length).toBeGreaterThanOrEqual(1);
  }, 30000);

  it("process_python_all_features: Python comprehensive source with all feature extraction enabled", () => {
    const result = process(
      'import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    """Process a file and return contents."""\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n',
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.comments = true;
        _u0.docstrings = true;
        _u0.imports = true;
        _u0.language = "python";
        _u0.structure = true;
        _u0.symbols = true;
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.structure.length).toBeGreaterThanOrEqual(2);
    expect(result.imports.length).toBeGreaterThanOrEqual(1);
    expect(result.comments.length).toBeGreaterThanOrEqual(1);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(10);
  }, 30000);

  it("process_python_comments: Python with comments, verify comment count", () => {
    const result = process(
      "# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.comments = true;
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.comments.length).toBeGreaterThanOrEqual(1);
  }, 30000);

  it("process_python_docstrings: Python with function docstring, verify docstring count", () => {
    const result = process(
      'def greet(name):\n    """Say hello to someone."""\n    return f"Hello {name}"\n',
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.docstrings = true;
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(3);
  }, 30000);

  it("process_python_imports_detail: Python with multiple imports, verify imports contain specific source", () => {
    const result = process(
      "import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.imports.length).toBeGreaterThanOrEqual(2);
    expect((result.imports ?? []).some((e) => String(e.source).includes("os"))).toBe(true);
  }, 30000);

  it("process_python_metrics_detail: Python code with metrics assertions", () => {
    const result = process(
      "# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(Number(result.metrics.codeLines)).toBeGreaterThanOrEqual(4);
    expect(Number(result.metrics.commentLines)).toBeGreaterThanOrEqual(1);
    expect(Number(result.metrics.maxDepth)).toBeGreaterThanOrEqual(1);
  }, 30000);

  it("process_python_symbols: Python with class and functions, verify symbol count", () => {
    const result = process(
      "MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "python";
        _u0.symbols = true;
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.symbols.length).toBeGreaterThanOrEqual(1);
  }, 30000);

  it("process_rust_structure_name: Rust struct with name, verify structure name contains value", () => {
    const result = process(
      "pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "rust";
        return _u0;
      })(),
    );
    expect(result.language).toBe("rust");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect(result.structure.some((item) => _alefE2eItemTexts(item).some((text) => text.includes("MyConfig")))).toBe(
      true,
    );
  }, 30000);

  it("python_chunking_medium: Intel: Python code with medium chunk size", () => {
    const result = process(
      "def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.chunkMaxSize = 50;
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.structure.length).toBeGreaterThanOrEqual(3);
    expect(Number(result.metrics.errorCount)).toBe(0);
    expect(result.chunks.length).toBeGreaterThanOrEqual(2);
  }, 30000);

  it("python_chunking_process_detail: Intel: chunk multi-function Python source into multiple pieces", () => {
    const result = process(
      "def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.chunkMaxSize = 30;
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(8);
    expect(result.chunks.length).toBeGreaterThanOrEqual(2);
  }, 30000);

  it("python_class_with_methods_process: Intel: extract nested structure from Python class with methods", () => {
    const result = process(
      "class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Class"))).toBe(true);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(6);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("python_class_with_methods_process_detail: Intel: extract nested structure from Python class with methods", () => {
    const result = process(
      "class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Class"))).toBe(true);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(6);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("python_error_diagnostics: Intel: Python code with syntax errors should report diagnostics", () => {
    const result = process(
      "def broken(\n    pass\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.diagnostics = true;
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(Number(result.metrics.errorCount)).toBe(1);
    {
      const _v = result.diagnostics;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);

  it("python_function_process: Intel: extract structure from Python function definition", () => {
    const result = process(
      "def greet(name):\n    return f'Hello, {name}!'\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(2);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("python_function_process_detail: Intel: extract structure from Python function definition", () => {
    const result = process(
      "def greet(name):\n    return f'Hello, {name}!'\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(2);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("python_malformed_code_process: Intel: detect diagnostics in malformed Python code", () => {
    const result = process(
      "def broken(\n    return\nclass",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.diagnostics = true;
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    {
      const _v = result.diagnostics;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);

  it("python_malformed_code_process_detail: Intel: detect diagnostics in malformed Python code", () => {
    const result = process(
      "def broken(\n    return\nclass",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.diagnostics = true;
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    {
      const _v = result.diagnostics;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);

  it("python_multi_import_process: Intel: detect multiple Python imports", () => {
    const result = process(
      "import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect(result.imports.length).toBeGreaterThanOrEqual(3);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(5);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("python_multi_import_process_detail: Intel: detect multiple Python imports", () => {
    const result = process(
      "import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "python";
        return _u0;
      })(),
    );
    expect(result.language).toBe("python");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect(result.imports.length).toBeGreaterThanOrEqual(3);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(5);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("ruby_class_process: Intel: Ruby class with method", () => {
    const result = process(
      "require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello #{name}\"\n  end\nend\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "ruby";
        return _u0;
      })(),
    );
    expect(result.language).toBe("ruby");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Class"))).toBe(true);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(7);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("rust_chunking_process: Intel: chunk multi-function Rust source into pieces", () => {
    const result = process(
      "fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.chunkMaxSize = 30;
        _u0.language = "rust";
        return _u0;
      })(),
    );
    expect(result.language).toBe("rust");
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(7);
    expect(result.chunks.length).toBeGreaterThanOrEqual(2);
  }, 30000);

  it("rust_chunking_process_detail: Intel: chunk multi-function Rust source into pieces", () => {
    const result = process(
      "fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.chunkMaxSize = 30;
        _u0.language = "rust";
        return _u0;
      })(),
    );
    expect(result.language).toBe("rust");
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(7);
    expect(result.chunks.length).toBeGreaterThanOrEqual(2);
  }, 30000);

  it("rust_function_process: Intel: extract structure from Rust function definition", () => {
    const result = process(
      "fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "rust";
        return _u0;
      })(),
    );
    expect(result.language).toBe("rust");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(3);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("rust_function_process_detail: Intel: extract structure from Rust function definition", () => {
    const result = process(
      "fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "rust";
        return _u0;
      })(),
    );
    expect(result.language).toBe("rust");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(3);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("typescript_function_process: Intel: extract structure from TypeScript function", () => {
    const result = process(
      "import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "typescript";
        return _u0;
      })(),
    );
    expect(result.language).toBe("typescript");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(result.imports.length).toBeGreaterThanOrEqual(1);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(5);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);

  it("typescript_function_process_detail: Intel: extract structure from TypeScript function", () => {
    const result = process(
      "import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n",
      (() => {
        const _u0 = WasmProcessConfig.default();
        _u0.language = "typescript";
        return _u0;
      })(),
    );
    expect(result.language).toBe("typescript");
    expect(result.structure.length).toBeGreaterThanOrEqual(1);
    expect((result.structure ?? []).some((e) => String(e.kind).includes("Function"))).toBe(true);
    expect(result.imports.length).toBeGreaterThanOrEqual(1);
    expect(Number(result.metrics.totalLines)).toBeGreaterThanOrEqual(5);
    expect(Number(result.metrics.errorCount)).toBe(0);
  }, 30000);
});
