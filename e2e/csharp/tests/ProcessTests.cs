// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ecd0890f86b8a89a291e54a55fd52ead11121e6d4c3b020d48dcd019fecda501
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using TreeSitterLanguagePack;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: process.</summary>
public class ProcessTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public void Test_CFunctionProcess()
    {
        // Intel: C function with include
        var result = TreeSitterLanguagePackLib.Process("#include <stdio.h>\n\nint main() {\n    printf(\"hello\");\n    return 0;\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"c\"}", ConfigOptions)!);
        Assert.Equal("c", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Metrics.TotalLines >= 6, "expected >= 6");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_ConfigAllPython()
    {
        // Intel: process with all features enabled
        var result = TreeSitterLanguagePackLib.Process("# A comment\ndef greet(name):\n    \"\"\"Say hello.\"\"\"\n    return f'Hi {name}'\n\nimport os\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Imports.Count >= 1, "expected at least 1 elements");
        Assert.True(result.Metrics.TotalLines >= 6, "expected >= 6");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_ConfigMinimalPython()
    {
        // Intel: process with minimal config - only metrics
        var result = TreeSitterLanguagePackLib.Process("def hello():\n    pass\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Metrics.TotalLines >= 2, "expected >= 2");
    }

    [Fact]
    public void Test_GoFunctionProcess()
    {
        // Intel: extract structure from Go function definition
        var result = TreeSitterLanguagePackLib.Process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"go\"}", ConfigOptions)!);
        Assert.Equal("go", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Imports.Count >= 1, "expected at least 1 elements");
        Assert.True(result.Metrics.TotalLines >= 7, "expected >= 7");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_GoFunctionProcessDetail()
    {
        // Intel: extract structure from Go function definition
        var result = TreeSitterLanguagePackLib.Process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"go\"}", ConfigOptions)!);
        Assert.Equal("go", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Imports.Count >= 1, "expected at least 1 elements");
        Assert.True(result.Metrics.TotalLines >= 7, "expected >= 7");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_JavaClassProcess()
    {
        // Intel: Java class with methods and imports
        var result = TreeSitterLanguagePackLib.Process("import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return \"Hello \" + name;\n    }\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"java\"}", ConfigOptions)!);
        Assert.Equal("java", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("class", result.Structure.ToString().ToLower());
        Assert.True(result.Imports.Count >= 1, "expected at least 1 elements");
        Assert.True(result.Metrics.TotalLines >= 7, "expected >= 7");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_JavaPackageDeclarationProcess()
    {
        // Intel: Java package declaration emitted as Module so callers can build FQNs (#112)
        var result = TreeSitterLanguagePackLib.Process("package com.example.widget;\n\npublic class Widget {\n    public String name() { return \"w\"; }\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"java\"}", ConfigOptions)!);
        Assert.Equal("java", result.Language.Trim());
        Assert.True(result.Structure.Count >= 2, "expected at least 2 elements");
        Assert.Contains("module", result.Structure.ToString().ToLower());
        Assert.Contains("class", result.Structure.ToString().ToLower());
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_JavascriptMultiImportProcess()
    {
        // Intel: detect multiple imports and function in JavaScript
        var result = TreeSitterLanguagePackLib.Process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"javascript\"}", ConfigOptions)!);
        Assert.Equal("javascript", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Imports.Count >= 2, "expected at least 2 elements");
        Assert.True(result.Metrics.TotalLines >= 6, "expected >= 6");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_JavascriptMultiImportProcessDetail()
    {
        // Intel: detect multiple imports and function in JavaScript
        var result = TreeSitterLanguagePackLib.Process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"javascript\"}", ConfigOptions)!);
        Assert.Equal("javascript", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Imports.Count >= 2, "expected at least 2 elements");
        Assert.True(result.Metrics.TotalLines >= 6, "expected >= 6");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_KotlinPackageClassProcess()
    {
        // Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112)
        var result = TreeSitterLanguagePackLib.Process("package foo.bar\n\nclass Widget {\n    fun greet(): String = \"hi\"\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"kotlin\"}", ConfigOptions)!);
        Assert.Equal("kotlin", result.Language.Trim());
        Assert.True(result.Structure.Count >= 2, "expected at least 2 elements");
        Assert.Contains("module", result.Structure.ToString().ToLower());
        Assert.Contains("class", result.Structure.ToString().ToLower());
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_ProcessJavascriptExportsCount()
    {
        // JavaScript with multiple exports, verify export count
        var result = TreeSitterLanguagePackLib.Process("export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"javascript\"}", ConfigOptions)!);
        Assert.Equal("javascript", result.Language.Trim());
        Assert.True(result.Exports.Count >= 2, "expected at least 2 elements");
    }

    [Fact]
    public void Test_ProcessJavascriptExportsDetail()
    {
        // JavaScript with exports, verify export count
        var result = TreeSitterLanguagePackLib.Process("export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"javascript\"}", ConfigOptions)!);
        Assert.Equal("javascript", result.Language.Trim());
        Assert.True(result.Exports.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public void Test_ProcessPythonAllFeatures()
    {
        // Python comprehensive source with all feature extraction enabled
        var result = TreeSitterLanguagePackLib.Process("import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", JsonSerializer.Deserialize<ProcessConfig>("{\"comments\":true,\"docstrings\":true,\"imports\":true,\"language\":\"python\",\"structure\":true,\"symbols\":true}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Structure.Count >= 2, "expected at least 2 elements");
        Assert.True(result.Imports.Count >= 1, "expected at least 1 elements");
        Assert.True(result.Comments.Count >= 1, "expected at least 1 elements");
        Assert.True(result.Metrics.TotalLines >= 10, "expected >= 10");
    }

    [Fact]
    public void Test_ProcessPythonComments()
    {
        // Python with comments, verify comment count
        var result = TreeSitterLanguagePackLib.Process("# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", JsonSerializer.Deserialize<ProcessConfig>("{\"comments\":true,\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Comments.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public void Test_ProcessPythonDocstrings()
    {
        // Python with function docstring, verify docstring count
        var result = TreeSitterLanguagePackLib.Process("def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", JsonSerializer.Deserialize<ProcessConfig>("{\"docstrings\":true,\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Metrics.TotalLines >= 3, "expected >= 3");
    }

    [Fact]
    public void Test_ProcessPythonImportsDetail()
    {
        // Python with multiple imports, verify imports contain specific source
        var result = TreeSitterLanguagePackLib.Process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Imports.Count >= 2, "expected at least 2 elements");
        Assert.Contains("os", result.Imports.ToString().ToLower());
    }

    [Fact]
    public void Test_ProcessPythonMetricsDetail()
    {
        // Python code with metrics assertions
        var result = TreeSitterLanguagePackLib.Process("# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Metrics.CodeLines >= 4, "expected >= 4");
        Assert.True(result.Metrics.CommentLines >= 1, "expected >= 1");
        Assert.True(result.Metrics.MaxDepth >= 1, "expected >= 1");
    }

    [Fact]
    public void Test_ProcessPythonSymbols()
    {
        // Python with class and functions, verify symbol count
        var result = TreeSitterLanguagePackLib.Process("MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"python\",\"symbols\":true}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Symbols.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public void Test_ProcessRustStructureName()
    {
        // Rust struct with name, verify structure name contains value
        var result = TreeSitterLanguagePackLib.Process("pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"rust\"}", ConfigOptions)!);
        Assert.Equal("rust", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("myconfig", result.Structure.ToString().ToLower());
    }

    [Fact]
    public void Test_PythonChunkingMedium()
    {
        // Intel: Python code with medium chunk size
        var result = TreeSitterLanguagePackLib.Process("def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n", JsonSerializer.Deserialize<ProcessConfig>("{\"chunk_max_size\":50,\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Structure.Count >= 3, "expected at least 3 elements");
        Assert.True(result.Metrics.ErrorCount == 0);
        Assert.True(result.Chunks.Count >= 2, "expected at least 2 elements");
    }

    [Fact]
    public void Test_PythonChunkingProcessDetail()
    {
        // Intel: chunk multi-function Python source into multiple pieces
        var result = TreeSitterLanguagePackLib.Process("def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n", JsonSerializer.Deserialize<ProcessConfig>("{\"chunk_max_size\":30,\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Metrics.TotalLines >= 8, "expected >= 8");
        Assert.True(result.Chunks.Count >= 2, "expected at least 2 elements");
    }

    [Fact]
    public void Test_PythonClassWithMethodsProcess()
    {
        // Intel: extract nested structure from Python class with methods
        var result = TreeSitterLanguagePackLib.Process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("class", result.Structure.ToString().ToLower());
        Assert.True(result.Metrics.TotalLines >= 6, "expected >= 6");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_PythonClassWithMethodsProcessDetail()
    {
        // Intel: extract nested structure from Python class with methods
        var result = TreeSitterLanguagePackLib.Process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("class", result.Structure.ToString().ToLower());
        Assert.True(result.Metrics.TotalLines >= 6, "expected >= 6");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_PythonErrorDiagnostics()
    {
        // Intel: Python code with syntax errors should report diagnostics
        var result = TreeSitterLanguagePackLib.Process("def broken(\n    pass\n", JsonSerializer.Deserialize<ProcessConfig>("{\"diagnostics\":true,\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Metrics.ErrorCount == 1);
        Assert.False(string.IsNullOrEmpty(result.Diagnostics?.ToString()));
    }

    [Fact]
    public void Test_PythonFunctionProcess()
    {
        // Intel: extract structure from Python function definition
        var result = TreeSitterLanguagePackLib.Process("def greet(name):\n    return f'Hello, {name}!'\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Metrics.TotalLines >= 2, "expected >= 2");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_PythonFunctionProcessDetail()
    {
        // Intel: extract structure from Python function definition
        var result = TreeSitterLanguagePackLib.Process("def greet(name):\n    return f'Hello, {name}!'\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Metrics.TotalLines >= 2, "expected >= 2");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_PythonMalformedCodeProcess()
    {
        // Intel: detect diagnostics in malformed Python code
        var result = TreeSitterLanguagePackLib.Process("def broken(\n    return\nclass", JsonSerializer.Deserialize<ProcessConfig>("{\"diagnostics\":true,\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.False(string.IsNullOrEmpty(result.Diagnostics?.ToString()));
    }

    [Fact]
    public void Test_PythonMalformedCodeProcessDetail()
    {
        // Intel: detect diagnostics in malformed Python code
        var result = TreeSitterLanguagePackLib.Process("def broken(\n    return\nclass", JsonSerializer.Deserialize<ProcessConfig>("{\"diagnostics\":true,\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.False(string.IsNullOrEmpty(result.Diagnostics?.ToString()));
    }

    [Fact]
    public void Test_PythonMultiImportProcess()
    {
        // Intel: detect multiple Python imports
        var result = TreeSitterLanguagePackLib.Process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.True(result.Imports.Count >= 3, "expected at least 3 elements");
        Assert.True(result.Metrics.TotalLines >= 5, "expected >= 5");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_PythonMultiImportProcessDetail()
    {
        // Intel: detect multiple Python imports
        var result = TreeSitterLanguagePackLib.Process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"python\"}", ConfigOptions)!);
        Assert.Equal("python", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.True(result.Imports.Count >= 3, "expected at least 3 elements");
        Assert.True(result.Metrics.TotalLines >= 5, "expected >= 5");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_RubyClassProcess()
    {
        // Intel: Ruby class with method
        var result = TreeSitterLanguagePackLib.Process("require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello #{name}\"\n  end\nend\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"ruby\"}", ConfigOptions)!);
        Assert.Equal("ruby", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("class", result.Structure.ToString().ToLower());
        Assert.True(result.Metrics.TotalLines >= 7, "expected >= 7");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_RustChunkingProcess()
    {
        // Intel: chunk multi-function Rust source into pieces
        var result = TreeSitterLanguagePackLib.Process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"chunk_max_size\":30,\"language\":\"rust\"}", ConfigOptions)!);
        Assert.Equal("rust", result.Language.Trim());
        Assert.True(result.Metrics.TotalLines >= 7, "expected >= 7");
        Assert.True(result.Chunks.Count >= 2, "expected at least 2 elements");
    }

    [Fact]
    public void Test_RustChunkingProcessDetail()
    {
        // Intel: chunk multi-function Rust source into pieces
        var result = TreeSitterLanguagePackLib.Process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"chunk_max_size\":30,\"language\":\"rust\"}", ConfigOptions)!);
        Assert.Equal("rust", result.Language.Trim());
        Assert.True(result.Metrics.TotalLines >= 7, "expected >= 7");
        Assert.True(result.Chunks.Count >= 2, "expected at least 2 elements");
    }

    [Fact]
    public void Test_RustFunctionProcess()
    {
        // Intel: extract structure from Rust function definition
        var result = TreeSitterLanguagePackLib.Process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"rust\"}", ConfigOptions)!);
        Assert.Equal("rust", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Metrics.TotalLines >= 3, "expected >= 3");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_RustFunctionProcessDetail()
    {
        // Intel: extract structure from Rust function definition
        var result = TreeSitterLanguagePackLib.Process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"rust\"}", ConfigOptions)!);
        Assert.Equal("rust", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Metrics.TotalLines >= 3, "expected >= 3");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_TypescriptFunctionProcess()
    {
        // Intel: extract structure from TypeScript function
        var result = TreeSitterLanguagePackLib.Process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"typescript\"}", ConfigOptions)!);
        Assert.Equal("typescript", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Imports.Count >= 1, "expected at least 1 elements");
        Assert.True(result.Metrics.TotalLines >= 5, "expected >= 5");
        Assert.True(result.Metrics.ErrorCount == 0);
    }

    [Fact]
    public void Test_TypescriptFunctionProcessDetail()
    {
        // Intel: extract structure from TypeScript function
        var result = TreeSitterLanguagePackLib.Process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", JsonSerializer.Deserialize<ProcessConfig>("{\"language\":\"typescript\"}", ConfigOptions)!);
        Assert.Equal("typescript", result.Language.Trim());
        Assert.True(result.Structure.Count >= 1, "expected at least 1 elements");
        Assert.Contains("function", result.Structure.ToString().ToLower());
        Assert.True(result.Imports.Count >= 1, "expected at least 1 elements");
        Assert.True(result.Metrics.TotalLines >= 5, "expected >= 5");
        Assert.True(result.Metrics.ErrorCount == 0);
    }
}
