// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:58b4212d8c469c379a5d143d6adda861ef5e30f5ba8f9db19ecd2d43502b3206
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.tslp.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.tslp.android.TreeSitterLanguagePack

/** E2e tests for category: language-detection. */
class LanguageDetectionTest {
    @Test
    fun testDetectContentBashShebang() = runBlocking {
        // detect_language_from_content recognizes #!/bin/bash shebang
        val result = TreeSitterLanguagePack.detectLanguageFromContent("#!/bin/bash\necho hi")
        assertEquals("bash", result.trim())
    }

    @Test
    fun testDetectContentNoShebang() = runBlocking {
        // detect_language_from_content returns None when no shebang present
        val result = TreeSitterLanguagePack.detectLanguageFromContent("no shebang here")
        assertTrue(result == null, "expected empty value")
    }

    @Test
    fun testDetectContentPythonShebang() = runBlocking {
        // detect_language_from_content recognizes #!/usr/bin/env python3 shebang
        val result = TreeSitterLanguagePack.detectLanguageFromContent("#!/usr/bin/env python3\npass")
        assertEquals("python", result.trim())
    }

    @Test
    fun testDetectExtCpp() = runBlocking {
        // detect_language_from_extension recognizes .cpp as cpp
        val result = TreeSitterLanguagePack.detectLanguageFromExtension("cpp")
        assertEquals("cpp", result.trim())
    }

    @Test
    fun testDetectExtGo() = runBlocking {
        // detect_language_from_extension recognizes .go as go
        val result = TreeSitterLanguagePack.detectLanguageFromExtension("go")
        assertEquals("go", result.trim())
    }

    @Test
    fun testDetectExtJava() = runBlocking {
        // detect_language_from_extension recognizes .java as java
        val result = TreeSitterLanguagePack.detectLanguageFromExtension("java")
        assertEquals("java", result.trim())
    }

    @Test
    fun testDetectExtJavascript() = runBlocking {
        // detect_language_from_extension recognizes .js as javascript
        val result = TreeSitterLanguagePack.detectLanguageFromExtension("js")
        assertEquals("javascript", result.trim())
    }

    @Test
    fun testDetectExtPhp() = runBlocking {
        // detect_language_from_extension recognizes .php as php
        val result = TreeSitterLanguagePack.detectLanguageFromExtension("php")
        assertEquals("php", result.trim())
    }

    @Test
    fun testDetectExtPython() = runBlocking {
        // detect_language_from_extension recognizes .py as python
        val result = TreeSitterLanguagePack.detectLanguageFromExtension("py")
        assertEquals("python", result.trim())
    }

    @Test
    fun testDetectExtRuby() = runBlocking {
        // detect_language_from_extension recognizes .rb as ruby
        val result = TreeSitterLanguagePack.detectLanguageFromExtension("rb")
        assertEquals("ruby", result.trim())
    }

    @Test
    fun testDetectExtRust() = runBlocking {
        // detect_language_from_extension recognizes .rs as rust
        val result = TreeSitterLanguagePack.detectLanguageFromExtension("rs")
        assertEquals("rust", result.trim())
    }

    @Test
    fun testDetectExtTypescript() = runBlocking {
        // detect_language_from_extension recognizes .ts as typescript
        val result = TreeSitterLanguagePack.detectLanguageFromExtension("ts")
        assertEquals("typescript", result.trim())
    }

    @Test
    fun testDetectExtUnknown() = runBlocking {
        // detect_language_from_extension returns None for unknown extension .xyz
        val result = TreeSitterLanguagePack.detectLanguageFromExtension("xyz")
        assertTrue(result == null, "expected empty value")
    }

    @Test
    fun testDetectPathDotfile() = runBlocking {
        // detect_language_from_path returns None for dotfile .gitignore (no standard extension)
        val result = TreeSitterLanguagePack.detectLanguageFromPath(".gitignore")
        assertTrue(result == null, "expected empty value")
    }

    @Test
    fun testDetectPathGoNested() = runBlocking {
        // detect_language_from_path extracts extension from nested path lib/server.go
        val result = TreeSitterLanguagePack.detectLanguageFromPath("lib/server.go")
        assertEquals("go", result.trim())
    }

    @Test
    fun testDetectPathJavaRoot() = runBlocking {
        // detect_language_from_path recognizes Main.java in root directory
        val result = TreeSitterLanguagePack.detectLanguageFromPath("Main.java")
        assertEquals("java", result.trim())
    }

    @Test
    fun testDetectPathJsRoot() = runBlocking {
        // detect_language_from_path recognizes app.js in root directory
        val result = TreeSitterLanguagePack.detectLanguageFromPath("app.js")
        assertEquals("javascript", result.trim())
    }

    @Test
    fun testDetectPathNested() = runBlocking {
        // detect_language_from_path extracts extension from nested path src/main.py
        val result = TreeSitterLanguagePack.detectLanguageFromPath("src/main.py")
        assertEquals("python", result.trim())
    }

    @Test
    fun testDetectPathNoExtension() = runBlocking {
        // detect_language_from_path returns None for extensionless file Makefile
        val result = TreeSitterLanguagePack.detectLanguageFromPath("Makefile")
        assertTrue(result == null, "expected empty value")
    }

    @Test
    fun testDetectPathRustSrc() = runBlocking {
        // detect_language_from_path extracts extension from nested path src/main.rs
        val result = TreeSitterLanguagePack.detectLanguageFromPath("src/main.rs")
        assertEquals("rust", result.trim())
    }

}
