// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:77e026bd773d6d9f86385b7ed6896453a6577be5718b969522de9f9f0a69a0a2
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.tslp.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.tslp.android.TreeSitterLanguagePack
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import dev.kreuzberg.tslp.android.PackConfig

/** E2e tests for category: download. */
class DownloadTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testDownloadCacheDir() = runBlocking {
        // Get the cache directory path and verify it is non-empty
        val result = TreeSitterLanguagePack.cacheDir()
        assertFalse(result.isEmpty(), "expected non-empty value")
    }

    @Test
    fun testDownloadCleanCache() = runBlocking {
        // Clean the parser cache directory
        val result = TreeSitterLanguagePack.cleanCache()
    }

    @Test
    fun testDownloadConfigureCustomDir() = runBlocking {
        // Configure language pack with a custom cache directory
        val config = MAPPER.readValue("{\"cache_dir\":\"/tmp/tslp_test_cache\"}", PackConfig::class.java)
        val result = TreeSitterLanguagePack.configure(config)
    }

    @Test
    fun testDownloadDownloadedEmpty() = runBlocking {
        // List downloaded languages (may be empty before any downloads)
        val result = TreeSitterLanguagePack.downloadedLanguages()
    }

    @Test
    fun testDownloadEmptyList() = runBlocking {
        // download([]) is a no-op that returns count 0
        val result = TreeSitterLanguagePack.download(listOf())
        assertEquals(0, result)
    }

    @Test
    fun testDownloadInitDefault() = runBlocking {
        // Initialize language pack with default config
        val config = MAPPER.readValue("{}", PackConfig::class.java)
        val result = TreeSitterLanguagePack.init(config)
    }

    @Test
    fun testDownloadInvalidLanguage() = runBlocking {
        // download with an unknown language name returns an error
        assertFailsWith<Exception> {
            TreeSitterLanguagePack.download(listOf("zzz_definitely_not_a_real_language_xyz"))
        }
        Unit
    }

    @Test
    fun testDownloadManifestLanguages() = runBlocking {
        // List manifest languages and verify the full downloadable catalog
        val result = TreeSitterLanguagePack.manifestLanguages()
        assertFalse(result.isEmpty(), "expected non-empty value")
        assertTrue(result.size >= 300, "expected at least 300 elements")
        assertTrue(result.contains("python"), "expected to contain: " + "python")
    }

    @Test
    fun testDownloadMultipleLanguages() = runBlocking {
        // download(['python', 'rust']) returns count >= 2
        val result = TreeSitterLanguagePack.download(listOf("python", "rust"))
        assertTrue(result >= 2, "expected >= 2")
    }

    @Test
    fun testDownloadSingleLanguage() = runBlocking {
        // Download a single language parser and verify count
        val result = TreeSitterLanguagePack.download(listOf("python"))
        assertTrue(result >= 1, "expected >= 1")
    }

}
