// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1114525a77b156f61207c42a565b7d7dd4a9ddedbff12b207f0a123df8f4a53a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it, beforeAll, afterAll } from "vitest";
import {
  process,
  cacheDir,
  cleanCache,
  configure,
  downloadedLanguages,
  download,
  init,
  manifestLanguages,
} from "@kreuzberg/tree-sitter-language-pack";
import { mkdtempSync, rmSync } from "node:fs";
import { join } from "node:path";
import { tmpdir } from "node:os";
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("download", () => {
  let _tslpTestCacheDir: string;

  beforeAll(() => {
    _tslpTestCacheDir = mkdtempSync(join(tmpdir(), "tslp-e2e-"));
    configure({ cacheDir: _tslpTestCacheDir });
  });

  afterAll(() => {
    configure({});
    try {
      rmSync(_tslpTestCacheDir, { recursive: true, force: true });
    } catch (_e) {}
  });
  it("download_cache_dir: Get the cache directory path and verify it is non-empty", () => {
    const result = cacheDir();
    {
      const _v = result;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  });
  it("download_clean_cache: Clean the parser cache directory", () => {
    cleanCache();
  });
  it("download_configure_custom_dir: Configure language pack with a custom cache directory", () => {
    configure({ cacheDir: "/tmp/tslp_test_cache" });
  });
  it("download_downloaded_empty: List downloaded languages (may be empty before any downloads)", () => {
    downloadedLanguages();
  });
  it("download_empty_list: download([]) is a no-op that returns count 0", () => {
    const result = download([]);
    expect(result).toBe(0);
  });
  it("download_init_default: Initialize language pack with default config", () => {
    init({});
  });
  it("download_invalid_language: download with an unknown language name returns an error", async () => {
    await expect(async () => {
      await download(["zzz_definitely_not_a_real_language_xyz"]);
    }).rejects.toThrow();
  });
  it("download_manifest_languages: List manifest languages and verify the full downloadable catalog", () => {
    const result = manifestLanguages();
    {
      const _v = result;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
    expect(result.length).toBeGreaterThanOrEqual(300);
    expect(result).toContain("python");
  });
  it("download_multiple_languages: download(['python', 'rust']) returns count >= 2", () => {
    const result = download(["python", "rust"]);
    expect(result).toBeGreaterThanOrEqual(2);
  });
  it("download_single_language: Download a single language parser and verify count", () => {
    const result = download(["python"]);
    expect(result).toBeGreaterThanOrEqual(1);
  });
});
