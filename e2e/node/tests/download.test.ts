// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ef91ff691f0ff459c7963b21c6d54649436a667cfd774699a080e4958f950c99
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { process, downloadAll, cacheDir, cleanCache, configure, downloadedLanguages, init, manifestLanguages, download } from '@kreuzberg/tree-sitter-language-pack';

describe('download', () => {
  it('download_all_languages: Download all language parsers and verify count is positive', () => {
    const result = downloadAll();
    expect(result).toBeGreaterThan(0);
  });

  it('download_cache_dir: Get the cache directory path and verify it is non-empty', () => {
    const result = cacheDir();
    expect(result.length).toBeGreaterThan(0);
  });

  it('download_clean_cache: Clean the parser cache directory', () => {
    cleanCache();
  });

  it('download_configure_custom_dir: Configure language pack with a custom cache directory', () => {
    configure({ cacheDir: "/tmp/tslp_test_cache" });
  });

  it('download_downloaded_empty: List downloaded languages (may be empty before any downloads)', () => {
    downloadedLanguages();
  });

  it('download_init_default: Initialize language pack with default config', () => {
    init({  });
  });

  it('download_manifest_languages: List manifest languages and verify python is included', () => {
    const result = manifestLanguages();
    expect(result.length).toBeGreaterThan(0);
    expect(result).toContain("python");
  });

  it('download_single_language: Download a single language parser and verify count', () => {
    const result = download(["python"]);
    expect(result).toBeGreaterThanOrEqual(1);
  });
});
