// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bad196f3dbc6af1f1e5dee175395af66c678a349268bfaf2d51092bd222f24ee
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import TreeSitterLanguagePack
import RustBridge

/// E2e tests for category: registry.
final class RegistryTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testGetLanguagePython() throws {
        // get_language('python') returns a valid Language object
        let language = try TreeSitterLanguagePack.getLanguage(name: "python")
    }

    func testGetLanguageUnknown() throws {
        // get_language('nonexistent_xyz') returns an error for unknown language
        do {
            _ = try TreeSitterLanguagePack.getLanguage(name: "nonexistent_xyz")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testGetParserPython() throws {
        // get_parser('python') returns a valid Parser object
        let parser = try TreeSitterLanguagePack.getParser(name: "python")
    }

    func testGetParserUnknown() throws {
        // get_parser('nonexistent_xyz') returns an error for unknown language
        do {
            _ = try TreeSitterLanguagePack.getParser(name: "nonexistent_xyz")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testRegistryGetLanguageAlias() throws {
        // get_language('shell') resolves the alias to 'bash' and returns ok
        let language = try TreeSitterLanguagePack.getLanguage(name: "shell")
    }

    func testRegistryGetParserAlias() throws {
        // get_parser('shell') resolves the alias and returns a Parser
        let parser = try TreeSitterLanguagePack.getParser(name: "shell")
    }

    func testRegistryHasLanguageAlias() throws {
        // has_language('shell') returns true via alias resolution to 'bash'
        let result = try TreeSitterLanguagePack.hasLanguage(name: "shell")
        XCTAssertTrue(result)
    }

    func testRegistryHasLanguageFalse() throws {
        // has_language('nonexistent') should return false
        let result = try TreeSitterLanguagePack.hasLanguage(name: "nonexistent")
        XCTAssertFalse(result)
    }

    func testRegistryHasLanguageTrue() throws {
        // has_language('python') should return true
        let result = try TreeSitterLanguagePack.hasLanguage(name: "python")
        XCTAssertTrue(result)
    }

    func testRegistryLanguageCount() throws {
        // language_count returns the number of languages available in the current build/cache
        let result = try TreeSitterLanguagePack.languageCount()
        XCTAssertGreaterThanOrEqual(result, 1)
    }

    func testRegistryListLanguages() throws {
        // available_languages should return a non-empty list
        let result = try TreeSitterLanguagePack.availableLanguages()
        XCTAssertFalse(result.isEmpty, "expected non-empty value")
    }

}
