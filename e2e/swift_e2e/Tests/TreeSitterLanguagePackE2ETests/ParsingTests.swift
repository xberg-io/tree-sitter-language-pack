// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import TreeSitterLanguagePack
import RustBridge

/// E2e tests for category: parsing.
final class ParsingTests: XCTestCase {
    func testParsingGoFunction() throws {
        // Parse a Go function declaration and assert node type
        let configObj = try processConfigFromJson("{\"language\":\"go\"}")
        let result = try process("package main\nfunc main() {}", configObj)
    }

    func testParsingHtmlElement() throws {
        // Parse an HTML element and assert node type
        let configObj = try processConfigFromJson("{\"language\":\"html\"}")
        let result = try process("<div>hello</div>", configObj)
    }

    func testParsingJavascriptClass() throws {
        // Parse a JavaScript class declaration.
        let configObj = try processConfigFromJson("{\"language\":\"javascript\"}")
        let result = try process("class Foo { bar() {} }", configObj)
    }

    func testParsingJavascriptVariable() throws {
        // Parse a JavaScript variable declaration and assert node type
        let configObj = try processConfigFromJson("{\"language\":\"javascript\"}")
        let result = try process("const x = 1;", configObj)
    }

    func testParsingPythonFunction() throws {
        // Parse a Python function definition and assert node type
        let configObj = try processConfigFromJson("{\"language\":\"python\"}")
        let result = try process("def hello(): pass", configObj)
    }

    func testParsingRustFunction() throws {
        // Parse a Rust function definition and assert node type
        let configObj = try processConfigFromJson("{\"language\":\"rust\"}")
        let result = try process("fn main() {}", configObj)
    }

    func testParsingRustStruct() throws {
        // Parse a Rust struct definition.
        let configObj = try processConfigFromJson("{\"language\":\"rust\"}")
        let result = try process("struct Point { x: f64, y: f64 }", configObj)
    }

    func testParsingTypescriptFunction() throws {
        // Parse a TypeScript function with type annotations and template literal return
        let configObj = try processConfigFromJson("{\"language\":\"typescript\"}")
        let result = try process("function greet(name: string): string { return `hi ${name}`; }", configObj)
    }

}
