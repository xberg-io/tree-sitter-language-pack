// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import TreeSitterLanguagePack
import RustBridge

/// E2e tests for category: smoke.
final class SmokeTests: XCTestCase {
    func testSmokeAbl() throws {
        // Smoke test: load abl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"abl\"}")
        let result = try process("x", configObj)
    }

    func testSmokeActionscript() throws {
        // Smoke test: load actionscript and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"actionscript\"}")
        let result = try process("var x:int = 1;", configObj)
    }

    func testSmokeAda() throws {
        // Smoke test: load ada and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ada\"}")
        let result = try process("procedure Main is begin null; end Main;", configObj)
    }

    func testSmokeAgda() throws {
        // Smoke test: load agda and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"agda\"}")
        let result = try process("module Main where", configObj)
    }

    func testSmokeAl() throws {
        // Smoke test: load al and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"al\"}")
        let result = try process("x", configObj)
    }

    func testSmokeAngular() throws {
        // Smoke test: load angular and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"angular\"}")
        let result = try process("x", configObj)
    }

    func testSmokeApex() throws {
        // Smoke test: load apex and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"apex\"}")
        let result = try process("public class Main {}", configObj)
    }

    func testSmokeArduino() throws {
        // Smoke test: load arduino and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"arduino\"}")
        let result = try process("void setup() {}", configObj)
    }

    func testSmokeAsciidoc() throws {
        // Smoke test: load asciidoc and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"asciidoc\"}")
        let result = try process("= Title\n\nParagraph.", configObj)
    }

    func testSmokeAsm() throws {
        // Smoke test: load asm and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"asm\"}")
        let result = try process("mov eax, 1", configObj)
    }

    func testSmokeAstro() throws {
        // Smoke test: load astro and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"astro\"}")
        let result = try process("---\n---\n<p>hello</p>", configObj)
    }

    func testSmokeAwk() throws {
        // Smoke test: load awk and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"awk\"}")
        let result = try process("BEGIN { print \"hello\" }", configObj)
    }

    func testSmokeBash() throws {
        // Smoke test: load bash and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"bash\"}")
        let result = try process("echo hello", configObj)
    }

    func testSmokeBass() throws {
        // Smoke test: load bass and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"bass\"}")
        let result = try process("x", configObj)
    }

    func testSmokeBatch() throws {
        // Smoke test: load batch and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"batch\"}")
        let result = try process("@echo off\necho hello", configObj)
    }

    func testSmokeBeancount() throws {
        // Smoke test: load beancount and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"beancount\"}")
        let result = try process("2024-01-01 open Assets:Bank USD", configObj)
    }

    func testSmokeBibtex() throws {
        // Smoke test: load bibtex and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"bibtex\"}")
        let result = try process("@article{key, title={A}}", configObj)
    }

    func testSmokeBicep() throws {
        // Smoke test: load bicep and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"bicep\"}")
        let result = try process("param name string", configObj)
    }

    func testSmokeBitbake() throws {
        // Smoke test: load bitbake and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"bitbake\"}")
        let result = try process("DESCRIPTION = \"hello\"", configObj)
    }

    func testSmokeBlade() throws {
        // Smoke test: load blade and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"blade\"}")
        let result = try process("x", configObj)
    }

    func testSmokeBrightscript() throws {
        // Smoke test: load brightscript and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"brightscript\"}")
        let result = try process("x", configObj)
    }

    func testSmokeBsl() throws {
        // Smoke test: load bsl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"bsl\"}")
        let result = try process("Procedure Main() EndProcedure", configObj)
    }

    func testSmokeC() throws {
        // Smoke test: load c and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"c\"}")
        let result = try process("int main() { return 0; }", configObj)
    }

    func testSmokeC3() throws {
        // Smoke test: load c3 and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"c3\"}")
        let result = try process("x", configObj)
    }

    func testSmokeCaddy() throws {
        // Smoke test: load caddy and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"caddy\"}")
        let result = try process(":8080 {\n\trespond \"Hello\"\n}", configObj)
    }

    func testSmokeCairo() throws {
        // Smoke test: load cairo and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cairo\"}")
        let result = try process("fn main() {}", configObj)
    }

    func testSmokeCapnp() throws {
        // Smoke test: load capnp and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"capnp\"}")
        let result = try process("@0xabcdef1234567890;", configObj)
    }

    func testSmokeCedar() throws {
        // Smoke test: load cedar and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cedar\"}")
        let result = try process("permit(principal, action, resource);", configObj)
    }

    func testSmokeCedarschema() throws {
        // Smoke test: load cedarschema and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cedarschema\"}")
        let result = try process("entity User;", configObj)
    }

    func testSmokeCel() throws {
        // Smoke test: load cel and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cel\"}")
        let result = try process("x", configObj)
    }

    func testSmokeCfml() throws {
        // Smoke test: load cfml and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cfml\"}")
        let result = try process("x", configObj)
    }

    func testSmokeChatito() throws {
        // Smoke test: load chatito and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"chatito\"}")
        let result = try process("%[greeting]\n    hello", configObj)
    }

    func testSmokeChuck() throws {
        // Smoke test: load chuck and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"chuck\"}")
        let result = try process("x", configObj)
    }

    func testSmokeCircom() throws {
        // Smoke test: load circom and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"circom\"}")
        let result = try process("x", configObj)
    }

    func testSmokeClarity() throws {
        // Smoke test: load clarity and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"clarity\"}")
        let result = try process("(define-public (hello) (ok true))", configObj)
    }

    func testSmokeClojure() throws {
        // Smoke test: load clojure and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"clojure\"}")
        let result = try process("(def x 1)", configObj)
    }

    func testSmokeCmake() throws {
        // Smoke test: load cmake and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cmake\"}")
        let result = try process("cmake_minimum_required(VERSION 3.0)", configObj)
    }

    func testSmokeCobol() throws {
        // Smoke test: load cobol and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cobol\"}")
        let result = try process("       IDENTIFICATION DIVISION.\n       PROGRAM-ID. HELLO.", configObj)
    }

    func testSmokeComment() throws {
        // Smoke test: load comment and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"comment\"}")
        let result = try process("TODO: fix this", configObj)
    }

    func testSmokeCommonlisp() throws {
        // Smoke test: load commonlisp and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"commonlisp\"}")
        let result = try process("(defun hello () (print \"hello\"))", configObj)
    }

    func testSmokeCooklang() throws {
        // Smoke test: load cooklang and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cooklang\"}")
        let result = try process("x", configObj)
    }

    func testSmokeCorn() throws {
        // Smoke test: load corn and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"corn\"}")
        let result = try process("{ key = \"value\" }", configObj)
    }

    func testSmokeCpon() throws {
        // Smoke test: load cpon and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cpon\"}")
        let result = try process("{\"key\": 1}", configObj)
    }

    func testSmokeCpp() throws {
        // Smoke test: load cpp and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cpp\"}")
        let result = try process("int main() { return 0; }", configObj)
    }

    func testSmokeCrystal() throws {
        // Smoke test: load crystal and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"crystal\"}")
        let result = try process("x", configObj)
    }

    func testSmokeCsharp() throws {
        // Smoke test: load csharp and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"csharp\"}")
        let result = try process("class Main {}", configObj)
    }

    func testSmokeCss() throws {
        // Smoke test: load css and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"css\"}")
        let result = try process("body { color: red; }", configObj)
    }

    func testSmokeCst() throws {
        // Smoke test: load cst and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cst\"}")
        let result = try process("x", configObj)
    }

    func testSmokeCsv() throws {
        // Smoke test: load csv and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"csv\"}")
        let result = try process("a,b,c\n1,2,3", configObj)
    }

    func testSmokeCuda() throws {
        // Smoke test: load cuda and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cuda\"}")
        let result = try process("__global__ void kernel() {}", configObj)
    }

    func testSmokeCue() throws {
        // Smoke test: load cue and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cue\"}")
        let result = try process("x", configObj)
    }

    func testSmokeCylc() throws {
        // Smoke test: load cylc and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"cylc\"}")
        let result = try process("x", configObj)
    }

    func testSmokeD() throws {
        // Smoke test: load d and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"d\"}")
        let result = try process("void main() {}", configObj)
    }

    func testSmokeDart() throws {
        // Smoke test: load dart and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"dart\"}")
        let result = try process("void main() {}", configObj)
    }

    func testSmokeDesktop() throws {
        // Smoke test: load desktop and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"desktop\"}")
        let result = try process("x", configObj)
    }

    func testSmokeDevicetree() throws {
        // Smoke test: load devicetree and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"devicetree\"}")
        let result = try process("/dts-v1/;\n/ { };", configObj)
    }

    func testSmokeDhall() throws {
        // Smoke test: load dhall and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"dhall\"}")
        let result = try process("x", configObj)
    }

    func testSmokeDiff() throws {
        // Smoke test: load diff and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"diff\"}")
        let result = try process("--- a/file\n+++ b/file\n@@ -1 +1 @@\n-old\n+new", configObj)
    }

    func testSmokeDjot() throws {
        // Smoke test: load djot and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"djot\"}")
        let result = try process("x", configObj)
    }

    func testSmokeDockerfile() throws {
        // Smoke test: load dockerfile and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"dockerfile\"}")
        let result = try process("FROM alpine", configObj)
    }

    func testSmokeDot() throws {
        // Smoke test: load dot and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"dot\"}")
        let result = try process("digraph G { A -> B; }", configObj)
    }

    func testSmokeDoxygen() throws {
        // Smoke test: load doxygen and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"doxygen\"}")
        let result = try process("/** @brief A function */", configObj)
    }

    func testSmokeDtd() throws {
        // Smoke test: load dtd and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"dtd\"}")
        let result = try process("<!ELEMENT note (body)>", configObj)
    }

    func testSmokeEarthfile() throws {
        // Smoke test: load earthfile and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"earthfile\"}")
        let result = try process("x", configObj)
    }

    func testSmokeEbnf() throws {
        // Smoke test: load ebnf and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ebnf\"}")
        let result = try process("x", configObj)
    }

    func testSmokeEditorconfig() throws {
        // Smoke test: load editorconfig and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"editorconfig\"}")
        let result = try process("x", configObj)
    }

    func testSmokeEds() throws {
        // Smoke test: load eds and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"eds\"}")
        let result = try process("x", configObj)
    }

    func testSmokeEex() throws {
        // Smoke test: load eex and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"eex\"}")
        let result = try process("x", configObj)
    }

    func testSmokeElisp() throws {
        // Smoke test: load elisp and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"elisp\"}")
        let result = try process("(defun hello () (message \"hello\"))", configObj)
    }

    func testSmokeElixir() throws {
        // Smoke test: load elixir and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"elixir\"}")
        let result = try process("IO.puts(\"hello\")", configObj)
    }

    func testSmokeElm() throws {
        // Smoke test: load elm and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"elm\"}")
        let result = try process("module Main exposing (..)", configObj)
    }

    func testSmokeElsa() throws {
        // Smoke test: load elsa and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"elsa\"}")
        let result = try process("x", configObj)
    }

    func testSmokeElvish() throws {
        // Smoke test: load elvish and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"elvish\"}")
        let result = try process("x", configObj)
    }

    func testSmokeEmbeddedtemplate() throws {
        // Smoke test: load embeddedtemplate and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"embeddedtemplate\"}")
        let result = try process("<%= value %>", configObj)
    }

    func testSmokeEnforce() throws {
        // Smoke test: load enforce and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"enforce\"}")
        let result = try process("x", configObj)
    }

    func testSmokeErlang() throws {
        // Smoke test: load erlang and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"erlang\"}")
        let result = try process("main() -> ok.", configObj)
    }

    func testSmokeFacility() throws {
        // Smoke test: load facility and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"facility\"}")
        let result = try process("x", configObj)
    }

    func testSmokeFaust() throws {
        // Smoke test: load faust and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"faust\"}")
        let result = try process("x", configObj)
    }

    func testSmokeFennel() throws {
        // Smoke test: load fennel and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"fennel\"}")
        let result = try process("(fn hello [] (print :hello))", configObj)
    }

    func testSmokeFidl() throws {
        // Smoke test: load fidl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"fidl\"}")
        let result = try process("x", configObj)
    }

    func testSmokeFirrtl() throws {
        // Smoke test: load firrtl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"firrtl\"}")
        let result = try process("circuit Main :", configObj)
    }

    func testSmokeFish() throws {
        // Smoke test: load fish and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"fish\"}")
        let result = try process("echo hello", configObj)
    }

    func testSmokeFoam() throws {
        // Smoke test: load foam and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"foam\"}")
        let result = try process("x", configObj)
    }

    func testSmokeForth() throws {
        // Smoke test: load forth and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"forth\"}")
        let result = try process("x", configObj)
    }

    func testSmokeFortran() throws {
        // Smoke test: load fortran and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"fortran\"}")
        let result = try process("program main\nend program main", configObj)
    }

    func testSmokeFsharp() throws {
        // Smoke test: load fsharp and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"fsharp\"}")
        let result = try process("let x = 1", configObj)
    }

    func testSmokeFsharpSignature() throws {
        // Smoke test: load fsharp_signature and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"fsharp_signature\"}")
        let result = try process("val x: int", configObj)
    }

    func testSmokeFunc() throws {
        // Smoke test: load func and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"func\"}")
        let result = try process("() recv_internal() {}", configObj)
    }

    func testSmokeGap() throws {
        // Smoke test: load gap and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gap\"}")
        let result = try process("x", configObj)
    }

    func testSmokeGdscript() throws {
        // Smoke test: load gdscript and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gdscript\"}")
        let result = try process("extends Node\nfunc _ready():\n\tpass", configObj)
    }

    func testSmokeGdshader() throws {
        // Smoke test: load gdshader and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gdshader\"}")
        let result = try process("x", configObj)
    }

    func testSmokeGitConfig() throws {
        // Smoke test: load git_config and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"git_config\"}")
        let result = try process("x", configObj)
    }

    func testSmokeGitRebase() throws {
        // Smoke test: load git_rebase and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"git_rebase\"}")
        let result = try process("x", configObj)
    }

    func testSmokeGitattributes() throws {
        // Smoke test: load gitattributes and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gitattributes\"}")
        let result = try process("*.txt text", configObj)
    }

    func testSmokeGitcommit() throws {
        // Smoke test: load gitcommit and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gitcommit\"}")
        let result = try process("feat: add feature\n\nBody text", configObj)
    }

    func testSmokeGitignore() throws {
        // Smoke test: load gitignore and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gitignore\"}")
        let result = try process("*.o\n*.log", configObj)
    }

    func testSmokeGleam() throws {
        // Smoke test: load gleam and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gleam\"}")
        let result = try process("pub fn main() { }", configObj)
    }

    func testSmokeGlimmer() throws {
        // Smoke test: load glimmer and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"glimmer\"}")
        let result = try process("x", configObj)
    }

    func testSmokeGlsl() throws {
        // Smoke test: load glsl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"glsl\"}")
        let result = try process("void main() { gl_Position = vec4(0.0); }", configObj)
    }

    func testSmokeGn() throws {
        // Smoke test: load gn and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gn\"}")
        let result = try process("group(\"hello\") {}", configObj)
    }

    func testSmokeGnuplot() throws {
        // Smoke test: load gnuplot and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gnuplot\"}")
        let result = try process("x", configObj)
    }

    func testSmokeGo() throws {
        // Smoke test: load go and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"go\"}")
        let result = try process("package main", configObj)
    }

    func testSmokeGodotResource() throws {
        // Smoke test: load godot_resource and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"godot_resource\"}")
        let result = try process("x", configObj)
    }

    func testSmokeGomod() throws {
        // Smoke test: load gomod and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gomod\"}")
        let result = try process("module example.com/hello\n\ngo 1.21", configObj)
    }

    func testSmokeGosum() throws {
        // Smoke test: load gosum and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gosum\"}")
        let result = try process("example.com/pkg v1.0.0 h1:abc=", configObj)
    }

    func testSmokeGotmpl() throws {
        // Smoke test: load gotmpl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gotmpl\"}")
        let result = try process("x", configObj)
    }

    func testSmokeGowork() throws {
        // Smoke test: load gowork and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gowork\"}")
        let result = try process("x", configObj)
    }

    func testSmokeGpg() throws {
        // Smoke test: load gpg and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gpg\"}")
        let result = try process("x", configObj)
    }

    func testSmokeGraphql() throws {
        // Smoke test: load graphql and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"graphql\"}")
        let result = try process("type Query { hello: String }", configObj)
    }

    func testSmokeGren() throws {
        // Smoke test: load gren and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gren\"}")
        let result = try process("module Main exposing (..)", configObj)
    }

    func testSmokeGroovy() throws {
        // Smoke test: load groovy and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"groovy\"}")
        let result = try process("def x = 1", configObj)
    }

    func testSmokeGstlaunch() throws {
        // Smoke test: load gstlaunch and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"gstlaunch\"}")
        let result = try process("fakesrc ! fakesink", configObj)
    }

    func testSmokeHack() throws {
        // Smoke test: load hack and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"hack\"}")
        let result = try process("<?hh\nfunction main(): void {}", configObj)
    }

    func testSmokeHare() throws {
        // Smoke test: load hare and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"hare\"}")
        let result = try process("export fn main() void = void;", configObj)
    }

    func testSmokeHaskell() throws {
        // Smoke test: load haskell and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"haskell\"}")
        let result = try process("main = putStrLn \"hello\"", configObj)
    }

    func testSmokeHaxe() throws {
        // Smoke test: load haxe and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"haxe\"}")
        let result = try process("class Main { static function main() {} }", configObj)
    }

    func testSmokeHcl() throws {
        // Smoke test: load hcl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"hcl\"}")
        let result = try process("variable \"name\" { type = string }", configObj)
    }

    func testSmokeHeex() throws {
        // Smoke test: load heex and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"heex\"}")
        let result = try process("<%= @greeting %>", configObj)
    }

    func testSmokeHjson() throws {
        // Smoke test: load hjson and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"hjson\"}")
        let result = try process("x", configObj)
    }

    func testSmokeHlsl() throws {
        // Smoke test: load hlsl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"hlsl\"}")
        let result = try process("float4 main() : SV_Target { return 0; }", configObj)
    }

    func testSmokeHocon() throws {
        // Smoke test: load hocon and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"hocon\"}")
        let result = try process("x", configObj)
    }

    func testSmokeHoon() throws {
        // Smoke test: load hoon and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"hoon\"}")
        let result = try process("x", configObj)
    }

    func testSmokeHtml() throws {
        // Smoke test: load html and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"html\"}")
        let result = try process("<p>hello</p>", configObj)
    }

    func testSmokeHtmldjango() throws {
        // Smoke test: load htmldjango and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"htmldjango\"}")
        let result = try process("x", configObj)
    }

    func testSmokeHttp() throws {
        // Smoke test: load http and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"http\"}")
        let result = try process("x", configObj)
    }

    func testSmokeHurl() throws {
        // Smoke test: load hurl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"hurl\"}")
        let result = try process("x", configObj)
    }

    func testSmokeHyprlang() throws {
        // Smoke test: load hyprlang and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"hyprlang\"}")
        let result = try process("general { border_size = 1 }", configObj)
    }

    func testSmokeIdris() throws {
        // Smoke test: load idris and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"idris\"}")
        let result = try process("module Main", configObj)
    }

    func testSmokeIni() throws {
        // Smoke test: load ini and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ini\"}")
        let result = try process("[section]\nkey = value", configObj)
    }

    func testSmokeIspc() throws {
        // Smoke test: load ispc and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ispc\"}")
        let result = try process("export void main() {}", configObj)
    }

    func testSmokeJai() throws {
        // Smoke test: load jai and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"jai\"}")
        let result = try process("x", configObj)
    }

    func testSmokeJanet() throws {
        // Smoke test: load janet and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"janet\"}")
        let result = try process("(print \"hello\")", configObj)
    }

    func testSmokeJava() throws {
        // Smoke test: load java and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"java\"}")
        let result = try process("class Main {}", configObj)
    }

    func testSmokeJavadoc() throws {
        // Smoke test: load javadoc and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"javadoc\"}")
        let result = try process("x", configObj)
    }

    func testSmokeJavascript() throws {
        // Smoke test: load javascript and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"javascript\"}")
        let result = try process("console.log('hello');", configObj)
    }

    func testSmokeJinja2() throws {
        // Smoke test: load jinja2 and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"jinja2\"}")
        let result = try process("{{ variable }}", configObj)
    }

    func testSmokeJq() throws {
        // Smoke test: load jq and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"jq\"}")
        let result = try process(".[] | select(.key)", configObj)
    }

    func testSmokeJsdoc() throws {
        // Smoke test: load jsdoc and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"jsdoc\"}")
        let result = try process("/** @param {string} name */", configObj)
    }

    func testSmokeJson() throws {
        // Smoke test: load json and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"json\"}")
        let result = try process("{\"key\": \"value\"}", configObj)
    }

    func testSmokeJson5() throws {
        // Smoke test: load json5 and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"json5\"}")
        let result = try process("x", configObj)
    }

    func testSmokeJsonnet() throws {
        // Smoke test: load jsonnet and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"jsonnet\"}")
        let result = try process("{ key: 'value' }", configObj)
    }

    func testSmokeJulia() throws {
        // Smoke test: load julia and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"julia\"}")
        let result = try process("function main() end", configObj)
    }

    func testSmokeJust() throws {
        // Smoke test: load just and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"just\"}")
        let result = try process("x", configObj)
    }

    func testSmokeKcl() throws {
        // Smoke test: load kcl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"kcl\"}")
        let result = try process("x", configObj)
    }

    func testSmokeKconfig() throws {
        // Smoke test: load kconfig and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"kconfig\"}")
        let result = try process("config FOO\n\tbool \"Enable foo\"", configObj)
    }

    func testSmokeKdl() throws {
        // Smoke test: load kdl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"kdl\"}")
        let result = try process("node \"value\"", configObj)
    }

    func testSmokeKotlin() throws {
        // Smoke test: load kotlin and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"kotlin\"}")
        let result = try process("fun main() {}", configObj)
    }

    func testSmokeLatex() throws {
        // Smoke test: load latex and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"latex\"}")
        let result = try process("\\documentclass{article}\n\\begin{document}\nHello\n\\end{document}", configObj)
    }

    func testSmokeLean() throws {
        // Smoke test: load lean and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"lean\"}")
        let result = try process("def main : IO Unit := pure ()", configObj)
    }

    func testSmokeLedger() throws {
        // Smoke test: load ledger and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ledger\"}")
        let result = try process("x", configObj)
    }

    func testSmokeLess() throws {
        // Smoke test: load less and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"less\"}")
        let result = try process("x", configObj)
    }

    func testSmokeLinkerscript() throws {
        // Smoke test: load linkerscript and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"linkerscript\"}")
        let result = try process("SECTIONS { .text : { *(.text) } }", configObj)
    }

    func testSmokeLiquid() throws {
        // Smoke test: load liquid and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"liquid\"}")
        let result = try process("x", configObj)
    }

    func testSmokeLlvm() throws {
        // Smoke test: load llvm and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"llvm\"}")
        let result = try process("define i32 @main() { ret i32 0 }", configObj)
    }

    func testSmokeLua() throws {
        // Smoke test: load lua and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"lua\"}")
        let result = try process("print('hello')", configObj)
    }

    func testSmokeLuadoc() throws {
        // Smoke test: load luadoc and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"luadoc\"}")
        let result = try process("---@param name string", configObj)
    }

    func testSmokeLuap() throws {
        // Smoke test: load luap and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"luap\"}")
        let result = try process("[a-z]+", configObj)
    }

    func testSmokeLuau() throws {
        // Smoke test: load luau and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"luau\"}")
        let result = try process("local x: number = 1", configObj)
    }

    func testSmokeMagik() throws {
        // Smoke test: load magik and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"magik\"}")
        let result = try process("_method object.hello\n_endmethod", configObj)
    }

    func testSmokeMake() throws {
        // Smoke test: load make and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"make\"}")
        let result = try process("all:\n\techo hello", configObj)
    }

    func testSmokeMarkdown() throws {
        // Smoke test: load markdown and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"markdown\"}")
        let result = try process("# Hello\n\nWorld", configObj)
    }

    func testSmokeMarkdownInline() throws {
        // Smoke test: load markdown_inline and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"markdown_inline\"}")
        let result = try process("**bold** and *italic*", configObj)
    }

    func testSmokeMatlab() throws {
        // Smoke test: load matlab and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"matlab\"}")
        let result = try process("function y = hello(x)\ny = x;\nend", configObj)
    }

    func testSmokeMermaid() throws {
        // Smoke test: load mermaid and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"mermaid\"}")
        let result = try process("graph TD\nA --> B", configObj)
    }

    func testSmokeMeson() throws {
        // Smoke test: load meson and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"meson\"}")
        let result = try process("project('hello', 'c')", configObj)
    }

    func testSmokeMlir() throws {
        // Smoke test: load mlir and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"mlir\"}")
        let result = try process("x", configObj)
    }

    func testSmokeMojo() throws {
        // Smoke test: load mojo and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"mojo\"}")
        let result = try process("x", configObj)
    }

    func testSmokeMove() throws {
        // Smoke test: load move and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"move\"}")
        let result = try process("x", configObj)
    }

    func testSmokeNasm() throws {
        // Smoke test: load nasm and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"nasm\"}")
        let result = try process("x", configObj)
    }

    func testSmokeNetlinx() throws {
        // Smoke test: load netlinx and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"netlinx\"}")
        let result = try process("PROGRAM_NAME='hello'", configObj)
    }

    func testSmokeNginx() throws {
        // Smoke test: load nginx and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"nginx\"}")
        let result = try process("x", configObj)
    }

    func testSmokeNickel() throws {
        // Smoke test: load nickel and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"nickel\"}")
        let result = try process("x", configObj)
    }

    func testSmokeNim() throws {
        // Smoke test: load nim and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"nim\"}")
        let result = try process("echo \"hello\"", configObj)
    }

    func testSmokeNinja() throws {
        // Smoke test: load ninja and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ninja\"}")
        let result = try process("rule cc\n  command = cc $in -o $out", configObj)
    }

    func testSmokeNix() throws {
        // Smoke test: load nix and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"nix\"}")
        let result = try process("{ pkgs ? import <nixpkgs> {} }: pkgs.hello", configObj)
    }

    func testSmokeNorg() throws {
        // Smoke test: load norg and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"norg\"}")
        let result = try process("x", configObj)
    }

    func testSmokeNorgMeta() throws {
        // Smoke test: load norg_meta and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"norg_meta\"}")
        let result = try process("x", configObj)
    }

    func testSmokeNqc() throws {
        // Smoke test: load nqc and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"nqc\"}")
        let result = try process("task main() {}", configObj)
    }

    func testSmokeNushell() throws {
        // Smoke test: load nushell and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"nushell\"}")
        let result = try process("x", configObj)
    }

    func testSmokeObjc() throws {
        // Smoke test: load objc and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"objc\"}")
        let result = try process("@interface Main @end", configObj)
    }

    func testSmokeOcaml() throws {
        // Smoke test: load ocaml and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ocaml\"}")
        let result = try process("let () = print_endline \"hello\"", configObj)
    }

    func testSmokeOcamlInterface() throws {
        // Smoke test: load ocaml_interface and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ocaml_interface\"}")
        let result = try process("val x : int", configObj)
    }

    func testSmokeOcamllex() throws {
        // Smoke test: load ocamllex and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ocamllex\"}")
        let result = try process("x", configObj)
    }

    func testSmokeOdin() throws {
        // Smoke test: load odin and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"odin\"}")
        let result = try process("package main", configObj)
    }

    func testSmokeOpenscad() throws {
        // Smoke test: load openscad and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"openscad\"}")
        let result = try process("x", configObj)
    }

    func testSmokeOrg() throws {
        // Smoke test: load org and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"org\"}")
        let result = try process("* Hello\nWorld", configObj)
    }

    func testSmokePascal() throws {
        // Smoke test: load pascal and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"pascal\"}")
        let result = try process("program Hello; begin end.", configObj)
    }

    func testSmokePem() throws {
        // Smoke test: load pem and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"pem\"}")
        let result = try process("-----BEGIN CERTIFICATE-----\ndata\n-----END CERTIFICATE-----", configObj)
    }

    func testSmokePerl() throws {
        // Smoke test: load perl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"perl\"}")
        let result = try process("print 'hello';", configObj)
    }

    func testSmokePgn() throws {
        // Smoke test: load pgn and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"pgn\"}")
        let result = try process("1. e4 e5 *", configObj)
    }

    func testSmokePhp() throws {
        // Smoke test: load php and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"php\"}")
        let result = try process("<?php echo 'hello'; ?>", configObj)
    }

    func testSmokePhpdoc() throws {
        // Smoke test: load phpdoc and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"phpdoc\"}")
        let result = try process("x", configObj)
    }

    func testSmokePkl() throws {
        // Smoke test: load pkl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"pkl\"}")
        let result = try process("name = \"hello\"", configObj)
    }

    func testSmokePo() throws {
        // Smoke test: load po and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"po\"}")
        let result = try process("msgid \"hello\"\nmsgstr \"world\"", configObj)
    }

    func testSmokePoeFilter() throws {
        // Smoke test: load poe_filter and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"poe_filter\"}")
        let result = try process("x", configObj)
    }

    func testSmokePony() throws {
        // Smoke test: load pony and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"pony\"}")
        let result = try process("actor Main\n  new create(env: Env) => None", configObj)
    }

    func testSmokePostscript() throws {
        // Smoke test: load postscript and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"postscript\"}")
        let result = try process("/hello { (Hello) show } def", configObj)
    }

    func testSmokePowershell() throws {
        // Smoke test: load powershell and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"powershell\"}")
        let result = try process("Write-Host 'hello'", configObj)
    }

    func testSmokePrintf() throws {
        // Smoke test: load printf and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"printf\"}")
        let result = try process("%d %s", configObj)
    }

    func testSmokePrisma() throws {
        // Smoke test: load prisma and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"prisma\"}")
        let result = try process("model User { id Int @id }", configObj)
    }

    func testSmokeProlog() throws {
        // Smoke test: load prolog and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"prolog\"}")
        let result = try process("hello :- write('hello'), nl.", configObj)
    }

    func testSmokePromql() throws {
        // Smoke test: load promql and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"promql\"}")
        let result = try process("x", configObj)
    }

    func testSmokeProperties() throws {
        // Smoke test: load properties and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"properties\"}")
        let result = try process("key=value", configObj)
    }

    func testSmokeProto() throws {
        // Smoke test: load proto and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"proto\"}")
        let result = try process("syntax = \"proto3\";", configObj)
    }

    func testSmokePrql() throws {
        // Smoke test: load prql and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"prql\"}")
        let result = try process("x", configObj)
    }

    func testSmokePsv() throws {
        // Smoke test: load psv and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"psv\"}")
        let result = try process("a|b|c\n1|2|3", configObj)
    }

    func testSmokePug() throws {
        // Smoke test: load pug and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"pug\"}")
        let result = try process("x", configObj)
    }

    func testSmokePuppet() throws {
        // Smoke test: load puppet and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"puppet\"}")
        let result = try process("notify { 'hello': }", configObj)
    }

    func testSmokePurescript() throws {
        // Smoke test: load purescript and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"purescript\"}")
        let result = try process("module Main where", configObj)
    }

    func testSmokePymanifest() throws {
        // Smoke test: load pymanifest and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"pymanifest\"}")
        let result = try process("include *.txt", configObj)
    }

    func testSmokePython() throws {
        // Smoke test: load python and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"python\"}")
        let result = try process("print('hello')", configObj)
    }

    func testSmokeQl() throws {
        // Smoke test: load ql and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ql\"}")
        let result = try process("x", configObj)
    }

    func testSmokeQmldir() throws {
        // Smoke test: load qmldir and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"qmldir\"}")
        let result = try process("module Example", configObj)
    }

    func testSmokeQmljs() throws {
        // Smoke test: load qmljs and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"qmljs\"}")
        let result = try process("import QtQuick 2.0\nItem {}", configObj)
    }

    func testSmokeQuery() throws {
        // Smoke test: load query and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"query\"}")
        let result = try process("(identifier) @name", configObj)
    }

    func testSmokeR() throws {
        // Smoke test: load r and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"r\"}")
        let result = try process("print('hello')", configObj)
    }

    func testSmokeRacket() throws {
        // Smoke test: load racket and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"racket\"}")
        let result = try process("#lang racket\n(define x 1)", configObj)
    }

    func testSmokeRasi() throws {
        // Smoke test: load rasi and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"rasi\"}")
        let result = try process("x", configObj)
    }

    func testSmokeRazor() throws {
        // Smoke test: load razor and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"razor\"}")
        let result = try process("x", configObj)
    }

    func testSmokeRbs() throws {
        // Smoke test: load rbs and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"rbs\"}")
        let result = try process("x", configObj)
    }

    func testSmokeRe2c() throws {
        // Smoke test: load re2c and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"re2c\"}")
        let result = try process("/*!re2c\n  [a-z]+ { return; }\n*/", configObj)
    }

    func testSmokeReadline() throws {
        // Smoke test: load readline and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"readline\"}")
        let result = try process("set editing-mode vi", configObj)
    }

    func testSmokeRegex() throws {
        // Smoke test: load regex and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"regex\"}")
        let result = try process("x", configObj)
    }

    func testSmokeRego() throws {
        // Smoke test: load rego and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"rego\"}")
        let result = try process("package main\ndefault allow = false", configObj)
    }

    func testSmokeRequirements() throws {
        // Smoke test: load requirements and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"requirements\"}")
        let result = try process("flask>=2.0", configObj)
    }

    func testSmokeRescript() throws {
        // Smoke test: load rescript and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"rescript\"}")
        let result = try process("let x = 1", configObj)
    }

    func testSmokeRobot() throws {
        // Smoke test: load robot and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"robot\"}")
        let result = try process("x", configObj)
    }

    func testSmokeRoc() throws {
        // Smoke test: load roc and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"roc\"}")
        let result = try process("x", configObj)
    }

    func testSmokeRon() throws {
        // Smoke test: load ron and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ron\"}")
        let result = try process("(key: \"value\")", configObj)
    }

    func testSmokeRst() throws {
        // Smoke test: load rst and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"rst\"}")
        let result = try process("Hello\n=====\n\nWorld", configObj)
    }

    func testSmokeRtf() throws {
        // Smoke test: load rtf and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"rtf\"}")
        let result = try process("{\\rtf1 hello}", configObj)
    }

    func testSmokeRuby() throws {
        // Smoke test: load ruby and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ruby\"}")
        let result = try process("puts 'hello'", configObj)
    }

    func testSmokeRust() throws {
        // Smoke test: load rust and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"rust\"}")
        let result = try process("fn main() {}", configObj)
    }

    func testSmokeScala() throws {
        // Smoke test: load scala and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"scala\"}")
        let result = try process("object Main", configObj)
    }

    func testSmokeScheme() throws {
        // Smoke test: load scheme and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"scheme\"}")
        let result = try process("(define x 1)", configObj)
    }

    func testSmokeScss() throws {
        // Smoke test: load scss and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"scss\"}")
        let result = try process("$color: red;\nbody { color: $color; }", configObj)
    }

    func testSmokeSlang() throws {
        // Smoke test: load slang and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"slang\"}")
        let result = try process("x", configObj)
    }

    func testSmokeSmali() throws {
        // Smoke test: load smali and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"smali\"}")
        let result = try process(".class public LMain;\n.super Ljava/lang/Object;", configObj)
    }

    func testSmokeSmalltalk() throws {
        // Smoke test: load smalltalk and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"smalltalk\"}")
        let result = try process("x", configObj)
    }

    func testSmokeSmithy() throws {
        // Smoke test: load smithy and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"smithy\"}")
        let result = try process("namespace example\nstring MyString", configObj)
    }

    func testSmokeSml() throws {
        // Smoke test: load sml and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"sml\"}")
        let result = try process("x", configObj)
    }

    func testSmokeSnakemake() throws {
        // Smoke test: load snakemake and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"snakemake\"}")
        let result = try process("x", configObj)
    }

    func testSmokeSolidity() throws {
        // Smoke test: load solidity and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"solidity\"}")
        let result = try process("pragma solidity ^0.8.0;\ncontract Main {}", configObj)
    }

    func testSmokeSouffle() throws {
        // Smoke test: load souffle and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"souffle\"}")
        let result = try process("x", configObj)
    }

    func testSmokeSourcepawn() throws {
        // Smoke test: load sourcepawn and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"sourcepawn\"}")
        let result = try process("x", configObj)
    }

    func testSmokeSparql() throws {
        // Smoke test: load sparql and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"sparql\"}")
        let result = try process("SELECT ?s WHERE { ?s ?p ?o }", configObj)
    }

    func testSmokeSql() throws {
        // Smoke test: load sql and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"sql\"}")
        let result = try process("SELECT 1;", configObj)
    }

    func testSmokeSqlBigquery() throws {
        // Smoke test: load sql_bigquery and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"sql_bigquery\"}")
        let result = try process("x", configObj)
    }

    func testSmokeSquirrel() throws {
        // Smoke test: load squirrel and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"squirrel\"}")
        let result = try process("function main() {}", configObj)
    }

    func testSmokeSshConfig() throws {
        // Smoke test: load ssh_config and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ssh_config\"}")
        let result = try process("Host example\n  HostName example.com", configObj)
    }

    func testSmokeStan() throws {
        // Smoke test: load stan and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"stan\"}")
        let result = try process("x", configObj)
    }

    func testSmokeStarlark() throws {
        // Smoke test: load starlark and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"starlark\"}")
        let result = try process("def hello(): pass", configObj)
    }

    func testSmokeSuperhtml() throws {
        // Smoke test: load superhtml and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"superhtml\"}")
        let result = try process("x", configObj)
    }

    func testSmokeSvelte() throws {
        // Smoke test: load svelte and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"svelte\"}")
        let result = try process("<script>let x = 1;</script>", configObj)
    }

    func testSmokeSway() throws {
        // Smoke test: load sway and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"sway\"}")
        let result = try process("x", configObj)
    }

    func testSmokeSwift() throws {
        // Smoke test: load swift and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"swift\"}")
        let result = try process("print(\"hello\")", configObj)
    }

    func testSmokeSystemverilog() throws {
        // Smoke test: load systemverilog and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"systemverilog\"}")
        let result = try process("x", configObj)
    }

    func testSmokeTablegen() throws {
        // Smoke test: load tablegen and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"tablegen\"}")
        let result = try process("def Hello : Base {}", configObj)
    }

    func testSmokeTact() throws {
        // Smoke test: load tact and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"tact\"}")
        let result = try process("x", configObj)
    }

    func testSmokeTcl() throws {
        // Smoke test: load tcl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"tcl\"}")
        let result = try process("puts hello", configObj)
    }

    func testSmokeTeal() throws {
        // Smoke test: load teal and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"teal\"}")
        let result = try process("x", configObj)
    }

    func testSmokeTempl() throws {
        // Smoke test: load templ and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"templ\"}")
        let result = try process("x", configObj)
    }

    func testSmokeTera() throws {
        // Smoke test: load tera and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"tera\"}")
        let result = try process("x", configObj)
    }

    func testSmokeTerraform() throws {
        // Smoke test: load terraform and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"terraform\"}")
        let result = try process("resource \"null_resource\" \"main\" {}", configObj)
    }

    func testSmokeTest() throws {
        // Smoke test: load test and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"test\"}")
        let result = try process("===========\nTest\n===========\n---\n(node)", configObj)
    }

    func testSmokeTextproto() throws {
        // Smoke test: load textproto and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"textproto\"}")
        let result = try process("key: \"value\"", configObj)
    }

    func testSmokeThrift() throws {
        // Smoke test: load thrift and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"thrift\"}")
        let result = try process("service HelloService {}", configObj)
    }

    func testSmokeTlaplus() throws {
        // Smoke test: load tlaplus and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"tlaplus\"}")
        let result = try process("---- MODULE Main ----\n====", configObj)
    }

    func testSmokeTmux() throws {
        // Smoke test: load tmux and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"tmux\"}")
        let result = try process("x", configObj)
    }

    func testSmokeTodotxt() throws {
        // Smoke test: load todotxt and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"todotxt\"}")
        let result = try process("x", configObj)
    }

    func testSmokeToml() throws {
        // Smoke test: load toml and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"toml\"}")
        let result = try process("key = \"value\"", configObj)
    }

    func testSmokeTsv() throws {
        // Smoke test: load tsv and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"tsv\"}")
        let result = try process("a\tb\tc\n1\t2\t3", configObj)
    }

    func testSmokeTsx() throws {
        // Smoke test: load tsx and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"tsx\"}")
        let result = try process("const App = () => <div />;", configObj)
    }

    func testSmokeTurtle() throws {
        // Smoke test: load turtle and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"turtle\"}")
        let result = try process("x", configObj)
    }

    func testSmokeTwig() throws {
        // Smoke test: load twig and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"twig\"}")
        let result = try process("{{ variable }}", configObj)
    }

    func testSmokeTypescript() throws {
        // Smoke test: load typescript and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"typescript\"}")
        let result = try process("const x: number = 42;", configObj)
    }

    func testSmokeTypespec() throws {
        // Smoke test: load typespec and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"typespec\"}")
        let result = try process("x", configObj)
    }

    func testSmokeTyposcript() throws {
        // Smoke test: load typoscript and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"typoscript\"}")
        let result = try process("x", configObj)
    }

    func testSmokeTypst() throws {
        // Smoke test: load typst and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"typst\"}")
        let result = try process("#let x = 1", configObj)
    }

    func testSmokeUdev() throws {
        // Smoke test: load udev and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"udev\"}")
        let result = try process("ACTION==\"add\", KERNEL==\"sd*\"", configObj)
    }

    func testSmokeUngrammar() throws {
        // Smoke test: load ungrammar and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ungrammar\"}")
        let result = try process("Root = Item*\nItem = 'token'", configObj)
    }

    func testSmokeUxntal() throws {
        // Smoke test: load uxntal and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"uxntal\"}")
        let result = try process("|0100 LIT 01", configObj)
    }

    func testSmokeV() throws {
        // Smoke test: load v and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"v\"}")
        let result = try process("fn main() {}", configObj)
    }

    func testSmokeVb() throws {
        // Smoke test: load vb and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"vb\"}")
        let result = try process("Module Main\nEnd Module", configObj)
    }

    func testSmokeVerilog() throws {
        // Smoke test: load verilog and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"verilog\"}")
        let result = try process("module main; endmodule", configObj)
    }

    func testSmokeVhdl() throws {
        // Smoke test: load vhdl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"vhdl\"}")
        let result = try process("entity main is end main;", configObj)
    }

    func testSmokeVhs() throws {
        // Smoke test: load vhs and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"vhs\"}")
        let result = try process("x", configObj)
    }

    func testSmokeVim() throws {
        // Smoke test: load vim and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"vim\"}")
        let result = try process("echo 'hello'", configObj)
    }

    func testSmokeVimdoc() throws {
        // Smoke test: load vimdoc and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"vimdoc\"}")
        let result = try process("x", configObj)
    }

    func testSmokeVrl() throws {
        // Smoke test: load vrl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"vrl\"}")
        let result = try process("x", configObj)
    }

    func testSmokeVue() throws {
        // Smoke test: load vue and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"vue\"}")
        let result = try process("<template><div>hello</div></template>", configObj)
    }

    func testSmokeWast() throws {
        // Smoke test: load wast and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"wast\"}")
        let result = try process("(module)", configObj)
    }

    func testSmokeWat() throws {
        // Smoke test: load wat and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"wat\"}")
        let result = try process("(module)", configObj)
    }

    func testSmokeWgsl() throws {
        // Smoke test: load wgsl and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"wgsl\"}")
        let result = try process("@vertex fn main() -> @builtin(position) vec4f { return vec4f(); }", configObj)
    }

    func testSmokeWgslBevy() throws {
        // Smoke test: load wgsl_bevy and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"wgsl_bevy\"}")
        let result = try process("x", configObj)
    }

    func testSmokeWit() throws {
        // Smoke test: load wit and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"wit\"}")
        let result = try process("package example:pkg;", configObj)
    }

    func testSmokeX86asm() throws {
        // Smoke test: load x86asm and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"x86asm\"}")
        let result = try process("x", configObj)
    }

    func testSmokeXcompose() throws {
        // Smoke test: load xcompose and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"xcompose\"}")
        let result = try process("<Multi_key> <a> : \"a\"", configObj)
    }

    func testSmokeXml() throws {
        // Smoke test: load xml and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"xml\"}")
        let result = try process("<?xml version=\"1.0\"?>\n<root>hello</root>", configObj)
    }

    func testSmokeYaml() throws {
        // Smoke test: load yaml and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"yaml\"}")
        let result = try process("key: value", configObj)
    }

    func testSmokeYuck() throws {
        // Smoke test: load yuck and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"yuck\"}")
        let result = try process("(defwidget main [] (label :text \"hi\"))", configObj)
    }

    func testSmokeZig() throws {
        // Smoke test: load zig and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"zig\"}")
        let result = try process("pub fn main() void {}", configObj)
    }

    func testSmokeZiggy() throws {
        // Smoke test: load ziggy and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ziggy\"}")
        let result = try process("x", configObj)
    }

    func testSmokeZiggySchema() throws {
        // Smoke test: load ziggy_schema and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"ziggy_schema\"}")
        let result = try process("x", configObj)
    }

    func testSmokeZsh() throws {
        // Smoke test: load zsh and parse a simple snippet
        let configObj = try processConfigFromJson("{\"language\":\"zsh\"}")
        let result = try process("echo hello", configObj)
    }

}
