// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:26c4710d296d1ec8b0c7d2d0f8c8d60306d8234c2519a3c0009269a2d04dfa30
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import TreeSitterLanguagePack
import RustBridge

/// E2e tests for category: language-detection.
final class LanguageDetectionTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testDetectContentBashShebang() throws {
        // detect_language_from_content recognizes #!/bin/bash shebang
        let result = try TreeSitterLanguagePack.detectLanguageFromContent(content: "#!/bin/bash\necho hi")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "bash")
    }

    func testDetectContentNoShebang() throws {
        // detect_language_from_content returns None when no shebang present
        let result = try TreeSitterLanguagePack.detectLanguageFromContent(content: "no shebang here")
        XCTAssertNil(result, "expected nil value")
    }

    func testDetectContentPythonShebang() throws {
        // detect_language_from_content recognizes #!/usr/bin/env python3 shebang
        let result = try TreeSitterLanguagePack.detectLanguageFromContent(content: "#!/usr/bin/env python3\npass")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
    }

    func testDetectExtCpp() throws {
        // detect_language_from_extension recognizes .cpp as cpp
        let result = try TreeSitterLanguagePack.detectLanguageFromExtension(ext: "cpp")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "cpp")
    }

    func testDetectExtGherkin() throws {
        // detect_language_from_extension recognizes .feature as gherkin
        let result = try TreeSitterLanguagePack.detectLanguageFromExtension(ext: "feature")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "gherkin")
    }

    func testDetectExtGo() throws {
        // detect_language_from_extension recognizes .go as go
        let result = try TreeSitterLanguagePack.detectLanguageFromExtension(ext: "go")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "go")
    }

    func testDetectExtJava() throws {
        // detect_language_from_extension recognizes .java as java
        let result = try TreeSitterLanguagePack.detectLanguageFromExtension(ext: "java")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "java")
    }

    func testDetectExtJavascript() throws {
        // detect_language_from_extension recognizes .js as javascript
        let result = try TreeSitterLanguagePack.detectLanguageFromExtension(ext: "js")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "javascript")
    }

    func testDetectExtPhp() throws {
        // detect_language_from_extension recognizes .php as php
        let result = try TreeSitterLanguagePack.detectLanguageFromExtension(ext: "php")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "php")
    }

    func testDetectExtPython() throws {
        // detect_language_from_extension recognizes .py as python
        let result = try TreeSitterLanguagePack.detectLanguageFromExtension(ext: "py")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
    }

    func testDetectExtRuby() throws {
        // detect_language_from_extension recognizes .rb as ruby
        let result = try TreeSitterLanguagePack.detectLanguageFromExtension(ext: "rb")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "ruby")
    }

    func testDetectExtRust() throws {
        // detect_language_from_extension recognizes .rs as rust
        let result = try TreeSitterLanguagePack.detectLanguageFromExtension(ext: "rs")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "rust")
    }

    func testDetectExtTypescript() throws {
        // detect_language_from_extension recognizes .ts as typescript
        let result = try TreeSitterLanguagePack.detectLanguageFromExtension(ext: "ts")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "typescript")
    }

    func testDetectExtUnknown() throws {
        // detect_language_from_extension returns None for unknown extension .xyz
        let result = try TreeSitterLanguagePack.detectLanguageFromExtension(ext: "xyz")
        XCTAssertNil(result, "expected nil value")
    }

    func testDetectPathDotfile() throws {
        // detect_language_from_path returns None for dotfile .gitignore (no standard extension)
        let result = try TreeSitterLanguagePack.detectLanguageFromPath(path: ".gitignore")
        XCTAssertNil(result, "expected nil value")
    }

    func testDetectPathGoNested() throws {
        // detect_language_from_path extracts extension from nested path lib/server.go
        let result = try TreeSitterLanguagePack.detectLanguageFromPath(path: "lib/server.go")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "go")
    }

    func testDetectPathJavaRoot() throws {
        // detect_language_from_path recognizes Main.java in root directory
        let result = try TreeSitterLanguagePack.detectLanguageFromPath(path: "Main.java")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "java")
    }

    func testDetectPathJsRoot() throws {
        // detect_language_from_path recognizes app.js in root directory
        let result = try TreeSitterLanguagePack.detectLanguageFromPath(path: "app.js")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "javascript")
    }

    func testDetectPathNested() throws {
        // detect_language_from_path extracts extension from nested path src/main.py
        let result = try TreeSitterLanguagePack.detectLanguageFromPath(path: "src/main.py")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
    }

    func testDetectPathNoExtension() throws {
        // detect_language_from_path returns None for extensionless file Makefile
        let result = try TreeSitterLanguagePack.detectLanguageFromPath(path: "Makefile")
        XCTAssertNil(result, "expected nil value")
    }

    func testDetectPathRustSrc() throws {
        // detect_language_from_path extracts extension from nested path src/main.rs
        let result = try TreeSitterLanguagePack.detectLanguageFromPath(path: "src/main.rs")
        XCTAssertEqual((result ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "rust")
    }

}
