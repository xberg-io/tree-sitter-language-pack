// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import TreeSitterLanguagePack
import RustBridge

/// E2e tests for category: language-detection.
final class LanguageDetectionTests: XCTestCase {
    func testDetectContentBashShebang() throws {
        // detect_language_from_content recognizes #!/bin/bash shebang
        let result = try detectLanguageFromContent("#!/bin/bash\necho hi")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "bash")
    }

    func testDetectContentNoShebang() throws {
        // detect_language_from_content returns None when no shebang present
        let result = try detectLanguageFromContent("no shebang here")
        XCTAssertNil(result, "expected nil value")
    }

    func testDetectContentPythonShebang() throws {
        // detect_language_from_content recognizes #!/usr/bin/env python3 shebang
        let result = try detectLanguageFromContent("#!/usr/bin/env python3\npass")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
    }

    func testDetectExtCpp() throws {
        // detect_language_from_extension recognizes .cpp as cpp
        let result = try detectLanguageFromExtension("cpp")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "cpp")
    }

    func testDetectExtGo() throws {
        // detect_language_from_extension recognizes .go as go
        let result = try detectLanguageFromExtension("go")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "go")
    }

    func testDetectExtJava() throws {
        // detect_language_from_extension recognizes .java as java
        let result = try detectLanguageFromExtension("java")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "java")
    }

    func testDetectExtJavascript() throws {
        // detect_language_from_extension recognizes .js as javascript
        let result = try detectLanguageFromExtension("js")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "javascript")
    }

    func testDetectExtPhp() throws {
        // detect_language_from_extension recognizes .php as php
        let result = try detectLanguageFromExtension("php")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "php")
    }

    func testDetectExtPython() throws {
        // detect_language_from_extension recognizes .py as python
        let result = try detectLanguageFromExtension("py")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
    }

    func testDetectExtRuby() throws {
        // detect_language_from_extension recognizes .rb as ruby
        let result = try detectLanguageFromExtension("rb")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "ruby")
    }

    func testDetectExtRust() throws {
        // detect_language_from_extension recognizes .rs as rust
        let result = try detectLanguageFromExtension("rs")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "rust")
    }

    func testDetectExtTypescript() throws {
        // detect_language_from_extension recognizes .ts as typescript
        let result = try detectLanguageFromExtension("ts")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "typescript")
    }

    func testDetectExtUnknown() throws {
        // detect_language_from_extension returns None for unknown extension .xyz
        let result = try detectLanguageFromExtension("xyz")
        XCTAssertNil(result, "expected nil value")
    }

    func testDetectPathDotfile() throws {
        // detect_language_from_path returns None for dotfile .gitignore (no standard extension)
        let result = try detectLanguageFromPath(".gitignore")
        XCTAssertNil(result, "expected nil value")
    }

    func testDetectPathGoNested() throws {
        // detect_language_from_path extracts extension from nested path lib/server.go
        let result = try detectLanguageFromPath("lib/server.go")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "go")
    }

    func testDetectPathJavaRoot() throws {
        // detect_language_from_path recognizes Main.java in root directory
        let result = try detectLanguageFromPath("Main.java")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "java")
    }

    func testDetectPathJsRoot() throws {
        // detect_language_from_path recognizes app.js in root directory
        let result = try detectLanguageFromPath("app.js")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "javascript")
    }

    func testDetectPathNested() throws {
        // detect_language_from_path extracts extension from nested path src/main.py
        let result = try detectLanguageFromPath("src/main.py")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
    }

    func testDetectPathNoExtension() throws {
        // detect_language_from_path returns None for extensionless file Makefile
        let result = try detectLanguageFromPath("Makefile")
        XCTAssertNil(result, "expected nil value")
    }

    func testDetectPathRustSrc() throws {
        // detect_language_from_path extracts extension from nested path src/main.rs
        let result = try detectLanguageFromPath("src/main.rs")
        XCTAssertEqual(result.toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "rust")
    }

}
