// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d369c053fee9246b63f21cad5291db523058405f072b0f8a8f4faadf775762ef
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const tree_sitter_language_pack = @import("tree_sitter_language_pack");

// E2e tests for category: process

test "c_function_process" {
    // Intel: C function with include
    const result = try tree_sitter_language_pack.process("#include <stdio.h>\n\nint main() {\n    printf(\"hello\");\n    return 0;\n}\n", "{\"language\":\"c\"}");
    try testing.expectEqual("c", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.metrics.total_lines >= 6);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "config_all_python" {
    // Intel: process with all features enabled
    const result = try tree_sitter_language_pack.process("# A comment\ndef greet(name):\n    \"\"\"Say hello.\"\"\"\n    return f'Hi {name}'\n\nimport os\n", "{\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.imports.len >= 1);
    try testing.expect(result.metrics.total_lines >= 6);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "config_minimal_python" {
    // Intel: process with minimal config - only metrics
    const result = try tree_sitter_language_pack.process("def hello():\n    pass\n", "{\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.metrics.total_lines >= 2);
}

test "go_function_process" {
    // Intel: extract structure from Go function definition
    const result = try tree_sitter_language_pack.process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", "{\"language\":\"go\"}");
    try testing.expectEqual("go", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.imports.len >= 1);
    try testing.expect(result.metrics.total_lines >= 7);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "go_function_process_detail" {
    // Intel: extract structure from Go function definition
    const result = try tree_sitter_language_pack.process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", "{\"language\":\"go\"}");
    try testing.expectEqual("go", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.imports.len >= 1);
    try testing.expect(result.metrics.total_lines >= 7);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "java_class_process" {
    // Intel: Java class with methods and imports
    const result = try tree_sitter_language_pack.process("import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return \"Hello \" + name;\n    }\n}\n", "{\"language\":\"java\"}");
    try testing.expectEqual("java", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Class") != null);
    try testing.expect(result.imports.len >= 1);
    try testing.expect(result.metrics.total_lines >= 7);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "java_package_declaration_process" {
    // Intel: Java package declaration emitted as Module so callers can build FQNs (#112)
    const result = try tree_sitter_language_pack.process("package com.example.widget;\n\npublic class Widget {\n    public String name() { return \"w\"; }\n}\n", "{\"language\":\"java\"}");
    try testing.expectEqual("java", result.language);
    try testing.expect(result.structure.len >= 2);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Module") != null);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Class") != null);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "javascript_multi_import_process" {
    // Intel: detect multiple imports and function in JavaScript
    const result = try tree_sitter_language_pack.process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", "{\"language\":\"javascript\"}");
    try testing.expectEqual("javascript", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.imports.len >= 2);
    try testing.expect(result.metrics.total_lines >= 6);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "javascript_multi_import_process_detail" {
    // Intel: detect multiple imports and function in JavaScript
    const result = try tree_sitter_language_pack.process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", "{\"language\":\"javascript\"}");
    try testing.expectEqual("javascript", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.imports.len >= 2);
    try testing.expect(result.metrics.total_lines >= 6);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "kotlin_package_class_process" {
    // Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112)
    const result = try tree_sitter_language_pack.process("package foo.bar\n\nclass Widget {\n    fun greet(): String = \"hi\"\n}\n", "{\"language\":\"kotlin\"}");
    try testing.expectEqual("kotlin", result.language);
    try testing.expect(result.structure.len >= 2);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Module") != null);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Class") != null);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "process_javascript_exports_count" {
    // JavaScript with multiple exports, verify export count
    const result = try tree_sitter_language_pack.process("export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", "{\"language\":\"javascript\"}");
    try testing.expectEqual("javascript", result.language);
    try testing.expect(result.exports.len >= 2);
}

test "process_javascript_exports_detail" {
    // JavaScript with exports, verify export count
    const result = try tree_sitter_language_pack.process("export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n", "{\"language\":\"javascript\"}");
    try testing.expectEqual("javascript", result.language);
    try testing.expect(result.exports.len >= 1);
}

test "process_python_all_features" {
    // Python comprehensive source with all feature extraction enabled
    const result = try tree_sitter_language_pack.process("import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", "{\"comments\":true,\"docstrings\":true,\"imports\":true,\"language\":\"python\",\"structure\":true,\"symbols\":true}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.structure.len >= 2);
    try testing.expect(result.imports.len >= 1);
    try testing.expect(result.comments.len >= 1);
    try testing.expect(result.metrics.total_lines >= 10);
}

test "process_python_comments" {
    // Python with comments, verify comment count
    const result = try tree_sitter_language_pack.process("# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", "{\"comments\":true,\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.comments.len >= 1);
}

test "process_python_docstrings" {
    // Python with function docstring, verify docstring count
    const result = try tree_sitter_language_pack.process("def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", "{\"docstrings\":true,\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.metrics.total_lines >= 3);
}

test "process_python_imports_detail" {
    // Python with multiple imports, verify imports contain specific source
    const result = try tree_sitter_language_pack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", "{\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.imports.len >= 2);
    try testing.expect(std.mem.indexOf(u8, result.imports, "os") != null);
}

test "process_python_metrics_detail" {
    // Python code with metrics assertions
    const result = try tree_sitter_language_pack.process("# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n", "{\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.metrics.code_lines >= 4);
    try testing.expect(result.metrics.comment_lines >= 1);
    try testing.expect(result.metrics.max_depth >= 1);
}

test "process_python_symbols" {
    // Python with class and functions, verify symbol count
    const result = try tree_sitter_language_pack.process("MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n", "{\"language\":\"python\",\"symbols\":true}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.symbols.len >= 1);
}

test "process_rust_structure_name" {
    // Rust struct with name, verify structure name contains value
    const result = try tree_sitter_language_pack.process("pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", "{\"language\":\"rust\"}");
    try testing.expectEqual("rust", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "MyConfig") != null);
}

test "python_chunking_medium" {
    // Intel: Python code with medium chunk size
    const result = try tree_sitter_language_pack.process("def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n", "{\"chunk_max_size\":50,\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.structure.len >= 3);
    try testing.expectEqual(0, result.metrics.error_count);
    try testing.expect(result.chunks.len >= 2);
}

test "python_chunking_process_detail" {
    // Intel: chunk multi-function Python source into multiple pieces
    const result = try tree_sitter_language_pack.process("def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n", "{\"chunk_max_size\":30,\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.metrics.total_lines >= 8);
    try testing.expect(result.chunks.len >= 2);
}

test "python_class_with_methods_process" {
    // Intel: extract nested structure from Python class with methods
    const result = try tree_sitter_language_pack.process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", "{\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Class") != null);
    try testing.expect(result.metrics.total_lines >= 6);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "python_class_with_methods_process_detail" {
    // Intel: extract nested structure from Python class with methods
    const result = try tree_sitter_language_pack.process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", "{\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Class") != null);
    try testing.expect(result.metrics.total_lines >= 6);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "python_error_diagnostics" {
    // Intel: Python code with syntax errors should report diagnostics
    const result = try tree_sitter_language_pack.process("def broken(\n    pass\n", "{\"diagnostics\":true,\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expectEqual(1, result.metrics.error_count);
    try testing.expect(result.diagnostics.len > 0);
}

test "python_function_process" {
    // Intel: extract structure from Python function definition
    const result = try tree_sitter_language_pack.process("def greet(name):\n    return f'Hello, {name}!'\n", "{\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.metrics.total_lines >= 2);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "python_function_process_detail" {
    // Intel: extract structure from Python function definition
    const result = try tree_sitter_language_pack.process("def greet(name):\n    return f'Hello, {name}!'\n", "{\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.metrics.total_lines >= 2);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "python_malformed_code_process" {
    // Intel: detect diagnostics in malformed Python code
    const result = try tree_sitter_language_pack.process("def broken(\n    return\nclass", "{\"diagnostics\":true,\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.diagnostics.len > 0);
}

test "python_malformed_code_process_detail" {
    // Intel: detect diagnostics in malformed Python code
    const result = try tree_sitter_language_pack.process("def broken(\n    return\nclass", "{\"diagnostics\":true,\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.diagnostics.len > 0);
}

test "python_multi_import_process" {
    // Intel: detect multiple Python imports
    const result = try tree_sitter_language_pack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", "{\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(result.imports.len >= 3);
    try testing.expect(result.metrics.total_lines >= 5);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "python_multi_import_process_detail" {
    // Intel: detect multiple Python imports
    const result = try tree_sitter_language_pack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", "{\"language\":\"python\"}");
    try testing.expectEqual("python", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(result.imports.len >= 3);
    try testing.expect(result.metrics.total_lines >= 5);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "ruby_class_process" {
    // Intel: Ruby class with method
    const result = try tree_sitter_language_pack.process("require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello #{name}\"\n  end\nend\n", "{\"language\":\"ruby\"}");
    try testing.expectEqual("ruby", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Class") != null);
    try testing.expect(result.metrics.total_lines >= 7);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "rust_chunking_process" {
    // Intel: chunk multi-function Rust source into pieces
    const result = try tree_sitter_language_pack.process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", "{\"chunk_max_size\":30,\"language\":\"rust\"}");
    try testing.expectEqual("rust", result.language);
    try testing.expect(result.metrics.total_lines >= 7);
    try testing.expect(result.chunks.len >= 2);
}

test "rust_chunking_process_detail" {
    // Intel: chunk multi-function Rust source into pieces
    const result = try tree_sitter_language_pack.process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", "{\"chunk_max_size\":30,\"language\":\"rust\"}");
    try testing.expectEqual("rust", result.language);
    try testing.expect(result.metrics.total_lines >= 7);
    try testing.expect(result.chunks.len >= 2);
}

test "rust_function_process" {
    // Intel: extract structure from Rust function definition
    const result = try tree_sitter_language_pack.process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", "{\"language\":\"rust\"}");
    try testing.expectEqual("rust", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.metrics.total_lines >= 3);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "rust_function_process_detail" {
    // Intel: extract structure from Rust function definition
    const result = try tree_sitter_language_pack.process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", "{\"language\":\"rust\"}");
    try testing.expectEqual("rust", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.metrics.total_lines >= 3);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "typescript_function_process" {
    // Intel: extract structure from TypeScript function
    const result = try tree_sitter_language_pack.process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", "{\"language\":\"typescript\"}");
    try testing.expectEqual("typescript", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.imports.len >= 1);
    try testing.expect(result.metrics.total_lines >= 5);
    try testing.expectEqual(0, result.metrics.error_count);
}

test "typescript_function_process_detail" {
    // Intel: extract structure from TypeScript function
    const result = try tree_sitter_language_pack.process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", "{\"language\":\"typescript\"}");
    try testing.expectEqual("typescript", result.language);
    try testing.expect(result.structure.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.structure, "Function") != null);
    try testing.expect(result.imports.len >= 1);
    try testing.expect(result.metrics.total_lines >= 5);
    try testing.expectEqual(0, result.metrics.error_count);
}
