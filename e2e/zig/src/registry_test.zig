// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fb70e01b8c2a331f18e5d9410fc232f6497d9e781de8a99b99b16377c5a34f3f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const tree_sitter_language_pack = @import("tree_sitter_language_pack");

// E2e tests for category: registry

test "get_language_python" {
    // get_language('python') returns a valid Language object
    _ = try tree_sitter_language_pack.get_language("python");
}

test "get_language_unknown" {
    // get_language('nonexistent_xyz') returns an error for unknown language
    const result = tree_sitter_language_pack.get_language("nonexistent_xyz") catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = result;
}

test "get_parser_python" {
    // get_parser('python') returns a valid Parser object
    _ = try tree_sitter_language_pack.get_parser("python");
}

test "get_parser_unknown" {
    // get_parser('nonexistent_xyz') returns an error for unknown language
    const result = tree_sitter_language_pack.get_parser("nonexistent_xyz") catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = result;
}

test "registry_get_language_alias" {
    // get_language('shell') resolves the alias to 'bash' and returns ok
    _ = try tree_sitter_language_pack.get_language("shell");
}

test "registry_get_parser_alias" {
    // get_parser('shell') resolves the alias and returns a Parser
    _ = try tree_sitter_language_pack.get_parser("shell");
}

test "registry_has_language_alias" {
    // has_language('shell') returns true via alias resolution to 'bash'
    const result = try tree_sitter_language_pack.has_language("shell");
    try testing.expect(result);
}

test "registry_has_language_false" {
    // has_language('nonexistent') should return false
    const result = try tree_sitter_language_pack.has_language("nonexistent");
    try testing.expect(!result);
}

test "registry_has_language_true" {
    // has_language('python') should return true
    const result = try tree_sitter_language_pack.has_language("python");
    try testing.expect(result);
}

test "registry_language_count" {
    // language_count returns the number of languages available in the current build/cache
    const result = tree_sitter_language_pack.language_count();
    try testing.expect(result >= 1);
}

test "registry_list_languages" {
    // available_languages should return a non-empty list
    const result = try tree_sitter_language_pack.available_languages();
    try testing.expect(result.len > 0);
}
