// Generated by alef. Do not edit by hand.

package dev.kreuzberg.tslp.android

import java.nio.file.Path

/** JNI-backed wrapper holding a native `Parser` handle. */
class Parser internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Configure the parser to use the language identified by name (e.g. `"python"`).
    //
    // Resolves the language through the global registry — auto-downloading
    // if necessary, when the `download` feature is enabled.
    //
    // # Errors
    //
    // Returns [`Error::LanguageNotFound`] if the language is not recognized,
    // or [`Error::ParserSetup`] if the language ABI is incompatible.
    fun setLanguage(name: String): Unit {
        TreeSitterLanguagePackBridge.nativeParserSetLanguage(handle, MAPPER.writeValueAsString(name))
    }

    // Parse a UTF-8 source string. Returns `None` if parsing was cancelled
    // or no language is set.
    fun parse(source: String): Tree? {
        val responseJson = TreeSitterLanguagePackBridge.nativeParserParse(handle, MAPPER.writeValueAsString(source))
        return MAPPER.readValue(responseJson, Tree::class.java)
    }

    // Parse a raw byte slice. Returns `None` if parsing was cancelled or
    // no language is set.
    fun parseBytes(source: ByteArray): Tree? {
        val responseJson = TreeSitterLanguagePackBridge.nativeParserParseBytes(handle, MAPPER.writeValueAsString(source))
        return MAPPER.readValue(responseJson, Tree::class.java)
    }

    // Reset internal state. The next call to [`parse`](Self::parse) will
    // not be incremental.
    fun reset(): Unit {
        TreeSitterLanguagePackBridge.nativeParserReset(handle)
    }

    override fun close() { TreeSitterLanguagePackBridge.nativeFreeParser(handle) }
}
/** JNI-backed wrapper holding a native `Tree` handle. */
class Tree internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Return the root [`Node`] of this tree.
    fun rootNode(): Node {
        val responseJson = TreeSitterLanguagePackBridge.nativeTreeRootNode(handle)
        return MAPPER.readValue(responseJson, Node::class.java)
    }

    // Return a [`TreeCursor`] positioned at the root.
    fun walk(): TreeCursor {
        val responseJson = TreeSitterLanguagePackBridge.nativeTreeWalk(handle)
        return MAPPER.readValue(responseJson, TreeCursor::class.java)
    }

    override fun close() { TreeSitterLanguagePackBridge.nativeFreeTree(handle) }
}
/** JNI-backed wrapper holding a native `Node` handle. */
class Node internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    fun clone(): Node {
        val responseJson = TreeSitterLanguagePackBridge.nativeNodeClone(handle)
        return MAPPER.readValue(responseJson, Node::class.java)
    }

    // Return the node's kind name (e.g. `"function_definition"`).
    fun kind(): String {
        return TreeSitterLanguagePackBridge.nativeNodeKind(handle)
    }

    // Return the node's numeric kind ID.
    fun kindId(): Short {
        return TreeSitterLanguagePackBridge.nativeNodeKindId(handle)
    }

    // Return the inclusive start byte offset of this node.
    fun startByte(): Long {
        return TreeSitterLanguagePackBridge.nativeNodeStartByte(handle)
    }

    // Return the exclusive end byte offset of this node.
    fun endByte(): Long {
        return TreeSitterLanguagePackBridge.nativeNodeEndByte(handle)
    }

    // Return the node's byte range as a [`ByteRange`].
    //
    // Callers should slice their own source bytes — this is a zero-copy
    // text accessor.
    fun byteRange(): ByteRange {
        val responseJson = TreeSitterLanguagePackBridge.nativeNodeByteRange(handle)
        return MAPPER.readValue(responseJson, ByteRange::class.java)
    }

    // Return the start [`Point`] (row, column).
    fun startPosition(): Point {
        val responseJson = TreeSitterLanguagePackBridge.nativeNodeStartPosition(handle)
        return MAPPER.readValue(responseJson, Point::class.java)
    }

    // Return the end [`Point`] (row, column).
    fun endPosition(): Point {
        val responseJson = TreeSitterLanguagePackBridge.nativeNodeEndPosition(handle)
        return MAPPER.readValue(responseJson, Point::class.java)
    }

    // True when this node is named (not punctuation/whitespace).
    fun isNamed(): Boolean {
        return TreeSitterLanguagePackBridge.nativeNodeIsNamed(handle)
    }

    // True when this is an error node.
    fun isError(): Boolean {
        return TreeSitterLanguagePackBridge.nativeNodeIsError(handle)
    }

    // True when this is a missing-token node.
    fun isMissing(): Boolean {
        return TreeSitterLanguagePackBridge.nativeNodeIsMissing(handle)
    }

    // True when this is an "extra" node (e.g. a comment).
    fun isExtra(): Boolean {
        return TreeSitterLanguagePackBridge.nativeNodeIsExtra(handle)
    }

    // True when this node or any descendant is an error.
    fun hasError(): Boolean {
        return TreeSitterLanguagePackBridge.nativeNodeHasError(handle)
    }

    // Return this node's parent, if any.
    fun parent(): Node? {
        val responseJson = TreeSitterLanguagePackBridge.nativeNodeParent(handle)
        return MAPPER.readValue(responseJson, Node::class.java)
    }

    // Return the i-th child of this node, if any.
    fun child(index: Int): Node? {
        val responseJson = TreeSitterLanguagePackBridge.nativeNodeChild(handle, MAPPER.writeValueAsString(index))
        return MAPPER.readValue(responseJson, Node::class.java)
    }

    // Total number of children (including unnamed).
    fun childCount(): Long {
        return TreeSitterLanguagePackBridge.nativeNodeChildCount(handle)
    }

    // Return the i-th named child of this node, if any.
    fun namedChild(index: Int): Node? {
        val responseJson = TreeSitterLanguagePackBridge.nativeNodeNamedChild(handle, MAPPER.writeValueAsString(index))
        return MAPPER.readValue(responseJson, Node::class.java)
    }

    // Number of named children of this node.
    fun namedChildCount(): Long {
        return TreeSitterLanguagePackBridge.nativeNodeNamedChildCount(handle)
    }

    // Look up a child by its grammar-defined field name.
    fun childByFieldName(name: String): Node? {
        val responseJson = TreeSitterLanguagePackBridge.nativeNodeChildByFieldName(handle, MAPPER.writeValueAsString(name))
        return MAPPER.readValue(responseJson, Node::class.java)
    }

    // Return the S-expression form of this node's subtree.
    fun toSexp(): String {
        return TreeSitterLanguagePackBridge.nativeNodeToSexp(handle)
    }

    // Return a [`TreeCursor`] positioned at this node.
    fun walk(): TreeCursor {
        val responseJson = TreeSitterLanguagePackBridge.nativeNodeWalk(handle)
        return MAPPER.readValue(responseJson, TreeCursor::class.java)
    }

    override fun close() { TreeSitterLanguagePackBridge.nativeFreeNode(handle) }
}
/** JNI-backed wrapper holding a native `TreeCursor` handle. */
class TreeCursor internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Return the [`Node`] at the cursor's current position.
    fun node(): Node {
        val responseJson = TreeSitterLanguagePackBridge.nativeTreeCursorNode(handle)
        return MAPPER.readValue(responseJson, Node::class.java)
    }

    // Move the cursor to the first child of the current node.
    // Returns `true` if a child existed.
    fun gotoFirstChild(): Boolean {
        return TreeSitterLanguagePackBridge.nativeTreeCursorGotoFirstChild(handle)
    }

    // Move the cursor to the parent of the current node.
    // Returns `true` if a parent existed.
    fun gotoParent(): Boolean {
        return TreeSitterLanguagePackBridge.nativeTreeCursorGotoParent(handle)
    }

    // Move the cursor to the next sibling of the current node.
    // Returns `true` if a sibling existed.
    fun gotoNextSibling(): Boolean {
        return TreeSitterLanguagePackBridge.nativeTreeCursorGotoNextSibling(handle)
    }

    // Return the field name for the current node, if any.
    fun fieldName(): String? {
        return TreeSitterLanguagePackBridge.nativeTreeCursorFieldName(handle)
    }

    override fun close() { TreeSitterLanguagePackBridge.nativeFreeTreeCursor(handle) }
}
/** JNI-backed wrapper holding a native `LanguageRegistry` handle. */
class LanguageRegistry internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Add an additional directory to search for dynamic libraries.
    //
    // When [`get_language`](Self::get_language) cannot find a grammar in the
    // primary library directory, it searches these extra directories in order.
    // Typically used by the download system to register its cache directory.
    //
    // Takes `&self` (not `&mut self`) because `extra_lib_dirs` uses interior
    // mutability via an `Arc<RwLock<...>>`, so the outer registry can remain
    // immutable while the directory list is updated.
    fun addExtraLibsDir(dir: Path): Unit {
        TreeSitterLanguagePackBridge.nativeLanguageRegistryAddExtraLibsDir(handle, MAPPER.writeValueAsString(dir))
    }

    // Get a tree-sitter [`Language`] by name.
    //
    // Resolves aliases (e.g., `"shell"` -> `"bash"`, `"makefile"` -> `"make"`),
    // then looks up the language in the static table. When the `dynamic-loading`
    // feature is enabled, falls back to loading a shared library on demand.
    //
    // # Errors
    //
    // Returns [`Error::LanguageNotFound`] if the name (after alias resolution)
    // does not match any known grammar.
    fun getLanguage(name: String): Language {
        val responseJson = TreeSitterLanguagePackBridge.nativeLanguageRegistryGetLanguage(handle, MAPPER.writeValueAsString(name))
        return MAPPER.readValue(responseJson, Language::class.java)
    }

    // List all available language names, sorted and deduplicated.
    //
    // Includes statically compiled languages, dynamically loadable languages
    // (if the `dynamic-loading` feature is enabled), and all configured aliases.
    fun availableLanguages(): List<String> {
        val responseJson = TreeSitterLanguagePackBridge.nativeLanguageRegistryAvailableLanguages(handle)
        return MAPPER.readValue(responseJson, List<String>::class.java)
    }

    // Check whether a language is available by name or alias.
    //
    // Returns `true` if the language can be loaded, either from the static
    // table or from a dynamic library on disk.
    fun hasLanguage(name: String): Boolean {
        return TreeSitterLanguagePackBridge.nativeLanguageRegistryHasLanguage(handle, MAPPER.writeValueAsString(name))
    }

    // Return the total number of available languages (including aliases).
    fun languageCount(): Long {
        return TreeSitterLanguagePackBridge.nativeLanguageRegistryLanguageCount(handle)
    }

    // Parse source code and extract file intelligence based on config in a single pass.
    fun process(source: String, config: ProcessConfig): ProcessResult {
        val responseJson = TreeSitterLanguagePackBridge.nativeLanguageRegistryProcess(handle, MAPPER.writeValueAsString(mapOf("source" to source, "config" to config)))
        return MAPPER.readValue(responseJson, ProcessResult::class.java)
    }

    override fun close() { TreeSitterLanguagePackBridge.nativeFreeLanguageRegistry(handle) }
}
/** JNI-backed wrapper holding a native `DownloadManager` handle. */
class DownloadManager internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Return the path to the libs cache directory.
    fun cacheDir(): Path {
        return TreeSitterLanguagePackBridge.nativeDownloadManagerCacheDir(handle)
    }

    // List languages that are already downloaded and cached.
    fun installedLanguages(): List<String> {
        val responseJson = TreeSitterLanguagePackBridge.nativeDownloadManagerInstalledLanguages(handle)
        return MAPPER.readValue(responseJson, List<String>::class.java)
    }

    // Ensure the specified languages are available in the cache.
    // Downloads the platform bundle if any requested languages are missing.
    fun ensureLanguages(names: List<String>): Unit {
        TreeSitterLanguagePackBridge.nativeDownloadManagerEnsureLanguages(handle, MAPPER.writeValueAsString(names))
    }

    // Ensure all languages in a named group are available.
    fun ensureGroup(group: String): Unit {
        TreeSitterLanguagePackBridge.nativeDownloadManagerEnsureGroup(handle, MAPPER.writeValueAsString(group))
    }

    // Get the expected path for a language's shared library in the cache.
    fun libPath(name: String): Path {
        return TreeSitterLanguagePackBridge.nativeDownloadManagerLibPath(handle, MAPPER.writeValueAsString(name))
    }

    // Fetch the parser manifest from GitHub Releases.
    fun fetchManifest(): ParserManifest {
        val responseJson = TreeSitterLanguagePackBridge.nativeDownloadManagerFetchManifest(handle)
        return MAPPER.readValue(responseJson, ParserManifest::class.java)
    }

    // Download the platform bundle and extract every library file it contains.
    //
    // Unlike [`ensure_languages`], this does not check the manifest language list
    // against archive contents — it simply extracts all `.so`/`.dylib`/`.dll` files
    // from the bundle. Languages in the manifest that are missing from the archive
    // are silently ignored rather than returning an error.
    //
    // Returns the number of library files extracted (including those already cached).
    fun downloadAllBestEffort(): Long {
        return TreeSitterLanguagePackBridge.nativeDownloadManagerDownloadAllBestEffort(handle)
    }

    // Remove all cached parser libraries.
    fun cleanCache(): Unit {
        TreeSitterLanguagePackBridge.nativeDownloadManagerCleanCache(handle)
    }

    override fun close() { TreeSitterLanguagePackBridge.nativeFreeDownloadManager(handle) }
}
