// Generated by alef. Do not edit by hand.
// alef:hash:76f3bac7536fa4e5b8ab9a93be197508306d06b566fe7cc519a38c1b9ebf9f42
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
)

package dev.kreuzberg.tslp.android

/**
 * The format of a docstring extracted from source code.
 *
 * Identifies the docstring convention used, which varies by language
 * (e.g., Python triple-quoted strings, JSDoc, Rustdoc `///` comments).
 */
@com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = DocstringFormatDeserializer::class)
@com.fasterxml.jackson.databind.annotation.JsonSerialize(using = DocstringFormatSerializer::class)
sealed class DocstringFormat {
    /** Python triple-quoted string docstring (`"""..."""`). */
    object PythonTripleQuote : DocstringFormat()
    /** JavaScript/TypeScript JSDoc comment (`/ ** ... * /`). */
    object JSDoc : DocstringFormat()
    /** Rust `///` or `//!` doc comment. */
    object Rustdoc : DocstringFormat()
    /** Go doc comment (a comment block immediately preceding a declaration). */
    object GoDoc : DocstringFormat()
    /** Java Javadoc comment (`/ ** ... * /`). */
    object JavaDoc : DocstringFormat()
    /** A language-specific docstring format not covered by the standard variants. */
    @com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = com.fasterxml.jackson.databind.JsonDeserializer.None::class)
    @com.fasterxml.jackson.databind.annotation.JsonSerialize(using = com.fasterxml.jackson.databind.JsonSerializer.None::class)
    data class Other(
        val value: String,
    ) : DocstringFormat()
}

private class DocstringFormatDeserializer : com.fasterxml.jackson.databind.deser.std.StdDeserializer<DocstringFormat>(DocstringFormat::class.java) {
    @Suppress("LongMethod")
    override fun deserialize(
        parser: com.fasterxml.jackson.core.JsonParser,
        ctx: com.fasterxml.jackson.databind.DeserializationContext,
    ): DocstringFormat {
        val node = parser.codec.readTree<com.fasterxml.jackson.databind.node.ObjectNode>(parser)
        val tag = node.get("kind")?.asText()
        @Suppress("UNCHECKED_CAST")
        val payload = (node.deepCopy() as com.fasterxml.jackson.databind.node.ObjectNode).apply { remove("kind") }
        return when (tag) {
            "PythonTripleQuote" -> DocstringFormat.PythonTripleQuote
            "JSDoc" -> DocstringFormat.JSDoc
            "Rustdoc" -> DocstringFormat.Rustdoc
            "GoDoc" -> DocstringFormat.GoDoc
            "JavaDoc" -> DocstringFormat.JavaDoc
            "Other" -> ctx.readTreeAsValue<DocstringFormat.Other>(payload, DocstringFormat.Other::class.java)
            else -> throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
                parser, "Unknown DocstringFormat tag", tag, DocstringFormat::class.java,
            )
        }
    }
}

private class DocstringFormatSerializer : com.fasterxml.jackson.databind.ser.std.StdSerializer<DocstringFormat>(DocstringFormat::class.java) {
    @Suppress("LongMethod")
    override fun serialize(
        value: DocstringFormat,
        gen: com.fasterxml.jackson.core.JsonGenerator,
        provider: com.fasterxml.jackson.databind.SerializerProvider,
    ) {
        @Suppress("UNCHECKED_CAST")
        val mapper = (gen.codec as? com.fasterxml.jackson.databind.ObjectMapper) ?: com.fasterxml.jackson.databind.ObjectMapper().findAndRegisterModules()
        val node: com.fasterxml.jackson.databind.node.ObjectNode = when (value) {
            is DocstringFormat.PythonTripleQuote -> {
                val n = mapper.createObjectNode()
                n.put("kind", "PythonTripleQuote")
                n
            }
            is DocstringFormat.JSDoc -> {
                val n = mapper.createObjectNode()
                n.put("kind", "JSDoc")
                n
            }
            is DocstringFormat.Rustdoc -> {
                val n = mapper.createObjectNode()
                n.put("kind", "Rustdoc")
                n
            }
            is DocstringFormat.GoDoc -> {
                val n = mapper.createObjectNode()
                n.put("kind", "GoDoc")
                n
            }
            is DocstringFormat.JavaDoc -> {
                val n = mapper.createObjectNode()
                n.put("kind", "JavaDoc")
                n
            }
            is DocstringFormat.Other -> {
                @Suppress("UNCHECKED_CAST")
                val n = mapper.valueToTree<com.fasterxml.jackson.databind.node.ObjectNode>(value as DocstringFormat.Other) as com.fasterxml.jackson.databind.node.ObjectNode
                n.put("kind", "Other")
                n
            }
        }
        mapper.writeTree(gen, node)
    }
}
