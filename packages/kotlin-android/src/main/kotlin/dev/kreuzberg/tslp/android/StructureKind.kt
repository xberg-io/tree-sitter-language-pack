// Generated by alef. Do not edit by hand.
// alef:hash:76f3bac7536fa4e5b8ab9a93be197508306d06b566fe7cc519a38c1b9ebf9f42
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
)

package dev.kreuzberg.tslp.android

/**
 * The kind of structural item found in source code.
 *
 * Categorizes top-level and nested declarations such as functions, classes,
 * structs, enums, traits, and more. Use `Other` for
 * language-specific constructs that do not fit a standard category.
 */
@com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = StructureKindDeserializer::class)
@com.fasterxml.jackson.databind.annotation.JsonSerialize(using = StructureKindSerializer::class)
sealed class StructureKind {
    /** A free-standing or associated function. */
    object Function : StructureKind()
    /** A method defined inside a class, struct, trait, or impl block. */
    object Method : StructureKind()
    /** A class definition. */
    object Class : StructureKind()
    /** A struct definition. */
    object Struct : StructureKind()
    /** An interface or protocol definition. */
    object Interface : StructureKind()
    /** An enum definition. */
    object Enum : StructureKind()
    /** A module or package declaration. */
    object Module : StructureKind()
    /** A trait definition. */
    object Trait : StructureKind()
    /** An impl block (Rust) or similar implementation block. */
    object Impl : StructureKind()
    /** A namespace declaration. */
    object Namespace : StructureKind()
    /**
     * A language-specific construct that does not fit any standard category.
     * The `value` field carries the language-specific kind label.
     */
    @com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = com.fasterxml.jackson.databind.JsonDeserializer.None::class)
    @com.fasterxml.jackson.databind.annotation.JsonSerialize(using = com.fasterxml.jackson.databind.JsonSerializer.None::class)
    data class Other(
        val value: String,
    ) : StructureKind()
}

private class StructureKindDeserializer : com.fasterxml.jackson.databind.deser.std.StdDeserializer<StructureKind>(StructureKind::class.java) {
    @Suppress("LongMethod")
    override fun deserialize(
        parser: com.fasterxml.jackson.core.JsonParser,
        ctx: com.fasterxml.jackson.databind.DeserializationContext,
    ): StructureKind {
        val node = parser.codec.readTree<com.fasterxml.jackson.databind.node.ObjectNode>(parser)
        val tag = node.get("kind")?.asText()
        @Suppress("UNCHECKED_CAST")
        val payload = (node.deepCopy() as com.fasterxml.jackson.databind.node.ObjectNode).apply { remove("kind") }
        return when (tag) {
            "Function" -> StructureKind.Function
            "Method" -> StructureKind.Method
            "Class" -> StructureKind.Class
            "Struct" -> StructureKind.Struct
            "Interface" -> StructureKind.Interface
            "Enum" -> StructureKind.Enum
            "Module" -> StructureKind.Module
            "Trait" -> StructureKind.Trait
            "Impl" -> StructureKind.Impl
            "Namespace" -> StructureKind.Namespace
            "Other" -> ctx.readTreeAsValue<StructureKind.Other>(payload, StructureKind.Other::class.java)
            else -> throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
                parser, "Unknown StructureKind tag", tag, StructureKind::class.java,
            )
        }
    }
}

private class StructureKindSerializer : com.fasterxml.jackson.databind.ser.std.StdSerializer<StructureKind>(StructureKind::class.java) {
    @Suppress("LongMethod")
    override fun serialize(
        value: StructureKind,
        gen: com.fasterxml.jackson.core.JsonGenerator,
        provider: com.fasterxml.jackson.databind.SerializerProvider,
    ) {
        @Suppress("UNCHECKED_CAST")
        val mapper = (gen.codec as? com.fasterxml.jackson.databind.ObjectMapper) ?: com.fasterxml.jackson.databind.ObjectMapper().findAndRegisterModules()
        val node: com.fasterxml.jackson.databind.node.ObjectNode = when (value) {
            is StructureKind.Function -> {
                val n = mapper.createObjectNode()
                n.put("kind", "Function")
                n
            }
            is StructureKind.Method -> {
                val n = mapper.createObjectNode()
                n.put("kind", "Method")
                n
            }
            is StructureKind.Class -> {
                val n = mapper.createObjectNode()
                n.put("kind", "Class")
                n
            }
            is StructureKind.Struct -> {
                val n = mapper.createObjectNode()
                n.put("kind", "Struct")
                n
            }
            is StructureKind.Interface -> {
                val n = mapper.createObjectNode()
                n.put("kind", "Interface")
                n
            }
            is StructureKind.Enum -> {
                val n = mapper.createObjectNode()
                n.put("kind", "Enum")
                n
            }
            is StructureKind.Module -> {
                val n = mapper.createObjectNode()
                n.put("kind", "Module")
                n
            }
            is StructureKind.Trait -> {
                val n = mapper.createObjectNode()
                n.put("kind", "Trait")
                n
            }
            is StructureKind.Impl -> {
                val n = mapper.createObjectNode()
                n.put("kind", "Impl")
                n
            }
            is StructureKind.Namespace -> {
                val n = mapper.createObjectNode()
                n.put("kind", "Namespace")
                n
            }
            is StructureKind.Other -> {
                @Suppress("UNCHECKED_CAST")
                val n = mapper.valueToTree<com.fasterxml.jackson.databind.node.ObjectNode>(value as StructureKind.Other) as com.fasterxml.jackson.databind.node.ObjectNode
                n.put("kind", "Other")
                n
            }
        }
        mapper.writeTree(gen, node)
    }
}
