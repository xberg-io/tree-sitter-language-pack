// Generated by alef. Do not edit by hand.
// alef:hash:ebb3be35bbc4cc18796f3a64783f3672cbe0d1a7e8347852ae893bf713fb937f
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
    "ReturnCount",
    "UnusedParameter",
)

package io.xberg.tslp.android

/**
 * Configuration for the `process()` function.
 *
 * Controls which analysis features are enabled and whether chunking is performed.
 */
data class ProcessConfig(
    /** Language name (required). */
    val language: String = "",
    /** Extract structural items (functions, classes, etc.). Default: true. */
    val structure: Boolean = true,
    /** Extract import statements. Default: true. */
    val imports: Boolean = true,
    /** Extract export statements. Default: true. */
    val exports: Boolean = true,
    /** Extract comments. Default: false. */
    val comments: Boolean = false,
    /** Extract docstrings. Default: false. */
    val docstrings: Boolean = false,
    /** Extract symbol definitions. Default: false. */
    val symbols: Boolean = false,
    /** Include parse diagnostics. Default: false. */
    val diagnostics: Boolean = false,
    /**
     * Maximum chunk size in bytes. `null` disables chunking.
     *
     * `Some(0)` is rejected by `ProcessConfig.validate` with
     * `Error.InvalidRange`. A zero-sized chunk
     * limit previously produced an empty chunk list and silently discarded the
     * whole source; use `null` to mean "do not chunk".
     */
    val chunkMaxSize: Long? = null,
    /**
     * Extract hierarchical key/value data tree from data-format files. Default: false.
     *
     * When `true`, `ProcessResult.data` is populated
     * with a `DataNode` tree for supported languages: JSON, YAML,
     * TOML, `.properties`, HCL/HOCON, INI, editorconfig, KDL, CUE, CSV, PSV, PO,
     * nginx config, Caddy config, XML, and DTD.
     *
     * For languages outside this set the field is left as `null`.
     */
    val dataExtraction: Boolean = false,
    /**
     * Reject source longer than this many bytes instead of parsing it.
     * Default: `null` (unbounded).
     *
     * Tree-sitter allocates and walks proportionally to input size, so an
     * unbounded parse of attacker-supplied input is a denial-of-service vector.
     * The default stays unbounded for backward compatibility; services handling
     * untrusted input should opt in, e.g. with
     * `RECOMMENDED_MAX_SOURCE_BYTES`.
     *
     * Exceeding the limit fails the call with
     * `Error.InvalidRange` — the source is never
     * silently truncated.
     */
    val maxSourceBytes: Long? = null,
    /**
     * Wall-clock budget for the parse step, in milliseconds.
     * Default: `null` (no timeout).
     *
     * Enforced through tree-sitter's parse progress callback, which the parser
     * invokes periodically; cancellation is therefore granular to that callback
     * interval rather than exact. A parse that exceeds the budget fails with
     * `Error.ParseTimeout`.
     */
    val parseTimeoutMs: Long? = null,
) {

    /** Enable chunking with the given maximum chunk size in bytes. */
    fun withChunking(maxSize: Long): ProcessConfig {
        val selfJson = VALUE_METHOD_MAPPER.writeValueAsString(this)
        val requestJson = VALUE_METHOD_MAPPER.writeValueAsString(
            mapOf(
                "max_size" to maxSize,
            ),
        )
        val resultJson = TreeSitterLanguagePackBridge.nativeProcessConfigWithChunking(selfJson, requestJson)
        return VALUE_METHOD_MAPPER.readValue(
            resultJson,
            object : com.fasterxml.jackson.core.type.TypeReference<ProcessConfig>() {},
        )
    }

    /**
     * Enable every analysis feature, including data extraction.
     *
     * Chunking is not an analysis feature and stays off; enable it with
     * `with_chunking`.
     */
    fun all(): ProcessConfig {
        val selfJson = VALUE_METHOD_MAPPER.writeValueAsString(this)
        val resultJson = TreeSitterLanguagePackBridge.nativeProcessConfigAll(selfJson)
        return VALUE_METHOD_MAPPER.readValue(
            resultJson,
            object : com.fasterxml.jackson.core.type.TypeReference<ProcessConfig>() {},
        )
    }

    /** Disable all analysis features (only metrics computed). */
    fun minimal(): ProcessConfig {
        val selfJson = VALUE_METHOD_MAPPER.writeValueAsString(this)
        val resultJson = TreeSitterLanguagePackBridge.nativeProcessConfigMinimal(selfJson)
        return VALUE_METHOD_MAPPER.readValue(
            resultJson,
            object : com.fasterxml.jackson.core.type.TypeReference<ProcessConfig>() {},
        )
    }

    /**
     * Enable or disable hierarchical data extraction for data-format files.
     *
     * When `true`, `ProcessResult.data` is
     * populated with a key/value tree for supported data-format languages.
     */
    fun withDataExtraction(enabled: Boolean): ProcessConfig {
        val selfJson = VALUE_METHOD_MAPPER.writeValueAsString(this)
        val requestJson = VALUE_METHOD_MAPPER.writeValueAsString(
            mapOf(
                "enabled" to enabled,
            ),
        )
        val resultJson = TreeSitterLanguagePackBridge.nativeProcessConfigWithDataExtraction(selfJson, requestJson)
        return VALUE_METHOD_MAPPER.readValue(
            resultJson,
            object : com.fasterxml.jackson.core.type.TypeReference<ProcessConfig>() {},
        )
    }

    /**
     * Check that the configured limits are usable before they drive a parse.
     *
     * Called by `process` and
     * `LanguageRegistry.process`; call it
     * directly to reject a bad configuration early.
     *
     * **Errors:**
     *
     * Returns `Error.InvalidRange` when
     * `chunk_max_size`, `max_source_bytes`, or `parse_timeout_ms` is `Some(0)`.
     * Zero is always a configuration mistake: `null` is how each of these is
     * disabled, so `Some(0)` could only mean "produce nothing".
     */
    fun validate(): Unit {
        val selfJson = VALUE_METHOD_MAPPER.writeValueAsString(this)
        TreeSitterLanguagePackBridge.nativeProcessConfigValidate(selfJson)
    }
}

// Marshals the receiver and the parameter object across the value-method JNI
// boundary. The naming strategy, null handling, and unknown-property tolerance
// must match the module facade's mapper — both feed the same Rust serde types.
private val VALUE_METHOD_MAPPER = com.fasterxml.jackson.module.kotlin.jacksonObjectMapper()
    .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
    .registerModule(
        com.fasterxml.jackson.databind.module.SimpleModule()
            .addSerializer(
                kotlin.time.Duration::class.java,
                object : com.fasterxml.jackson.databind.JsonSerializer<kotlin.time.Duration>() {
                    override fun serialize(
                        value: kotlin.time.Duration,
                        gen: com.fasterxml.jackson.core.JsonGenerator,
                        serializers: com.fasterxml.jackson.databind.SerializerProvider,
                    ) {
                        gen.writeNumber(value.inWholeMilliseconds)
                    }
                },
            )
            .addDeserializer(
                kotlin.time.Duration::class.java,
                object : com.fasterxml.jackson.databind.JsonDeserializer<kotlin.time.Duration>() {
                    override fun deserialize(
                        p: com.fasterxml.jackson.core.JsonParser,
                        ctxt: com.fasterxml.jackson.databind.DeserializationContext,
                    ): kotlin.time.Duration = with(kotlin.time.Duration) { p.longValue.milliseconds }
                },
            ),
    )
    .registerModule(
        com.fasterxml.jackson.module.kotlin.KotlinModule.Builder()
            .configure(com.fasterxml.jackson.module.kotlin.KotlinFeature.NullIsSameAsDefault, true)
            .configure(com.fasterxml.jackson.module.kotlin.KotlinFeature.NullToEmptyCollection, true)
            .configure(com.fasterxml.jackson.module.kotlin.KotlinFeature.NullToEmptyMap, true)
            .build(),
    )
    .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    .setDefaultPropertyInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
    .configure(com.fasterxml.jackson.databind.DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, false)
