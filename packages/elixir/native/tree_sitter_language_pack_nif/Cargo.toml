# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:8c009c19f2384d816f968d90cd8dd05b01005a7d617f863dc58cfb3b21424b45
# To regenerate: alef generate
# To verify freshness: alef verify

[package]
name = "tree_sitter_language_pack_nif"
version = "1.19.0"
edition = "2024"
license = "MIT"
description = "Pre-compiled tree-sitter grammars for 371 programming languages"
readme = false
keywords = ["language-pack", "parser", "syntax", "tree-sitter"]
categories = []

[package.metadata.cargo-machete]
ignored = ["alloc-no-stdlib", "alloc-stdlib", "brotli-decompressor"]

[workspace]

[lib]
name = "tree_sitter_language_pack_nif"

crate-type = ["cdylib"]

[features]
default = ["download"]
download = ["tree-sitter-language-pack/download"]

[dependencies]
alloc-no-stdlib = "=3.0.0"
alloc-stdlib = "=0.3.0"
brotli-decompressor = "=6.0.0"
rustler = "0.38"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tree-sitter-language-pack = { version = "1.19.0", path = "../../../../crates/ts-pack-core", features = ["serde", "config", "download"] }
[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(feature, values("download"))'] }

# This crate deliberately does not use `[lints]` / `workspace = true`: its C-ABI /
# PyO3 / napi / ext-php-rs / NIF boundary requires `unsafe` code, and the workspace's
# `[workspace.lints.rust]` sets `unsafe_code = "deny"` -- an all-or-nothing table that
# would turn every such boundary into a compile error. The `[lints.clippy]` block below
# instead carries the subset of the workspace's deny-by-default lint policy this crate
# can actually satisfy. ~keep
[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
