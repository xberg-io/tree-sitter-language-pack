# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:4bdab1777b8127e2485c15a13425c51516b5d15a5d96013fb5726b7f54d8f93c
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule TreeSitterLanguagePack.DocstringFormat do
  @moduledoc """
  The format of a docstring extracted from source code.

  Identifies the docstring convention used, which varies by language
  (e.g., Python triple-quoted strings, JSDoc, Rustdoc `///` comments).

  # Wire format (public JSON contract)

  Unit variants serialize as a bare string (`"JSDoc"`); the `Other`
  variant serializes as a single-keyed object (`{"Other": "rst"}`). DO
  NOT add `#[serde(tag = "...")]`. Covered by `tests/wire_format.rs`.
  """

  @typedoc "The format of a docstring extracted from source code."
  @type t :: term()

  @typedoc "Python triple-quoted string docstring (`\"\"\"...\"\"\"`)."
  @type python_triple_quote :: :python_triple_quote
  @typedoc "JavaScript/TypeScript JSDoc block comment (opens with two stars, closes with star-slash)."
  @type js_doc :: :js_doc
  @typedoc "Rust `///` or `//!` doc comment."
  @type rustdoc :: :rustdoc
  @typedoc "Go doc comment (a comment block immediately preceding a declaration)."
  @type go_doc :: :go_doc
  @typedoc "Java Javadoc block comment (opens with two stars, closes with star-slash)."
  @type java_doc :: :java_doc
  @typedoc "A language-specific docstring format not covered by the standard variants."
  @type other :: %{type: :other, value: String.t()}

  @doc "Returns the serde wire value this variant carries on the JSON boundary (see `wire_variant_value` in alef's naming module) -- distinct from the atom's Elixir spelling."
  @spec wire_value(t()) :: String.t()
  def wire_value(:python_triple_quote), do: "PythonTripleQuote"
  def wire_value(:js_doc), do: "JSDoc"
  def wire_value(:rustdoc), do: "Rustdoc"
  def wire_value(:go_doc), do: "GoDoc"
  def wire_value(:java_doc), do: "JavaDoc"
  def wire_value(:other), do: "Other"
  def wire_value(value) when is_tuple(value), do: wire_value(elem(value, 0))
  def wire_value(value) when is_map(value), do: Map.fetch!(value, :type)

  defimpl String.Chars, for: TreeSitterLanguagePack.DocstringFormat do
    @doc false
    def to_string(value), do: TreeSitterLanguagePack.DocstringFormat.wire_value(value)
  end
end
