# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:f6ff272069ed976a7982d245579124486b3a15dd7b4c700a34b13f45a0a26e86
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
defmodule TreeSitterLanguagePack.DownloadManager do
  @moduledoc "Manages downloading and caching of pre-built parser shared libraries."

  alias TreeSitterLanguagePack.Native

  defstruct [:ref]

  @type t :: %__MODULE__{ref: reference()}

  @doc "Create a new download manager for the given version."
  def new(version) do
    Native.downloadmanager_new(version)
  end

  @doc "Create a download manager with a custom cache directory."
  def with_cache_dir(version, cache_dir) do
    Native.downloadmanager_with_cache_dir(version, cache_dir)
  end

  @doc "Default cache directory: `~/.cache/tree-sitter-language-pack/v{version}/libs/`"
  def default_cache_dir(version) do
    Native.downloadmanager_default_cache_dir(version)
  end

  @doc "Return the path to the libs cache directory."
  def cache_dir(obj) do
    Native.downloadmanager_cache_dir(obj.ref)
  end

  @doc "List languages that are already downloaded and cached."
  def installed_languages(obj) do
    Native.downloadmanager_installed_languages(obj.ref)
  end

  @doc "Ensure the specified languages are available in the cache."
  def ensure_languages(obj, names) do
    Native.downloadmanager_ensure_languages(obj.ref, names)
  end

  @doc "Ensure all languages in a named group are available."
  def ensure_group(obj, group) do
    Native.downloadmanager_ensure_group(obj.ref, group)
  end

  @doc "Get the expected path for a language's shared library in the cache."
  def lib_path(obj, name) do
    Native.downloadmanager_lib_path(obj.ref, name)
  end

  @doc "Fetch the parser manifest from GitHub Releases."
  def fetch_manifest(obj) do
    Native.downloadmanager_fetch_manifest(obj.ref)
  end

  @doc "Download the platform bundle and extract every library file it contains."
  def download_all_best_effort(obj) do
    Native.downloadmanager_download_all_best_effort(obj.ref)
  end

  @doc "Remove all cached parser libraries."
  def clean_cache(obj) do
    Native.downloadmanager_clean_cache(obj.ref)
  end
end
