# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:176bd229b9d21fa569b07c6c779fe41ef58085c2c87ed6805a1302365e230eda
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule TreeSitterLanguagePack.StructureKind do
  @moduledoc """
  The kind of structural item found in source code.

  Categorizes top-level and nested declarations such as functions, classes,
  structs, enums, traits, and more. Use [`Other`](StructureKind::Other) for
  language-specific constructs that do not fit a standard category.

  # Wire format (public JSON contract)

  Unit variants serialize as a bare string (`"Function"`); the `Other`
  variant serializes as a single-keyed object (`{"Other": "macro"}`). DO
  NOT add `#[serde(tag = "...")]` or rename variants — every language
  binding has a hand-written deserializer matching this exact shape, and
  any change breaks all bindings' `process()` tests simultaneously.
  Covered by `tests/wire_format.rs`.
  """

  @typedoc "The kind of structural item found in source code."
  @type t :: term()

  @typedoc "A free-standing or associated function."
  @type function_variant :: :function
  @typedoc "A method defined inside a class, struct, trait, or impl block."
  @type method :: :method
  @typedoc "A class definition."
  @type class :: :class
  @typedoc "A struct definition."
  @type struct_variant :: :struct
  @typedoc "An interface or protocol definition."
  @type interface :: :interface
  @typedoc "An enum definition."
  @type enum :: :enum
  @typedoc "A module or package declaration."
  @type module_variant :: :module
  @typedoc "A trait definition."
  @type trait :: :trait
  @typedoc "An impl block (Rust) or similar implementation block."
  @type impl :: :impl
  @typedoc "A namespace declaration."
  @type namespace :: :namespace
  @typedoc "A language-specific construct that does not fit any standard category."
  @type other :: %{type: :other, value: String.t()}

  @doc "Returns the serde wire value this variant carries on the JSON boundary (see `wire_variant_value` in alef's naming module) -- distinct from the atom's Elixir spelling."
  @spec wire_value(t()) :: String.t()
  def wire_value(:function), do: "Function"
  def wire_value(:method), do: "Method"
  def wire_value(:class), do: "Class"
  def wire_value(:struct), do: "Struct"
  def wire_value(:interface), do: "Interface"
  def wire_value(:enum), do: "Enum"
  def wire_value(:module), do: "Module"
  def wire_value(:trait), do: "Trait"
  def wire_value(:impl), do: "Impl"
  def wire_value(:namespace), do: "Namespace"
  def wire_value(:other), do: "Other"
  def wire_value(value) when is_tuple(value), do: wire_value(elem(value, 0))
  def wire_value(value) when is_map(value), do: Map.fetch!(value, :type)

  defimpl String.Chars, for: TreeSitterLanguagePack.StructureKind do
    @doc false
    def to_string(value), do: TreeSitterLanguagePack.StructureKind.wire_value(value)
  end
end
