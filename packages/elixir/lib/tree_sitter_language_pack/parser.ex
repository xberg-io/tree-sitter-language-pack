# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:8c8d8459e65ecf6e07ef0e90a7ca56ce444f5dd3dc7dcd39a71510f9126440ae
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule TreeSitterLanguagePack.Parser do
  @moduledoc """
  A tree-sitter parser configured for one language at a time.

  # Example

  ```no_run
  use tree_sitter_language_pack::Parser;

  let mut parser = Parser::new();
  parser.set_language("python")?;
  let tree = parser.parse("def hello(): pass").expect("parse failed");
  assert_eq!(tree.root_node().kind(), "module");
  # Ok::<(), tree_sitter_language_pack::Error>(())
  ```
  """

  alias TreeSitterLanguagePack.Native
  defstruct [:ref]

  @typedoc "A tree-sitter parser configured for one language at a time."
  @type t :: %__MODULE__{ref: reference()}

  @doc "Build a default instance."
  @spec new() :: t()
  def new do
    %__MODULE__{ref: Native.parser_default()}
  end

  @doc "Configure the parser to use the language identified by name (e.g. `\"python\"`)."
  def set_language(obj, name) do
    Native.parser_set_language(obj.ref, name)
  end

  @doc "Parse a UTF-8 source string. Returns `None` if parsing was cancelled"
  def parse(obj, source) do
    Native.parser_parse(obj.ref, source)
  end

  @doc "Parse a raw byte slice. Returns `None` if parsing was cancelled or"
  def parse_bytes(obj, source) do
    Native.parser_parse_bytes(obj.ref, source)
  end

  @doc "Reset internal state. The next call to [`parse`](Self::parse) will"
  def reset(obj) do
    Native.parser_reset(obj.ref)
  end

  def default() do
    ref = Native.parser_default()
    %__MODULE__{ref: ref}
  end
end
