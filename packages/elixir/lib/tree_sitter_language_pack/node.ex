# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:926a796b392ab8c09225220c250f0b9ace12ecda2da21df552e86ff96d5b8dba
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
defmodule TreeSitterLanguagePack.Node do
  @moduledoc "A single syntax node within a [`Tree`]."

  alias TreeSitterLanguagePack.Native

  defstruct [:ref]

  @type t :: %__MODULE__{ref: reference()}

  def clone(obj) do
    Native.node_clone(obj.ref)
  end

  @doc "Return the node's kind name (e.g. `\"function_definition\"`)."
  def kind(obj) do
    Native.node_kind(obj.ref)
  end

  @doc "Return the node's numeric kind ID."
  def kind_id(obj) do
    Native.node_kind_id(obj.ref)
  end

  @doc "Return the inclusive start byte offset of this node."
  def start_byte(obj) do
    Native.node_start_byte(obj.ref)
  end

  @doc "Return the exclusive end byte offset of this node."
  def end_byte(obj) do
    Native.node_end_byte(obj.ref)
  end

  @doc "Return the node's byte range as a [`ByteRange`]."
  def byte_range(obj) do
    Native.node_byte_range(obj.ref)
  end

  @doc "Return the start [`Point`] (row, column)."
  def start_position(obj) do
    Native.node_start_position(obj.ref)
  end

  @doc "Return the end [`Point`] (row, column)."
  def end_position(obj) do
    Native.node_end_position(obj.ref)
  end

  @doc "True when this node is named (not punctuation/whitespace)."
  def is_named(obj) do
    Native.node_is_named(obj.ref)
  end

  @doc "True when this is an error node."
  def is_error(obj) do
    Native.node_is_error(obj.ref)
  end

  @doc "True when this is a missing-token node."
  def is_missing(obj) do
    Native.node_is_missing(obj.ref)
  end

  @doc "True when this is an \"extra\" node (e.g. a comment)."
  def is_extra(obj) do
    Native.node_is_extra(obj.ref)
  end

  @doc "True when this node or any descendant is an error."
  def has_error(obj) do
    Native.node_has_error(obj.ref)
  end

  @doc "Return this node's parent, if any."
  def parent(obj) do
    Native.node_parent(obj.ref)
  end

  @doc "Return the i-th child of this node, if any."
  def child(obj, index) do
    Native.node_child(obj.ref, index)
  end

  @doc "Total number of children (including unnamed)."
  def child_count(obj) do
    Native.node_child_count(obj.ref)
  end

  @doc "Return the i-th named child of this node, if any."
  def named_child(obj, index) do
    Native.node_named_child(obj.ref, index)
  end

  @doc "Number of named children of this node."
  def named_child_count(obj) do
    Native.node_named_child_count(obj.ref)
  end

  @doc "Look up a child by its grammar-defined field name."
  def child_by_field_name(obj, name) do
    Native.node_child_by_field_name(obj.ref, name)
  end

  @doc "Return the S-expression form of this node's subtree."
  def to_sexp(obj) do
    Native.node_to_sexp(obj.ref)
  end

  @doc "Return a [`TreeCursor`] positioned at this node."
  def walk(obj) do
    Native.node_walk(obj.ref)
  end
end
