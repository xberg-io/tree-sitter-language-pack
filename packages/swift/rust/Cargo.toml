# Generated by alef. Do not edit by hand.
[package]
name = "tree-sitter-language-pack-swift"
version = "1.9.0-rc.7"
edition = "2024"
license = "MIT"

# `ahash`, `async-trait`, `serde`, `serde_json`, and `tokio` are referenced
# conditionally by alef-emitted code (ahash map shims, async streaming
# adapters, JSON DTO conversions, runtime spawn). They are listed
# unconditionally in `[dependencies]` so the manifest is stable across
# regens, and ignored here so cargo-machete does not flag any subset
# whose conditional path the current crate config does not exercise.
[package.metadata.cargo-machete]
ignored = ["ahash", "async-trait", "serde", "serde_json", "tokio"]

[lib]
crate-type = ["cdylib", "staticlib"]
# The `extern "Swift"` block emits linker references that are only resolvable
# when the crate is linked into a Swift target. `cargo test --workspace` on
# pure-Rust runners (e.g. windows-latest) would otherwise fail with
# undefined `__swift_bridge__$*$alef_visit_*` symbols.
test = false
doctest = false
bench = false

[dependencies]
ahash = "0.8"
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
swift-bridge = "0.1.59"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }
tree_sitter_language_pack = { version = "1.9.0-rc.6", path = "../../../crates/ts-pack-core", features = [
    "serde",
    "config",
    "download",
], package = "tree-sitter-language-pack" }

[build-dependencies]
swift-bridge-build = "0.1.59"
