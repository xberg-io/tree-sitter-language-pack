#ifndef TS_PACK_H
#define TS_PACK_H

#pragma once

/* This file is auto-generated by alef. DO NOT EDIT. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>
/* Opaque type forward declarations */
typedef struct TS_PACKChunkContext TS_PACKChunkContext;
typedef struct TS_PACKCodeChunk TS_PACKCodeChunk;
typedef struct TS_PACKCommentInfo TS_PACKCommentInfo;
typedef struct TS_PACKCommentKind TS_PACKCommentKind;
typedef struct TS_PACKDiagnostic TS_PACKDiagnostic;
typedef struct TS_PACKDiagnosticSeverity TS_PACKDiagnosticSeverity;
typedef struct TS_PACKDocSection TS_PACKDocSection;
typedef struct TS_PACKDocstringFormat TS_PACKDocstringFormat;
typedef struct TS_PACKDocstringInfo TS_PACKDocstringInfo;
typedef struct TS_PACKDownloadManager TS_PACKDownloadManager;
typedef struct TS_PACKExportInfo TS_PACKExportInfo;
typedef struct TS_PACKExportKind TS_PACKExportKind;
typedef struct TS_PACKFileMetrics TS_PACKFileMetrics;
typedef struct TS_PACKImportInfo TS_PACKImportInfo;
typedef struct TS_PACKLanguage TS_PACKLanguage;
typedef struct TS_PACKLanguageInfo TS_PACKLanguageInfo;
typedef struct TS_PACKLanguageRegistry TS_PACKLanguageRegistry;
typedef struct TS_PACKPackConfig TS_PACKPackConfig;
typedef struct TS_PACKParser TS_PACKParser;
typedef struct TS_PACKParserManifest TS_PACKParserManifest;
typedef struct TS_PACKPlatformBundle TS_PACKPlatformBundle;
typedef struct TS_PACKProcessConfig TS_PACKProcessConfig;
typedef struct TS_PACKProcessResult TS_PACKProcessResult;
typedef struct TS_PACKSpan TS_PACKSpan;
typedef struct TS_PACKStructureItem TS_PACKStructureItem;
typedef struct TS_PACKStructureKind TS_PACKStructureKind;
typedef struct TS_PACKSymbolInfo TS_PACKSymbolInfo;
typedef struct TS_PACKSymbolKind TS_PACKSymbolKind;
typedef struct TS_PACKTree TS_PACKTree;


/**
 * Return the last error code (0 means no error).
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_last_error_code(void);

/**
 * Return the last error message. The pointer is valid until the next FFI call on this thread.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
const char *ts_pack_last_error_context(void);

/**
 * Free a string previously returned by this library.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_free_string(char *ptr);

/**
 * Return the library version string. The pointer is static and must NOT be freed.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
const char *ts_pack_version(void);

/**
 * Create a `Span` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_span_free`.
 */
TS_PACKSpan *ts_pack_span_from_json(const char *json);

/**
 * Serialize a `Span` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_span_to_json(const TS_PACKSpan *ptr);

/**
 * Free a `Span` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_span_free(TS_PACKSpan *ptr);

/**
 * Get the `start_byte` field from a `Span`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_span_start_byte(const TS_PACKSpan *ptr);

/**
 * Get the `end_byte` field from a `Span`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_span_end_byte(const TS_PACKSpan *ptr);

/**
 * Get the `start_line` field from a `Span`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_span_start_line(const TS_PACKSpan *ptr);

/**
 * Get the `start_column` field from a `Span`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_span_start_column(const TS_PACKSpan *ptr);

/**
 * Get the `end_line` field from a `Span`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_span_end_line(const TS_PACKSpan *ptr);

/**
 * Get the `end_column` field from a `Span`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_span_end_column(const TS_PACKSpan *ptr);

/**
 * Create a `ProcessResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_process_result_free`.
 */
TS_PACKProcessResult *ts_pack_process_result_from_json(const char *json);

/**
 * Serialize a `ProcessResult` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_process_result_to_json(const TS_PACKProcessResult *ptr);

/**
 * Free a `ProcessResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_process_result_free(TS_PACKProcessResult *ptr);

/**
 * Get the `language` field from a `ProcessResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_process_result_language(const TS_PACKProcessResult *ptr);

/**
 * Get the `metrics` field from a `ProcessResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKFileMetrics *ts_pack_process_result_metrics(const TS_PACKProcessResult *ptr);

/**
 * Get the `structure` field from a `ProcessResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_process_result_structure(const TS_PACKProcessResult *ptr);

/**
 * Get the `imports` field from a `ProcessResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_process_result_imports(const TS_PACKProcessResult *ptr);

/**
 * Get the `exports` field from a `ProcessResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_process_result_exports(const TS_PACKProcessResult *ptr);

/**
 * Get the `comments` field from a `ProcessResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_process_result_comments(const TS_PACKProcessResult *ptr);

/**
 * Get the `docstrings` field from a `ProcessResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_process_result_docstrings(const TS_PACKProcessResult *ptr);

/**
 * Get the `symbols` field from a `ProcessResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_process_result_symbols(const TS_PACKProcessResult *ptr);

/**
 * Get the `diagnostics` field from a `ProcessResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_process_result_diagnostics(const TS_PACKProcessResult *ptr);

/**
 * Get the `chunks` field from a `ProcessResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_process_result_chunks(const TS_PACKProcessResult *ptr);

/**
 * Create a `FileMetrics` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_file_metrics_free`.
 */
TS_PACKFileMetrics *ts_pack_file_metrics_from_json(const char *json);

/**
 * Serialize a `FileMetrics` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_file_metrics_to_json(const TS_PACKFileMetrics *ptr);

/**
 * Free a `FileMetrics` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_file_metrics_free(TS_PACKFileMetrics *ptr);

/**
 * Get the `total_lines` field from a `FileMetrics`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_file_metrics_total_lines(const TS_PACKFileMetrics *ptr);

/**
 * Get the `code_lines` field from a `FileMetrics`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_file_metrics_code_lines(const TS_PACKFileMetrics *ptr);

/**
 * Get the `comment_lines` field from a `FileMetrics`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_file_metrics_comment_lines(const TS_PACKFileMetrics *ptr);

/**
 * Get the `blank_lines` field from a `FileMetrics`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_file_metrics_blank_lines(const TS_PACKFileMetrics *ptr);

/**
 * Get the `total_bytes` field from a `FileMetrics`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_file_metrics_total_bytes(const TS_PACKFileMetrics *ptr);

/**
 * Get the `node_count` field from a `FileMetrics`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_file_metrics_node_count(const TS_PACKFileMetrics *ptr);

/**
 * Get the `error_count` field from a `FileMetrics`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_file_metrics_error_count(const TS_PACKFileMetrics *ptr);

/**
 * Get the `max_depth` field from a `FileMetrics`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_file_metrics_max_depth(const TS_PACKFileMetrics *ptr);

/**
 * Create a `StructureItem` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_structure_item_free`.
 */
TS_PACKStructureItem *ts_pack_structure_item_from_json(const char *json);

/**
 * Serialize a `StructureItem` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_structure_item_to_json(const TS_PACKStructureItem *ptr);

/**
 * Free a `StructureItem` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_structure_item_free(TS_PACKStructureItem *ptr);

/**
 * Get the `kind` field from a `StructureItem`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKStructureKind *ts_pack_structure_item_kind(const TS_PACKStructureItem *ptr);

/**
 * Get the `name` field from a `StructureItem`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_structure_item_name(const TS_PACKStructureItem *ptr);

/**
 * Get the `visibility` field from a `StructureItem`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_structure_item_visibility(const TS_PACKStructureItem *ptr);

/**
 * Get the `span` field from a `StructureItem`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKSpan *ts_pack_structure_item_span(const TS_PACKStructureItem *ptr);

/**
 * Get the `children` field from a `StructureItem`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_structure_item_children(const TS_PACKStructureItem *ptr);

/**
 * Get the `decorators` field from a `StructureItem`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_structure_item_decorators(const TS_PACKStructureItem *ptr);

/**
 * Get the `doc_comment` field from a `StructureItem`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_structure_item_doc_comment(const TS_PACKStructureItem *ptr);

/**
 * Get the `signature` field from a `StructureItem`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_structure_item_signature(const TS_PACKStructureItem *ptr);

/**
 * Get the `body_span` field from a `StructureItem`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKSpan *ts_pack_structure_item_body_span(const TS_PACKStructureItem *ptr);

/**
 * Create a `CommentInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_comment_info_free`.
 */
TS_PACKCommentInfo *ts_pack_comment_info_from_json(const char *json);

/**
 * Serialize a `CommentInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_comment_info_to_json(const TS_PACKCommentInfo *ptr);

/**
 * Free a `CommentInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_comment_info_free(TS_PACKCommentInfo *ptr);

/**
 * Get the `text` field from a `CommentInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_comment_info_text(const TS_PACKCommentInfo *ptr);

/**
 * Get the `kind` field from a `CommentInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKCommentKind *ts_pack_comment_info_kind(const TS_PACKCommentInfo *ptr);

/**
 * Get the `span` field from a `CommentInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKSpan *ts_pack_comment_info_span(const TS_PACKCommentInfo *ptr);

/**
 * Get the `associated_node` field from a `CommentInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_comment_info_associated_node(const TS_PACKCommentInfo *ptr);

/**
 * Create a `DocstringInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_docstring_info_free`.
 */
TS_PACKDocstringInfo *ts_pack_docstring_info_from_json(const char *json);

/**
 * Serialize a `DocstringInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_docstring_info_to_json(const TS_PACKDocstringInfo *ptr);

/**
 * Free a `DocstringInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_docstring_info_free(TS_PACKDocstringInfo *ptr);

/**
 * Get the `text` field from a `DocstringInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_docstring_info_text(const TS_PACKDocstringInfo *ptr);

/**
 * Get the `format` field from a `DocstringInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKDocstringFormat *ts_pack_docstring_info_format(const TS_PACKDocstringInfo *ptr);

/**
 * Get the `span` field from a `DocstringInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKSpan *ts_pack_docstring_info_span(const TS_PACKDocstringInfo *ptr);

/**
 * Get the `associated_item` field from a `DocstringInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_docstring_info_associated_item(const TS_PACKDocstringInfo *ptr);

/**
 * Get the `parsed_sections` field from a `DocstringInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_docstring_info_parsed_sections(const TS_PACKDocstringInfo *ptr);

/**
 * Create a `DocSection` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_doc_section_free`.
 */
TS_PACKDocSection *ts_pack_doc_section_from_json(const char *json);

/**
 * Serialize a `DocSection` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_doc_section_to_json(const TS_PACKDocSection *ptr);

/**
 * Free a `DocSection` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_doc_section_free(TS_PACKDocSection *ptr);

/**
 * Get the `kind` field from a `DocSection`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_doc_section_kind(const TS_PACKDocSection *ptr);

/**
 * Get the `name` field from a `DocSection`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_doc_section_name(const TS_PACKDocSection *ptr);

/**
 * Get the `description` field from a `DocSection`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_doc_section_description(const TS_PACKDocSection *ptr);

/**
 * Create a `ImportInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_import_info_free`.
 */
TS_PACKImportInfo *ts_pack_import_info_from_json(const char *json);

/**
 * Serialize a `ImportInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_import_info_to_json(const TS_PACKImportInfo *ptr);

/**
 * Free a `ImportInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_import_info_free(TS_PACKImportInfo *ptr);

/**
 * Get the `source` field from a `ImportInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_import_info_source(const TS_PACKImportInfo *ptr);

/**
 * Get the `items` field from a `ImportInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_import_info_items(const TS_PACKImportInfo *ptr);

/**
 * Get the `alias` field from a `ImportInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_import_info_alias(const TS_PACKImportInfo *ptr);

/**
 * Get the `is_wildcard` field from a `ImportInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t ts_pack_import_info_is_wildcard(const TS_PACKImportInfo *ptr);

/**
 * Get the `span` field from a `ImportInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKSpan *ts_pack_import_info_span(const TS_PACKImportInfo *ptr);

/**
 * Create a `ExportInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_export_info_free`.
 */
TS_PACKExportInfo *ts_pack_export_info_from_json(const char *json);

/**
 * Serialize a `ExportInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_export_info_to_json(const TS_PACKExportInfo *ptr);

/**
 * Free a `ExportInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_export_info_free(TS_PACKExportInfo *ptr);

/**
 * Get the `name` field from a `ExportInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_export_info_name(const TS_PACKExportInfo *ptr);

/**
 * Get the `kind` field from a `ExportInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKExportKind *ts_pack_export_info_kind(const TS_PACKExportInfo *ptr);

/**
 * Get the `span` field from a `ExportInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKSpan *ts_pack_export_info_span(const TS_PACKExportInfo *ptr);

/**
 * Create a `SymbolInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_symbol_info_free`.
 */
TS_PACKSymbolInfo *ts_pack_symbol_info_from_json(const char *json);

/**
 * Serialize a `SymbolInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_symbol_info_to_json(const TS_PACKSymbolInfo *ptr);

/**
 * Free a `SymbolInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_symbol_info_free(TS_PACKSymbolInfo *ptr);

/**
 * Get the `name` field from a `SymbolInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_symbol_info_name(const TS_PACKSymbolInfo *ptr);

/**
 * Get the `kind` field from a `SymbolInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKSymbolKind *ts_pack_symbol_info_kind(const TS_PACKSymbolInfo *ptr);

/**
 * Get the `span` field from a `SymbolInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKSpan *ts_pack_symbol_info_span(const TS_PACKSymbolInfo *ptr);

/**
 * Get the `type_annotation` field from a `SymbolInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_symbol_info_type_annotation(const TS_PACKSymbolInfo *ptr);

/**
 * Get the `doc` field from a `SymbolInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_symbol_info_doc(const TS_PACKSymbolInfo *ptr);

/**
 * Create a `Diagnostic` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_diagnostic_free`.
 */
TS_PACKDiagnostic *ts_pack_diagnostic_from_json(const char *json);

/**
 * Serialize a `Diagnostic` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_diagnostic_to_json(const TS_PACKDiagnostic *ptr);

/**
 * Free a `Diagnostic` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_diagnostic_free(TS_PACKDiagnostic *ptr);

/**
 * Get the `message` field from a `Diagnostic`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_diagnostic_message(const TS_PACKDiagnostic *ptr);

/**
 * Get the `severity` field from a `Diagnostic`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKDiagnosticSeverity *ts_pack_diagnostic_severity(const TS_PACKDiagnostic *ptr);

/**
 * Get the `span` field from a `Diagnostic`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKSpan *ts_pack_diagnostic_span(const TS_PACKDiagnostic *ptr);

/**
 * Create a `CodeChunk` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_code_chunk_free`.
 */
TS_PACKCodeChunk *ts_pack_code_chunk_from_json(const char *json);

/**
 * Serialize a `CodeChunk` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_code_chunk_to_json(const TS_PACKCodeChunk *ptr);

/**
 * Free a `CodeChunk` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_code_chunk_free(TS_PACKCodeChunk *ptr);

/**
 * Get the `content` field from a `CodeChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_code_chunk_content(const TS_PACKCodeChunk *ptr);

/**
 * Get the `start_byte` field from a `CodeChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_code_chunk_start_byte(const TS_PACKCodeChunk *ptr);

/**
 * Get the `end_byte` field from a `CodeChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_code_chunk_end_byte(const TS_PACKCodeChunk *ptr);

/**
 * Get the `start_line` field from a `CodeChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_code_chunk_start_line(const TS_PACKCodeChunk *ptr);

/**
 * Get the `end_line` field from a `CodeChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_code_chunk_end_line(const TS_PACKCodeChunk *ptr);

/**
 * Get the `metadata` field from a `CodeChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
TS_PACKChunkContext *ts_pack_code_chunk_metadata(const TS_PACKCodeChunk *ptr);

/**
 * Create a `ChunkContext` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_chunk_context_free`.
 */
TS_PACKChunkContext *ts_pack_chunk_context_from_json(const char *json);

/**
 * Serialize a `ChunkContext` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_chunk_context_to_json(const TS_PACKChunkContext *ptr);

/**
 * Free a `ChunkContext` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_chunk_context_free(TS_PACKChunkContext *ptr);

/**
 * Get the `language` field from a `ChunkContext`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_chunk_context_language(const TS_PACKChunkContext *ptr);

/**
 * Get the `chunk_index` field from a `ChunkContext`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_chunk_context_chunk_index(const TS_PACKChunkContext *ptr);

/**
 * Get the `total_chunks` field from a `ChunkContext`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_chunk_context_total_chunks(const TS_PACKChunkContext *ptr);

/**
 * Get the `node_types` field from a `ChunkContext`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_chunk_context_node_types(const TS_PACKChunkContext *ptr);

/**
 * Get the `context_path` field from a `ChunkContext`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_chunk_context_context_path(const TS_PACKChunkContext *ptr);

/**
 * Get the `symbols_defined` field from a `ChunkContext`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_chunk_context_symbols_defined(const TS_PACKChunkContext *ptr);

/**
 * Get the `comments` field from a `ChunkContext`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_chunk_context_comments(const TS_PACKChunkContext *ptr);

/**
 * Get the `docstrings` field from a `ChunkContext`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_chunk_context_docstrings(const TS_PACKChunkContext *ptr);

/**
 * Get the `has_error_nodes` field from a `ChunkContext`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t ts_pack_chunk_context_has_error_nodes(const TS_PACKChunkContext *ptr);

/**
 * Create a `PackConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_pack_config_free`.
 */
TS_PACKPackConfig *ts_pack_pack_config_from_json(const char *json);

/**
 * Serialize a `PackConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_pack_config_to_json(const TS_PACKPackConfig *ptr);

/**
 * Free a `PackConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_pack_config_free(TS_PACKPackConfig *ptr);

/**
 * Get the `cache_dir` field from a `PackConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_pack_config_cache_dir(const TS_PACKPackConfig *ptr);

/**
 * Get the `languages` field from a `PackConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_pack_config_languages(const TS_PACKPackConfig *ptr);

/**
 * Get the `groups` field from a `PackConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_pack_config_groups(const TS_PACKPackConfig *ptr);

/**
 * Load configuration from a TOML file.
 *
 * # Errors
 *
 * Returns an error if the file cannot be read or the TOML is invalid.
 *
 * # Example
 *
 * ```no_run
 * use std::path::Path;
 * use tree_sitter_language_pack::PackConfig;
 *
 * let config = PackConfig::from_toml_file(Path::new("language-pack.toml")).unwrap();
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKPackConfig *ts_pack_pack_config_from_toml_file(const char *path);

/**
 * Discover configuration by searching for `language-pack.toml` in:
 *
 * 1. Current directory and up to 10 parent directories
 * 2. `$XDG_CONFIG_HOME/tree-sitter-language-pack/config.toml`
 * 3. `~/.config/tree-sitter-language-pack/config.toml`
 *
 * Returns `None` if no configuration file is found.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::PackConfig;
 *
 * if let Some(config) = PackConfig::discover() {
 *     println!("Found config with {:?} languages", config.languages);
 * }
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKPackConfig *ts_pack_pack_config_discover(void);

/**
 * Create a `ProcessConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_process_config_free`.
 */
TS_PACKProcessConfig *ts_pack_process_config_from_json(const char *json);

/**
 * Serialize a `ProcessConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_process_config_to_json(const TS_PACKProcessConfig *ptr);

/**
 * Free a `ProcessConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_process_config_free(TS_PACKProcessConfig *ptr);

/**
 * Get the `structure` field from a `ProcessConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t ts_pack_process_config_structure(const TS_PACKProcessConfig *ptr);

/**
 * Get the `imports` field from a `ProcessConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t ts_pack_process_config_imports(const TS_PACKProcessConfig *ptr);

/**
 * Get the `exports` field from a `ProcessConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t ts_pack_process_config_exports(const TS_PACKProcessConfig *ptr);

/**
 * Get the `comments` field from a `ProcessConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t ts_pack_process_config_comments(const TS_PACKProcessConfig *ptr);

/**
 * Get the `docstrings` field from a `ProcessConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t ts_pack_process_config_docstrings(const TS_PACKProcessConfig *ptr);

/**
 * Get the `symbols` field from a `ProcessConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t ts_pack_process_config_symbols(const TS_PACKProcessConfig *ptr);

/**
 * Get the `diagnostics` field from a `ProcessConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t ts_pack_process_config_diagnostics(const TS_PACKProcessConfig *ptr);

/**
 * Get the `chunk_max_size` field from a `ProcessConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t ts_pack_process_config_chunk_max_size(const TS_PACKProcessConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKProcessConfig *ts_pack_process_config_default(void);

/**
 * Enable chunking with the given maximum chunk size in bytes.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKProcessConfig *ts_pack_process_config_with_chunking(TS_PACKProcessConfig *this_,
                                                           uintptr_t max_size);

/**
 * Enable all analysis features.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKProcessConfig *ts_pack_process_config_all(TS_PACKProcessConfig *this_);

/**
 * Disable all analysis features (only metrics computed).
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKProcessConfig *ts_pack_process_config_minimal(TS_PACKProcessConfig *this_);

/**
 * Free a `LanguageRegistry` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_language_registry_free(TS_PACKLanguageRegistry *ptr);

/**
 * Create a registry with a custom directory for dynamic libraries.
 *
 * Overrides the default build-time library directory. Useful when
 * dynamic grammar shared libraries are stored in a non-standard location.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKLanguageRegistry *ts_pack_language_registry_with_libs_dir(const char *libs_dir);

/**
 * Add an additional directory to search for dynamic libraries.
 *
 * When [`get_language`](Self::get_language) cannot find a grammar in the
 * primary library directory, it searches these extra directories in order.
 * Typically used by the download system to register its cache directory.
 *
 * Takes `&self` (not `&mut self`) because `extra_lib_dirs` uses interior
 * mutability via an `Arc<RwLock<...>>`, so the outer registry can remain
 * immutable while the directory list is updated.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
void ts_pack_language_registry_add_extra_libs_dir(const TS_PACKLanguageRegistry *this_,
                                                  const char *dir);

/**
 * Get a tree-sitter [`Language`] by name.
 *
 * Resolves aliases (e.g., `"shell"` -> `"bash"`, `"makefile"` -> `"make"`),
 * then looks up the language in the static table. When the `dynamic-loading`
 * feature is enabled, falls back to loading a shared library on demand.
 *
 * # Errors
 *
 * Returns [`Error::LanguageNotFound`] if the name (after alias resolution)
 * does not match any known grammar.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKLanguage *ts_pack_language_registry_get_language(const TS_PACKLanguageRegistry *this_,
                                                        const char *name);

/**
 * List all available language names, sorted and deduplicated.
 *
 * Includes statically compiled languages, dynamically loadable languages
 * (if the `dynamic-loading` feature is enabled), and all configured aliases.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_language_registry_available_languages(const TS_PACKLanguageRegistry *this_);

/**
 * Check whether a language is available by name or alias.
 *
 * Returns `true` if the language can be loaded, either from the static
 * table or from a dynamic library on disk.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_language_registry_has_language(const TS_PACKLanguageRegistry *this_,
                                               const char *name);

/**
 * Return the total number of available languages (including aliases).
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
uintptr_t ts_pack_language_registry_language_count(const TS_PACKLanguageRegistry *this_);

/**
 * Parse source code and extract file intelligence based on config in a single pass.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKProcessResult *ts_pack_language_registry_process(const TS_PACKLanguageRegistry *this_,
                                                        const char *source,
                                                        const TS_PACKProcessConfig *config);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKLanguageRegistry *ts_pack_language_registry_default(void);

/**
 * Create a `ParserManifest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_parser_manifest_free`.
 */
TS_PACKParserManifest *ts_pack_parser_manifest_from_json(const char *json);

/**
 * Serialize a `ParserManifest` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_parser_manifest_to_json(const TS_PACKParserManifest *ptr);

/**
 * Free a `ParserManifest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_parser_manifest_free(TS_PACKParserManifest *ptr);

/**
 * Get the `version` field from a `ParserManifest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_parser_manifest_version(const TS_PACKParserManifest *ptr);

/**
 * Get the `platforms` field from a `ParserManifest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_parser_manifest_platforms(const TS_PACKParserManifest *ptr);

/**
 * Get the `languages` field from a `ParserManifest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_parser_manifest_languages(const TS_PACKParserManifest *ptr);

/**
 * Get the `groups` field from a `ParserManifest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_parser_manifest_groups(const TS_PACKParserManifest *ptr);

/**
 * Create a `PlatformBundle` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_platform_bundle_free`.
 */
TS_PACKPlatformBundle *ts_pack_platform_bundle_from_json(const char *json);

/**
 * Serialize a `PlatformBundle` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_platform_bundle_to_json(const TS_PACKPlatformBundle *ptr);

/**
 * Free a `PlatformBundle` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_platform_bundle_free(TS_PACKPlatformBundle *ptr);

/**
 * Get the `url` field from a `PlatformBundle`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_platform_bundle_url(const TS_PACKPlatformBundle *ptr);

/**
 * Get the `sha256` field from a `PlatformBundle`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_platform_bundle_sha256(const TS_PACKPlatformBundle *ptr);

/**
 * Get the `size` field from a `PlatformBundle`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t ts_pack_platform_bundle_size(const TS_PACKPlatformBundle *ptr);

/**
 * Create a `LanguageInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `ts_pack_language_info_free`.
 */
TS_PACKLanguageInfo *ts_pack_language_info_from_json(const char *json);

/**
 * Serialize a `LanguageInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
 * The returned string must be freed with `ts_pack_free_string`.
 */
char *ts_pack_language_info_to_json(const TS_PACKLanguageInfo *ptr);

/**
 * Free a `LanguageInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_language_info_free(TS_PACKLanguageInfo *ptr);

/**
 * Get the `group` field from a `LanguageInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *ts_pack_language_info_group(const TS_PACKLanguageInfo *ptr);

/**
 * Get the `size` field from a `LanguageInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t ts_pack_language_info_size(const TS_PACKLanguageInfo *ptr);

/**
 * Free a `DownloadManager` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_download_manager_free(TS_PACKDownloadManager *ptr);

/**
 * Create a new download manager for the given version.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKDownloadManager *ts_pack_download_manager_new(const char *version);

/**
 * Create a download manager with a custom cache directory.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKDownloadManager *ts_pack_download_manager_with_cache_dir(const char *version,
                                                                const char *cache_dir);

/**
 * Default cache directory: `~/.cache/tree-sitter-language-pack/v{version}/libs/`
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_download_manager_default_cache_dir(const char *version);

/**
 * Return the path to the libs cache directory.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_download_manager_cache_dir(const TS_PACKDownloadManager *this_);

/**
 * List languages that are already downloaded and cached.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_download_manager_installed_languages(const TS_PACKDownloadManager *this_);

/**
 * Ensure the specified languages are available in the cache.
 * Downloads the platform bundle if any requested languages are missing.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_download_manager_ensure_languages(const TS_PACKDownloadManager *this_,
                                                  const char *names);

/**
 * Ensure all languages in a named group are available.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_download_manager_ensure_group(const TS_PACKDownloadManager *this_,
                                              const char *group);

/**
 * Get the expected path for a language's shared library in the cache.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_download_manager_lib_path(const TS_PACKDownloadManager *this_,
                                        const char *name);

/**
 * Fetch the parser manifest from GitHub Releases.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKParserManifest *ts_pack_download_manager_fetch_manifest(const TS_PACKDownloadManager *this_);

/**
 * Download the platform bundle and extract every library file it contains.
 *
 * Unlike [`ensure_languages`], this does not check the manifest language list
 * against archive contents — it simply extracts all `.so`/`.dylib`/`.dll` files
 * from the bundle. Languages in the manifest that are missing from the archive
 * are silently ignored rather than returning an error.
 *
 * Returns the number of library files extracted (including those already cached).
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
uintptr_t ts_pack_download_manager_download_all_best_effort(const TS_PACKDownloadManager *this_);

/**
 * Remove all cached parser libraries.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_download_manager_clean_cache(const TS_PACKDownloadManager *this_);

/**
 * Free a `Language` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_language_free(TS_PACKLanguage *ptr);

/**
 * Free a `Parser` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_parser_free(TS_PACKParser *ptr);

/**
 * Free a `Tree` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void ts_pack_tree_free(TS_PACKTree *ptr);

/**
 * Convert an integer to a `StructureKind` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_structure_kind_from_i32(int32_t value);

/**
 * Convert a `StructureKind` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t ts_pack_structure_kind_from_str(const char *name);

/**
 * Convert an integer to a `CommentKind` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_comment_kind_from_i32(int32_t value);

/**
 * Convert a `CommentKind` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t ts_pack_comment_kind_from_str(const char *name);

/**
 * Convert an integer to a `DocstringFormat` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_docstring_format_from_i32(int32_t value);

/**
 * Convert a `DocstringFormat` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t ts_pack_docstring_format_from_str(const char *name);

/**
 * Convert an integer to a `ExportKind` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_export_kind_from_i32(int32_t value);

/**
 * Convert a `ExportKind` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t ts_pack_export_kind_from_str(const char *name);

/**
 * Convert an integer to a `SymbolKind` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_symbol_kind_from_i32(int32_t value);

/**
 * Convert a `SymbolKind` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t ts_pack_symbol_kind_from_str(const char *name);

/**
 * Convert an integer to a `DiagnosticSeverity` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_diagnostic_severity_from_i32(int32_t value);

/**
 * Convert a `DiagnosticSeverity` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t ts_pack_diagnostic_severity_from_str(const char *name);

/**
 * Detect language name from a file extension (without leading dot).
 *
 * Returns `None` for unrecognized extensions. The match is case-insensitive.
 *
 * ```
 * use tree_sitter_language_pack::detect_language_from_extension;
 * assert_eq!(detect_language_from_extension("py"), Some("python"));
 * assert_eq!(detect_language_from_extension("RS"), Some("rust"));
 * assert_eq!(detect_language_from_extension("xyz"), None);
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_detect_language_from_extension(const char *ext);

/**
 * Detect language name from a file path.
 *
 * Extracts the file extension and looks it up. Returns `None` if the
 * path has no extension or the extension is not recognized.
 *
 * ```
 * use tree_sitter_language_pack::detect_language_from_path;
 * assert_eq!(detect_language_from_path("src/main.rs"), Some("rust"));
 * assert_eq!(detect_language_from_path("README.md"), Some("markdown"));
 * assert_eq!(detect_language_from_path("Makefile"), None);
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_detect_language_from_path(const char *path);

/**
 * Detect language name from file content using the shebang line (`#!`).
 *
 * Inspects only the first line of `content`. If it begins with `#!`, the
 * interpreter name is extracted and mapped to a language name.
 *
 * Handles common patterns:
 * - `#!/usr/bin/env python3` → `"python"`
 * - `#!/bin/bash` → `"bash"`
 * - `#!/usr/bin/env node` → `"javascript"`
 *
 * The `-S` flag accepted by some `env` implementations is skipped automatically.
 * Version suffixes (e.g. `python3.11`, `ruby3.2`) are stripped before matching.
 *
 * Returns `None` when content does not start with `#!`, the shebang is
 * malformed, or the interpreter is not recognised.
 *
 * ```
 * use tree_sitter_language_pack::detect_language_from_content;
 * assert_eq!(detect_language_from_content("#!/usr/bin/env python3\npass"), Some("python"));
 * assert_eq!(detect_language_from_content("#!/bin/bash\necho hi"), Some("bash"));
 * assert_eq!(detect_language_from_content("no shebang here"), None);
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_detect_language_from_content(const char *content);

/**
 * Get the highlights query for a language, if bundled.
 *
 * Returns the contents of `highlights.scm` as a static string, or `None`
 * if no highlights query is bundled for this language.
 *
 * # Example
 *
 * ```
 * use tree_sitter_language_pack::get_highlights_query;
 *
 * // Returns Some(...) for languages with bundled queries
 * let query = get_highlights_query("python");
 * // Returns None for languages without bundled highlights queries
 * let missing = get_highlights_query("nonexistent_lang");
 * assert!(missing.is_none());
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_get_highlights_query(const char *language);

/**
 * Get the injections query for a language, if bundled.
 *
 * Returns the contents of `injections.scm` as a static string, or `None`
 * if no injections query is bundled for this language.
 *
 * # Example
 *
 * ```
 * use tree_sitter_language_pack::get_injections_query;
 *
 * let query = get_injections_query("markdown");
 * // Returns None for languages without bundled injections queries
 * let missing = get_injections_query("nonexistent_lang");
 * assert!(missing.is_none());
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_get_injections_query(const char *language);

/**
 * Get the locals query for a language, if bundled.
 *
 * Returns the contents of `locals.scm` as a static string, or `None`
 * if no locals query is bundled for this language.
 *
 * # Example
 *
 * ```
 * use tree_sitter_language_pack::get_locals_query;
 *
 * let query = get_locals_query("python");
 * // Returns None for languages without bundled locals queries
 * let missing = get_locals_query("nonexistent_lang");
 * assert!(missing.is_none());
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_get_locals_query(const char *language);

/**
 * Get a tree-sitter [`Language`] by name using the global registry.
 *
 * Resolves language aliases (e.g., `"shell"` maps to `"bash"`).
 * When the `download` feature is enabled (default), automatically downloads
 * the parser from GitHub releases if not found locally.
 *
 * # Errors
 *
 * Returns [`Error::LanguageNotFound`] if the language is not recognized,
 * or [`Error::Download`] if auto-download fails.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::get_language;
 *
 * let lang = get_language("python").unwrap();
 * // Use the Language with a tree-sitter Parser
 * let mut parser = tree_sitter::Parser::new();
 * parser.set_language(&lang).unwrap();
 * let tree = parser.parse("x = 1", None).unwrap();
 * assert_eq!(tree.root_node().kind(), "module");
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKLanguage *ts_pack_get_language(const char *name);

/**
 * Get a tree-sitter [`Parser`] pre-configured for the given language.
 *
 * This is a convenience function that calls [`get_language`] and configures
 * a new parser in one step.
 *
 * # Errors
 *
 * Returns [`Error::LanguageNotFound`] if the language is not recognized, or
 * [`Error::ParserSetup`] if the language cannot be applied to the parser.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::get_parser;
 *
 * let mut parser = get_parser("rust").unwrap();
 * let tree = parser.parse("fn main() {}", None).unwrap();
 * assert!(!tree.root_node().has_error());
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKParser *ts_pack_get_parser(const char *name);

/**
 * Detect language name from a file path or extension.
 *
 * This compatibility alias matches the pre-Alef Python binding API.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_detect_language(const char *path);

/**
 * List all available language names (sorted, deduplicated, includes aliases).
 *
 * Returns names of both statically compiled and dynamically loadable languages,
 * plus any configured aliases.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::available_languages;
 *
 * let langs = available_languages();
 * for name in &langs {
 *     println!("{}", name);
 * }
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_available_languages(void);

/**
 * Check if a language is available by name or alias.
 *
 * Returns `true` if the language can be loaded (statically compiled,
 * dynamically available, or a known alias for one of these).
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::has_language;
 *
 * assert!(has_language("python"));
 * assert!(has_language("shell")); // alias for "bash"
 * assert!(!has_language("nonexistent_language"));
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_has_language(const char *name);

/**
 * Return the number of available languages.
 *
 * Includes statically compiled languages, dynamically loadable languages,
 * and aliases.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::language_count;
 *
 * let count = language_count();
 * println!("{} languages available", count);
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
uintptr_t ts_pack_language_count(void);

/**
 * Process source code and extract file intelligence using the global registry.
 *
 * Parses the source with tree-sitter and extracts metrics, structure, imports,
 * exports, comments, docstrings, symbols, diagnostics, and/or chunks based on
 * the flags set in [`ProcessConfig`].
 *
 * # Errors
 *
 * Returns an error if the language is not found or parsing fails.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::{ProcessConfig, process};
 *
 * let config = ProcessConfig::new("python").all();
 * let result = process("def hello(): pass", &config).unwrap();
 * println!("Language: {}", result.language);
 * println!("Lines: {}", result.metrics.total_lines);
 * println!("Structures: {}", result.structure.len());
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
TS_PACKProcessResult *ts_pack_process(const char *source,
                                      const TS_PACKProcessConfig *config);

/**
 * Initialize the language pack with the given configuration.
 *
 * Applies any custom cache directory, then downloads all languages and groups
 * specified in the config. This is the recommended entry point when you want
 * to pre-warm the cache before use.
 *
 * # Errors
 *
 * Returns an error if configuration cannot be applied or if downloads fail.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::{PackConfig, init};
 *
 * let config = PackConfig {
 *     cache_dir: None,
 *     languages: Some(vec!["python".to_string(), "rust".to_string()]),
 *     groups: None,
 * };
 * init(&config).unwrap();
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_init(const TS_PACKPackConfig *config);

/**
 * Apply download configuration without downloading anything.
 *
 * Use this to set a custom cache directory before the first call to
 * [`get_language`] or any download function. Changing the cache dir
 * after languages have been registered has no effect on already-loaded
 * languages.
 *
 * # Errors
 *
 * Returns an error if the lock cannot be acquired.
 *
 * # Example
 *
 * ```no_run
 * use std::path::PathBuf;
 * use tree_sitter_language_pack::{PackConfig, configure};
 *
 * let config = PackConfig {
 *     cache_dir: Some(PathBuf::from("/tmp/my-parsers")),
 *     languages: None,
 *     groups: None,
 * };
 * configure(&config).unwrap();
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_configure(const TS_PACKPackConfig *config);

/**
 * Download specific languages to the local cache.
 *
 * Returns the number of requested languages available after the call. Already
 * compiled or cached languages are included in the count.
 *
 * # Errors
 *
 * Returns an error if any language is not available in the manifest or if
 * the download fails.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::download;
 *
 * let count = download(&["python", "rust", "typescript"]).unwrap();
 * println!("Ensured {} languages", count);
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
uintptr_t ts_pack_download(const char *names);

/**
 * Download all available languages from the remote manifest.
 *
 * Downloads the platform bundle and extracts every library it contains.
 * Languages that appear in the manifest but are absent from the bundle
 * (e.g. grammars that failed to compile at release time) are silently
 * skipped — they are not treated as an error.
 *
 * Returns the total number of languages now available (statically compiled
 * plus downloaded and cached).
 *
 * # Errors
 *
 * Returns an error if the manifest cannot be fetched or the bundle download fails.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::download_all;
 *
 * let count = download_all().unwrap();
 * println!("{} languages available", count);
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
uintptr_t ts_pack_download_all(void);

/**
 * Return all language names available in the remote manifest (305).
 *
 * Fetches (and caches) the remote manifest to discover the full list of
 * downloadable languages. Use [`downloaded_languages`] to list what is
 * already cached locally.
 *
 * # Errors
 *
 * Returns an error if the manifest cannot be fetched.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::manifest_languages;
 *
 * let langs = manifest_languages().unwrap();
 * println!("{} languages available for download", langs.len());
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_manifest_languages(void);

/**
 * Return languages that are already downloaded and cached locally.
 *
 * Does not perform any network requests. Returns an empty list if the
 * cache directory does not exist or cannot be read.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::downloaded_languages;
 *
 * let langs = downloaded_languages();
 * println!("{} languages already cached", langs.len());
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_downloaded_languages(void);

/**
 * Delete all cached parser shared libraries.
 *
 * Resets the cache registration so the next call to [`get_language`] or
 * a download function will re-register the (now empty) cache directory.
 *
 * # Errors
 *
 * Returns an error if the cache directory cannot be removed.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::clean_cache;
 *
 * clean_cache().unwrap();
 * println!("Cache cleared");
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t ts_pack_clean_cache(void);

/**
 * Return the effective cache directory path.
 *
 * This is either the custom path set via [`configure`] / [`init`] or the
 * default: `~/.cache/tree-sitter-language-pack/v{version}/libs/`.
 *
 * # Errors
 *
 * Returns an error if the system cache directory cannot be determined.
 *
 * # Example
 *
 * ```no_run
 * use tree_sitter_language_pack::cache_dir;
 *
 * let dir = cache_dir().unwrap();
 * println!("Cache directory: {}", dir.display());
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *ts_pack_cache_dir(void);

#endif  /* TS_PACK_H */
