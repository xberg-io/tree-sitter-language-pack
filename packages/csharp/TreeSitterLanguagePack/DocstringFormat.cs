// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:76f3bac7536fa4e5b8ab9a93be197508306d06b566fe7cc519a38c1b9ebf9f42
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.IO;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace TreeSitterLanguagePack;

/// <summary>
/// The format of a docstring extracted from source code.
///
/// Identifies the docstring convention used, which varies by language
/// (e.g., Python triple-quoted strings, JSDoc, Rustdoc `///` comments).
/// </summary>
    [JsonConverter(typeof(DocstringFormatJsonConverter))]
public abstract record DocstringFormat
{
    /// <summary>
    /// Python triple-quoted string docstring (`"""..."""`).
    /// </summary>
    public sealed record PythonTripleQuote() : DocstringFormat;

    /// <summary>
    /// JavaScript/TypeScript JSDoc comment (`/** ... */`).
    /// </summary>
    public sealed record JsDoc() : DocstringFormat;

    /// <summary>
    /// Rust `///` or `//!` doc comment.
    /// </summary>
    public sealed record Rustdoc() : DocstringFormat;

    /// <summary>
    /// Go doc comment (a comment block immediately preceding a declaration).
    /// </summary>
    public sealed record GoDoc() : DocstringFormat;

    /// <summary>
    /// Java Javadoc comment (`/** ... */`).
    /// </summary>
    public sealed record JavaDoc() : DocstringFormat;

    /// <summary>
    /// A language-specific docstring format not covered by the standard variants.
    /// </summary>
    public sealed record Other(
        [property: JsonPropertyName("value")] string Value
    ) : DocstringFormat;

}

/// <summary>
/// Custom converter for DocstringFormat sealed union with flattened variant fields.
/// </summary>
/// <remarks>
/// Handles JSON objects with a discriminator field (kind) and variant-specific
/// fields at the same level. System.Text.Json's [JsonPolymorphic] cannot handle
/// this layout, so we manually deserialize here.
/// </remarks>
public sealed class DocstringFormatJsonConverter : JsonConverter<DocstringFormat>
{
    public override DocstringFormat Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        if (reader.TokenType != JsonTokenType.StartObject)
        {
            throw new JsonException($"Expected JSON object, got {reader.TokenType}");
        }

        using var doc = JsonDocument.ParseValue(ref reader);
        var root = doc.RootElement;

        if (!root.TryGetProperty("kind", out var tagElement))
        {
            throw new JsonException($"Missing discriminator field: kind");
        }

        var tagValue = tagElement.GetString();
        if (tagValue == null)
        {
            throw new JsonException("Discriminator field is null");
        }

        // Tuple-variant records (`Variant(InnerStruct value)`) expect a single
        // "Value" field holding the inner struct's JSON, so wrap the remaining
        // fields under "Value". Struct-variant records (`Variant { field1,
        // field2 }`) have positional record components annotated with
        // [JsonPropertyName(...)] for each named field, so pass the remaining
        // fields through directly without the wrap.
        using var ms = new MemoryStream();
        using var writer = new Utf8JsonWriter(ms);
        writer.WriteStartObject();
        foreach (var prop in root.EnumerateObject())
        {
            if (prop.Name != "kind")
            {
                writer.WritePropertyName(prop.Name);
                prop.Value.WriteTo(writer);
            }
        }
        writer.WriteEndObject();
        writer.Flush();
        ms.Position = 0;
        var flatJson = ms.ToArray();

        using var msWrapped = new MemoryStream();
        using var writerWrapped = new Utf8JsonWriter(msWrapped);
        writerWrapped.WriteStartObject();
        writerWrapped.WritePropertyName("Value");
        writerWrapped.WriteStartObject();
        foreach (var prop in root.EnumerateObject())
        {
            if (prop.Name != "kind")
            {
                writerWrapped.WritePropertyName(prop.Name);
                prop.Value.WriteTo(writerWrapped);
            }
        }
        writerWrapped.WriteEndObject();
        writerWrapped.WriteEndObject();
        writerWrapped.Flush();
        msWrapped.Position = 0;
        var wrappedJson = msWrapped.ToArray();

        return tagValue switch
        {            "PythonTripleQuote" => new DocstringFormat.PythonTripleQuote(),            "JSDoc" => new DocstringFormat.JsDoc(),            "Rustdoc" => new DocstringFormat.Rustdoc(),            "GoDoc" => new DocstringFormat.GoDoc(),            "JavaDoc" => new DocstringFormat.JavaDoc(),            "Other" => JsonSerializer.Deserialize<DocstringFormat.Other>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),            // Unknown discriminator: forward compatibility for unknown variants
            _ => throw new JsonException($"Unknown DocstringFormat discriminator: {tagValue}")
        };
    }

    public override void Write(Utf8JsonWriter writer, DocstringFormat value, JsonSerializerOptions options)
    {
        // Emit the discriminator tag plus the inner variant's fields flattened at
        // the same level — mirrors the Java sealed-union serializer pattern. Turn
        // `Message.User(UserMessage value)` into `{"kind":"user","content":...}`
        // not `{"value":{...}}`. Without this, sending a chat request to FFI fails
        // with "missing field kind" inside Rust serde.
        string tag;
        object? inner;
        switch (value)
        {            case DocstringFormat.PythonTripleQuote _:
                tag = "PythonTripleQuote";
                inner = null;
                break;            case DocstringFormat.JsDoc _:
                tag = "JSDoc";
                inner = null;
                break;            case DocstringFormat.Rustdoc _:
                tag = "Rustdoc";
                inner = null;
                break;            case DocstringFormat.GoDoc _:
                tag = "GoDoc";
                inner = null;
                break;            case DocstringFormat.JavaDoc _:
                tag = "JavaDoc";
                inner = null;
                break;            case DocstringFormat.Other v_other:
                tag = "Other";                inner = v_other;                break;            default:
                throw new JsonException($"Unknown DocstringFormat variant: {value.GetType().Name}");
        }

        writer.WriteStartObject();
        writer.WriteString("kind", tag);
        if (inner != null)
        {
            using var doc = JsonSerializer.SerializeToDocument(inner, inner.GetType(), options);
            if (doc.RootElement.ValueKind == JsonValueKind.Object)
            {
                foreach (var prop in doc.RootElement.EnumerateObject())
                {
                    writer.WritePropertyName(prop.Name);
                    prop.Value.WriteTo(writer);
                }
            }
        }
        writer.WriteEndObject();
    }
}
