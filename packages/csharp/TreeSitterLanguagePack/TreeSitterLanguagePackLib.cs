// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:fb34b11832d531d6f2a7376aacfc26ac72d926baf3683e41e3ace175b718b4b8
#nullable enable

using System;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;
namespace TreeSitterLanguagePack;

public static class TreeSitterLanguagePackLib
{
    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull
    };

    /// <summary>
    /// Detect language name from a file extension (without leading dot).
    ///
    /// Returns `None` for unrecognized extensions. The match is case-insensitive.
    ///
    /// ```
    /// use tree_sitter_language_pack::detect_language_from_extension;
    /// assert_eq!(detect_language_from_extension("py"), Some("python"));
    /// assert_eq!(detect_language_from_extension("RS"), Some("rust"));
    /// assert_eq!(detect_language_from_extension("xyz"), None);
    /// ```
    /// </summary>
    /// <param name="ext"></param>
    public static string? DetectLanguageFromExtension(string ext)
    {
        ArgumentNullException.ThrowIfNull(ext);
        var nativeResult = NativeMethods.DetectLanguageFromExtension(
            ext
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Detect language name from a file path.
    ///
    /// Extracts the file extension and looks it up. Returns `None` if the
    /// path has no extension or the extension is not recognized.
    ///
    /// ```
    /// use tree_sitter_language_pack::detect_language_from_path;
    /// assert_eq!(detect_language_from_path("src/main.rs"), Some("rust"));
    /// assert_eq!(detect_language_from_path("README.md"), Some("markdown"));
    /// assert_eq!(detect_language_from_path("Makefile"), None);
    /// ```
    /// </summary>
    /// <param name="path"></param>
    public static string? DetectLanguageFromPath(string path)
    {
        ArgumentNullException.ThrowIfNull(path);
        var nativeResult = NativeMethods.DetectLanguageFromPath(
            path
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Detect language name from file content using the shebang line (`#!`).
    ///
    /// Inspects only the first line of `content`. If it begins with `#!`, the
    /// interpreter name is extracted and mapped to a language name.
    ///
    /// Handles common patterns:
    /// - `#!/usr/bin/env python3` → `"python"`
    /// - `#!/bin/bash` → `"bash"`
    /// - `#!/usr/bin/env node` → `"javascript"`
    ///
    /// The `-S` flag accepted by some `env` implementations is skipped automatically.
    /// Version suffixes (e.g. `python3.11`, `ruby3.2`) are stripped before matching.
    ///
    /// Returns `None` when content does not start with `#!`, the shebang is
    /// malformed, or the interpreter is not recognised.
    ///
    /// ```
    /// use tree_sitter_language_pack::detect_language_from_content;
    /// assert_eq!(detect_language_from_content("#!/usr/bin/env python3\npass"), Some("python"));
    /// assert_eq!(detect_language_from_content("#!/bin/bash\necho hi"), Some("bash"));
    /// assert_eq!(detect_language_from_content("no shebang here"), None);
    /// ```
    /// </summary>
    /// <param name="content"></param>
    public static string? DetectLanguageFromContent(string content)
    {
        ArgumentNullException.ThrowIfNull(content);
        var nativeResult = NativeMethods.DetectLanguageFromContent(
            content
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get the highlights query for a language, if bundled.
    ///
    /// Returns the contents of `highlights.scm` as a static string, or `None`
    /// if no highlights query is bundled for this language.
    /// </summary>
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::get_highlights_query;
    ///
    /// // Returns Some(...) for languages with bundled queries
    /// let query = get_highlights_query("python");
    /// // Returns None for languages without bundled highlights queries
    /// let missing = get_highlights_query("nonexistent_lang");
    /// assert!(missing.is_none());
    /// </code></example>
    /// <param name="language"></param>
    public static string? GetHighlightsQuery(string language)
    {
        ArgumentNullException.ThrowIfNull(language);
        var nativeResult = NativeMethods.GetHighlightsQuery(
            language
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get the injections query for a language, if bundled.
    ///
    /// Returns the contents of `injections.scm` as a static string, or `None`
    /// if no injections query is bundled for this language.
    /// </summary>
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::get_injections_query;
    ///
    /// let query = get_injections_query("markdown");
    /// // Returns None for languages without bundled injections queries
    /// let missing = get_injections_query("nonexistent_lang");
    /// assert!(missing.is_none());
    /// </code></example>
    /// <param name="language"></param>
    public static string? GetInjectionsQuery(string language)
    {
        ArgumentNullException.ThrowIfNull(language);
        var nativeResult = NativeMethods.GetInjectionsQuery(
            language
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get the locals query for a language, if bundled.
    ///
    /// Returns the contents of `locals.scm` as a static string, or `None`
    /// if no locals query is bundled for this language.
    /// </summary>
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::get_locals_query;
    ///
    /// let query = get_locals_query("python");
    /// // Returns None for languages without bundled locals queries
    /// let missing = get_locals_query("nonexistent_lang");
    /// assert!(missing.is_none());
    /// </code></example>
    /// <param name="language"></param>
    public static string? GetLocalsQuery(string language)
    {
        ArgumentNullException.ThrowIfNull(language);
        var nativeResult = NativeMethods.GetLocalsQuery(
            language
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get a tree-sitter [`Language`] by name using the global registry.
    ///
    /// Resolves language aliases (e.g., `"shell"` maps to `"bash"`).
    /// When the `download` feature is enabled (default), automatically downloads
    /// the parser from GitHub releases if not found locally.
    /// </summary>
    /// <exception cref="KreuzbergException">Returns [`Error::LanguageNotFound`] if the language is not recognized,
    /// or [`Error::Download`] if auto-download fails.</exception>
    ///
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::get_language;
    ///
    /// let lang = get_language("python").unwrap();
    /// // Use the Language with a tree-sitter Parser
    /// let mut parser = tree_sitter::Parser::new();
    /// parser.set_language(&lang).unwrap();
    /// let tree = parser.parse("x = 1", None).unwrap();
    /// assert_eq!(tree.root_node().kind(), "module");
    /// </code></example>
    /// <param name="name"></param>
    public static Language GetLanguage(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        var nativeResult = NativeMethods.GetLanguage(
            name
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw GetLastError();
        }
        var returnValue = new Language(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get a tree-sitter [`Parser`] pre-configured for the given language.
    ///
    /// This is a convenience function that calls [`get_language`] and configures
    /// a new parser in one step.
    /// </summary>
    /// <exception cref="KreuzbergException">Returns [`Error::LanguageNotFound`] if the language is not recognized, or
    /// [`Error::ParserSetup`] if the language cannot be applied to the parser.</exception>
    ///
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::get_parser;
    ///
    /// let mut parser = get_parser("rust").unwrap();
    /// let tree = parser.parse("fn main() {}", None).unwrap();
    /// assert!(!tree.root_node().has_error());
    /// </code></example>
    /// <param name="name"></param>
    public static Parser GetParser(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        var nativeResult = NativeMethods.GetParser(
            name
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw GetLastError();
        }
        var returnValue = new Parser(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Detect language name from a file path or extension.
    ///
    /// This compatibility alias matches the pre-Alef Python binding API.
    /// </summary>
    /// <param name="path"></param>
    public static string? DetectLanguage(string path)
    {
        ArgumentNullException.ThrowIfNull(path);
        var nativeResult = NativeMethods.DetectLanguage(
            path
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// List all available language names (sorted, deduplicated, includes aliases).
    ///
    /// Returns names of both statically compiled and dynamically loadable languages,
    /// plus any configured aliases.
    /// </summary>
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::available_languages;
    ///
    /// let langs = available_languages();
    /// for name in &langs {
    ///     println!("{}", name);
    /// }
    /// </code></example>
    public static List<string> AvailableLanguages()
    {
        var nativeResult = NativeMethods.AvailableLanguages();
        if (nativeResult == IntPtr.Zero)
        {
            throw GetLastError();
        }
        var json = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<List<string>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Check if a language is available by name or alias.
    ///
    /// Returns `true` if the language can be loaded (statically compiled,
    /// dynamically available, or a known alias for one of these).
    /// </summary>
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::has_language;
    ///
    /// assert!(has_language("python"));
    /// assert!(has_language("shell")); // alias for "bash"
    /// assert!(!has_language("nonexistent_language"));
    /// </code></example>
    /// <param name="name"></param>
    public static bool HasLanguage(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        var nativeResult = NativeMethods.HasLanguage(
            name
        );
        var returnValue = nativeResult != 0;
        return returnValue;
    }

    /// <summary>
    /// Return the number of available languages.
    ///
    /// Includes statically compiled languages, dynamically loadable languages,
    /// and aliases.
    /// </summary>
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::language_count;
    ///
    /// let count = language_count();
    /// println!("{} languages available", count);
    /// </code></example>
    public static ulong LanguageCount()
    {
        var nativeResult = NativeMethods.LanguageCount();
        var returnValue = nativeResult;
        return returnValue;
    }

    /// <summary>
    /// Process source code and extract file intelligence using the global registry.
    ///
    /// Parses the source with tree-sitter and extracts metrics, structure, imports,
    /// exports, comments, docstrings, symbols, diagnostics, and/or chunks based on
    /// the flags set in [`ProcessConfig`].
    /// </summary>
    /// <exception cref="KreuzbergException">Returns an error if the language is not found or parsing fails.</exception>
    ///
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::{ProcessConfig, process};
    ///
    /// let config = ProcessConfig::new("python").all();
    /// let result = process("def hello(): pass", &config).unwrap();
    /// println!("Language: {}", result.language);
    /// println!("Lines: {}", result.metrics.total_lines);
    /// println!("Structures: {}", result.structure.len());
    /// </code></example>
    /// <param name="source"></param>
    /// <param name="config"></param>
    public static ProcessResult Process(string source, ProcessConfig config)
    {
        ArgumentNullException.ThrowIfNull(source);
        ArgumentNullException.ThrowIfNull(config);
        var configJson = JsonSerializer.Serialize(config, JsonOptions);
        var configHandle = NativeMethods.ProcessConfigFromJson(configJson);
        var nativeResult = NativeMethods.Process(
            source,
            configHandle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw GetLastError();
        }
        var jsonPtr = NativeMethods.ProcessResultToJson(nativeResult);
        var json = Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessResultFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessResult>(json ?? "null", JsonOptions)!;
        NativeMethods.ProcessConfigFree(configHandle);
        return returnValue;
    }

    /// <summary>
    /// Initialize the language pack with the given configuration.
    ///
    /// Applies any custom cache directory, then downloads all languages and groups
    /// specified in the config. This is the recommended entry point when you want
    /// to pre-warm the cache before use.
    /// </summary>
    /// <exception cref="KreuzbergException">Returns an error if configuration cannot be applied or if downloads fail.</exception>
    ///
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::{PackConfig, init};
    ///
    /// let config = PackConfig {
    ///     cache_dir: None,
    ///     languages: Some(vec!["python".to_string(), "rust".to_string()]),
    ///     groups: None,
    /// };
    /// init(&config).unwrap();
    /// </code></example>
    /// <param name="config"></param>
    public static void Init(PackConfig config)
    {
        ArgumentNullException.ThrowIfNull(config);
        var configJson = JsonSerializer.Serialize(config, JsonOptions);
        var configHandle = NativeMethods.PackConfigFromJson(configJson);
        NativeMethods.Init(
            configHandle
        );
        NativeMethods.PackConfigFree(configHandle);
    }

    /// <summary>
    /// Apply download configuration without downloading anything.
    ///
    /// Use this to set a custom cache directory before the first call to
    /// [`get_language`] or any download function. Changing the cache dir
    /// after languages have been registered has no effect on already-loaded
    /// languages.
    /// </summary>
    /// <exception cref="KreuzbergException">Returns an error if the lock cannot be acquired.</exception>
    ///
    /// <example><code language="csharp">
    /// use std::path::PathBuf;
    /// use tree_sitter_language_pack::{PackConfig, configure};
    ///
    /// let config = PackConfig {
    ///     cache_dir: Some(PathBuf::from("/tmp/my-parsers")),
    ///     languages: None,
    ///     groups: None,
    /// };
    /// configure(&config).unwrap();
    /// </code></example>
    /// <param name="config"></param>
    public static void Configure(PackConfig config)
    {
        ArgumentNullException.ThrowIfNull(config);
        var configJson = JsonSerializer.Serialize(config, JsonOptions);
        var configHandle = NativeMethods.PackConfigFromJson(configJson);
        NativeMethods.Configure(
            configHandle
        );
        NativeMethods.PackConfigFree(configHandle);
    }

    /// <summary>
    /// Download specific languages to the local cache.
    ///
    /// Returns the number of requested languages available after the call. Already
    /// compiled or cached languages are included in the count.
    /// </summary>
    /// <exception cref="KreuzbergException">Returns an error if any language is not available in the manifest or if
    /// the download fails.</exception>
    ///
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::download;
    ///
    /// let count = download(&["python", "rust", "typescript"]).unwrap();
    /// println!("Ensured {} languages", count);
    /// </code></example>
    /// <param name="names"></param>
    public static ulong Download(List<string> names)
    {
        var namesJson = JsonSerializer.Serialize(names, JsonOptions);
        var namesHandle = Marshal.StringToHGlobalAnsi(namesJson);
        var nativeResult = NativeMethods.Download(
            namesHandle
        );
        var returnValue = nativeResult;
        Marshal.FreeHGlobal(namesHandle);
        return returnValue;
    }

    /// <summary>
    /// Download all available languages from the remote manifest.
    ///
    /// Downloads the platform bundle and extracts every library it contains.
    /// Languages that appear in the manifest but are absent from the bundle
    /// (e.g. grammars that failed to compile at release time) are silently
    /// skipped — they are not treated as an error.
    ///
    /// Returns the total number of languages now available (statically compiled
    /// plus downloaded and cached).
    /// </summary>
    /// <exception cref="KreuzbergException">Returns an error if the manifest cannot be fetched or the bundle download fails.</exception>
    ///
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::download_all;
    ///
    /// let count = download_all().unwrap();
    /// println!("{} languages available", count);
    /// </code></example>
    public static ulong DownloadAll()
    {
        var nativeResult = NativeMethods.DownloadAll();
        var returnValue = nativeResult;
        return returnValue;
    }

    /// <summary>
    /// Return all language names available in the remote manifest (305).
    ///
    /// Fetches (and caches) the remote manifest to discover the full list of
    /// downloadable languages. Use [`downloaded_languages`] to list what is
    /// already cached locally.
    /// </summary>
    /// <exception cref="KreuzbergException">Returns an error if the manifest cannot be fetched.</exception>
    ///
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::manifest_languages;
    ///
    /// let langs = manifest_languages().unwrap();
    /// println!("{} languages available for download", langs.len());
    /// </code></example>
    public static List<string> ManifestLanguages()
    {
        var nativeResult = NativeMethods.ManifestLanguages();
        if (nativeResult == IntPtr.Zero)
        {
            throw GetLastError();
        }
        var json = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<List<string>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Return languages that are already downloaded and cached locally.
    ///
    /// Does not perform any network requests. Returns an empty list if the
    /// cache directory does not exist or cannot be read.
    /// </summary>
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::downloaded_languages;
    ///
    /// let langs = downloaded_languages();
    /// println!("{} languages already cached", langs.len());
    /// </code></example>
    public static List<string> DownloadedLanguages()
    {
        var nativeResult = NativeMethods.DownloadedLanguages();
        if (nativeResult == IntPtr.Zero)
        {
            throw GetLastError();
        }
        var json = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<List<string>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Delete all cached parser shared libraries.
    ///
    /// Resets the cache registration so the next call to [`get_language`] or
    /// a download function will re-register the (now empty) cache directory.
    /// </summary>
    /// <exception cref="KreuzbergException">Returns an error if the cache directory cannot be removed.</exception>
    ///
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::clean_cache;
    ///
    /// clean_cache().unwrap();
    /// println!("Cache cleared");
    /// </code></example>
    public static void CleanCache()
    {
        NativeMethods.CleanCache();
    }

    /// <summary>
    /// Return the effective cache directory path.
    ///
    /// This is either the custom path set via [`configure`] / [`init`] or the
    /// default: `~/.cache/tree-sitter-language-pack/v{version}/libs/`.
    /// </summary>
    /// <exception cref="KreuzbergException">Returns an error if the system cache directory cannot be determined.</exception>
    ///
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::cache_dir;
    ///
    /// let dir = cache_dir().unwrap();
    /// println!("Cache directory: {dir}");
    /// </code></example>
    public static string CacheDir()
    {
        var nativeResult = NativeMethods.CacheDir();
        if (nativeResult == IntPtr.Zero)
        {
            throw GetLastError();
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Load configuration from a TOML file.
    /// </summary>
    /// <exception cref="KreuzbergException">Returns an error if the file cannot be read or the TOML is invalid.</exception>
    ///
    /// <example><code language="csharp">
    /// use std::path::Path;
    /// use tree_sitter_language_pack::PackConfig;
    ///
    /// let config = PackConfig::from_toml_file(Path::new("language-pack.toml")).unwrap();
    /// </code></example>
    /// <param name="path"></param>
    public static PackConfig PackConfigFromTomlFile(string path)
    {
        var nativeResult = NativeMethods.PackConfigFromTomlFile(
            path
        );
        var jsonPtr = NativeMethods.PackConfigToJson(nativeResult);
        var json = Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.PackConfigFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<PackConfig>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Discover configuration by searching for `language-pack.toml` in:
    ///
    /// 1. Current directory and up to 10 parent directories
    /// 2. `$XDG_CONFIG_HOME/tree-sitter-language-pack/config.toml`
    /// 3. `~/.config/tree-sitter-language-pack/config.toml`
    ///
    /// Returns `None` if no configuration file is found.
    /// </summary>
    /// <example><code language="csharp">
    /// use tree_sitter_language_pack::PackConfig;
    ///
    /// if let Some(config) = PackConfig::discover() {
    ///     println!("Found config with {:?} languages", config.languages);
    /// }
    /// </code></example>
    public static PackConfig? PackConfigDiscover()
    {
        var nativeResult = NativeMethods.PackConfigDiscover();
        var json = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<PackConfig?>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    public static ProcessConfig ProcessConfigDefault()
    {
        var nativeResult = NativeMethods.ProcessConfigDefault();
        var jsonPtr = NativeMethods.ProcessConfigToJson(nativeResult);
        var json = Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessConfigFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessConfig>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Enable chunking with the given maximum chunk size in bytes.
    /// </summary>
    /// <param name="maxSize"></param>
    public static ProcessConfig ProcessConfigWithChunking(IntPtr handle, ulong maxSize)
    {
        var nativeResult = NativeMethods.ProcessConfigWithChunking(
            handle,
            maxSize
        );
        var jsonPtr = NativeMethods.ProcessConfigToJson(nativeResult);
        var json = Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessConfigFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessConfig>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Enable all analysis features.
    /// </summary>
    public static ProcessConfig ProcessConfigAll(IntPtr handle)
    {
        var nativeResult = NativeMethods.ProcessConfigAll(
            handle
        );
        var jsonPtr = NativeMethods.ProcessConfigToJson(nativeResult);
        var json = Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessConfigFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessConfig>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Disable all analysis features (only metrics computed).
    /// </summary>
    public static ProcessConfig ProcessConfigMinimal(IntPtr handle)
    {
        var nativeResult = NativeMethods.ProcessConfigMinimal(
            handle
        );
        var jsonPtr = NativeMethods.ProcessConfigToJson(nativeResult);
        var json = Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessConfigFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessConfig>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    private static Exception GetLastError()
    {
        var code = NativeMethods.LastErrorCode();
        var ctxPtr = NativeMethods.LastErrorContext();
        var message = Marshal.PtrToStringUTF8(ctxPtr) ?? "Unknown error"; if (code == 2) return new ErrorException(message); return new TreeSitterLanguagePackException(code, message);
    }
}
