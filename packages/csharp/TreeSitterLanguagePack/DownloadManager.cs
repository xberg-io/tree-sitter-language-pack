// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:4fba80521883afd0c1972d7d674ddbb027f93e53aacfb9e002b2b1d239c359e5
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;using System.Text.Json;
using System.Text.Json.Serialization;using System.Collections.Generic;
namespace TreeSitterLanguagePack;

internal sealed class DownloadManagerSafeHandle : SafeHandle
{
    internal DownloadManagerSafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.DownloadManagerFree(handle);
        return true;
    }
}
/// <summary>/// Manages downloading and caching of pre-built parser shared libraries./// </summary>
public sealed class DownloadManager : IDisposable
{    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull
    };
    private readonly DownloadManagerSafeHandle _safeHandle;

    internal DownloadManager(IntPtr handle)
    {
        _safeHandle = new DownloadManagerSafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    /// <summary>
    /// Create a new download manager for the given version.
    /// </summary>
    public static DownloadManager New(string version)
    {
        var nativeResult = NativeMethods.DownloadManagerNew(
            version
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "DownloadManagerNew failed";
            throw new TreeSitterLanguagePackException(ec, msg);
        }
        var returnValue = new DownloadManager(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Create a download manager with a custom cache directory.
    /// </summary>
    public static DownloadManager WithCacheDir(string version, string cacheDir)
    {
        var nativeResult = NativeMethods.DownloadManagerWithCacheDir(
            version,
            cacheDir
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "DownloadManagerWithCacheDir failed";
            throw new TreeSitterLanguagePackException(ec, msg);
        }
        var returnValue = new DownloadManager(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Default cache directory: `~/.cache/tree-sitter-language-pack/v{version}/libs/`
    /// </summary>
    public static string DefaultCacheDir(string version)
    {
        var nativeResult = NativeMethods.DownloadManagerDefaultCacheDir(
            version
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "DownloadManagerDefaultCacheDir failed";
            throw new TreeSitterLanguagePackException(ec, msg);
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Return the path to the libs cache directory.
    /// </summary>
    public string CacheDir()
    {
        var nativeResult = NativeMethods.DownloadManagerCacheDir(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "DownloadManagerCacheDir failed";
            throw new TreeSitterLanguagePackException(ec, msg);
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// List languages that are already downloaded and cached.
    /// </summary>
    public List<string> InstalledLanguages()
    {
        var nativeResult = NativeMethods.DownloadManagerInstalledLanguages(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "DownloadManagerInstalledLanguages failed";
            throw new TreeSitterLanguagePackException(ec, msg);
        }
        var json = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<List<string>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Ensure the specified languages are available in the cache.
    /// Downloads the platform bundle if any requested languages are missing.
    /// </summary>
    public void EnsureLanguages(List<string> names)
    {
        var namesJson = JsonSerializer.Serialize(names, JsonOptions);
        var namesHandle = Marshal.StringToHGlobalAnsi(namesJson);
        NativeMethods.DownloadManagerEnsureLanguages(
            Handle,
            namesHandle
        );
        Marshal.FreeHGlobal(namesHandle);
    }

    /// <summary>
    /// Ensure all languages in a named group are available.
    /// </summary>
    public void EnsureGroup(string group)
    {
        NativeMethods.DownloadManagerEnsureGroup(
            Handle,
            group
        );
    }

    /// <summary>
    /// Get the expected path for a language's shared library in the cache.
    /// </summary>
    public string LibPath(string name)
    {
        var nativeResult = NativeMethods.DownloadManagerLibPath(
            Handle,
            name
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "DownloadManagerLibPath failed";
            throw new TreeSitterLanguagePackException(ec, msg);
        }
        var returnValue = Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Fetch the parser manifest from GitHub Releases.
    /// </summary>
    public ParserManifest FetchManifest()
    {
        var nativeResult = NativeMethods.DownloadManagerFetchManifest(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "DownloadManagerFetchManifest failed";
            throw new TreeSitterLanguagePackException(ec, msg);
        }
        var jsonPtr = NativeMethods.ParserManifestToJson(nativeResult);
        var json = Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ParserManifestFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ParserManifest>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Download the platform bundle and extract every library file it contains.
    ///
    /// Unlike [`ensure_languages`], this does not check the manifest language list
    /// against archive contents — it simply extracts all `.so`/`.dylib`/`.dll` files
    /// from the bundle. Languages in the manifest that are missing from the archive
    /// are silently ignored rather than returning an error.
    ///
    /// Returns the number of library files extracted (including those already cached).
    /// </summary>
    public ulong DownloadAllBestEffort()
    {
        var nativeResult = NativeMethods.DownloadManagerDownloadAllBestEffort(
            Handle
        );
        var returnValue = nativeResult;
        return returnValue;
    }

    /// <summary>
    /// Remove all cached parser libraries.
    /// </summary>
    public void CleanCache()
    {
        NativeMethods.DownloadManagerCleanCache(
            Handle
        );
    }
}
