// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:184b0ef1fb6ff0f3949ad143def7cc6f57c96b45851c20fe1050a0a7cfb9af29
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;using System.Text.Json;
using System.Text.Json.Serialization;using System.Collections.Generic;
namespace TreeSitterLanguagePack;

internal sealed class LanguageRegistrySafeHandle : SafeHandle
{
    internal LanguageRegistrySafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.LanguageRegistryFree(handle);
        return true;
    }
}
/// <summary>/// Thread-safe registry of tree-sitter language parsers./// /// Manages both statically compiled and dynamically loaded language grammars./// Use [`LanguageRegistry::new()`] for the default registry, or access the/// global instance via the module-level convenience functions/// (`get_language`, `available_languages`, etc.)./// /// # Example/// /// ```no_run/// use tree_sitter_language_pack::{LanguageRegistry, ProcessConfig};/// /// let registry = LanguageRegistry::new();/// let langs = registry.available_languages();/// println!("Available: {:?}", langs);/// /// let config = ProcessConfig::new("python").all();/// let result = registry.process("def hello(): pass", &config).unwrap();/// println!("Structure: {:?}", result.structure);/// ```/// </summary>
public sealed class LanguageRegistry : IDisposable
{    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull
    };
    private readonly LanguageRegistrySafeHandle _safeHandle;

    internal LanguageRegistry(IntPtr handle)
    {
        _safeHandle = new LanguageRegistrySafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    /// <summary>
    /// Create a registry with a custom directory for dynamic libraries.
    ///
    /// Overrides the default build-time library directory. Useful when
    /// dynamic grammar shared libraries are stored in a non-standard location.
    /// </summary>
    public static LanguageRegistry WithLibsDir(string libsDir)
    {
        var nativeResult = NativeMethods.LanguageRegistryWithLibsDir(
            libsDir
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new TreeSitterLanguagePackException(0, "LanguageRegistryWithLibsDir failed");
        }
        var returnValue = new LanguageRegistry(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Add an additional directory to search for dynamic libraries.
    ///
    /// When [`get_language`](Self::get_language) cannot find a grammar in the
    /// primary library directory, it searches these extra directories in order.
    /// Typically used by the download system to register its cache directory.
    ///
    /// Takes `&self` (not `&mut self`) because `extra_lib_dirs` uses interior
    /// mutability via an `Arc<RwLock<...>>`, so the outer registry can remain
    /// immutable while the directory list is updated.
    /// </summary>
    public void AddExtraLibsDir(string dir)
    {
        NativeMethods.LanguageRegistryAddExtraLibsDir(
            Handle,
            dir
        );
    }

    /// <summary>
    /// Get a tree-sitter [`Language`] by name.
    ///
    /// Resolves aliases (e.g., `"shell"` -> `"bash"`, `"makefile"` -> `"make"`),
    /// then looks up the language in the static table. When the `dynamic-loading`
    /// feature is enabled, falls back to loading a shared library on demand.
    ///
    /// # Errors
    ///
    /// Returns [`Error::LanguageNotFound`] if the name (after alias resolution)
    /// does not match any known grammar.
    /// </summary>
    public Language GetLanguage(string name)
    {
        var nativeResult = NativeMethods.LanguageRegistryGetLanguage(
            Handle,
            name
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new TreeSitterLanguagePackException(0, "LanguageRegistryGetLanguage failed");
        }
        var returnValue = new Language(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// List all available language names, sorted and deduplicated.
    ///
    /// Includes statically compiled languages, dynamically loadable languages
    /// (if the `dynamic-loading` feature is enabled), and all configured aliases.
    /// </summary>
    public List<string> AvailableLanguages()
    {
        var nativeResult = NativeMethods.LanguageRegistryAvailableLanguages(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new TreeSitterLanguagePackException(0, "LanguageRegistryAvailableLanguages failed");
        }
        var json = Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<List<string>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Check whether a language is available by name or alias.
    ///
    /// Returns `true` if the language can be loaded, either from the static
    /// table or from a dynamic library on disk.
    /// </summary>
    public bool HasLanguage(string name)
    {
        var nativeResult = NativeMethods.LanguageRegistryHasLanguage(
            Handle,
            name
        );
        var returnValue = nativeResult != 0;
        return returnValue;
    }

    /// <summary>
    /// Return the total number of available languages (including aliases).
    /// </summary>
    public ulong LanguageCount()
    {
        var nativeResult = NativeMethods.LanguageRegistryLanguageCount(
            Handle
        );
        var returnValue = nativeResult;
        return returnValue;
    }

    /// <summary>
    /// Parse source code and extract file intelligence based on config in a single pass.
    /// </summary>
    public ProcessResult Process(string source, ProcessConfig config)
    {
        var configJson = JsonSerializer.Serialize((config ?? new ProcessConfig()), JsonOptions);
        var configHandle = NativeMethods.ProcessConfigFromJson(configJson);
        var nativeResult = NativeMethods.LanguageRegistryProcess(
            Handle,
            source,
            configHandle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new TreeSitterLanguagePackException(0, "LanguageRegistryProcess failed");
        }
        var jsonPtr = NativeMethods.ProcessResultToJson(nativeResult);
        var json = Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessResultFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessResult>(json ?? "null", JsonOptions)!;
        NativeMethods.ProcessConfigFree(configHandle);
        return returnValue;
    }

    public static LanguageRegistry Default()
    {
        var nativeResult = NativeMethods.LanguageRegistryDefault(

        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new TreeSitterLanguagePackException(0, "LanguageRegistryDefault failed");
        }
        var returnValue = new LanguageRegistry(nativeResult);
        return returnValue;
    }
}
