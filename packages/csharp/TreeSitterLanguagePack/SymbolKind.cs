// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:76f3bac7536fa4e5b8ab9a93be197508306d06b566fe7cc519a38c1b9ebf9f42
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.IO;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace TreeSitterLanguagePack;

/// <summary>
/// The kind of a symbol definition found in source code.
///
/// Categorizes symbol definitions such as variables, constants, functions,
/// classes, types, interfaces, enums, and modules.
/// </summary>
    [JsonConverter(typeof(SymbolKindJsonConverter))]
public abstract record SymbolKind
{
    /// <summary>
    /// A variable binding.
    /// </summary>
    public sealed record Variable() : SymbolKind;

    /// <summary>
    /// A constant (immutable binding).
    /// </summary>
    public sealed record Constant() : SymbolKind;

    /// <summary>
    /// A function definition.
    /// </summary>
    public sealed record Function() : SymbolKind;

    /// <summary>
    /// A class definition.
    /// </summary>
    public sealed record Class() : SymbolKind;

    /// <summary>
    /// A type alias or typedef.
    /// </summary>
    public sealed record Type() : SymbolKind;

    /// <summary>
    /// An interface definition.
    /// </summary>
    public sealed record Interface() : SymbolKind;

    /// <summary>
    /// An enum definition.
    /// </summary>
    public sealed record Enum() : SymbolKind;

    /// <summary>
    /// A module declaration.
    /// </summary>
    public sealed record Module() : SymbolKind;

    /// <summary>
    /// A symbol kind not covered by the standard variants.
    /// </summary>
    public sealed record Other(
        [property: JsonPropertyName("value")] string Value
    ) : SymbolKind;

}

/// <summary>
/// Custom converter for SymbolKind sealed union with flattened variant fields.
/// </summary>
/// <remarks>
/// Handles JSON objects with a discriminator field (kind) and variant-specific
/// fields at the same level. System.Text.Json's [JsonPolymorphic] cannot handle
/// this layout, so we manually deserialize here.
/// </remarks>
public sealed class SymbolKindJsonConverter : JsonConverter<SymbolKind>
{
    public override SymbolKind Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        if (reader.TokenType != JsonTokenType.StartObject)
        {
            throw new JsonException($"Expected JSON object, got {reader.TokenType}");
        }

        using var doc = JsonDocument.ParseValue(ref reader);
        var root = doc.RootElement;

        if (!root.TryGetProperty("kind", out var tagElement))
        {
            throw new JsonException($"Missing discriminator field: kind");
        }

        var tagValue = tagElement.GetString();
        if (tagValue == null)
        {
            throw new JsonException("Discriminator field is null");
        }

        // Tuple-variant records (`Variant(InnerStruct value)`) expect a single
        // "Value" field holding the inner struct's JSON, so wrap the remaining
        // fields under "Value". Struct-variant records (`Variant { field1,
        // field2 }`) have positional record components annotated with
        // [JsonPropertyName(...)] for each named field, so pass the remaining
        // fields through directly without the wrap.
        using var ms = new MemoryStream();
        using var writer = new Utf8JsonWriter(ms);
        writer.WriteStartObject();
        foreach (var prop in root.EnumerateObject())
        {
            if (prop.Name != "kind")
            {
                writer.WritePropertyName(prop.Name);
                prop.Value.WriteTo(writer);
            }
        }
        writer.WriteEndObject();
        writer.Flush();
        ms.Position = 0;
        var flatJson = ms.ToArray();

        using var msWrapped = new MemoryStream();
        using var writerWrapped = new Utf8JsonWriter(msWrapped);
        writerWrapped.WriteStartObject();
        writerWrapped.WritePropertyName("Value");
        writerWrapped.WriteStartObject();
        foreach (var prop in root.EnumerateObject())
        {
            if (prop.Name != "kind")
            {
                writerWrapped.WritePropertyName(prop.Name);
                prop.Value.WriteTo(writerWrapped);
            }
        }
        writerWrapped.WriteEndObject();
        writerWrapped.WriteEndObject();
        writerWrapped.Flush();
        msWrapped.Position = 0;
        var wrappedJson = msWrapped.ToArray();

        return tagValue switch
        {            "Variable" => new SymbolKind.Variable(),            "Constant" => new SymbolKind.Constant(),            "Function" => new SymbolKind.Function(),            "Class" => new SymbolKind.Class(),            "Type" => new SymbolKind.Type(),            "Interface" => new SymbolKind.Interface(),            "Enum" => new SymbolKind.Enum(),            "Module" => new SymbolKind.Module(),            "Other" => JsonSerializer.Deserialize<SymbolKind.Other>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),            // Unknown discriminator: forward compatibility for unknown variants
            _ => throw new JsonException($"Unknown SymbolKind discriminator: {tagValue}")
        };
    }

    public override void Write(Utf8JsonWriter writer, SymbolKind value, JsonSerializerOptions options)
    {
        // Emit the discriminator tag plus the inner variant's fields flattened at
        // the same level — mirrors the Java sealed-union serializer pattern. Turn
        // `Message.User(UserMessage value)` into `{"kind":"user","content":...}`
        // not `{"value":{...}}`. Without this, sending a chat request to FFI fails
        // with "missing field kind" inside Rust serde.
        string tag;
        object? inner;
        switch (value)
        {            case SymbolKind.Variable _:
                tag = "Variable";
                inner = null;
                break;            case SymbolKind.Constant _:
                tag = "Constant";
                inner = null;
                break;            case SymbolKind.Function _:
                tag = "Function";
                inner = null;
                break;            case SymbolKind.Class _:
                tag = "Class";
                inner = null;
                break;            case SymbolKind.Type _:
                tag = "Type";
                inner = null;
                break;            case SymbolKind.Interface _:
                tag = "Interface";
                inner = null;
                break;            case SymbolKind.Enum _:
                tag = "Enum";
                inner = null;
                break;            case SymbolKind.Module _:
                tag = "Module";
                inner = null;
                break;            case SymbolKind.Other v_other:
                tag = "Other";                inner = v_other;                break;            default:
                throw new JsonException($"Unknown SymbolKind variant: {value.GetType().Name}");
        }

        writer.WriteStartObject();
        writer.WriteString("kind", tag);
        if (inner != null)
        {
            using var doc = JsonSerializer.SerializeToDocument(inner, inner.GetType(), options);
            if (doc.RootElement.ValueKind == JsonValueKind.Object)
            {
                foreach (var prop in doc.RootElement.EnumerateObject())
                {
                    writer.WritePropertyName(prop.Name);
                    prop.Value.WriteTo(writer);
                }
            }
        }
        writer.WriteEndObject();
    }
}
