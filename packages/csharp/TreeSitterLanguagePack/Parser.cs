// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:81ca67ffe18ff4f6f7c668a2315e0f14a9c22211af6085f4bec3af08c524b9d3
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;using System.Text.Json;
using System.Text.Json.Serialization;
namespace TreeSitterLanguagePack;

internal sealed class ParserSafeHandle : SafeHandle
{
    internal ParserSafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.ParserFree(handle);
        return true;
    }
}
/// <summary>/// A tree-sitter parser configured for one language at a time./// /// # Example/// /// ```no_run/// use tree_sitter_language_pack::Parser;/// /// let mut parser = Parser::new();/// parser.set_language("python")?;/// let tree = parser.parse("def hello(): pass").expect("parse failed");/// assert_eq!(tree.root_node().kind(), "module");/// # Ok::<(), tree_sitter_language_pack::Error>(())/// ```/// </summary>
public sealed class Parser : IDisposable
{    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull
    };
    private readonly ParserSafeHandle _safeHandle;

    internal Parser(IntPtr handle)
    {
        _safeHandle = new ParserSafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    /// <summary>
    /// Configure the parser to use the language identified by name (e.g. `"python"`).
    ///
    /// Resolves the language through the global registry — auto-downloading
    /// if necessary, when the `download` feature is enabled.
    ///
    /// # Errors
    ///
    /// Returns [`Error::LanguageNotFound`] if the language is not recognized,
    /// or [`Error::ParserSetup`] if the language ABI is incompatible.
    /// </summary>
    public void SetLanguage(string name)
    {
        NativeMethods.ParserSetLanguage(
            Handle,
            name
        );
    }

    /// <summary>
    /// Parse a UTF-8 source string. Returns `None` if parsing was cancelled
    /// or no language is set.
    /// </summary>
    public Tree? Parse(string source)
    {
        var nativeResult = NativeMethods.ParserParse(
            Handle,
            source
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = new Tree(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Parse a raw byte slice. Returns `None` if parsing was cancelled or
    /// no language is set.
    /// </summary>
    public Tree? ParseBytes(byte[] source)
    {
        var sourceHandle = GCHandle.Alloc(source, GCHandleType.Pinned);
        var nativeResult = NativeMethods.ParserParseBytes(
            Handle,
            sourceHandle.AddrOfPinnedObject(),
            (nuint)source.Length
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = new Tree(nativeResult);
        sourceHandle.Free();
        return returnValue;
    }

    /// <summary>
    /// Reset internal state. The next call to [`parse`](Self::parse) will
    /// not be incremental.
    /// </summary>
    public void Reset()
    {
        NativeMethods.ParserReset(
            Handle
        );
    }

    public static Parser Default()
    {
        var nativeResult = NativeMethods.ParserDefault(

        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "ParserDefault failed";
            throw new TreeSitterLanguagePackException(ec, msg);
        }
        var returnValue = new Parser(nativeResult);
        return returnValue;
    }
}
