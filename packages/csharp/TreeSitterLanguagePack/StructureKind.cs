// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:76f3bac7536fa4e5b8ab9a93be197508306d06b566fe7cc519a38c1b9ebf9f42
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.IO;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace TreeSitterLanguagePack;

/// <summary>
/// The kind of structural item found in source code.
///
/// Categorizes top-level and nested declarations such as functions, classes,
/// structs, enums, traits, and more. Use `Other`(StructureKind.Other) for
/// language-specific constructs that do not fit a standard category.
/// </summary>
    [JsonConverter(typeof(StructureKindJsonConverter))]
public abstract record StructureKind
{
    /// <summary>
    /// A free-standing or associated function.
    /// </summary>
    public sealed record Function() : StructureKind;

    /// <summary>
    /// A method defined inside a class, struct, trait, or impl block.
    /// </summary>
    public sealed record Method() : StructureKind;

    /// <summary>
    /// A class definition.
    /// </summary>
    public sealed record Class() : StructureKind;

    /// <summary>
    /// A struct definition.
    /// </summary>
    public sealed record Struct() : StructureKind;

    /// <summary>
    /// An interface or protocol definition.
    /// </summary>
    public sealed record Interface() : StructureKind;

    /// <summary>
    /// An enum definition.
    /// </summary>
    public sealed record Enum() : StructureKind;

    /// <summary>
    /// A module or package declaration.
    /// </summary>
    public sealed record Module() : StructureKind;

    /// <summary>
    /// A trait definition.
    /// </summary>
    public sealed record Trait() : StructureKind;

    /// <summary>
    /// An impl block (Rust) or similar implementation block.
    /// </summary>
    public sealed record Impl() : StructureKind;

    /// <summary>
    /// A namespace declaration.
    /// </summary>
    public sealed record Namespace() : StructureKind;

    /// <summary>
    /// A language-specific construct that does not fit any standard category.
    /// The `value` field carries the language-specific kind label.
    /// </summary>
    public sealed record Other(
        [property: JsonPropertyName("value")] string Value
    ) : StructureKind;

}

/// <summary>
/// Custom converter for StructureKind sealed union with flattened variant fields.
/// </summary>
/// <remarks>
/// Handles JSON objects with a discriminator field (kind) and variant-specific
/// fields at the same level. System.Text.Json's [JsonPolymorphic] cannot handle
/// this layout, so we manually deserialize here.
/// </remarks>
public sealed class StructureKindJsonConverter : JsonConverter<StructureKind>
{
    public override StructureKind Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        if (reader.TokenType != JsonTokenType.StartObject)
        {
            throw new JsonException($"Expected JSON object, got {reader.TokenType}");
        }

        using var doc = JsonDocument.ParseValue(ref reader);
        var root = doc.RootElement;

        if (!root.TryGetProperty("kind", out var tagElement))
        {
            throw new JsonException($"Missing discriminator field: kind");
        }

        var tagValue = tagElement.GetString();
        if (tagValue == null)
        {
            throw new JsonException("Discriminator field is null");
        }

        // Tuple-variant records (`Variant(InnerStruct value)`) expect a single
        // "Value" field holding the inner struct's JSON, so wrap the remaining
        // fields under "Value". Struct-variant records (`Variant { field1,
        // field2 }`) have positional record components annotated with
        // [JsonPropertyName(...)] for each named field, so pass the remaining
        // fields through directly without the wrap.
        using var ms = new MemoryStream();
        using var writer = new Utf8JsonWriter(ms);
        writer.WriteStartObject();
        foreach (var prop in root.EnumerateObject())
        {
            if (prop.Name != "kind")
            {
                writer.WritePropertyName(prop.Name);
                prop.Value.WriteTo(writer);
            }
        }
        writer.WriteEndObject();
        writer.Flush();
        ms.Position = 0;
        var flatJson = ms.ToArray();

        using var msWrapped = new MemoryStream();
        using var writerWrapped = new Utf8JsonWriter(msWrapped);
        writerWrapped.WriteStartObject();
        writerWrapped.WritePropertyName("Value");
        writerWrapped.WriteStartObject();
        foreach (var prop in root.EnumerateObject())
        {
            if (prop.Name != "kind")
            {
                writerWrapped.WritePropertyName(prop.Name);
                prop.Value.WriteTo(writerWrapped);
            }
        }
        writerWrapped.WriteEndObject();
        writerWrapped.WriteEndObject();
        writerWrapped.Flush();
        msWrapped.Position = 0;
        var wrappedJson = msWrapped.ToArray();

        return tagValue switch
        {            "Function" => new StructureKind.Function(),            "Method" => new StructureKind.Method(),            "Class" => new StructureKind.Class(),            "Struct" => new StructureKind.Struct(),            "Interface" => new StructureKind.Interface(),            "Enum" => new StructureKind.Enum(),            "Module" => new StructureKind.Module(),            "Trait" => new StructureKind.Trait(),            "Impl" => new StructureKind.Impl(),            "Namespace" => new StructureKind.Namespace(),            "Other" => JsonSerializer.Deserialize<StructureKind.Other>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),            // Unknown discriminator: forward compatibility for unknown variants
            _ => throw new JsonException($"Unknown StructureKind discriminator: {tagValue}")
        };
    }

    public override void Write(Utf8JsonWriter writer, StructureKind value, JsonSerializerOptions options)
    {
        // Emit the discriminator tag plus the inner variant's fields flattened at
        // the same level — mirrors the Java sealed-union serializer pattern. Turn
        // `Message.User(UserMessage value)` into `{"kind":"user","content":...}`
        // not `{"value":{...}}`. Without this, sending a chat request to FFI fails
        // with "missing field kind" inside Rust serde.
        string tag;
        object? inner;
        switch (value)
        {            case StructureKind.Function _:
                tag = "Function";
                inner = null;
                break;            case StructureKind.Method _:
                tag = "Method";
                inner = null;
                break;            case StructureKind.Class _:
                tag = "Class";
                inner = null;
                break;            case StructureKind.Struct _:
                tag = "Struct";
                inner = null;
                break;            case StructureKind.Interface _:
                tag = "Interface";
                inner = null;
                break;            case StructureKind.Enum _:
                tag = "Enum";
                inner = null;
                break;            case StructureKind.Module _:
                tag = "Module";
                inner = null;
                break;            case StructureKind.Trait _:
                tag = "Trait";
                inner = null;
                break;            case StructureKind.Impl _:
                tag = "Impl";
                inner = null;
                break;            case StructureKind.Namespace _:
                tag = "Namespace";
                inner = null;
                break;            case StructureKind.Other v_other:
                tag = "Other";                inner = v_other;                break;            default:
                throw new JsonException($"Unknown StructureKind variant: {value.GetType().Name}");
        }

        writer.WriteStartObject();
        writer.WriteString("kind", tag);
        if (inner != null)
        {
            using var doc = JsonSerializer.SerializeToDocument(inner, inner.GetType(), options);
            if (doc.RootElement.ValueKind == JsonValueKind.Object)
            {
                foreach (var prop in doc.RootElement.EnumerateObject())
                {
                    writer.WritePropertyName(prop.Name);
                    prop.Value.WriteTo(writer);
                }
            }
        }
        writer.WriteEndObject();
    }
}
