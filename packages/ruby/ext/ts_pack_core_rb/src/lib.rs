// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:4993dc3af968cc6e9768b51784b0c5db49116b6922f6cd34ad67079fb48c7514
// Re-generate with: alef generate
#![allow(dead_code, unused_imports, unused_variables)]
#![allow(
    clippy::too_many_arguments,
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::map_identity,
    clippy::just_underscores_and_digits,
    clippy::unnecessary_cast,
    clippy::unused_unit,
    clippy::unwrap_or_default,
    clippy::derivable_impls,
    clippy::needless_borrows_for_generic_args,
    clippy::unnecessary_fallible_conversions,
    clippy::type_complexity,
    clippy::useless_conversion,
    clippy::clone_on_copy
)]

use magnus::{Error, IntoValueFromNative, Ruby, function, method, prelude::*, try_convert::TryConvertOwned};
use std::sync::Arc;
use std::sync::Mutex;

fn json_to_ruby(handle: &Ruby, val: serde_json::Value) -> magnus::Value {
    use magnus::IntoValue;
    match val {
        serde_json::Value::Null => handle.qnil().into_value_with(handle),
        serde_json::Value::Bool(b) => b.into_value_with(handle),
        serde_json::Value::Number(n) => {
            if let Some(i) = n.as_i64() {
                i.into_value_with(handle)
            } else if let Some(f) = n.as_f64() {
                f.into_value_with(handle)
            } else {
                handle.qnil().into_value_with(handle)
            }
        }
        serde_json::Value::String(s) => s.into_value_with(handle),
        serde_json::Value::Array(arr) => {
            let ruby_arr = handle.ary_new_capa(arr.len());
            for item in arr {
                let _ = ruby_arr.push(json_to_ruby(handle, item));
            }
            ruby_arr.into_value_with(handle)
        }
        serde_json::Value::Object(map) => {
            let hash = handle.hash_new();
            for (k, v) in map {
                let key = handle.to_symbol(&k);
                let val = json_to_ruby(handle, v);
                let _ = hash.aset(key, val);
            }
            hash.into_value_with(handle)
        }
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::Span")]
pub struct Span {
    start_byte: usize,
    end_byte: usize,
    start_line: usize,
    start_column: usize,
    end_line: usize,
    end_column: usize,
}

unsafe impl IntoValueFromNative for Span {}

impl magnus::TryConvert for Span {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&Span as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into Span: {}", e),
                )
            })?
        };
        serde_json::from_str::<Span>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize Span: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for Span {}

impl Default for Span {
    fn default() -> Self {
        tree_sitter_language_pack::Span::default().into()
    }
}

impl Span {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            start_byte: kwargs
                .get(ruby.to_symbol("start_byte"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            end_byte: kwargs
                .get(ruby.to_symbol("end_byte"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            start_line: kwargs
                .get(ruby.to_symbol("start_line"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            start_column: kwargs
                .get(ruby.to_symbol("start_column"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            end_line: kwargs
                .get(ruby.to_symbol("end_line"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            end_column: kwargs
                .get(ruby.to_symbol("end_column"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn start_byte(&self) -> usize {
        self.start_byte
    }

    fn end_byte(&self) -> usize {
        self.end_byte
    }

    fn start_line(&self) -> usize {
        self.start_line
    }

    fn start_column(&self) -> usize {
        self.start_column
    }

    fn end_line(&self) -> usize {
        self.end_line
    }

    fn end_column(&self) -> usize {
        self.end_column
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::ProcessResult")]
pub struct ProcessResult {
    language: String,
    metrics: FileMetrics,
    structure: Vec<StructureItem>,
    imports: Vec<ImportInfo>,
    exports: Vec<ExportInfo>,
    comments: Vec<CommentInfo>,
    docstrings: Vec<DocstringInfo>,
    symbols: Vec<SymbolInfo>,
    diagnostics: Vec<Diagnostic>,
    chunks: Vec<CodeChunk>,
}

unsafe impl IntoValueFromNative for ProcessResult {}

impl magnus::TryConvert for ProcessResult {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&ProcessResult as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into ProcessResult: {}", e),
                )
            })?
        };
        serde_json::from_str::<ProcessResult>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize ProcessResult: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for ProcessResult {}

impl Default for ProcessResult {
    fn default() -> Self {
        tree_sitter_language_pack::ProcessResult::default().into()
    }
}

impl ProcessResult {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            language: kwargs
                .get(ruby.to_symbol("language"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
            metrics: kwargs
                .get(ruby.to_symbol("metrics"))
                .and_then(|v| FileMetrics::try_convert(v).ok())
                .unwrap_or_default(),
            structure: kwargs
                .get(ruby.to_symbol("structure"))
                .and_then(|v| <Vec<StructureItem>>::try_convert(v).ok())
                .unwrap_or_default(),
            imports: kwargs
                .get(ruby.to_symbol("imports"))
                .and_then(|v| <Vec<ImportInfo>>::try_convert(v).ok())
                .unwrap_or_default(),
            exports: kwargs
                .get(ruby.to_symbol("exports"))
                .and_then(|v| <Vec<ExportInfo>>::try_convert(v).ok())
                .unwrap_or_default(),
            comments: kwargs
                .get(ruby.to_symbol("comments"))
                .and_then(|v| <Vec<CommentInfo>>::try_convert(v).ok())
                .unwrap_or_default(),
            docstrings: kwargs
                .get(ruby.to_symbol("docstrings"))
                .and_then(|v| <Vec<DocstringInfo>>::try_convert(v).ok())
                .unwrap_or_default(),
            symbols: kwargs
                .get(ruby.to_symbol("symbols"))
                .and_then(|v| <Vec<SymbolInfo>>::try_convert(v).ok())
                .unwrap_or_default(),
            diagnostics: kwargs
                .get(ruby.to_symbol("diagnostics"))
                .and_then(|v| <Vec<Diagnostic>>::try_convert(v).ok())
                .unwrap_or_default(),
            chunks: kwargs
                .get(ruby.to_symbol("chunks"))
                .and_then(|v| <Vec<CodeChunk>>::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn language(&self) -> String {
        self.language.clone()
    }

    fn metrics(&self) -> FileMetrics {
        self.metrics.clone()
    }

    fn structure(&self) -> Vec<StructureItem> {
        self.structure.clone()
    }

    fn imports(&self) -> Vec<ImportInfo> {
        self.imports.clone()
    }

    fn exports(&self) -> Vec<ExportInfo> {
        self.exports.clone()
    }

    fn comments(&self) -> Vec<CommentInfo> {
        self.comments.clone()
    }

    fn docstrings(&self) -> Vec<DocstringInfo> {
        self.docstrings.clone()
    }

    fn symbols(&self) -> Vec<SymbolInfo> {
        self.symbols.clone()
    }

    fn diagnostics(&self) -> Vec<Diagnostic> {
        self.diagnostics.clone()
    }

    fn chunks(&self) -> Vec<CodeChunk> {
        self.chunks.clone()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::FileMetrics")]
pub struct FileMetrics {
    total_lines: usize,
    code_lines: usize,
    comment_lines: usize,
    blank_lines: usize,
    total_bytes: usize,
    node_count: usize,
    error_count: usize,
    max_depth: usize,
}

unsafe impl IntoValueFromNative for FileMetrics {}

impl magnus::TryConvert for FileMetrics {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&FileMetrics as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into FileMetrics: {}", e),
                )
            })?
        };
        serde_json::from_str::<FileMetrics>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize FileMetrics: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for FileMetrics {}

impl Default for FileMetrics {
    fn default() -> Self {
        tree_sitter_language_pack::FileMetrics::default().into()
    }
}

impl FileMetrics {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            total_lines: kwargs
                .get(ruby.to_symbol("total_lines"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            code_lines: kwargs
                .get(ruby.to_symbol("code_lines"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            comment_lines: kwargs
                .get(ruby.to_symbol("comment_lines"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            blank_lines: kwargs
                .get(ruby.to_symbol("blank_lines"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            total_bytes: kwargs
                .get(ruby.to_symbol("total_bytes"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            node_count: kwargs
                .get(ruby.to_symbol("node_count"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            error_count: kwargs
                .get(ruby.to_symbol("error_count"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            max_depth: kwargs
                .get(ruby.to_symbol("max_depth"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn total_lines(&self) -> usize {
        self.total_lines
    }

    fn code_lines(&self) -> usize {
        self.code_lines
    }

    fn comment_lines(&self) -> usize {
        self.comment_lines
    }

    fn blank_lines(&self) -> usize {
        self.blank_lines
    }

    fn total_bytes(&self) -> usize {
        self.total_bytes
    }

    fn node_count(&self) -> usize {
        self.node_count
    }

    fn error_count(&self) -> usize {
        self.error_count
    }

    fn max_depth(&self) -> usize {
        self.max_depth
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::StructureItem")]
pub struct StructureItem {
    kind: StructureKind,
    name: Option<String>,
    visibility: Option<String>,
    span: Span,
    children: Vec<StructureItem>,
    decorators: Vec<String>,
    doc_comment: Option<String>,
    signature: Option<String>,
    body_span: Option<Span>,
}

unsafe impl IntoValueFromNative for StructureItem {}

impl magnus::TryConvert for StructureItem {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&StructureItem as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into StructureItem: {}", e),
                )
            })?
        };
        serde_json::from_str::<StructureItem>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize StructureItem: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for StructureItem {}

impl Default for StructureItem {
    fn default() -> Self {
        Self {
            kind: Default::default(),
            name: None,
            visibility: None,
            span: Default::default(),
            children: vec![],
            decorators: vec![],
            doc_comment: None,
            signature: None,
            body_span: None,
        }
    }
}

impl StructureItem {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            kind: kwargs
                .get(ruby.to_symbol("kind"))
                .and_then(|v| StructureKind::try_convert(v).ok())
                .unwrap_or_default(),
            name: kwargs
                .get(ruby.to_symbol("name"))
                .and_then(|v| String::try_convert(v).ok()),
            visibility: kwargs
                .get(ruby.to_symbol("visibility"))
                .and_then(|v| String::try_convert(v).ok()),
            span: kwargs
                .get(ruby.to_symbol("span"))
                .and_then(|v| Span::try_convert(v).ok())
                .unwrap_or_default(),
            children: kwargs
                .get(ruby.to_symbol("children"))
                .and_then(|v| <Vec<StructureItem>>::try_convert(v).ok())
                .unwrap_or_default(),
            decorators: kwargs
                .get(ruby.to_symbol("decorators"))
                .and_then(|v| <Vec<String>>::try_convert(v).ok())
                .unwrap_or_default(),
            doc_comment: kwargs
                .get(ruby.to_symbol("doc_comment"))
                .and_then(|v| String::try_convert(v).ok()),
            signature: kwargs
                .get(ruby.to_symbol("signature"))
                .and_then(|v| String::try_convert(v).ok()),
            body_span: kwargs
                .get(ruby.to_symbol("body_span"))
                .and_then(|v| Span::try_convert(v).ok()),
        })
    }

    fn kind(&self) -> StructureKind {
        self.kind.clone()
    }

    fn name(&self) -> Option<String> {
        self.name.clone()
    }

    fn visibility(&self) -> Option<String> {
        self.visibility.clone()
    }

    fn span(&self) -> Span {
        self.span.clone()
    }

    fn children(&self) -> Vec<StructureItem> {
        self.children.clone()
    }

    fn decorators(&self) -> Vec<String> {
        self.decorators.clone()
    }

    fn doc_comment(&self) -> Option<String> {
        self.doc_comment.clone()
    }

    fn signature(&self) -> Option<String> {
        self.signature.clone()
    }

    fn body_span(&self) -> Option<Span> {
        self.body_span.clone()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::CommentInfo")]
pub struct CommentInfo {
    text: String,
    kind: CommentKind,
    span: Span,
    associated_node: Option<String>,
}

unsafe impl IntoValueFromNative for CommentInfo {}

impl magnus::TryConvert for CommentInfo {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&CommentInfo as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into CommentInfo: {}", e),
                )
            })?
        };
        serde_json::from_str::<CommentInfo>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize CommentInfo: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for CommentInfo {}

impl Default for CommentInfo {
    fn default() -> Self {
        Self {
            text: String::new(),
            kind: Default::default(),
            span: Default::default(),
            associated_node: None,
        }
    }
}

impl CommentInfo {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            text: kwargs
                .get(ruby.to_symbol("text"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
            kind: kwargs
                .get(ruby.to_symbol("kind"))
                .and_then(|v| CommentKind::try_convert(v).ok())
                .unwrap_or_default(),
            span: kwargs
                .get(ruby.to_symbol("span"))
                .and_then(|v| Span::try_convert(v).ok())
                .unwrap_or_default(),
            associated_node: kwargs
                .get(ruby.to_symbol("associated_node"))
                .and_then(|v| String::try_convert(v).ok()),
        })
    }

    fn text(&self) -> String {
        self.text.clone()
    }

    fn kind(&self) -> CommentKind {
        self.kind.clone()
    }

    fn span(&self) -> Span {
        self.span.clone()
    }

    fn associated_node(&self) -> Option<String> {
        self.associated_node.clone()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::DocstringInfo")]
pub struct DocstringInfo {
    text: String,
    format: DocstringFormat,
    span: Span,
    associated_item: Option<String>,
    parsed_sections: Vec<DocSection>,
}

unsafe impl IntoValueFromNative for DocstringInfo {}

impl magnus::TryConvert for DocstringInfo {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&DocstringInfo as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into DocstringInfo: {}", e),
                )
            })?
        };
        serde_json::from_str::<DocstringInfo>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize DocstringInfo: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for DocstringInfo {}

impl Default for DocstringInfo {
    fn default() -> Self {
        Self {
            text: String::new(),
            format: Default::default(),
            span: Default::default(),
            associated_item: None,
            parsed_sections: vec![],
        }
    }
}

impl DocstringInfo {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            text: kwargs
                .get(ruby.to_symbol("text"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
            format: kwargs
                .get(ruby.to_symbol("format"))
                .and_then(|v| DocstringFormat::try_convert(v).ok())
                .unwrap_or_default(),
            span: kwargs
                .get(ruby.to_symbol("span"))
                .and_then(|v| Span::try_convert(v).ok())
                .unwrap_or_default(),
            associated_item: kwargs
                .get(ruby.to_symbol("associated_item"))
                .and_then(|v| String::try_convert(v).ok()),
            parsed_sections: kwargs
                .get(ruby.to_symbol("parsed_sections"))
                .and_then(|v| <Vec<DocSection>>::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn text(&self) -> String {
        self.text.clone()
    }

    fn format(&self) -> DocstringFormat {
        self.format.clone()
    }

    fn span(&self) -> Span {
        self.span.clone()
    }

    fn associated_item(&self) -> Option<String> {
        self.associated_item.clone()
    }

    fn parsed_sections(&self) -> Vec<DocSection> {
        self.parsed_sections.clone()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::DocSection")]
pub struct DocSection {
    kind: String,
    name: Option<String>,
    description: String,
}

unsafe impl IntoValueFromNative for DocSection {}

impl magnus::TryConvert for DocSection {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&DocSection as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into DocSection: {}", e),
                )
            })?
        };
        serde_json::from_str::<DocSection>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize DocSection: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for DocSection {}

impl Default for DocSection {
    fn default() -> Self {
        tree_sitter_language_pack::DocSection::default().into()
    }
}

impl DocSection {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            kind: kwargs
                .get(ruby.to_symbol("kind"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
            name: kwargs
                .get(ruby.to_symbol("name"))
                .and_then(|v| String::try_convert(v).ok()),
            description: kwargs
                .get(ruby.to_symbol("description"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn kind(&self) -> String {
        self.kind.clone()
    }

    fn name(&self) -> Option<String> {
        self.name.clone()
    }

    fn description(&self) -> String {
        self.description.clone()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::ImportInfo")]
pub struct ImportInfo {
    source: String,
    items: Vec<String>,
    alias: Option<String>,
    is_wildcard: bool,
    span: Span,
}

unsafe impl IntoValueFromNative for ImportInfo {}

impl magnus::TryConvert for ImportInfo {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&ImportInfo as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into ImportInfo: {}", e),
                )
            })?
        };
        serde_json::from_str::<ImportInfo>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize ImportInfo: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for ImportInfo {}

impl Default for ImportInfo {
    fn default() -> Self {
        tree_sitter_language_pack::ImportInfo::default().into()
    }
}

impl ImportInfo {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            source: kwargs
                .get(ruby.to_symbol("source"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
            items: kwargs
                .get(ruby.to_symbol("items"))
                .and_then(|v| <Vec<String>>::try_convert(v).ok())
                .unwrap_or_default(),
            alias: kwargs
                .get(ruby.to_symbol("alias"))
                .and_then(|v| String::try_convert(v).ok()),
            is_wildcard: kwargs
                .get(ruby.to_symbol("is_wildcard"))
                .and_then(|v| bool::try_convert(v).ok())
                .unwrap_or_default(),
            span: kwargs
                .get(ruby.to_symbol("span"))
                .and_then(|v| Span::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn source(&self) -> String {
        self.source.clone()
    }

    fn items(&self) -> Vec<String> {
        self.items.clone()
    }

    fn alias(&self) -> Option<String> {
        self.alias.clone()
    }

    fn is_wildcard(&self) -> bool {
        self.is_wildcard
    }

    fn span(&self) -> Span {
        self.span.clone()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::ExportInfo")]
pub struct ExportInfo {
    name: String,
    kind: ExportKind,
    span: Span,
}

unsafe impl IntoValueFromNative for ExportInfo {}

impl magnus::TryConvert for ExportInfo {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&ExportInfo as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into ExportInfo: {}", e),
                )
            })?
        };
        serde_json::from_str::<ExportInfo>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize ExportInfo: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for ExportInfo {}

impl Default for ExportInfo {
    fn default() -> Self {
        Self {
            name: String::new(),
            kind: Default::default(),
            span: Default::default(),
        }
    }
}

impl ExportInfo {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            name: kwargs
                .get(ruby.to_symbol("name"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
            kind: kwargs
                .get(ruby.to_symbol("kind"))
                .and_then(|v| ExportKind::try_convert(v).ok())
                .unwrap_or_default(),
            span: kwargs
                .get(ruby.to_symbol("span"))
                .and_then(|v| Span::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn name(&self) -> String {
        self.name.clone()
    }

    fn kind(&self) -> ExportKind {
        self.kind.clone()
    }

    fn span(&self) -> Span {
        self.span.clone()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::SymbolInfo")]
pub struct SymbolInfo {
    name: String,
    kind: SymbolKind,
    span: Span,
    type_annotation: Option<String>,
    doc: Option<String>,
}

unsafe impl IntoValueFromNative for SymbolInfo {}

impl magnus::TryConvert for SymbolInfo {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&SymbolInfo as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into SymbolInfo: {}", e),
                )
            })?
        };
        serde_json::from_str::<SymbolInfo>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize SymbolInfo: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for SymbolInfo {}

impl Default for SymbolInfo {
    fn default() -> Self {
        Self {
            name: String::new(),
            kind: Default::default(),
            span: Default::default(),
            type_annotation: None,
            doc: None,
        }
    }
}

impl SymbolInfo {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            name: kwargs
                .get(ruby.to_symbol("name"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
            kind: kwargs
                .get(ruby.to_symbol("kind"))
                .and_then(|v| SymbolKind::try_convert(v).ok())
                .unwrap_or_default(),
            span: kwargs
                .get(ruby.to_symbol("span"))
                .and_then(|v| Span::try_convert(v).ok())
                .unwrap_or_default(),
            type_annotation: kwargs
                .get(ruby.to_symbol("type_annotation"))
                .and_then(|v| String::try_convert(v).ok()),
            doc: kwargs
                .get(ruby.to_symbol("doc"))
                .and_then(|v| String::try_convert(v).ok()),
        })
    }

    fn name(&self) -> String {
        self.name.clone()
    }

    fn kind(&self) -> SymbolKind {
        self.kind.clone()
    }

    fn span(&self) -> Span {
        self.span.clone()
    }

    fn type_annotation(&self) -> Option<String> {
        self.type_annotation.clone()
    }

    fn doc(&self) -> Option<String> {
        self.doc.clone()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::Diagnostic")]
pub struct Diagnostic {
    message: String,
    severity: DiagnosticSeverity,
    span: Span,
}

unsafe impl IntoValueFromNative for Diagnostic {}

impl magnus::TryConvert for Diagnostic {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&Diagnostic as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into Diagnostic: {}", e),
                )
            })?
        };
        serde_json::from_str::<Diagnostic>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize Diagnostic: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for Diagnostic {}

impl Default for Diagnostic {
    fn default() -> Self {
        Self {
            message: String::new(),
            severity: Default::default(),
            span: Default::default(),
        }
    }
}

impl Diagnostic {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            message: kwargs
                .get(ruby.to_symbol("message"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
            severity: kwargs
                .get(ruby.to_symbol("severity"))
                .and_then(|v| DiagnosticSeverity::try_convert(v).ok())
                .unwrap_or_default(),
            span: kwargs
                .get(ruby.to_symbol("span"))
                .and_then(|v| Span::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn message(&self) -> String {
        self.message.clone()
    }

    fn severity(&self) -> DiagnosticSeverity {
        self.severity.clone()
    }

    fn span(&self) -> Span {
        self.span.clone()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::CodeChunk")]
pub struct CodeChunk {
    content: String,
    start_byte: usize,
    end_byte: usize,
    start_line: usize,
    end_line: usize,
    metadata: ChunkContext,
}

unsafe impl IntoValueFromNative for CodeChunk {}

impl magnus::TryConvert for CodeChunk {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&CodeChunk as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into CodeChunk: {}", e),
                )
            })?
        };
        serde_json::from_str::<CodeChunk>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize CodeChunk: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for CodeChunk {}

impl Default for CodeChunk {
    fn default() -> Self {
        tree_sitter_language_pack::CodeChunk::default().into()
    }
}

impl CodeChunk {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            content: kwargs
                .get(ruby.to_symbol("content"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
            start_byte: kwargs
                .get(ruby.to_symbol("start_byte"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            end_byte: kwargs
                .get(ruby.to_symbol("end_byte"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            start_line: kwargs
                .get(ruby.to_symbol("start_line"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            end_line: kwargs
                .get(ruby.to_symbol("end_line"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            metadata: kwargs
                .get(ruby.to_symbol("metadata"))
                .and_then(|v| ChunkContext::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn content(&self) -> String {
        self.content.clone()
    }

    fn start_byte(&self) -> usize {
        self.start_byte
    }

    fn end_byte(&self) -> usize {
        self.end_byte
    }

    fn start_line(&self) -> usize {
        self.start_line
    }

    fn end_line(&self) -> usize {
        self.end_line
    }

    fn metadata(&self) -> ChunkContext {
        self.metadata.clone()
    }

    #[allow(clippy::should_implement_trait)]
    fn to_s(&self) -> String {
        self.content.clone()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::ChunkContext")]
pub struct ChunkContext {
    language: String,
    chunk_index: usize,
    total_chunks: usize,
    node_types: Vec<String>,
    context_path: Vec<String>,
    symbols_defined: Vec<String>,
    comments: Vec<CommentInfo>,
    docstrings: Vec<DocstringInfo>,
    has_error_nodes: bool,
}

unsafe impl IntoValueFromNative for ChunkContext {}

impl magnus::TryConvert for ChunkContext {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&ChunkContext as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into ChunkContext: {}", e),
                )
            })?
        };
        serde_json::from_str::<ChunkContext>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize ChunkContext: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for ChunkContext {}

impl Default for ChunkContext {
    fn default() -> Self {
        tree_sitter_language_pack::ChunkContext::default().into()
    }
}

impl ChunkContext {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            language: kwargs
                .get(ruby.to_symbol("language"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
            chunk_index: kwargs
                .get(ruby.to_symbol("chunk_index"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            total_chunks: kwargs
                .get(ruby.to_symbol("total_chunks"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            node_types: kwargs
                .get(ruby.to_symbol("node_types"))
                .and_then(|v| <Vec<String>>::try_convert(v).ok())
                .unwrap_or_default(),
            context_path: kwargs
                .get(ruby.to_symbol("context_path"))
                .and_then(|v| <Vec<String>>::try_convert(v).ok())
                .unwrap_or_default(),
            symbols_defined: kwargs
                .get(ruby.to_symbol("symbols_defined"))
                .and_then(|v| <Vec<String>>::try_convert(v).ok())
                .unwrap_or_default(),
            comments: kwargs
                .get(ruby.to_symbol("comments"))
                .and_then(|v| <Vec<CommentInfo>>::try_convert(v).ok())
                .unwrap_or_default(),
            docstrings: kwargs
                .get(ruby.to_symbol("docstrings"))
                .and_then(|v| <Vec<DocstringInfo>>::try_convert(v).ok())
                .unwrap_or_default(),
            has_error_nodes: kwargs
                .get(ruby.to_symbol("has_error_nodes"))
                .and_then(|v| bool::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn language(&self) -> String {
        self.language.clone()
    }

    fn chunk_index(&self) -> usize {
        self.chunk_index
    }

    fn total_chunks(&self) -> usize {
        self.total_chunks
    }

    fn node_types(&self) -> Vec<String> {
        self.node_types.clone()
    }

    fn context_path(&self) -> Vec<String> {
        self.context_path.clone()
    }

    fn symbols_defined(&self) -> Vec<String> {
        self.symbols_defined.clone()
    }

    fn comments(&self) -> Vec<CommentInfo> {
        self.comments.clone()
    }

    fn docstrings(&self) -> Vec<DocstringInfo> {
        self.docstrings.clone()
    }

    fn has_error_nodes(&self) -> bool {
        self.has_error_nodes
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::PackConfig")]
pub struct PackConfig {
    cache_dir: Option<String>,
    languages: Option<Vec<String>>,
    groups: Option<Vec<String>>,
}

unsafe impl IntoValueFromNative for PackConfig {}

impl magnus::TryConvert for PackConfig {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&PackConfig as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into PackConfig: {}", e),
                )
            })?
        };
        serde_json::from_str::<PackConfig>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize PackConfig: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for PackConfig {}

impl Default for PackConfig {
    fn default() -> Self {
        tree_sitter_language_pack::PackConfig::default().into()
    }
}

impl PackConfig {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            cache_dir: kwargs
                .get(ruby.to_symbol("cache_dir"))
                .and_then(|v| String::try_convert(v).ok()),
            languages: kwargs
                .get(ruby.to_symbol("languages"))
                .and_then(|v| <Vec<String>>::try_convert(v).ok()),
            groups: kwargs
                .get(ruby.to_symbol("groups"))
                .and_then(|v| <Vec<String>>::try_convert(v).ok()),
        })
    }

    fn cache_dir(&self) -> Option<String> {
        self.cache_dir.clone()
    }

    fn languages(&self) -> Option<Vec<String>> {
        self.languages.clone()
    }

    fn groups(&self) -> Option<Vec<String>> {
        self.groups.clone()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[magnus::wrap(class = "TreeSitterLanguagePack::Point")]
pub struct Point {
    row: usize,
    column: usize,
}

unsafe impl IntoValueFromNative for Point {}

impl magnus::TryConvert for Point {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&Point as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into Point: {}", e),
                )
            })?
        };
        serde_json::from_str::<Point>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize Point: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for Point {}

impl Point {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            row: kwargs
                .get(ruby.to_symbol("row"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            column: kwargs
                .get(ruby.to_symbol("column"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn row(&self) -> usize {
        self.row
    }

    fn column(&self) -> usize {
        self.column
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[magnus::wrap(class = "TreeSitterLanguagePack::ByteRange")]
pub struct ByteRange {
    start: usize,
    end: usize,
}

unsafe impl IntoValueFromNative for ByteRange {}

impl magnus::TryConvert for ByteRange {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&ByteRange as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into ByteRange: {}", e),
                )
            })?
        };
        serde_json::from_str::<ByteRange>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize ByteRange: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for ByteRange {}

impl ByteRange {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            start: kwargs
                .get(ruby.to_symbol("start"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
            end: kwargs
                .get(ruby.to_symbol("end"))
                .and_then(|v| usize::try_convert(v).ok())
                .unwrap_or_default(),
        })
    }

    fn start(&self) -> usize {
        self.start
    }

    fn end(&self) -> usize {
        self.end
    }
}

#[derive(Clone)]
#[magnus::wrap(class = "TreeSitterLanguagePack::Parser")]
pub struct Parser {
    inner: Arc<std::sync::Mutex<tree_sitter_language_pack::Parser>>,
}

unsafe impl IntoValueFromNative for Parser {}

impl magnus::TryConvert for Parser {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        let r: &Parser = magnus::TryConvert::try_convert(val)?;
        Ok(r.clone())
    }
}

unsafe impl TryConvertOwned for Parser {}

impl Parser {
    fn set_language(&self, name: String) -> Result<(), Error> {
        self.inner.lock().unwrap().set_language(&name).map_err(|e| {
            magnus::Error::new(
                unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
                e.to_string(),
            )
        })?;
        Ok(())
    }

    fn parse(&self, source: String) -> Option<Tree> {
        self.inner
            .lock()
            .unwrap()
            .parse(&source)
            .map(|v| Tree { inner: Arc::new(v) })
    }

    fn parse_bytes(&self, source: Vec<u8>) -> Option<Tree> {
        self.inner
            .lock()
            .unwrap()
            .parse_bytes(&source)
            .map(|v| Tree { inner: Arc::new(v) })
    }

    fn reset(&self) -> () {
        self.inner.lock().unwrap().reset()
    }
}

#[derive(Clone)]
#[magnus::wrap(class = "TreeSitterLanguagePack::Tree")]
pub struct Tree {
    inner: Arc<tree_sitter_language_pack::Tree>,
}

unsafe impl IntoValueFromNative for Tree {}

impl magnus::TryConvert for Tree {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        let r: &Tree = magnus::TryConvert::try_convert(val)?;
        Ok(r.clone())
    }
}

unsafe impl TryConvertOwned for Tree {}

impl Tree {
    fn root_node(&self) -> Node {
        Node {
            inner: Arc::new(self.inner.root_node()),
        }
    }

    fn walk(&self) -> TreeCursor {
        TreeCursor {
            inner: Arc::new(std::sync::Mutex::new(self.inner.walk())),
        }
    }
}

#[derive(Clone)]
#[magnus::wrap(class = "TreeSitterLanguagePack::Node")]
pub struct Node {
    inner: Arc<tree_sitter_language_pack::Node>,
}

unsafe impl IntoValueFromNative for Node {}

impl magnus::TryConvert for Node {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        let r: &Node = magnus::TryConvert::try_convert(val)?;
        Ok(r.clone())
    }
}

unsafe impl TryConvertOwned for Node {}

impl Node {
    fn clone(&self) -> Node {
        Self {
            inner: self.inner.clone(),
        }
    }

    fn kind(&self) -> String {
        self.inner.kind()
    }

    fn kind_id(&self) -> u16 {
        self.inner.kind_id()
    }

    fn start_byte(&self) -> usize {
        self.inner.start_byte()
    }

    fn end_byte(&self) -> usize {
        self.inner.end_byte()
    }

    fn byte_range(&self) -> ByteRange {
        self.inner.byte_range().into()
    }

    fn start_position(&self) -> Point {
        self.inner.start_position().into()
    }

    fn end_position(&self) -> Point {
        self.inner.end_position().into()
    }

    fn is_named(&self) -> bool {
        self.inner.is_named()
    }

    fn is_error(&self) -> bool {
        self.inner.is_error()
    }

    fn is_missing(&self) -> bool {
        self.inner.is_missing()
    }

    fn is_extra(&self) -> bool {
        self.inner.is_extra()
    }

    fn has_error(&self) -> bool {
        self.inner.has_error()
    }

    fn parent(&self) -> Option<Node> {
        self.inner.parent().map(|v| Node { inner: Arc::new(v) })
    }

    fn child(&self, index: u32) -> Option<Node> {
        self.inner.child(index).map(|v| Node { inner: Arc::new(v) })
    }

    fn child_count(&self) -> usize {
        self.inner.child_count()
    }

    fn named_child(&self, index: u32) -> Option<Node> {
        self.inner.named_child(index).map(|v| Node { inner: Arc::new(v) })
    }

    fn named_child_count(&self) -> usize {
        self.inner.named_child_count()
    }

    fn child_by_field_name(&self, name: String) -> Option<Node> {
        self.inner
            .child_by_field_name(&name)
            .map(|v| Node { inner: Arc::new(v) })
    }

    fn to_sexp(&self) -> String {
        self.inner.to_sexp()
    }

    fn walk(&self) -> TreeCursor {
        TreeCursor {
            inner: Arc::new(std::sync::Mutex::new(self.inner.walk())),
        }
    }
}

#[derive(Clone)]
#[magnus::wrap(class = "TreeSitterLanguagePack::TreeCursor")]
pub struct TreeCursor {
    inner: Arc<std::sync::Mutex<tree_sitter_language_pack::TreeCursor>>,
}

unsafe impl IntoValueFromNative for TreeCursor {}

impl magnus::TryConvert for TreeCursor {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        let r: &TreeCursor = magnus::TryConvert::try_convert(val)?;
        Ok(r.clone())
    }
}

unsafe impl TryConvertOwned for TreeCursor {}

impl TreeCursor {
    fn node(&self) -> Node {
        Node {
            inner: Arc::new(self.inner.lock().unwrap().node()),
        }
    }

    fn goto_first_child(&self) -> bool {
        self.inner.lock().unwrap().goto_first_child()
    }

    fn goto_parent(&self) -> bool {
        self.inner.lock().unwrap().goto_parent()
    }

    fn goto_next_sibling(&self) -> bool {
        self.inner.lock().unwrap().goto_next_sibling()
    }

    fn field_name(&self) -> Option<String> {
        self.inner.lock().unwrap().field_name()
    }
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
#[serde(default)]
#[magnus::wrap(class = "TreeSitterLanguagePack::ProcessConfig")]
pub struct ProcessConfig {
    language: String,
    structure: bool,
    imports: bool,
    exports: bool,
    comments: bool,
    docstrings: bool,
    symbols: bool,
    diagnostics: bool,
    chunk_max_size: Option<usize>,
}

unsafe impl IntoValueFromNative for ProcessConfig {}

impl magnus::TryConvert for ProcessConfig {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        if let Ok(r) = <&ProcessConfig as magnus::TryConvert>::try_convert(val) {
            return Ok(r.clone());
        }
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into ProcessConfig: {}", e),
                )
            })?
        };
        serde_json::from_str::<ProcessConfig>(&json_str).map_err(|e| {
            magnus::Error::new(
                unsafe { magnus::Ruby::get_unchecked() }.exception_type_error(),
                format!("failed to deserialize ProcessConfig: {}", e),
            )
        })
    }
}

unsafe impl TryConvertOwned for ProcessConfig {}

impl Default for ProcessConfig {
    fn default() -> Self {
        tree_sitter_language_pack::ProcessConfig::default().into()
    }
}

impl ProcessConfig {
    fn new(args: &[magnus::Value]) -> Result<Self, magnus::Error> {
        let ruby = unsafe { magnus::Ruby::get_unchecked() };
        let args = magnus::scan_args::scan_args::<(), (Option<magnus::RHash>,), (), (), (), ()>(args)?;
        let (kwargs_opt,) = args.optional;
        let kwargs = kwargs_opt.unwrap_or_else(|| ruby.hash_new());
        Ok(Self {
            language: kwargs
                .get(ruby.to_symbol("language"))
                .and_then(|v| String::try_convert(v).ok())
                .unwrap_or_default(),
            structure: kwargs
                .get(ruby.to_symbol("structure"))
                .and_then(|v| bool::try_convert(v).ok())
                .unwrap_or(true),
            imports: kwargs
                .get(ruby.to_symbol("imports"))
                .and_then(|v| bool::try_convert(v).ok())
                .unwrap_or(true),
            exports: kwargs
                .get(ruby.to_symbol("exports"))
                .and_then(|v| bool::try_convert(v).ok())
                .unwrap_or(true),
            comments: kwargs
                .get(ruby.to_symbol("comments"))
                .and_then(|v| bool::try_convert(v).ok())
                .unwrap_or(false),
            docstrings: kwargs
                .get(ruby.to_symbol("docstrings"))
                .and_then(|v| bool::try_convert(v).ok())
                .unwrap_or(false),
            symbols: kwargs
                .get(ruby.to_symbol("symbols"))
                .and_then(|v| bool::try_convert(v).ok())
                .unwrap_or(false),
            diagnostics: kwargs
                .get(ruby.to_symbol("diagnostics"))
                .and_then(|v| bool::try_convert(v).ok())
                .unwrap_or(false),
            chunk_max_size: kwargs
                .get(ruby.to_symbol("chunk_max_size"))
                .and_then(|v| usize::try_convert(v).ok()),
        })
    }

    fn language(&self) -> String {
        self.language.clone()
    }

    fn structure(&self) -> bool {
        self.structure
    }

    fn imports(&self) -> bool {
        self.imports
    }

    fn exports(&self) -> bool {
        self.exports
    }

    fn comments(&self) -> bool {
        self.comments
    }

    fn docstrings(&self) -> bool {
        self.docstrings
    }

    fn symbols(&self) -> bool {
        self.symbols
    }

    fn diagnostics(&self) -> bool {
        self.diagnostics
    }

    fn chunk_max_size(&self) -> Option<usize> {
        self.chunk_max_size
    }

    fn with_chunking(&self, max_size: usize) -> ProcessConfig {
        let core_self = tree_sitter_language_pack::ProcessConfig {
            language: self.language.clone().into(),

            structure: self.structure,

            imports: self.imports,

            exports: self.exports,

            comments: self.comments,

            docstrings: self.docstrings,

            symbols: self.symbols,

            diagnostics: self.diagnostics,

            chunk_max_size: self.chunk_max_size,
        };
        core_self.with_chunking(max_size).into()
    }

    fn all(&self) -> ProcessConfig {
        let core_self = tree_sitter_language_pack::ProcessConfig {
            language: self.language.clone().into(),

            structure: self.structure,

            imports: self.imports,

            exports: self.exports,

            comments: self.comments,

            docstrings: self.docstrings,

            symbols: self.symbols,

            diagnostics: self.diagnostics,

            chunk_max_size: self.chunk_max_size,
        };
        core_self.all().into()
    }

    fn minimal(&self) -> ProcessConfig {
        let core_self = tree_sitter_language_pack::ProcessConfig {
            language: self.language.clone().into(),

            structure: self.structure,

            imports: self.imports,

            exports: self.exports,

            comments: self.comments,

            docstrings: self.docstrings,

            symbols: self.symbols,

            diagnostics: self.diagnostics,

            chunk_max_size: self.chunk_max_size,
        };
        core_self.minimal().into()
    }
}

#[derive(Clone)]
#[magnus::wrap(class = "TreeSitterLanguagePack::LanguageRegistry")]
pub struct LanguageRegistry {
    inner: Arc<tree_sitter_language_pack::LanguageRegistry>,
}

unsafe impl IntoValueFromNative for LanguageRegistry {}

impl magnus::TryConvert for LanguageRegistry {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        let r: &LanguageRegistry = magnus::TryConvert::try_convert(val)?;
        Ok(r.clone())
    }
}

unsafe impl TryConvertOwned for LanguageRegistry {}

impl LanguageRegistry {
    fn get_language(&self, name: String) -> Result<Language, Error> {
        let result = self.inner.get_language(&name).map_err(|e| {
            magnus::Error::new(
                unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
                e.to_string(),
            )
        })?;
        Ok(Language {
            inner: Arc::new(result),
        })
    }

    fn available_languages(&self) -> Vec<String> {
        self.inner.available_languages()
    }

    fn has_parser(&self, name: String) -> bool {
        self.inner.has_parser(&name)
    }

    fn has_language(&self, name: String) -> bool {
        self.inner.has_language(&name)
    }

    fn language_count(&self) -> usize {
        self.inner.language_count()
    }

    fn process(&self, source: String, config: ProcessConfig) -> Result<ProcessResult, Error> {
        let config_core: tree_sitter_language_pack::ProcessConfig = config.into();
        let result = self.inner.process(&source, &config_core).map_err(|e| {
            magnus::Error::new(
                unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
                e.to_string(),
            )
        })?;
        Ok(result.into())
    }
}

#[derive(Clone)]
#[magnus::wrap(class = "TreeSitterLanguagePack::DownloadManager")]
pub struct DownloadManager {
    inner: Arc<tree_sitter_language_pack::DownloadManager>,
}

unsafe impl IntoValueFromNative for DownloadManager {}

impl magnus::TryConvert for DownloadManager {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        let r: &DownloadManager = magnus::TryConvert::try_convert(val)?;
        Ok(r.clone())
    }
}

unsafe impl TryConvertOwned for DownloadManager {}

impl DownloadManager {
    fn installed_languages(&self) -> Vec<String> {
        self.inner.installed_languages()
    }

    fn download_all_best_effort(&self) -> Result<usize, Error> {
        let result = self.inner.download_all_best_effort().map_err(|e| {
            magnus::Error::new(
                unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
                e.to_string(),
            )
        })?;
        Ok(result)
    }

    fn clean_cache(&self) -> Result<(), Error> {
        self.inner.clean_cache().map_err(|e| {
            magnus::Error::new(
                unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
                e.to_string(),
            )
        })?;
        Ok(())
    }
}

#[derive(Clone)]
#[magnus::wrap(class = "TreeSitterLanguagePack::Language")]
pub struct Language {
    inner: Arc<tree_sitter_language_pack::Language>,
}

unsafe impl IntoValueFromNative for Language {}

impl magnus::TryConvert for Language {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        let r: &Language = magnus::TryConvert::try_convert(val)?;
        Ok(r.clone())
    }
}

unsafe impl TryConvertOwned for Language {}

impl Language {}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub enum StructureKind {
    Function,
    Method,
    Class,
    Struct,
    Interface,
    Enum,
    Module,
    Trait,
    Impl,
    Namespace,
    Other { _0: String },
}

impl Default for StructureKind {
    fn default() -> Self {
        Self::Function
    }
}

impl magnus::IntoValue for StructureKind {
    fn into_value_with(self, handle: &Ruby) -> magnus::Value {
        match serde_json::to_value(&self) {
            Ok(v) => json_to_ruby(handle, v),
            Err(_) => handle.qnil().into_value_with(handle),
        }
    }
}

impl magnus::TryConvert for StructureKind {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        // For data enums with fields (e.g., PageAction), try to deserialize from JSON first.
        // For unit enums or when passed as a string, fall back to string-based conversion.
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into StructureKind: {}", e),
                )
            })?
        };
        // Try deserializing as JSON first (handles JSON strings like "\"markdown\"" or "{\"click\":{\"selector\":\"...\"}}\"")
        // If that fails, try treating it as a plain string value and wrap in quotes
        // If both fail, try as Custom variant (for untagged enum support)
        serde_json::from_str(&json_str)
            .or_else(|_| serde_json::from_str(&format!("\"{json_str}\"")))
            .or_else(|_| {
                // Try as a JSON string for Custom variant (untagged enums accept any remaining value)
                match serde_json::to_value(&json_str) {
                    Ok(val) => serde_json::from_value(val),
                    Err(e) => Err(e),
                }
            })
            .map_err(|e| magnus::Error::new(unsafe { Ruby::get_unchecked() }.exception_type_error(), e.to_string()))
    }
}

unsafe impl IntoValueFromNative for StructureKind {}
unsafe impl TryConvertOwned for StructureKind {}

#[derive(Clone, Copy, PartialEq, Eq, Debug, serde::Serialize, serde::Deserialize)]
pub enum CommentKind {
    Line,
    Block,
    Doc,
}

impl Default for CommentKind {
    fn default() -> Self {
        Self::Line
    }
}

impl magnus::IntoValue for CommentKind {
    fn into_value_with(self, handle: &Ruby) -> magnus::Value {
        let sym = match self {
            CommentKind::Line => "line",
            CommentKind::Block => "block",
            CommentKind::Doc => "doc",
        };
        handle.to_symbol(sym).into_value_with(handle)
    }
}

impl magnus::TryConvert for CommentKind {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        let s: String = magnus::TryConvert::try_convert(val)?;
        // Accept the serde wire name (snake_case), the PascalCase Rust variant name,
        // and a lowercase fallback so fixtures written in any of those styles work.
        match s.as_str() {
            "line" | "Line" => Ok(CommentKind::Line),
            "block" | "Block" => Ok(CommentKind::Block),
            "doc" | "Doc" => Ok(CommentKind::Doc),
            other => Err(magnus::Error::new(
                unsafe { Ruby::get_unchecked() }.exception_arg_error(),
                format!("invalid CommentKind value: {other}"),
            )),
        }
    }
}

unsafe impl IntoValueFromNative for CommentKind {}
unsafe impl TryConvertOwned for CommentKind {}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub enum DocstringFormat {
    PythonTripleQuote,
    JSDoc,
    Rustdoc,
    GoDoc,
    JavaDoc,
    Other { _0: String },
}

impl Default for DocstringFormat {
    fn default() -> Self {
        Self::PythonTripleQuote
    }
}

impl magnus::IntoValue for DocstringFormat {
    fn into_value_with(self, handle: &Ruby) -> magnus::Value {
        match serde_json::to_value(&self) {
            Ok(v) => json_to_ruby(handle, v),
            Err(_) => handle.qnil().into_value_with(handle),
        }
    }
}

impl magnus::TryConvert for DocstringFormat {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        // For data enums with fields (e.g., PageAction), try to deserialize from JSON first.
        // For unit enums or when passed as a string, fall back to string-based conversion.
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into DocstringFormat: {}", e),
                )
            })?
        };
        // Try deserializing as JSON first (handles JSON strings like "\"markdown\"" or "{\"click\":{\"selector\":\"...\"}}\"")
        // If that fails, try treating it as a plain string value and wrap in quotes
        // If both fail, try as Custom variant (for untagged enum support)
        serde_json::from_str(&json_str)
            .or_else(|_| serde_json::from_str(&format!("\"{json_str}\"")))
            .or_else(|_| {
                // Try as a JSON string for Custom variant (untagged enums accept any remaining value)
                match serde_json::to_value(&json_str) {
                    Ok(val) => serde_json::from_value(val),
                    Err(e) => Err(e),
                }
            })
            .map_err(|e| magnus::Error::new(unsafe { Ruby::get_unchecked() }.exception_type_error(), e.to_string()))
    }
}

unsafe impl IntoValueFromNative for DocstringFormat {}
unsafe impl TryConvertOwned for DocstringFormat {}

#[derive(Clone, Copy, PartialEq, Eq, Debug, serde::Serialize, serde::Deserialize)]
pub enum ExportKind {
    Named,
    Default,
    ReExport,
}

impl Default for ExportKind {
    fn default() -> Self {
        Self::Named
    }
}

impl magnus::IntoValue for ExportKind {
    fn into_value_with(self, handle: &Ruby) -> magnus::Value {
        let sym = match self {
            ExportKind::Named => "named",
            ExportKind::Default => "default",
            ExportKind::ReExport => "re_export",
        };
        handle.to_symbol(sym).into_value_with(handle)
    }
}

impl magnus::TryConvert for ExportKind {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        let s: String = magnus::TryConvert::try_convert(val)?;
        // Accept the serde wire name (snake_case), the PascalCase Rust variant name,
        // and a lowercase fallback so fixtures written in any of those styles work.
        match s.as_str() {
            "named" | "Named" => Ok(ExportKind::Named),
            "default" | "Default" => Ok(ExportKind::Default),
            "re_export" | "ReExport" => Ok(ExportKind::ReExport),
            other => Err(magnus::Error::new(
                unsafe { Ruby::get_unchecked() }.exception_arg_error(),
                format!("invalid ExportKind value: {other}"),
            )),
        }
    }
}

unsafe impl IntoValueFromNative for ExportKind {}
unsafe impl TryConvertOwned for ExportKind {}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub enum SymbolKind {
    Variable,
    Constant,
    Function,
    Class,
    Type,
    Interface,
    Enum,
    Module,
    Other { _0: String },
}

impl Default for SymbolKind {
    fn default() -> Self {
        Self::Variable
    }
}

impl magnus::IntoValue for SymbolKind {
    fn into_value_with(self, handle: &Ruby) -> magnus::Value {
        match serde_json::to_value(&self) {
            Ok(v) => json_to_ruby(handle, v),
            Err(_) => handle.qnil().into_value_with(handle),
        }
    }
}

impl magnus::TryConvert for SymbolKind {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        // For data enums with fields (e.g., PageAction), try to deserialize from JSON first.
        // For unit enums or when passed as a string, fall back to string-based conversion.
        let json_str: String = if let Ok(s) = <String as magnus::TryConvert>::try_convert(val) {
            s
        } else {
            val.funcall::<_, _, String>("to_json", ()).map_err(|e| {
                magnus::Error::new(
                    unsafe { Ruby::get_unchecked() }.exception_type_error(),
                    format!("no implicit conversion into SymbolKind: {}", e),
                )
            })?
        };
        // Try deserializing as JSON first (handles JSON strings like "\"markdown\"" or "{\"click\":{\"selector\":\"...\"}}\"")
        // If that fails, try treating it as a plain string value and wrap in quotes
        // If both fail, try as Custom variant (for untagged enum support)
        serde_json::from_str(&json_str)
            .or_else(|_| serde_json::from_str(&format!("\"{json_str}\"")))
            .or_else(|_| {
                // Try as a JSON string for Custom variant (untagged enums accept any remaining value)
                match serde_json::to_value(&json_str) {
                    Ok(val) => serde_json::from_value(val),
                    Err(e) => Err(e),
                }
            })
            .map_err(|e| magnus::Error::new(unsafe { Ruby::get_unchecked() }.exception_type_error(), e.to_string()))
    }
}

unsafe impl IntoValueFromNative for SymbolKind {}
unsafe impl TryConvertOwned for SymbolKind {}

#[derive(Clone, Copy, PartialEq, Eq, Debug, serde::Serialize, serde::Deserialize)]
pub enum DiagnosticSeverity {
    Error,
    Warning,
    Info,
}

impl Default for DiagnosticSeverity {
    fn default() -> Self {
        Self::Error
    }
}

impl magnus::IntoValue for DiagnosticSeverity {
    fn into_value_with(self, handle: &Ruby) -> magnus::Value {
        let sym = match self {
            DiagnosticSeverity::Error => "error",
            DiagnosticSeverity::Warning => "warning",
            DiagnosticSeverity::Info => "info",
        };
        handle.to_symbol(sym).into_value_with(handle)
    }
}

impl magnus::TryConvert for DiagnosticSeverity {
    fn try_convert(val: magnus::Value) -> Result<Self, magnus::Error> {
        let s: String = magnus::TryConvert::try_convert(val)?;
        // Accept the serde wire name (snake_case), the PascalCase Rust variant name,
        // and a lowercase fallback so fixtures written in any of those styles work.
        match s.as_str() {
            "error" | "Error" => Ok(DiagnosticSeverity::Error),
            "warning" | "Warning" => Ok(DiagnosticSeverity::Warning),
            "info" | "Info" => Ok(DiagnosticSeverity::Info),
            other => Err(magnus::Error::new(
                unsafe { Ruby::get_unchecked() }.exception_arg_error(),
                format!("invalid DiagnosticSeverity value: {other}"),
            )),
        }
    }
}

unsafe impl IntoValueFromNative for DiagnosticSeverity {}
unsafe impl TryConvertOwned for DiagnosticSeverity {}

fn detect_language_from_extension(ext: String) -> Option<String> {
    tree_sitter_language_pack::detect_language_from_extension(&ext).map(Into::into)
}

fn detect_language_from_path(path: String) -> Option<String> {
    tree_sitter_language_pack::detect_language_from_path(&path).map(Into::into)
}

fn detect_language_from_content(content: String) -> Option<String> {
    tree_sitter_language_pack::detect_language_from_content(&content).map(Into::into)
}

fn get_highlights_query(language: String) -> Option<String> {
    tree_sitter_language_pack::get_highlights_query(&language).map(Into::into)
}

fn get_injections_query(language: String) -> Option<String> {
    tree_sitter_language_pack::get_injections_query(&language).map(Into::into)
}

fn get_locals_query(language: String) -> Option<String> {
    tree_sitter_language_pack::get_locals_query(&language).map(Into::into)
}

fn get_language(name: String) -> Result<Language, Error> {
    let result = tree_sitter_language_pack::get_language(&name).map_err(|e| {
        magnus::Error::new(
            unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
            e.to_string(),
        )
    })?;
    Ok(Language {
        inner: Arc::new(result),
    })
}

fn get_parser(name: String) -> Result<Parser, Error> {
    let result = tree_sitter_language_pack::get_parser(&name).map_err(|e| {
        magnus::Error::new(
            unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
            e.to_string(),
        )
    })?;
    Ok(Parser {
        inner: Arc::new(std::sync::Mutex::new(result)),
    })
}

fn detect_language(path: String) -> Option<String> {
    tree_sitter_language_pack::detect_language(&path).map(Into::into)
}

fn available_languages() -> Vec<String> {
    tree_sitter_language_pack::available_languages()
}

fn has_language(name: String) -> bool {
    tree_sitter_language_pack::has_language(&name)
}

fn language_count() -> usize {
    tree_sitter_language_pack::language_count()
}

fn process(args: &[magnus::Value]) -> Result<ProcessResult, Error> {
    let args = magnus::scan_args::scan_args::<(String,), (Option<magnus::Value>,), (), (), (), ()>(args)?;
    let (source,) = args.required;

    let (config,) = args.optional;

    let config_core: tree_sitter_language_pack::ProcessConfig = match config {
        Some(_v) if !_v.is_nil() => {
            let binding_val: ProcessConfig = ProcessConfig::try_convert(_v).map_err(|e| {
                magnus::Error::new(
                    unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
                    e.to_string(),
                )
            })?;
            binding_val.into()
        }
        _ => Default::default(),
    };
    let result = tree_sitter_language_pack::process(&source, &config_core).map_err(|e| {
        magnus::Error::new(
            unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
            e.to_string(),
        )
    })?;
    Ok(result.into())
}

fn init(args: &[magnus::Value]) -> Result<(), Error> {
    let args = magnus::scan_args::scan_args::<(), (Option<magnus::Value>,), (), (), (), ()>(args)?;
    let (config,) = args.optional;

    let config_core: tree_sitter_language_pack::PackConfig = match config {
        Some(_v) if !_v.is_nil() => {
            let binding_val: PackConfig = PackConfig::try_convert(_v).map_err(|e| {
                magnus::Error::new(
                    unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
                    e.to_string(),
                )
            })?;
            binding_val.into()
        }
        _ => Default::default(),
    };
    let result = tree_sitter_language_pack::init(&config_core).map_err(|e| {
        magnus::Error::new(
            unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
            e.to_string(),
        )
    })?;
    Ok(result)
}

fn configure(args: &[magnus::Value]) -> Result<(), Error> {
    let args = magnus::scan_args::scan_args::<(), (Option<magnus::Value>,), (), (), (), ()>(args)?;
    let (config,) = args.optional;

    let config_core: tree_sitter_language_pack::PackConfig = match config {
        Some(_v) if !_v.is_nil() => {
            let binding_val: PackConfig = PackConfig::try_convert(_v).map_err(|e| {
                magnus::Error::new(
                    unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
                    e.to_string(),
                )
            })?;
            binding_val.into()
        }
        _ => Default::default(),
    };
    let result = tree_sitter_language_pack::configure(&config_core).map_err(|e| {
        magnus::Error::new(
            unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
            e.to_string(),
        )
    })?;
    Ok(result)
}

fn download(names: Vec<String>) -> Result<usize, Error> {
    let names_refs: Vec<&str> = names.iter().map(|s| s.as_str()).collect();
    let result = tree_sitter_language_pack::download(&names_refs).map_err(|e| {
        magnus::Error::new(
            unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
            e.to_string(),
        )
    })?;
    Ok(result)
}

fn download_all() -> Result<usize, Error> {
    let result = tree_sitter_language_pack::download_all().map_err(|e| {
        magnus::Error::new(
            unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
            e.to_string(),
        )
    })?;
    Ok(result)
}

fn download_group(name: String) -> Result<usize, Error> {
    let result = tree_sitter_language_pack::download_group(&name).map_err(|e| {
        magnus::Error::new(
            unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
            e.to_string(),
        )
    })?;
    Ok(result)
}

fn manifest_languages() -> Result<Vec<String>, Error> {
    let result = tree_sitter_language_pack::manifest_languages().map_err(|e| {
        magnus::Error::new(
            unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
            e.to_string(),
        )
    })?;
    Ok(result)
}

fn downloaded_languages() -> Vec<String> {
    tree_sitter_language_pack::downloaded_languages()
}

fn clean_cache() -> Result<(), Error> {
    let result = tree_sitter_language_pack::clean_cache().map_err(|e| {
        magnus::Error::new(
            unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
            e.to_string(),
        )
    })?;
    Ok(result)
}

fn cache_dir() -> Result<String, Error> {
    let result = tree_sitter_language_pack::cache_dir().map_err(|e| {
        magnus::Error::new(
            unsafe { Ruby::get_unchecked() }.exception_runtime_error(),
            e.to_string(),
        )
    })?;
    Ok(result)
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<Span> for tree_sitter_language_pack::Span {
    fn from(val: Span) -> Self {
        Self {
            start_byte: val.start_byte,
            end_byte: val.end_byte,
            start_line: val.start_line,
            start_column: val.start_column,
            end_line: val.end_line,
            end_column: val.end_column,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::Span> for Span {
    fn from(val: tree_sitter_language_pack::Span) -> Self {
        Self {
            start_byte: val.start_byte,
            end_byte: val.end_byte,
            start_line: val.start_line,
            start_column: val.start_column,
            end_line: val.end_line,
            end_column: val.end_column,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ProcessResult> for tree_sitter_language_pack::ProcessResult {
    fn from(val: ProcessResult) -> Self {
        Self {
            language: val.language,
            metrics: val.metrics.into(),
            structure: val.structure.into_iter().map(Into::into).collect(),
            imports: val.imports.into_iter().map(Into::into).collect(),
            exports: val.exports.into_iter().map(Into::into).collect(),
            comments: val.comments.into_iter().map(Into::into).collect(),
            docstrings: val.docstrings.into_iter().map(Into::into).collect(),
            symbols: val.symbols.into_iter().map(Into::into).collect(),
            diagnostics: val.diagnostics.into_iter().map(Into::into).collect(),
            chunks: val.chunks.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::ProcessResult> for ProcessResult {
    fn from(val: tree_sitter_language_pack::ProcessResult) -> Self {
        Self {
            language: val.language.to_string(),
            metrics: val.metrics.into(),
            structure: val.structure.into_iter().map(Into::into).collect(),
            imports: val.imports.into_iter().map(Into::into).collect(),
            exports: val.exports.into_iter().map(Into::into).collect(),
            comments: val.comments.into_iter().map(Into::into).collect(),
            docstrings: val.docstrings.into_iter().map(Into::into).collect(),
            symbols: val.symbols.into_iter().map(Into::into).collect(),
            diagnostics: val.diagnostics.into_iter().map(Into::into).collect(),
            chunks: val.chunks.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<FileMetrics> for tree_sitter_language_pack::FileMetrics {
    fn from(val: FileMetrics) -> Self {
        Self {
            total_lines: val.total_lines,
            code_lines: val.code_lines,
            comment_lines: val.comment_lines,
            blank_lines: val.blank_lines,
            total_bytes: val.total_bytes,
            node_count: val.node_count,
            error_count: val.error_count,
            max_depth: val.max_depth,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::FileMetrics> for FileMetrics {
    fn from(val: tree_sitter_language_pack::FileMetrics) -> Self {
        Self {
            total_lines: val.total_lines,
            code_lines: val.code_lines,
            comment_lines: val.comment_lines,
            blank_lines: val.blank_lines,
            total_bytes: val.total_bytes,
            node_count: val.node_count,
            error_count: val.error_count,
            max_depth: val.max_depth,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<StructureItem> for tree_sitter_language_pack::StructureItem {
    fn from(val: StructureItem) -> Self {
        Self {
            kind: val.kind.into(),
            name: val.name,
            visibility: val.visibility,
            span: val.span.into(),
            children: val.children.into_iter().map(Into::into).collect(),
            decorators: val.decorators.into_iter().collect(),
            doc_comment: val.doc_comment,
            signature: val.signature,
            body_span: val.body_span.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::StructureItem> for StructureItem {
    fn from(val: tree_sitter_language_pack::StructureItem) -> Self {
        Self {
            kind: val.kind.into(),
            name: val.name.map(|v| v.to_string()),
            visibility: val.visibility.map(|v| v.to_string()),
            span: val.span.into(),
            children: val.children.into_iter().map(Into::into).collect(),
            decorators: val.decorators.into_iter().collect(),
            doc_comment: val.doc_comment.map(|v| v.to_string()),
            signature: val.signature.map(|v| v.to_string()),
            body_span: val.body_span.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<CommentInfo> for tree_sitter_language_pack::CommentInfo {
    fn from(val: CommentInfo) -> Self {
        Self {
            text: val.text,
            kind: val.kind.into(),
            span: val.span.into(),
            associated_node: val.associated_node,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::CommentInfo> for CommentInfo {
    fn from(val: tree_sitter_language_pack::CommentInfo) -> Self {
        Self {
            text: val.text.to_string(),
            kind: val.kind.into(),
            span: val.span.into(),
            associated_node: val.associated_node.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<DocstringInfo> for tree_sitter_language_pack::DocstringInfo {
    fn from(val: DocstringInfo) -> Self {
        Self {
            text: val.text,
            format: val.format.into(),
            span: val.span.into(),
            associated_item: val.associated_item,
            parsed_sections: val.parsed_sections.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::DocstringInfo> for DocstringInfo {
    fn from(val: tree_sitter_language_pack::DocstringInfo) -> Self {
        Self {
            text: val.text.to_string(),
            format: val.format.into(),
            span: val.span.into(),
            associated_item: val.associated_item.map(|v| v.to_string()),
            parsed_sections: val.parsed_sections.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<DocSection> for tree_sitter_language_pack::DocSection {
    fn from(val: DocSection) -> Self {
        Self {
            kind: val.kind,
            name: val.name,
            description: val.description,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::DocSection> for DocSection {
    fn from(val: tree_sitter_language_pack::DocSection) -> Self {
        Self {
            kind: val.kind.to_string(),
            name: val.name.map(|v| v.to_string()),
            description: val.description.to_string(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ImportInfo> for tree_sitter_language_pack::ImportInfo {
    fn from(val: ImportInfo) -> Self {
        Self {
            source: val.source,
            items: val.items.into_iter().collect(),
            alias: val.alias,
            is_wildcard: val.is_wildcard,
            span: val.span.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::ImportInfo> for ImportInfo {
    fn from(val: tree_sitter_language_pack::ImportInfo) -> Self {
        Self {
            source: val.source.to_string(),
            items: val.items.into_iter().collect(),
            alias: val.alias.map(|v| v.to_string()),
            is_wildcard: val.is_wildcard,
            span: val.span.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ExportInfo> for tree_sitter_language_pack::ExportInfo {
    fn from(val: ExportInfo) -> Self {
        Self {
            name: val.name,
            kind: val.kind.into(),
            span: val.span.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::ExportInfo> for ExportInfo {
    fn from(val: tree_sitter_language_pack::ExportInfo) -> Self {
        Self {
            name: val.name.to_string(),
            kind: val.kind.into(),
            span: val.span.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<SymbolInfo> for tree_sitter_language_pack::SymbolInfo {
    fn from(val: SymbolInfo) -> Self {
        Self {
            name: val.name,
            kind: val.kind.into(),
            span: val.span.into(),
            type_annotation: val.type_annotation,
            doc: val.doc,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::SymbolInfo> for SymbolInfo {
    fn from(val: tree_sitter_language_pack::SymbolInfo) -> Self {
        Self {
            name: val.name.to_string(),
            kind: val.kind.into(),
            span: val.span.into(),
            type_annotation: val.type_annotation.map(|v| v.to_string()),
            doc: val.doc.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<Diagnostic> for tree_sitter_language_pack::Diagnostic {
    fn from(val: Diagnostic) -> Self {
        Self {
            message: val.message,
            severity: val.severity.into(),
            span: val.span.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::Diagnostic> for Diagnostic {
    fn from(val: tree_sitter_language_pack::Diagnostic) -> Self {
        Self {
            message: val.message.to_string(),
            severity: val.severity.into(),
            span: val.span.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<CodeChunk> for tree_sitter_language_pack::CodeChunk {
    fn from(val: CodeChunk) -> Self {
        Self {
            content: val.content,
            start_byte: val.start_byte,
            end_byte: val.end_byte,
            start_line: val.start_line,
            end_line: val.end_line,
            metadata: val.metadata.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::CodeChunk> for CodeChunk {
    fn from(val: tree_sitter_language_pack::CodeChunk) -> Self {
        Self {
            content: val.content.to_string(),
            start_byte: val.start_byte,
            end_byte: val.end_byte,
            start_line: val.start_line,
            end_line: val.end_line,
            metadata: val.metadata.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ChunkContext> for tree_sitter_language_pack::ChunkContext {
    fn from(val: ChunkContext) -> Self {
        Self {
            language: val.language,
            chunk_index: val.chunk_index,
            total_chunks: val.total_chunks,
            node_types: val.node_types.into_iter().collect(),
            context_path: val.context_path.into_iter().collect(),
            symbols_defined: val.symbols_defined.into_iter().collect(),
            comments: val.comments.into_iter().map(Into::into).collect(),
            docstrings: val.docstrings.into_iter().map(Into::into).collect(),
            has_error_nodes: val.has_error_nodes,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::ChunkContext> for ChunkContext {
    fn from(val: tree_sitter_language_pack::ChunkContext) -> Self {
        Self {
            language: val.language.to_string(),
            chunk_index: val.chunk_index,
            total_chunks: val.total_chunks,
            node_types: val.node_types.into_iter().collect(),
            context_path: val.context_path.into_iter().collect(),
            symbols_defined: val.symbols_defined.into_iter().collect(),
            comments: val.comments.into_iter().map(Into::into).collect(),
            docstrings: val.docstrings.into_iter().map(Into::into).collect(),
            has_error_nodes: val.has_error_nodes,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<PackConfig> for tree_sitter_language_pack::PackConfig {
    fn from(val: PackConfig) -> Self {
        Self {
            cache_dir: val.cache_dir.map(Into::into),
            languages: val.languages.map(|v| v.into_iter().collect()),
            groups: val.groups.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::PackConfig> for PackConfig {
    fn from(val: tree_sitter_language_pack::PackConfig) -> Self {
        Self {
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            languages: val.languages.map(|v| v.into_iter().collect()),
            groups: val.groups.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<Point> for tree_sitter_language_pack::Point {
    fn from(val: Point) -> Self {
        Self {
            row: val.row,
            column: val.column,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::Point> for Point {
    fn from(val: tree_sitter_language_pack::Point) -> Self {
        Self {
            row: val.row,
            column: val.column,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ByteRange> for tree_sitter_language_pack::ByteRange {
    fn from(val: ByteRange) -> Self {
        Self {
            start: val.start,
            end: val.end,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::ByteRange> for ByteRange {
    fn from(val: tree_sitter_language_pack::ByteRange) -> Self {
        Self {
            start: val.start,
            end: val.end,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ProcessConfig> for tree_sitter_language_pack::ProcessConfig {
    fn from(val: ProcessConfig) -> Self {
        Self {
            language: val.language.into(),
            structure: val.structure,
            imports: val.imports,
            exports: val.exports,
            comments: val.comments,
            docstrings: val.docstrings,
            symbols: val.symbols,
            diagnostics: val.diagnostics,
            chunk_max_size: val.chunk_max_size,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<tree_sitter_language_pack::ProcessConfig> for ProcessConfig {
    fn from(val: tree_sitter_language_pack::ProcessConfig) -> Self {
        Self {
            language: val.language.to_string(),
            structure: val.structure,
            imports: val.imports,
            exports: val.exports,
            comments: val.comments,
            docstrings: val.docstrings,
            symbols: val.symbols,
            diagnostics: val.diagnostics,
            chunk_max_size: val.chunk_max_size,
        }
    }
}

impl From<StructureKind> for tree_sitter_language_pack::StructureKind {
    fn from(val: StructureKind) -> Self {
        match val {
            StructureKind::Function => Self::Function,
            StructureKind::Method => Self::Method,
            StructureKind::Class => Self::Class,
            StructureKind::Struct => Self::Struct,
            StructureKind::Interface => Self::Interface,
            StructureKind::Enum => Self::Enum,
            StructureKind::Module => Self::Module,
            StructureKind::Trait => Self::Trait,
            StructureKind::Impl => Self::Impl,
            StructureKind::Namespace => Self::Namespace,
            StructureKind::Other { _0 } => Self::Other(_0),
        }
    }
}

impl From<tree_sitter_language_pack::StructureKind> for StructureKind {
    fn from(val: tree_sitter_language_pack::StructureKind) -> Self {
        match val {
            tree_sitter_language_pack::StructureKind::Function => Self::Function,
            tree_sitter_language_pack::StructureKind::Method => Self::Method,
            tree_sitter_language_pack::StructureKind::Class => Self::Class,
            tree_sitter_language_pack::StructureKind::Struct => Self::Struct,
            tree_sitter_language_pack::StructureKind::Interface => Self::Interface,
            tree_sitter_language_pack::StructureKind::Enum => Self::Enum,
            tree_sitter_language_pack::StructureKind::Module => Self::Module,
            tree_sitter_language_pack::StructureKind::Trait => Self::Trait,
            tree_sitter_language_pack::StructureKind::Impl => Self::Impl,
            tree_sitter_language_pack::StructureKind::Namespace => Self::Namespace,
            tree_sitter_language_pack::StructureKind::Other(_0) => Self::Other { _0: _0.to_string() },
        }
    }
}

impl From<CommentKind> for tree_sitter_language_pack::CommentKind {
    fn from(val: CommentKind) -> Self {
        match val {
            CommentKind::Line => Self::Line,
            CommentKind::Block => Self::Block,
            CommentKind::Doc => Self::Doc,
        }
    }
}

impl From<tree_sitter_language_pack::CommentKind> for CommentKind {
    fn from(val: tree_sitter_language_pack::CommentKind) -> Self {
        match val {
            tree_sitter_language_pack::CommentKind::Line => Self::Line,
            tree_sitter_language_pack::CommentKind::Block => Self::Block,
            tree_sitter_language_pack::CommentKind::Doc => Self::Doc,
        }
    }
}

impl From<DocstringFormat> for tree_sitter_language_pack::DocstringFormat {
    fn from(val: DocstringFormat) -> Self {
        match val {
            DocstringFormat::PythonTripleQuote => Self::PythonTripleQuote,
            DocstringFormat::JSDoc => Self::JSDoc,
            DocstringFormat::Rustdoc => Self::Rustdoc,
            DocstringFormat::GoDoc => Self::GoDoc,
            DocstringFormat::JavaDoc => Self::JavaDoc,
            DocstringFormat::Other { _0 } => Self::Other(_0),
        }
    }
}

impl From<tree_sitter_language_pack::DocstringFormat> for DocstringFormat {
    fn from(val: tree_sitter_language_pack::DocstringFormat) -> Self {
        match val {
            tree_sitter_language_pack::DocstringFormat::PythonTripleQuote => Self::PythonTripleQuote,
            tree_sitter_language_pack::DocstringFormat::JSDoc => Self::JSDoc,
            tree_sitter_language_pack::DocstringFormat::Rustdoc => Self::Rustdoc,
            tree_sitter_language_pack::DocstringFormat::GoDoc => Self::GoDoc,
            tree_sitter_language_pack::DocstringFormat::JavaDoc => Self::JavaDoc,
            tree_sitter_language_pack::DocstringFormat::Other(_0) => Self::Other { _0: _0.to_string() },
        }
    }
}

impl From<ExportKind> for tree_sitter_language_pack::ExportKind {
    fn from(val: ExportKind) -> Self {
        match val {
            ExportKind::Named => Self::Named,
            ExportKind::Default => Self::Default,
            ExportKind::ReExport => Self::ReExport,
        }
    }
}

impl From<tree_sitter_language_pack::ExportKind> for ExportKind {
    fn from(val: tree_sitter_language_pack::ExportKind) -> Self {
        match val {
            tree_sitter_language_pack::ExportKind::Named => Self::Named,
            tree_sitter_language_pack::ExportKind::Default => Self::Default,
            tree_sitter_language_pack::ExportKind::ReExport => Self::ReExport,
        }
    }
}

impl From<SymbolKind> for tree_sitter_language_pack::SymbolKind {
    fn from(val: SymbolKind) -> Self {
        match val {
            SymbolKind::Variable => Self::Variable,
            SymbolKind::Constant => Self::Constant,
            SymbolKind::Function => Self::Function,
            SymbolKind::Class => Self::Class,
            SymbolKind::Type => Self::Type,
            SymbolKind::Interface => Self::Interface,
            SymbolKind::Enum => Self::Enum,
            SymbolKind::Module => Self::Module,
            SymbolKind::Other { _0 } => Self::Other(_0),
        }
    }
}

impl From<tree_sitter_language_pack::SymbolKind> for SymbolKind {
    fn from(val: tree_sitter_language_pack::SymbolKind) -> Self {
        match val {
            tree_sitter_language_pack::SymbolKind::Variable => Self::Variable,
            tree_sitter_language_pack::SymbolKind::Constant => Self::Constant,
            tree_sitter_language_pack::SymbolKind::Function => Self::Function,
            tree_sitter_language_pack::SymbolKind::Class => Self::Class,
            tree_sitter_language_pack::SymbolKind::Type => Self::Type,
            tree_sitter_language_pack::SymbolKind::Interface => Self::Interface,
            tree_sitter_language_pack::SymbolKind::Enum => Self::Enum,
            tree_sitter_language_pack::SymbolKind::Module => Self::Module,
            tree_sitter_language_pack::SymbolKind::Other(_0) => Self::Other { _0: _0.to_string() },
        }
    }
}

impl From<DiagnosticSeverity> for tree_sitter_language_pack::DiagnosticSeverity {
    fn from(val: DiagnosticSeverity) -> Self {
        match val {
            DiagnosticSeverity::Error => Self::Error,
            DiagnosticSeverity::Warning => Self::Warning,
            DiagnosticSeverity::Info => Self::Info,
        }
    }
}

impl From<tree_sitter_language_pack::DiagnosticSeverity> for DiagnosticSeverity {
    fn from(val: tree_sitter_language_pack::DiagnosticSeverity) -> Self {
        match val {
            tree_sitter_language_pack::DiagnosticSeverity::Error => Self::Error,
            tree_sitter_language_pack::DiagnosticSeverity::Warning => Self::Warning,
            tree_sitter_language_pack::DiagnosticSeverity::Info => Self::Info,
        }
    }
}

/// Convert a `tree_sitter_language_pack::error::Error` error to a Magnus runtime error.
#[allow(dead_code)]
fn error_to_magnus_err(e: tree_sitter_language_pack::error::Error) -> magnus::Error {
    let msg = e.to_string();
    magnus::Error::new(unsafe { magnus::Ruby::get_unchecked() }.exception_runtime_error(), msg)
}

#[magnus::init]
fn ruby_init(ruby: &Ruby) -> Result<(), Error> {
    let module = ruby.define_module("TreeSitterLanguagePack")?;

    // Ensure JSON library is loaded for Hash#to_json
    let _ = ruby.eval::<magnus::Value>("require \"json\"");

    let class = module.define_class("Span", ruby.class_object())?;

    class.define_singleton_method("new", function!(Span::new, -1))?;

    class.define_method("start_byte", method!(Span::start_byte, 0))?;

    class.define_method("end_byte", method!(Span::end_byte, 0))?;

    class.define_method("start_line", method!(Span::start_line, 0))?;

    class.define_method("start_column", method!(Span::start_column, 0))?;

    class.define_method("end_line", method!(Span::end_line, 0))?;

    class.define_method("end_column", method!(Span::end_column, 0))?;

    let class = module.define_class("ProcessResult", ruby.class_object())?;

    class.define_singleton_method("new", function!(ProcessResult::new, -1))?;

    class.define_method("language", method!(ProcessResult::language, 0))?;

    class.define_method("metrics", method!(ProcessResult::metrics, 0))?;

    class.define_method("structure", method!(ProcessResult::structure, 0))?;

    class.define_method("imports", method!(ProcessResult::imports, 0))?;

    class.define_method("exports", method!(ProcessResult::exports, 0))?;

    class.define_method("comments", method!(ProcessResult::comments, 0))?;

    class.define_method("docstrings", method!(ProcessResult::docstrings, 0))?;

    class.define_method("symbols", method!(ProcessResult::symbols, 0))?;

    class.define_method("diagnostics", method!(ProcessResult::diagnostics, 0))?;

    class.define_method("chunks", method!(ProcessResult::chunks, 0))?;

    let class = module.define_class("FileMetrics", ruby.class_object())?;

    class.define_singleton_method("new", function!(FileMetrics::new, -1))?;

    class.define_method("total_lines", method!(FileMetrics::total_lines, 0))?;

    class.define_method("code_lines", method!(FileMetrics::code_lines, 0))?;

    class.define_method("comment_lines", method!(FileMetrics::comment_lines, 0))?;

    class.define_method("blank_lines", method!(FileMetrics::blank_lines, 0))?;

    class.define_method("total_bytes", method!(FileMetrics::total_bytes, 0))?;

    class.define_method("node_count", method!(FileMetrics::node_count, 0))?;

    class.define_method("error_count", method!(FileMetrics::error_count, 0))?;

    class.define_method("max_depth", method!(FileMetrics::max_depth, 0))?;

    let class = module.define_class("StructureItem", ruby.class_object())?;

    class.define_singleton_method("new", function!(StructureItem::new, -1))?;

    class.define_method("kind", method!(StructureItem::kind, 0))?;

    class.define_method("name", method!(StructureItem::name, 0))?;

    class.define_method("visibility", method!(StructureItem::visibility, 0))?;

    class.define_method("span", method!(StructureItem::span, 0))?;

    class.define_method("children", method!(StructureItem::children, 0))?;

    class.define_method("decorators", method!(StructureItem::decorators, 0))?;

    class.define_method("doc_comment", method!(StructureItem::doc_comment, 0))?;

    class.define_method("signature", method!(StructureItem::signature, 0))?;

    class.define_method("body_span", method!(StructureItem::body_span, 0))?;

    let class = module.define_class("CommentInfo", ruby.class_object())?;

    class.define_singleton_method("new", function!(CommentInfo::new, -1))?;

    class.define_method("text", method!(CommentInfo::text, 0))?;

    class.define_method("kind", method!(CommentInfo::kind, 0))?;

    class.define_method("span", method!(CommentInfo::span, 0))?;

    class.define_method("associated_node", method!(CommentInfo::associated_node, 0))?;

    let class = module.define_class("DocstringInfo", ruby.class_object())?;

    class.define_singleton_method("new", function!(DocstringInfo::new, -1))?;

    class.define_method("text", method!(DocstringInfo::text, 0))?;

    class.define_method("format", method!(DocstringInfo::format, 0))?;

    class.define_method("span", method!(DocstringInfo::span, 0))?;

    class.define_method("associated_item", method!(DocstringInfo::associated_item, 0))?;

    class.define_method("parsed_sections", method!(DocstringInfo::parsed_sections, 0))?;

    let class = module.define_class("DocSection", ruby.class_object())?;

    class.define_singleton_method("new", function!(DocSection::new, -1))?;

    class.define_method("kind", method!(DocSection::kind, 0))?;

    class.define_method("name", method!(DocSection::name, 0))?;

    class.define_method("description", method!(DocSection::description, 0))?;

    let class = module.define_class("ImportInfo", ruby.class_object())?;

    class.define_singleton_method("new", function!(ImportInfo::new, -1))?;

    class.define_method("source", method!(ImportInfo::source, 0))?;

    class.define_method("items", method!(ImportInfo::items, 0))?;

    class.define_method("alias", method!(ImportInfo::alias, 0))?;

    class.define_method("is_wildcard", method!(ImportInfo::is_wildcard, 0))?;

    class.define_method("span", method!(ImportInfo::span, 0))?;

    let class = module.define_class("ExportInfo", ruby.class_object())?;

    class.define_singleton_method("new", function!(ExportInfo::new, -1))?;

    class.define_method("name", method!(ExportInfo::name, 0))?;

    class.define_method("kind", method!(ExportInfo::kind, 0))?;

    class.define_method("span", method!(ExportInfo::span, 0))?;

    let class = module.define_class("SymbolInfo", ruby.class_object())?;

    class.define_singleton_method("new", function!(SymbolInfo::new, -1))?;

    class.define_method("name", method!(SymbolInfo::name, 0))?;

    class.define_method("kind", method!(SymbolInfo::kind, 0))?;

    class.define_method("span", method!(SymbolInfo::span, 0))?;

    class.define_method("type_annotation", method!(SymbolInfo::type_annotation, 0))?;

    class.define_method("doc", method!(SymbolInfo::doc, 0))?;

    let class = module.define_class("Diagnostic", ruby.class_object())?;

    class.define_singleton_method("new", function!(Diagnostic::new, -1))?;

    class.define_method("message", method!(Diagnostic::message, 0))?;

    class.define_method("severity", method!(Diagnostic::severity, 0))?;

    class.define_method("span", method!(Diagnostic::span, 0))?;

    let class = module.define_class("CodeChunk", ruby.class_object())?;

    class.define_singleton_method("new", function!(CodeChunk::new, -1))?;

    class.define_method("content", method!(CodeChunk::content, 0))?;

    class.define_method("start_byte", method!(CodeChunk::start_byte, 0))?;

    class.define_method("end_byte", method!(CodeChunk::end_byte, 0))?;

    class.define_method("start_line", method!(CodeChunk::start_line, 0))?;

    class.define_method("end_line", method!(CodeChunk::end_line, 0))?;

    class.define_method("metadata", method!(CodeChunk::metadata, 0))?;

    class.define_method("to_s", method!(CodeChunk::to_s, 0))?;

    let class = module.define_class("ChunkContext", ruby.class_object())?;

    class.define_singleton_method("new", function!(ChunkContext::new, -1))?;

    class.define_method("language", method!(ChunkContext::language, 0))?;

    class.define_method("chunk_index", method!(ChunkContext::chunk_index, 0))?;

    class.define_method("total_chunks", method!(ChunkContext::total_chunks, 0))?;

    class.define_method("node_types", method!(ChunkContext::node_types, 0))?;

    class.define_method("context_path", method!(ChunkContext::context_path, 0))?;

    class.define_method("symbols_defined", method!(ChunkContext::symbols_defined, 0))?;

    class.define_method("comments", method!(ChunkContext::comments, 0))?;

    class.define_method("docstrings", method!(ChunkContext::docstrings, 0))?;

    class.define_method("has_error_nodes", method!(ChunkContext::has_error_nodes, 0))?;

    let class = module.define_class("PackConfig", ruby.class_object())?;

    class.define_singleton_method("new", function!(PackConfig::new, -1))?;

    class.define_method("cache_dir", method!(PackConfig::cache_dir, 0))?;

    class.define_method("languages", method!(PackConfig::languages, 0))?;

    class.define_method("groups", method!(PackConfig::groups, 0))?;

    let class = module.define_class("Point", ruby.class_object())?;

    class.define_singleton_method("new", function!(Point::new, -1))?;

    class.define_method("row", method!(Point::row, 0))?;

    class.define_method("column", method!(Point::column, 0))?;

    let class = module.define_class("ByteRange", ruby.class_object())?;

    class.define_singleton_method("new", function!(ByteRange::new, -1))?;

    class.define_method("start", method!(ByteRange::start, 0))?;

    class.define_method("end", method!(ByteRange::end, 0))?;

    let class = module.define_class("Parser", ruby.class_object())?;

    let class = module.define_class("Tree", ruby.class_object())?;

    class.define_method("root_node", method!(Tree::root_node, 0))?;

    class.define_method("walk", method!(Tree::walk, 0))?;

    let class = module.define_class("Node", ruby.class_object())?;

    class.define_method("clone", method!(Node::clone, 0))?;

    class.define_method("kind", method!(Node::kind, 0))?;

    class.define_method("kind_id", method!(Node::kind_id, 0))?;

    class.define_method("start_byte", method!(Node::start_byte, 0))?;

    class.define_method("end_byte", method!(Node::end_byte, 0))?;

    class.define_method("byte_range", method!(Node::byte_range, 0))?;

    class.define_method("start_position", method!(Node::start_position, 0))?;

    class.define_method("end_position", method!(Node::end_position, 0))?;

    class.define_method("is_named", method!(Node::is_named, 0))?;

    class.define_method("is_error", method!(Node::is_error, 0))?;

    class.define_method("is_missing", method!(Node::is_missing, 0))?;

    class.define_method("is_extra", method!(Node::is_extra, 0))?;

    class.define_method("has_error", method!(Node::has_error, 0))?;

    class.define_method("parent", method!(Node::parent, 0))?;

    class.define_method("child", method!(Node::child, 1))?;

    class.define_method("child_count", method!(Node::child_count, 0))?;

    class.define_method("named_child", method!(Node::named_child, 1))?;

    class.define_method("named_child_count", method!(Node::named_child_count, 0))?;

    class.define_method("child_by_field_name", method!(Node::child_by_field_name, 1))?;

    class.define_method("to_sexp", method!(Node::to_sexp, 0))?;

    class.define_method("walk", method!(Node::walk, 0))?;

    let class = module.define_class("TreeCursor", ruby.class_object())?;

    class.define_method("node", method!(TreeCursor::node, 0))?;

    class.define_method("field_name", method!(TreeCursor::field_name, 0))?;

    let class = module.define_class("ProcessConfig", ruby.class_object())?;

    class.define_singleton_method("new", function!(ProcessConfig::new, -1))?;

    class.define_method("language", method!(ProcessConfig::language, 0))?;

    class.define_method("structure", method!(ProcessConfig::structure, 0))?;

    class.define_method("imports", method!(ProcessConfig::imports, 0))?;

    class.define_method("exports", method!(ProcessConfig::exports, 0))?;

    class.define_method("comments", method!(ProcessConfig::comments, 0))?;

    class.define_method("docstrings", method!(ProcessConfig::docstrings, 0))?;

    class.define_method("symbols", method!(ProcessConfig::symbols, 0))?;

    class.define_method("diagnostics", method!(ProcessConfig::diagnostics, 0))?;

    class.define_method("chunk_max_size", method!(ProcessConfig::chunk_max_size, 0))?;

    class.define_method("with_chunking", method!(ProcessConfig::with_chunking, 1))?;

    class.define_method("all", method!(ProcessConfig::all, 0))?;

    class.define_method("minimal", method!(ProcessConfig::minimal, 0))?;

    let class = module.define_class("LanguageRegistry", ruby.class_object())?;

    class.define_method("get_language", method!(LanguageRegistry::get_language, 1))?;

    class.define_method("available_languages", method!(LanguageRegistry::available_languages, 0))?;

    class.define_method("has_parser", method!(LanguageRegistry::has_parser, 1))?;

    class.define_method("has_language", method!(LanguageRegistry::has_language, 1))?;

    class.define_method("language_count", method!(LanguageRegistry::language_count, 0))?;

    class.define_method("process", method!(LanguageRegistry::process, 2))?;

    let class = module.define_class("DownloadManager", ruby.class_object())?;

    class.define_method("installed_languages", method!(DownloadManager::installed_languages, 0))?;

    class.define_method(
        "download_all_best_effort",
        method!(DownloadManager::download_all_best_effort, 0),
    )?;

    class.define_method("clean_cache", method!(DownloadManager::clean_cache, 0))?;

    let class = module.define_class("Language", ruby.class_object())?;

    module.define_module_function(
        "detect_language_from_extension",
        function!(detect_language_from_extension, 1),
    )?;

    module.define_module_function("detect_language_from_path", function!(detect_language_from_path, 1))?;

    module.define_module_function(
        "detect_language_from_content",
        function!(detect_language_from_content, 1),
    )?;

    module.define_module_function("get_highlights_query", function!(get_highlights_query, 1))?;

    module.define_module_function("get_injections_query", function!(get_injections_query, 1))?;

    module.define_module_function("get_locals_query", function!(get_locals_query, 1))?;

    module.define_module_function("get_language", function!(get_language, 1))?;

    module.define_module_function("get_parser", function!(get_parser, 1))?;

    module.define_module_function("detect_language", function!(detect_language, 1))?;

    module.define_module_function("available_languages", function!(available_languages, 0))?;

    module.define_module_function("has_language", function!(has_language, 1))?;

    module.define_module_function("language_count", function!(language_count, 0))?;

    module.define_module_function("process", function!(process, -1))?;

    module.define_module_function("init", function!(init, -1))?;

    module.define_module_function("configure", function!(configure, -1))?;

    module.define_module_function("download", function!(download, 1))?;

    module.define_module_function("download_all", function!(download_all, 0))?;

    module.define_module_function("download_group", function!(download_group, 1))?;

    module.define_module_function("manifest_languages", function!(manifest_languages, 0))?;

    module.define_module_function("downloaded_languages", function!(downloaded_languages, 0))?;

    module.define_module_function("clean_cache", function!(clean_cache, 0))?;

    module.define_module_function("cache_dir", function!(cache_dir, 0))?;

    Ok(())
}
