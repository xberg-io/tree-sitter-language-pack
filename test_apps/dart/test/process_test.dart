// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4efd25381a195b69485af505206b6aa26a1942f9c95bec5727aa491e538b1690
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// ignore_for_file: unused_local_variable, unused_element

import 'package:test/test.dart';
import 'package:tree_sitter_language_pack/tree_sitter_language_pack.dart';
import 'package:tree_sitter_language_pack/src/tree_sitter_language_pack_bridge_generated/frb_generated.dart' show RustLib;

// E2e tests for category: process

String _alefE2eText(Object? value) {
  if (value == null) return '';
  // Check if it's an enum by examining its toString representation.
  final str = value.toString();
  if (str.contains('.')) {
    // Enum.toString() returns 'EnumName.variantName'. Extract the variant name.
    final parts = str.split('.');
    if (parts.length == 2) {
      final variantName = parts[1];
      // Convert camelCase variant names to snake_case for serde compatibility.
      // E.g. 'toolCalls' -> 'tool_calls', 'stop' -> 'stop'.
      return _camelToSnake(variantName);
    }
  }
  return str;
}

String _camelToSnake(String camel) {
  final buffer = StringBuffer();
  for (int i = 0; i < camel.length; i++) {
    final char = camel[i];
    if (char.contains(RegExp(r'[A-Z]'))) {
      if (i > 0) buffer.write('_');
      buffer.write(char.toLowerCase());
    } else {
      buffer.write(char);
    }
  }
  return buffer.toString();
}

void main() {
  var _rustLibInitialized = false;

  setUpAll(() async {
    await RustLib.init();
    _rustLibInitialized = true;
  });

  tearDownAll(() async {
    if (_rustLibInitialized) {
      RustLib.dispose();
    }
  });

  test('Intel: C function with include', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"c"}');
    final result = await TreeSitterLanguagePackBridge.process('#include <stdio.h>\n\nint main() {\n    printf("hello");\n    return 0;\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('c'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.metrics.totalLines, greaterThanOrEqualTo(6));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: process with all features enabled', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('# A comment\ndef greet(name):\n    """Say hello."""\n    return f\'Hi {name}\'\n\nimport os\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.imports.length, greaterThanOrEqualTo(1));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(6));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: process with minimal config - only metrics', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('def hello():\n    pass\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(2));
  });

  test('Intel: extract structure from Go function definition', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"go"}');
    final result = await TreeSitterLanguagePackBridge.process('package main\n\nimport "fmt"\n\nfunc main() {\n\tfmt.Println("hello")\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('go'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.imports.length, greaterThanOrEqualTo(1));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(7));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: extract structure from Go function definition', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"go"}');
    final result = await TreeSitterLanguagePackBridge.process('package main\n\nimport "fmt"\n\nfunc main() {\n\tfmt.Println("hello")\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('go'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.imports.length, greaterThanOrEqualTo(1));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(7));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: Java class with methods and imports', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"java"}');
    final result = await TreeSitterLanguagePackBridge.process('import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return "Hello " + name;\n    }\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('java'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Class').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.imports.length, greaterThanOrEqualTo(1));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(7));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: Java package declaration emitted as Module so callers can build FQNs (#112)', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"java"}');
    final result = await TreeSitterLanguagePackBridge.process('package com.example.widget;\n\npublic class Widget {\n    public String name() { return "w"; }\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('java'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(2));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Module').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Class').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: detect multiple imports and function in JavaScript', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"javascript"}');
    final result = await TreeSitterLanguagePackBridge.process('import fs from \'fs\';\nimport path from \'path\';\n\nfunction process(input) {\n    return input.trim();\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('javascript'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.imports.length, greaterThanOrEqualTo(2));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(6));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: detect multiple imports and function in JavaScript', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"javascript"}');
    final result = await TreeSitterLanguagePackBridge.process('import fs from \'fs\';\nimport path from \'path\';\n\nfunction process(input) {\n    return input.trim();\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('javascript'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.imports.length, greaterThanOrEqualTo(2));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(6));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112)', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"kotlin"}');
    final result = await TreeSitterLanguagePackBridge.process('package foo.bar\n\nclass Widget {\n    fun greet(): String = "hi"\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('kotlin'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(2));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Module').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Class').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.metrics.errorCount, equals(0));
  });

  test('JavaScript with multiple exports, verify export count', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"javascript"}');
    final result = await TreeSitterLanguagePackBridge.process('export function greet() { return \'hi\'; }\nexport const VERSION = \'1.0\';\nexport default class App {}\n', config: _config);
    expect(result.language.toString().trim(), equals('javascript'.toString().trim()));
    expect(result.exports.length, greaterThanOrEqualTo(2));
  });

  test('JavaScript with exports, verify export count', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"javascript"}');
    final result = await TreeSitterLanguagePackBridge.process('export function greet(name) {\n  return `Hello \${name}`;\n}\n\nexport const VERSION = \'1.0\';\n', config: _config);
    expect(result.language.toString().trim(), equals('javascript'.toString().trim()));
    expect(result.exports.length, greaterThanOrEqualTo(1));
  });

  test('Python comprehensive source with all feature extraction enabled', () async {
    final _config = await createProcessConfigFromJson(json: '{"comments":true,"docstrings":true,"imports":true,"language":"python","structure":true,"symbols":true}');
    final result = await TreeSitterLanguagePackBridge.process('import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    """Process a file and return contents."""\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(2));
    expect(result.imports.length, greaterThanOrEqualTo(1));
    expect(result.comments.length, greaterThanOrEqualTo(1));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(10));
  });

  test('Python with comments, verify comment count', () async {
    final _config = await createProcessConfigFromJson(json: '{"comments":true,"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.comments.length, greaterThanOrEqualTo(1));
  });

  test('Python with function docstring, verify docstring count', () async {
    final _config = await createProcessConfigFromJson(json: '{"docstrings":true,"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('def greet(name):\n    """Say hello to someone."""\n    return f"Hello {name}"\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(3));
  });

  test('Python with multiple imports, verify imports contain specific source', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.imports.length, greaterThanOrEqualTo(2));
    expect(result.imports.where((item) {
            final texts = <String>[];
            texts.add(item.source.toString());
            texts.addAll(item.items.map((e) => e.toString()));
            final v_alias = item.alias;
            if (v_alias != null) texts.add(v_alias.toString());
            return texts.any((t) => t.toLowerCase().contains(('os').toString().toLowerCase()));
          }).isEmpty, isFalse);
  });

  test('Python code with metrics assertions', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print(\'hello\')\n\ndef world():\n    print(\'world\')\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.metrics.codeLines, greaterThanOrEqualTo(4));
    expect(result.metrics.commentLines, greaterThanOrEqualTo(1));
    expect(result.metrics.maxDepth, greaterThanOrEqualTo(1));
  });

  test('Python with class and functions, verify symbol count', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"python","symbols":true}');
    final result = await TreeSitterLanguagePackBridge.process('MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.symbols.length, greaterThanOrEqualTo(1));
  });

  test('Rust struct with name, verify structure name contains value', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"rust"}');
    final result = await TreeSitterLanguagePackBridge.process('pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('rust'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('MyConfig').toString().toLowerCase()));
          }).isEmpty, isFalse);
  });

  test('Intel: Python code with medium chunk size', () async {
    final _config = await createProcessConfigFromJson(json: '{"chunk_max_size":50,"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(3));
    expect(result.metrics.errorCount, equals(0));
    expect(result.chunks.length, greaterThanOrEqualTo(2));
  });

  test('Intel: chunk multi-function Python source into multiple pieces', () async {
    final _config = await createProcessConfigFromJson(json: '{"chunk_max_size":30,"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(8));
    expect(result.chunks.length, greaterThanOrEqualTo(2));
  });

  test('Intel: extract nested structure from Python class with methods', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Class').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.metrics.totalLines, greaterThanOrEqualTo(6));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: extract nested structure from Python class with methods', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Class').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.metrics.totalLines, greaterThanOrEqualTo(6));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: Python code with syntax errors should report diagnostics', () async {
    final _config = await createProcessConfigFromJson(json: '{"diagnostics":true,"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('def broken(\n    pass\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.metrics.errorCount, equals(1));
    expect(result.diagnostics, isNotNull);
  });

  test('Intel: extract structure from Python function definition', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('def greet(name):\n    return f\'Hello, {name}!\'\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.metrics.totalLines, greaterThanOrEqualTo(2));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: extract structure from Python function definition', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('def greet(name):\n    return f\'Hello, {name}!\'\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.metrics.totalLines, greaterThanOrEqualTo(2));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: detect diagnostics in malformed Python code', () async {
    final _config = await createProcessConfigFromJson(json: '{"diagnostics":true,"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('def broken(\n    return\nclass', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.diagnostics, isNotNull);
  });

  test('Intel: detect diagnostics in malformed Python code', () async {
    final _config = await createProcessConfigFromJson(json: '{"diagnostics":true,"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('def broken(\n    return\nclass', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.diagnostics, isNotNull);
  });

  test('Intel: detect multiple Python imports', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.imports.length, greaterThanOrEqualTo(3));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(5));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: detect multiple Python imports', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"python"}');
    final result = await TreeSitterLanguagePackBridge.process('import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n', config: _config);
    expect(result.language.toString().trim(), equals('python'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.imports.length, greaterThanOrEqualTo(3));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(5));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: Ruby class with method', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"ruby"}');
    final result = await TreeSitterLanguagePackBridge.process('require \'json\'\n\nclass Greeter\n  def greet(name)\n    "Hello #{name}"\n  end\nend\n', config: _config);
    expect(result.language.toString().trim(), equals('ruby'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Class').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.metrics.totalLines, greaterThanOrEqualTo(7));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: chunk multi-function Rust source into pieces', () async {
    final _config = await createProcessConfigFromJson(json: '{"chunk_max_size":30,"language":"rust"}');
    final result = await TreeSitterLanguagePackBridge.process('fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n', config: _config);
    expect(result.language.toString().trim(), equals('rust'.toString().trim()));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(7));
    expect(result.chunks.length, greaterThanOrEqualTo(2));
  });

  test('Intel: chunk multi-function Rust source into pieces', () async {
    final _config = await createProcessConfigFromJson(json: '{"chunk_max_size":30,"language":"rust"}');
    final result = await TreeSitterLanguagePackBridge.process('fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n', config: _config);
    expect(result.language.toString().trim(), equals('rust'.toString().trim()));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(7));
    expect(result.chunks.length, greaterThanOrEqualTo(2));
  });

  test('Intel: extract structure from Rust function definition', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"rust"}');
    final result = await TreeSitterLanguagePackBridge.process('fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('rust'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.metrics.totalLines, greaterThanOrEqualTo(3));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: extract structure from Rust function definition', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"rust"}');
    final result = await TreeSitterLanguagePackBridge.process('fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('rust'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.metrics.totalLines, greaterThanOrEqualTo(3));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: extract structure from TypeScript function', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"typescript"}');
    final result = await TreeSitterLanguagePackBridge.process('import { readFile } from \'fs\';\n\nfunction greet(name: string): string {\n    return `Hello, \${name}!`;\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('typescript'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.imports.length, greaterThanOrEqualTo(1));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(5));
    expect(result.metrics.errorCount, equals(0));
  });

  test('Intel: extract structure from TypeScript function', () async {
    final _config = await createProcessConfigFromJson(json: '{"language":"typescript"}');
    final result = await TreeSitterLanguagePackBridge.process('import { readFile } from \'fs\';\n\nfunction greet(name: string): string {\n    return `Hello, \${name}!`;\n}\n', config: _config);
    expect(result.language.toString().trim(), equals('typescript'.toString().trim()));
    expect(result.structure.length, greaterThanOrEqualTo(1));
    expect(result.structure.where((item) {
            final texts = <String>[];
            texts.add(item.kind.toString());
            final v_name = item.name;
            if (v_name != null) texts.add(v_name.toString());
            final v_visibility = item.visibility;
            if (v_visibility != null) texts.add(v_visibility.toString());
            texts.addAll(item.decorators.map((e) => e.toString()));
            final v_docComment = item.docComment;
            if (v_docComment != null) texts.add(v_docComment.toString());
            final v_signature = item.signature;
            if (v_signature != null) texts.add(v_signature.toString());
            return texts.any((t) => t.toLowerCase().contains(('Function').toString().toLowerCase()));
          }).isEmpty, isFalse);
    expect(result.imports.length, greaterThanOrEqualTo(1));
    expect(result.metrics.totalLines, greaterThanOrEqualTo(5));
    expect(result.metrics.errorCount, equals(0));
  });

}
