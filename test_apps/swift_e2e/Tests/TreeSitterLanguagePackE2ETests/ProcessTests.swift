// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:566b56263e58c3b3401f54855ed85f2b157836dfb2b6d3a2c1aa27e9441d2093
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import TreeSitterLanguagePack
import RustBridge

/// E2e tests for category: process.
final class ProcessTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testCFunctionProcess() throws {
        // Intel: C function with include
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"c\"}")
        let result = try TreeSitterLanguagePack.process(source: "#include <stdio.h>\n\nint main() {\n    printf(\"hello\");\n    return 0;\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "c")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 6)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testConfigAllPython() throws {
        // Intel: process with all features enabled
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "# A comment\ndef greet(name):\n    \"\"\"Say hello.\"\"\"\n    return f'Hi {name}'\n\nimport os\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 6)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testConfigMinimalPython() throws {
        // Intel: process with minimal config - only metrics
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "def hello():\n    pass\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 2)
    }

    func testGoFunctionProcess() throws {
        // Intel: extract structure from Go function definition
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"go\"}")
        let result = try TreeSitterLanguagePack.process(source: "package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "go")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 7)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testGoFunctionProcessDetail() throws {
        // Intel: extract structure from Go function definition
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"go\"}")
        let result = try TreeSitterLanguagePack.process(source: "package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "go")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 7)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testJavaClassProcess() throws {
        // Intel: Java class with methods and imports
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"java\"}")
        let result = try TreeSitterLanguagePack.process(source: "import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return \"Hello \" + name;\n    }\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "java")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Class") })
        }), "expected to contain: \("Class")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 7)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testJavaPackageDeclarationProcess() throws {
        // Intel: Java package declaration emitted as Module so callers can build FQNs (#112)
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"java\"}")
        let result = try TreeSitterLanguagePack.process(source: "package com.example.widget;\n\npublic class Widget {\n    public String name() { return \"w\"; }\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "java")
        XCTAssertGreaterThanOrEqual(result.structure().count, 2)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Module") })
        }), "expected to contain: \("Module")")
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Class") })
        }), "expected to contain: \("Class")")
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testJavascriptMultiImportProcess() throws {
        // Intel: detect multiple imports and function in JavaScript
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"javascript\"}")
        let result = try TreeSitterLanguagePack.process(source: "import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "javascript")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 2)
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 6)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testJavascriptMultiImportProcessDetail() throws {
        // Intel: detect multiple imports and function in JavaScript
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"javascript\"}")
        let result = try TreeSitterLanguagePack.process(source: "import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "javascript")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 2)
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 6)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testKotlinPackageClassProcess() throws {
        // Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112)
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"kotlin\"}")
        let result = try TreeSitterLanguagePack.process(source: "package foo.bar\n\nclass Widget {\n    fun greet(): String = \"hi\"\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "kotlin")
        XCTAssertGreaterThanOrEqual(result.structure().count, 2)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Module") })
        }), "expected to contain: \("Module")")
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Class") })
        }), "expected to contain: \("Class")")
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testProcessJavascriptExportsCount() throws {
        // JavaScript with multiple exports, verify export count
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"javascript\"}")
        let result = try TreeSitterLanguagePack.process(source: "export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "javascript")
        XCTAssertGreaterThanOrEqual(result.exports().count, 2)
    }

    func testProcessJavascriptExportsDetail() throws {
        // JavaScript with exports, verify export count
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"javascript\"}")
        let result = try TreeSitterLanguagePack.process(source: "export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "javascript")
        XCTAssertGreaterThanOrEqual(result.exports().count, 1)
    }

    func testProcessPythonAllFeatures() throws {
        // Python comprehensive source with all feature extraction enabled
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"comments\":true,\"docstrings\":true,\"imports\":true,\"language\":\"python\",\"structure\":true,\"symbols\":true}")
        let result = try TreeSitterLanguagePack.process(source: "import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 2)
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.comments().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 10)
    }

    func testProcessPythonComments() throws {
        // Python with comments, verify comment count
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"comments\":true,\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.comments().count, 1)
    }

    func testProcessPythonDocstrings() throws {
        // Python with function docstring, verify docstring count
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"docstrings\":true,\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 3)
    }

    func testProcessPythonImportsDetail() throws {
        // Python with multiple imports, verify imports contain specific source
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.imports().count, 2)
        XCTAssertTrue(result.imports().contains(where: { item in
            var texts = [String]()
                texts.append(item.source().toString())
                texts.append(contentsOf: item.items().map { $0.as_str().toString() })
                if let v = item.alias() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("os") })
        }), "expected to contain: \("os")")
    }

    func testProcessPythonMetricsDetail() throws {
        // Python code with metrics assertions
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.metrics().codeLines(), 4)
        XCTAssertGreaterThanOrEqual(result.metrics().commentLines(), 1)
        XCTAssertGreaterThanOrEqual(result.metrics().maxDepth(), 1)
    }

    func testProcessPythonSymbols() throws {
        // Python with class and functions, verify symbol count
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"python\",\"symbols\":true}")
        let result = try TreeSitterLanguagePack.process(source: "MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.symbols().count, 1)
    }

    func testProcessRustStructureName() throws {
        // Rust struct with name, verify structure name contains value
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"rust\"}")
        let result = try TreeSitterLanguagePack.process(source: "pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "rust")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("MyConfig") })
        }), "expected to contain: \("MyConfig")")
    }

    func testPythonChunkingMedium() throws {
        // Intel: Python code with medium chunk size
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"chunk_max_size\":50,\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 3)
        XCTAssertEqual(result.metrics().errorCount(), 0)
        XCTAssertGreaterThanOrEqual(result.chunks().count, 2)
    }

    func testPythonChunkingProcessDetail() throws {
        // Intel: chunk multi-function Python source into multiple pieces
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"chunk_max_size\":30,\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 8)
        XCTAssertGreaterThanOrEqual(result.chunks().count, 2)
    }

    func testPythonClassWithMethodsProcess() throws {
        // Intel: extract nested structure from Python class with methods
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Class") })
        }), "expected to contain: \("Class")")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 6)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testPythonClassWithMethodsProcessDetail() throws {
        // Intel: extract nested structure from Python class with methods
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Class") })
        }), "expected to contain: \("Class")")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 6)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testPythonErrorDiagnostics() throws {
        // Intel: Python code with syntax errors should report diagnostics
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"diagnostics\":true,\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "def broken(\n    pass\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertEqual(result.metrics().errorCount(), 1)
        XCTAssertGreaterThan(result.diagnostics().count, 0, "expected non-empty value")
    }

    func testPythonFunctionProcess() throws {
        // Intel: extract structure from Python function definition
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "def greet(name):\n    return f'Hello, {name}!'\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 2)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testPythonFunctionProcessDetail() throws {
        // Intel: extract structure from Python function definition
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "def greet(name):\n    return f'Hello, {name}!'\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 2)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testPythonMalformedCodeProcess() throws {
        // Intel: detect diagnostics in malformed Python code
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"diagnostics\":true,\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "def broken(\n    return\nclass", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThan(result.diagnostics().count, 0, "expected non-empty value")
    }

    func testPythonMalformedCodeProcessDetail() throws {
        // Intel: detect diagnostics in malformed Python code
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"diagnostics\":true,\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "def broken(\n    return\nclass", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThan(result.diagnostics().count, 0, "expected non-empty value")
    }

    func testPythonMultiImportProcess() throws {
        // Intel: detect multiple Python imports
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertGreaterThanOrEqual(result.imports().count, 3)
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 5)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testPythonMultiImportProcessDetail() throws {
        // Intel: detect multiple Python imports
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"python\"}")
        let result = try TreeSitterLanguagePack.process(source: "import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertGreaterThanOrEqual(result.imports().count, 3)
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 5)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testRubyClassProcess() throws {
        // Intel: Ruby class with method
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"ruby\"}")
        let result = try TreeSitterLanguagePack.process(source: "require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello #{name}\"\n  end\nend\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "ruby")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Class") })
        }), "expected to contain: \("Class")")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 7)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testRustChunkingProcess() throws {
        // Intel: chunk multi-function Rust source into pieces
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"chunk_max_size\":30,\"language\":\"rust\"}")
        let result = try TreeSitterLanguagePack.process(source: "fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "rust")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 7)
        XCTAssertGreaterThanOrEqual(result.chunks().count, 2)
    }

    func testRustChunkingProcessDetail() throws {
        // Intel: chunk multi-function Rust source into pieces
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"chunk_max_size\":30,\"language\":\"rust\"}")
        let result = try TreeSitterLanguagePack.process(source: "fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "rust")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 7)
        XCTAssertGreaterThanOrEqual(result.chunks().count, 2)
    }

    func testRustFunctionProcess() throws {
        // Intel: extract structure from Rust function definition
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"rust\"}")
        let result = try TreeSitterLanguagePack.process(source: "fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "rust")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 3)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testRustFunctionProcessDetail() throws {
        // Intel: extract structure from Rust function definition
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"rust\"}")
        let result = try TreeSitterLanguagePack.process(source: "fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "rust")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 3)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testTypescriptFunctionProcess() throws {
        // Intel: extract structure from TypeScript function
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"typescript\"}")
        let result = try TreeSitterLanguagePack.process(source: "import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "typescript")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 5)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

    func testTypescriptFunctionProcessDetail() throws {
        // Intel: extract structure from TypeScript function
        let configObj = try TreeSitterLanguagePack.processConfigFromJson("{\"language\":\"typescript\"}")
        let result = try TreeSitterLanguagePack.process(source: "import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", config: configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "typescript")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue(result.structure().contains(where: { item in
            var texts = [String]()
                texts.append(item.kind().toString())
                if let v = item.name() { texts.append(v.toString()) }
                if let v = item.visibility() { texts.append(v.toString()) }
                texts.append(contentsOf: item.decorators().map { $0.as_str().toString() })
                if let v = item.docComment() { texts.append(v.toString()) }
                if let v = item.signature() { texts.append(v.toString()) }
            return texts.contains(where: { $0.contains("Function") })
        }), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().totalLines(), 5)
        XCTAssertEqual(result.metrics().errorCount(), 0)
    }

}
