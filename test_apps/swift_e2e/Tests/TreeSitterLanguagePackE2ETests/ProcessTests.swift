// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:562e58fdd97b56a8a812ba580e24d0c336048708b923dec1abfdd8dce3dacb5c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import TreeSitterLanguagePack
import RustBridge

/// E2e tests for category: process.
final class ProcessTests: XCTestCase {
    func testCFunctionProcess() throws {
        // Intel: C function with include
        let configObj = try extractionConfigFromJson("{\"language\":\"c\"}")
        let result = try process("#include <stdio.h>\n\nint main() {\n    printf(\"hello\");\n    return 0;\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "c")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 6)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testConfigAllPython() throws {
        // Intel: process with all features enabled
        let configObj = try extractionConfigFromJson("{\"language\":\"python\"}")
        let result = try process("# A comment\ndef greet(name):\n    \"\"\"Say hello.\"\"\"\n    return f'Hi {name}'\n\nimport os\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 6)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testConfigMinimalPython() throws {
        // Intel: process with minimal config - only metrics
        let configObj = try extractionConfigFromJson("{\"language\":\"python\"}")
        let result = try process("def hello():\n    pass\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 2)
    }

    func testGoFunctionProcess() throws {
        // Intel: extract structure from Go function definition
        let configObj = try extractionConfigFromJson("{\"language\":\"go\"}")
        let result = try process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "go")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 7)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testGoFunctionProcessDetail() throws {
        // Intel: extract structure from Go function definition
        let configObj = try extractionConfigFromJson("{\"language\":\"go\"}")
        let result = try process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "go")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 7)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testJavaClassProcess() throws {
        // Intel: Java class with methods and imports
        let configObj = try extractionConfigFromJson("{\"language\":\"java\"}")
        let result = try process("import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return \"Hello \" + name;\n    }\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "java")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Class"), "expected to contain: \("Class")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 7)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testJavaPackageDeclarationProcess() throws {
        // Intel: Java package declaration emitted as Module so callers can build FQNs (#112)
        let configObj = try extractionConfigFromJson("{\"language\":\"java\"}")
        let result = try process("package com.example.widget;\n\npublic class Widget {\n    public String name() { return \"w\"; }\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "java")
        XCTAssertGreaterThanOrEqual(result.structure().count, 2)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Module"), "expected to contain: \("Module")")
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Class"), "expected to contain: \("Class")")
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testJavascriptMultiImportProcess() throws {
        // Intel: detect multiple imports and function in JavaScript
        let configObj = try extractionConfigFromJson("{\"language\":\"javascript\"}")
        let result = try process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "javascript")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 2)
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 6)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testJavascriptMultiImportProcessDetail() throws {
        // Intel: detect multiple imports and function in JavaScript
        let configObj = try extractionConfigFromJson("{\"language\":\"javascript\"}")
        let result = try process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "javascript")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 2)
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 6)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testKotlinPackageClassProcess() throws {
        // Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112)
        let configObj = try extractionConfigFromJson("{\"language\":\"kotlin\"}")
        let result = try process("package foo.bar\n\nclass Widget {\n    fun greet(): String = \"hi\"\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "kotlin")
        XCTAssertGreaterThanOrEqual(result.structure().count, 2)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Module"), "expected to contain: \("Module")")
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Class"), "expected to contain: \("Class")")
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testProcessJavascriptExportsCount() throws {
        // JavaScript with multiple exports, verify export count
        let configObj = try extractionConfigFromJson("{\"language\":\"javascript\"}")
        let result = try process("export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "javascript")
        XCTAssertGreaterThanOrEqual(result.exports().count, 2)
    }

    func testProcessJavascriptExportsDetail() throws {
        // JavaScript with exports, verify export count
        let configObj = try extractionConfigFromJson("{\"language\":\"javascript\"}")
        let result = try process("export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "javascript")
        XCTAssertGreaterThanOrEqual(result.exports().count, 1)
    }

    func testProcessPythonAllFeatures() throws {
        // Python comprehensive source with all feature extraction enabled
        let configObj = try extractionConfigFromJson("{\"comments\":true,\"docstrings\":true,\"imports\":true,\"language\":\"python\",\"structure\":true,\"symbols\":true}")
        let result = try process("import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 2)
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.comments().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 10)
    }

    func testProcessPythonComments() throws {
        // Python with comments, verify comment count
        let configObj = try extractionConfigFromJson("{\"comments\":true,\"language\":\"python\"}")
        let result = try process("# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.comments().count, 1)
    }

    func testProcessPythonDocstrings() throws {
        // Python with function docstring, verify docstring count
        let configObj = try extractionConfigFromJson("{\"docstrings\":true,\"language\":\"python\"}")
        let result = try process("def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 3)
    }

    func testProcessPythonImportsDetail() throws {
        // Python with multiple imports, verify imports contain specific source
        let configObj = try extractionConfigFromJson("{\"language\":\"python\"}")
        let result = try process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.imports().count, 2)
        XCTAssertTrue((result.imports()?.map { $0.as_str().toString() } ?? []).contains("os"), "expected to contain: \("os")")
    }

    func testProcessPythonMetricsDetail() throws {
        // Python code with metrics assertions
        let configObj = try extractionConfigFromJson("{\"language\":\"python\"}")
        let result = try process("# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.metrics().code_lines(), 4)
        XCTAssertGreaterThanOrEqual(result.metrics().comment_lines(), 1)
        XCTAssertGreaterThanOrEqual(result.metrics().max_depth(), 1)
    }

    func testProcessPythonSymbols() throws {
        // Python with class and functions, verify symbol count
        let configObj = try extractionConfigFromJson("{\"language\":\"python\",\"symbols\":true}")
        let result = try process("MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.symbols().count, 1)
    }

    func testProcessRustStructureName() throws {
        // Rust struct with name, verify structure name contains value
        let configObj = try extractionConfigFromJson("{\"language\":\"rust\"}")
        let result = try process("pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "rust")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("MyConfig"), "expected to contain: \("MyConfig")")
    }

    func testPythonChunkingMedium() throws {
        // Intel: Python code with medium chunk size
        let configObj = try extractionConfigFromJson("{\"chunk_max_size\":50,\"language\":\"python\"}")
        let result = try process("def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 3)
        XCTAssertEqual(result.metrics().error_count(), 0)
        XCTAssertGreaterThanOrEqual(chunks.count, 2)
    }

    func testPythonChunkingProcessDetail() throws {
        // Intel: chunk multi-function Python source into multiple pieces
        let configObj = try extractionConfigFromJson("{\"chunk_max_size\":30,\"language\":\"python\"}")
        let result = try process("def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 8)
        XCTAssertGreaterThanOrEqual(chunks.count, 2)
    }

    func testPythonClassWithMethodsProcess() throws {
        // Intel: extract nested structure from Python class with methods
        let configObj = try extractionConfigFromJson("{\"language\":\"python\"}")
        let result = try process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Class"), "expected to contain: \("Class")")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 6)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testPythonClassWithMethodsProcessDetail() throws {
        // Intel: extract nested structure from Python class with methods
        let configObj = try extractionConfigFromJson("{\"language\":\"python\"}")
        let result = try process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Class"), "expected to contain: \("Class")")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 6)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testPythonErrorDiagnostics() throws {
        // Intel: Python code with syntax errors should report diagnostics
        let configObj = try extractionConfigFromJson("{\"diagnostics\":true,\"language\":\"python\"}")
        let result = try process("def broken(\n    pass\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertEqual(result.metrics().error_count(), 1)
        XCTAssertGreaterThan(result.diagnostics().len(), 0, "expected non-empty value")
    }

    func testPythonFunctionProcess() throws {
        // Intel: extract structure from Python function definition
        let configObj = try extractionConfigFromJson("{\"language\":\"python\"}")
        let result = try process("def greet(name):\n    return f'Hello, {name}!'\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 2)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testPythonFunctionProcessDetail() throws {
        // Intel: extract structure from Python function definition
        let configObj = try extractionConfigFromJson("{\"language\":\"python\"}")
        let result = try process("def greet(name):\n    return f'Hello, {name}!'\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 2)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testPythonMalformedCodeProcess() throws {
        // Intel: detect diagnostics in malformed Python code
        let configObj = try extractionConfigFromJson("{\"diagnostics\":true,\"language\":\"python\"}")
        let result = try process("def broken(\n    return\nclass", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThan(result.diagnostics().len(), 0, "expected non-empty value")
    }

    func testPythonMalformedCodeProcessDetail() throws {
        // Intel: detect diagnostics in malformed Python code
        let configObj = try extractionConfigFromJson("{\"diagnostics\":true,\"language\":\"python\"}")
        let result = try process("def broken(\n    return\nclass", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThan(result.diagnostics().len(), 0, "expected non-empty value")
    }

    func testPythonMultiImportProcess() throws {
        // Intel: detect multiple Python imports
        let configObj = try extractionConfigFromJson("{\"language\":\"python\"}")
        let result = try process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertGreaterThanOrEqual(result.imports().count, 3)
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 5)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testPythonMultiImportProcessDetail() throws {
        // Intel: detect multiple Python imports
        let configObj = try extractionConfigFromJson("{\"language\":\"python\"}")
        let result = try process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertGreaterThanOrEqual(result.imports().count, 3)
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 5)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testRubyClassProcess() throws {
        // Intel: Ruby class with method
        let configObj = try extractionConfigFromJson("{\"language\":\"ruby\"}")
        let result = try process("require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello #{name}\"\n  end\nend\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "ruby")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Class"), "expected to contain: \("Class")")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 7)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testRustChunkingProcess() throws {
        // Intel: chunk multi-function Rust source into pieces
        let configObj = try extractionConfigFromJson("{\"chunk_max_size\":30,\"language\":\"rust\"}")
        let result = try process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "rust")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 7)
        XCTAssertGreaterThanOrEqual(chunks.count, 2)
    }

    func testRustChunkingProcessDetail() throws {
        // Intel: chunk multi-function Rust source into pieces
        let configObj = try extractionConfigFromJson("{\"chunk_max_size\":30,\"language\":\"rust\"}")
        let result = try process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "rust")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 7)
        XCTAssertGreaterThanOrEqual(chunks.count, 2)
    }

    func testRustFunctionProcess() throws {
        // Intel: extract structure from Rust function definition
        let configObj = try extractionConfigFromJson("{\"language\":\"rust\"}")
        let result = try process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "rust")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 3)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testRustFunctionProcessDetail() throws {
        // Intel: extract structure from Rust function definition
        let configObj = try extractionConfigFromJson("{\"language\":\"rust\"}")
        let result = try process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "rust")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 3)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testTypescriptFunctionProcess() throws {
        // Intel: extract structure from TypeScript function
        let configObj = try extractionConfigFromJson("{\"language\":\"typescript\"}")
        let result = try process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "typescript")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 5)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

    func testTypescriptFunctionProcessDetail() throws {
        // Intel: extract structure from TypeScript function
        let configObj = try extractionConfigFromJson("{\"language\":\"typescript\"}")
        let result = try process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", configObj)
        XCTAssertEqual(result.language().toString().trimmingCharacters(in: CharacterSet.whitespaces), "typescript")
        XCTAssertGreaterThanOrEqual(result.structure().count, 1)
        XCTAssertTrue((result.structure()?.map { $0.as_str().toString() } ?? []).contains("Function"), "expected to contain: \("Function")")
        XCTAssertGreaterThanOrEqual(result.imports().count, 1)
        XCTAssertGreaterThanOrEqual(result.metrics().total_lines(), 5)
        XCTAssertEqual(result.metrics().error_count(), 0)
    }

}
