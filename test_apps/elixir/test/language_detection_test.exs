# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:10aa1e9a92cfbf4b4c6ed24b19b15a5dd32dbc5c78fee9598c936dbe07ceb7e8
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: language-detection
defmodule E2e.LanguageDetectionTest do
  use ExUnit.Case, async: false

  describe "detect_content_bash_shebang" do
    test "detect_content_bash_shebang" do
      result = TreeSitterLanguagePack.detect_language_from_content("\#!/bin/bash\necho hi")
      assert String.trim(result) == "bash"
    end
  end

  describe "detect_content_no_shebang" do
    test "detect_content_no_shebang" do
      result = TreeSitterLanguagePack.detect_language_from_content("no shebang here")
      assert is_nil(result) or String.trim(result) == ""
    end
  end

  describe "detect_content_python_shebang" do
    test "detect_content_python_shebang" do
      result = TreeSitterLanguagePack.detect_language_from_content("\#!/usr/bin/env python3\npass")
      assert String.trim(result) == "python"
    end
  end

  describe "detect_ext_cpp" do
    test "detect_ext_cpp" do
      result = TreeSitterLanguagePack.detect_language_from_extension("cpp")
      assert String.trim(result) == "cpp"
    end
  end

  describe "detect_ext_gherkin" do
    test "detect_ext_gherkin" do
      result = TreeSitterLanguagePack.detect_language_from_extension("feature")
      assert String.trim(result) == "gherkin"
    end
  end

  describe "detect_ext_go" do
    test "detect_ext_go" do
      result = TreeSitterLanguagePack.detect_language_from_extension("go")
      assert String.trim(result) == "go"
    end
  end

  describe "detect_ext_java" do
    test "detect_ext_java" do
      result = TreeSitterLanguagePack.detect_language_from_extension("java")
      assert String.trim(result) == "java"
    end
  end

  describe "detect_ext_javascript" do
    test "detect_ext_javascript" do
      result = TreeSitterLanguagePack.detect_language_from_extension("js")
      assert String.trim(result) == "javascript"
    end
  end

  describe "detect_ext_php" do
    test "detect_ext_php" do
      result = TreeSitterLanguagePack.detect_language_from_extension("php")
      assert String.trim(result) == "php"
    end
  end

  describe "detect_ext_python" do
    test "detect_ext_python" do
      result = TreeSitterLanguagePack.detect_language_from_extension("py")
      assert String.trim(result) == "python"
    end
  end

  describe "detect_ext_ruby" do
    test "detect_ext_ruby" do
      result = TreeSitterLanguagePack.detect_language_from_extension("rb")
      assert String.trim(result) == "ruby"
    end
  end

  describe "detect_ext_rust" do
    test "detect_ext_rust" do
      result = TreeSitterLanguagePack.detect_language_from_extension("rs")
      assert String.trim(result) == "rust"
    end
  end

  describe "detect_ext_typescript" do
    test "detect_ext_typescript" do
      result = TreeSitterLanguagePack.detect_language_from_extension("ts")
      assert String.trim(result) == "typescript"
    end
  end

  describe "detect_ext_unknown" do
    test "detect_ext_unknown" do
      result = TreeSitterLanguagePack.detect_language_from_extension("xyz")
      assert is_nil(result) or String.trim(result) == ""
    end
  end

  describe "detect_path_dotfile" do
    test "detect_path_dotfile" do
      result = TreeSitterLanguagePack.detect_language_from_path(".gitignore")
      assert is_nil(result) or String.trim(result) == ""
    end
  end

  describe "detect_path_go_nested" do
    test "detect_path_go_nested" do
      result = TreeSitterLanguagePack.detect_language_from_path("lib/server.go")
      assert String.trim(result) == "go"
    end
  end

  describe "detect_path_java_root" do
    test "detect_path_java_root" do
      result = TreeSitterLanguagePack.detect_language_from_path("Main.java")
      assert String.trim(result) == "java"
    end
  end

  describe "detect_path_js_root" do
    test "detect_path_js_root" do
      result = TreeSitterLanguagePack.detect_language_from_path("app.js")
      assert String.trim(result) == "javascript"
    end
  end

  describe "detect_path_nested" do
    test "detect_path_nested" do
      result = TreeSitterLanguagePack.detect_language_from_path("src/main.py")
      assert String.trim(result) == "python"
    end
  end

  describe "detect_path_no_extension" do
    test "detect_path_no_extension" do
      result = TreeSitterLanguagePack.detect_language_from_path("Makefile")
      assert is_nil(result) or String.trim(result) == ""
    end
  end

  describe "detect_path_rust_src" do
    test "detect_path_rust_src" do
      result = TreeSitterLanguagePack.detect_language_from_path("src/main.rs")
      assert String.trim(result) == "rust"
    end
  end
end
