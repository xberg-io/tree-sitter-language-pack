# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:d48e9c9b5cf56b6759dd9b5a66cf827f4231d5fcdd44b842506e5f50fea4df34
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: process
defmodule E2e.ProcessTest do
  use ExUnit.Case, async: false

  defp alef_e2e_item_texts(item) when is_binary(item), do: [item]
  defp alef_e2e_item_texts(item) do
    [:kind, :name, :signature, :path, :alias, :text, :source]
    |> Enum.filter(&Map.has_key?(item, &1))
    |> Enum.flat_map(fn attr ->
      case Map.get(item, attr) do
        nil -> []
        atom when is_atom(atom) -> [atom |> to_string() |> String.capitalize()]
        str -> [to_string(str)]
      end
    end)
  end

  describe "c_function_process" do
    test "c_function_process" do
      {:ok, result} = TreeSitterLanguagePack.process("\#include <stdio.h>\n\nint main() {\n    printf(\"hello\");\n    return 0;\n}\n", "{\"language\":\"c\"}")
      assert String.trim(result.language) == "c"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert result.metrics.total_lines >= 6
      assert result.metrics.error_count == 0
    end
  end

  describe "config_all_python" do
    test "config_all_python" do
      {:ok, result} = TreeSitterLanguagePack.process("\# A comment\ndef greet(name):\n    \"\"\"Say hello.\"\"\"\n    return f'Hi {name}'\n\nimport os\n", "{\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert length(result.imports) >= 1
      assert result.metrics.total_lines >= 6
      assert result.metrics.error_count == 0
    end
  end

  describe "config_minimal_python" do
    test "config_minimal_python" do
      {:ok, result} = TreeSitterLanguagePack.process("def hello():\n    pass\n", "{\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert result.metrics.total_lines >= 2
    end
  end

  describe "go_function_process" do
    test "go_function_process" do
      {:ok, result} = TreeSitterLanguagePack.process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", "{\"language\":\"go\"}")
      assert String.trim(result.language) == "go"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert length(result.imports) >= 1
      assert result.metrics.total_lines >= 7
      assert result.metrics.error_count == 0
    end
  end

  describe "go_function_process_detail" do
    test "go_function_process_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", "{\"language\":\"go\"}")
      assert String.trim(result.language) == "go"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert length(result.imports) >= 1
      assert result.metrics.total_lines >= 7
      assert result.metrics.error_count == 0
    end
  end

  describe "java_class_process" do
    test "java_class_process" do
      {:ok, result} = TreeSitterLanguagePack.process("import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return \"Hello \" + name;\n    }\n}\n", "{\"language\":\"java\"}")
      assert String.trim(result.language) == "java"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Class")) end)
      assert length(result.imports) >= 1
      assert result.metrics.total_lines >= 7
      assert result.metrics.error_count == 0
    end
  end

  describe "java_package_declaration_process" do
    test "java_package_declaration_process" do
      {:ok, result} = TreeSitterLanguagePack.process("package com.example.widget;\n\npublic class Widget {\n    public String name() { return \"w\"; }\n}\n", "{\"language\":\"java\"}")
      assert String.trim(result.language) == "java"
      assert length(result.structure) >= 2
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Module")) end)
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Class")) end)
      assert result.metrics.error_count == 0
    end
  end

  describe "javascript_multi_import_process" do
    test "javascript_multi_import_process" do
      {:ok, result} = TreeSitterLanguagePack.process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", "{\"language\":\"javascript\"}")
      assert String.trim(result.language) == "javascript"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert length(result.imports) >= 2
      assert result.metrics.total_lines >= 6
      assert result.metrics.error_count == 0
    end
  end

  describe "javascript_multi_import_process_detail" do
    test "javascript_multi_import_process_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", "{\"language\":\"javascript\"}")
      assert String.trim(result.language) == "javascript"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert length(result.imports) >= 2
      assert result.metrics.total_lines >= 6
      assert result.metrics.error_count == 0
    end
  end

  describe "kotlin_package_class_process" do
    test "kotlin_package_class_process" do
      {:ok, result} = TreeSitterLanguagePack.process("package foo.bar\n\nclass Widget {\n    fun greet(): String = \"hi\"\n}\n", "{\"language\":\"kotlin\"}")
      assert String.trim(result.language) == "kotlin"
      assert length(result.structure) >= 2
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Module")) end)
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Class")) end)
      assert result.metrics.error_count == 0
    end
  end

  describe "process_javascript_exports_count" do
    test "process_javascript_exports_count" do
      {:ok, result} = TreeSitterLanguagePack.process("export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", "{\"language\":\"javascript\"}")
      assert String.trim(result.language) == "javascript"
      assert length(result.exports) >= 2
    end
  end

  describe "process_javascript_exports_detail" do
    test "process_javascript_exports_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n", "{\"language\":\"javascript\"}")
      assert String.trim(result.language) == "javascript"
      assert length(result.exports) >= 1
    end
  end

  describe "process_python_all_features" do
    test "process_python_all_features" do
      {:ok, result} = TreeSitterLanguagePack.process("import os\nfrom pathlib import Path\n\n\# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", "{\"comments\":true,\"docstrings\":true,\"imports\":true,\"language\":\"python\",\"structure\":true,\"symbols\":true}")
      assert String.trim(result.language) == "python"
      assert length(result.structure) >= 2
      assert length(result.imports) >= 1
      assert length(result.comments) >= 1
      assert result.metrics.total_lines >= 10
    end
  end

  describe "process_python_comments" do
    test "process_python_comments" do
      {:ok, result} = TreeSitterLanguagePack.process("\# This is a comment\n\# Another comment\ndef hello():\n    \# inline comment\n    pass\n", "{\"comments\":true,\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert length(result.comments) >= 1
    end
  end

  describe "process_python_docstrings" do
    test "process_python_docstrings" do
      {:ok, result} = TreeSitterLanguagePack.process("def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", "{\"docstrings\":true,\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert result.metrics.total_lines >= 3
    end
  end

  describe "process_python_imports_detail" do
    test "process_python_imports_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", "{\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert length(result.imports) >= 2
      assert Enum.any?(result.imports, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "os")) end)
    end
  end

  describe "process_python_metrics_detail" do
    test "process_python_metrics_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("\# module docstring\nimport os\n\ndef hello():\n    \# greeting\n    print('hello')\n\ndef world():\n    print('world')\n", "{\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert result.metrics.code_lines >= 4
      assert result.metrics.comment_lines >= 1
      assert result.metrics.max_depth >= 1
    end
  end

  describe "process_python_symbols" do
    test "process_python_symbols" do
      {:ok, result} = TreeSitterLanguagePack.process("MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n", "{\"language\":\"python\",\"symbols\":true}")
      assert String.trim(result.language) == "python"
      assert length(result.symbols) >= 1
    end
  end

  describe "process_rust_structure_name" do
    test "process_rust_structure_name" do
      {:ok, result} = TreeSitterLanguagePack.process("pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", "{\"language\":\"rust\"}")
      assert String.trim(result.language) == "rust"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "MyConfig")) end)
    end
  end

  describe "python_chunking_medium" do
    test "python_chunking_medium" do
      {:ok, result} = TreeSitterLanguagePack.process("def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n", "{\"chunk_max_size\":50,\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert length(result.structure) >= 3
      assert result.metrics.error_count == 0
      assert length(result.chunks) >= 2
    end
  end

  describe "python_chunking_process_detail" do
    test "python_chunking_process_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n", "{\"chunk_max_size\":30,\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert result.metrics.total_lines >= 8
      assert length(result.chunks) >= 2
    end
  end

  describe "python_class_with_methods_process" do
    test "python_class_with_methods_process" do
      {:ok, result} = TreeSitterLanguagePack.process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", "{\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Class")) end)
      assert result.metrics.total_lines >= 6
      assert result.metrics.error_count == 0
    end
  end

  describe "python_class_with_methods_process_detail" do
    test "python_class_with_methods_process_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", "{\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Class")) end)
      assert result.metrics.total_lines >= 6
      assert result.metrics.error_count == 0
    end
  end

  describe "python_error_diagnostics" do
    test "python_error_diagnostics" do
      {:ok, result} = TreeSitterLanguagePack.process("def broken(\n    pass\n", "{\"diagnostics\":true,\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert result.metrics.error_count == 1
      assert result.diagnostics != ""
    end
  end

  describe "python_function_process" do
    test "python_function_process" do
      {:ok, result} = TreeSitterLanguagePack.process("def greet(name):\n    return f'Hello, {name}!'\n", "{\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert result.metrics.total_lines >= 2
      assert result.metrics.error_count == 0
    end
  end

  describe "python_function_process_detail" do
    test "python_function_process_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("def greet(name):\n    return f'Hello, {name}!'\n", "{\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert result.metrics.total_lines >= 2
      assert result.metrics.error_count == 0
    end
  end

  describe "python_malformed_code_process" do
    test "python_malformed_code_process" do
      {:ok, result} = TreeSitterLanguagePack.process("def broken(\n    return\nclass", "{\"diagnostics\":true,\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert result.diagnostics != ""
    end
  end

  describe "python_malformed_code_process_detail" do
    test "python_malformed_code_process_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("def broken(\n    return\nclass", "{\"diagnostics\":true,\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert result.diagnostics != ""
    end
  end

  describe "python_multi_import_process" do
    test "python_multi_import_process" do
      {:ok, result} = TreeSitterLanguagePack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", "{\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert length(result.structure) >= 1
      assert length(result.imports) >= 3
      assert result.metrics.total_lines >= 5
      assert result.metrics.error_count == 0
    end
  end

  describe "python_multi_import_process_detail" do
    test "python_multi_import_process_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", "{\"language\":\"python\"}")
      assert String.trim(result.language) == "python"
      assert length(result.structure) >= 1
      assert length(result.imports) >= 3
      assert result.metrics.total_lines >= 5
      assert result.metrics.error_count == 0
    end
  end

  describe "ruby_class_process" do
    test "ruby_class_process" do
      {:ok, result} = TreeSitterLanguagePack.process("require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello \#{name}\"\n  end\nend\n", "{\"language\":\"ruby\"}")
      assert String.trim(result.language) == "ruby"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Class")) end)
      assert result.metrics.total_lines >= 7
      assert result.metrics.error_count == 0
    end
  end

  describe "rust_chunking_process" do
    test "rust_chunking_process" do
      {:ok, result} = TreeSitterLanguagePack.process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", "{\"chunk_max_size\":30,\"language\":\"rust\"}")
      assert String.trim(result.language) == "rust"
      assert result.metrics.total_lines >= 7
      assert length(result.chunks) >= 2
    end
  end

  describe "rust_chunking_process_detail" do
    test "rust_chunking_process_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", "{\"chunk_max_size\":30,\"language\":\"rust\"}")
      assert String.trim(result.language) == "rust"
      assert result.metrics.total_lines >= 7
      assert length(result.chunks) >= 2
    end
  end

  describe "rust_function_process" do
    test "rust_function_process" do
      {:ok, result} = TreeSitterLanguagePack.process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", "{\"language\":\"rust\"}")
      assert String.trim(result.language) == "rust"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert result.metrics.total_lines >= 3
      assert result.metrics.error_count == 0
    end
  end

  describe "rust_function_process_detail" do
    test "rust_function_process_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", "{\"language\":\"rust\"}")
      assert String.trim(result.language) == "rust"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert result.metrics.total_lines >= 3
      assert result.metrics.error_count == 0
    end
  end

  describe "typescript_function_process" do
    test "typescript_function_process" do
      {:ok, result} = TreeSitterLanguagePack.process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", "{\"language\":\"typescript\"}")
      assert String.trim(result.language) == "typescript"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert length(result.imports) >= 1
      assert result.metrics.total_lines >= 5
      assert result.metrics.error_count == 0
    end
  end

  describe "typescript_function_process_detail" do
    test "typescript_function_process_detail" do
      {:ok, result} = TreeSitterLanguagePack.process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", "{\"language\":\"typescript\"}")
      assert String.trim(result.language) == "typescript"
      assert length(result.structure) >= 1
      assert Enum.any?(result.structure, fn item -> Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Function")) end)
      assert length(result.imports) >= 1
      assert result.metrics.total_lines >= 5
      assert result.metrics.error_count == 0
    end
  end
end
