// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e4ed6c34f1ccf13edcf2a95326b6a48f660b9502efea160580280da409d1761a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.tslp.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.tslp.android.TreeSitterLanguagePack
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import dev.kreuzberg.tslp.android.ProcessConfig

/** E2e tests for category: parsing. */
class ParsingTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testParsingGoFunction() = runBlocking {
        // Parse a Go function declaration and assert node type
        val config = MAPPER.readValue("{\"language\":\"go\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("package main\nfunc main() {}", config)
    }

    @Test
    fun testParsingHtmlElement() = runBlocking {
        // Parse an HTML element and assert node type
        val config = MAPPER.readValue("{\"language\":\"html\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<div>hello</div>", config)
    }

    @Test
    fun testParsingJavascriptClass() = runBlocking {
        // Parse a JavaScript class declaration.
        val config = MAPPER.readValue("{\"language\":\"javascript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("class Foo { bar() {} }", config)
    }

    @Test
    fun testParsingJavascriptVariable() = runBlocking {
        // Parse a JavaScript variable declaration and assert node type
        val config = MAPPER.readValue("{\"language\":\"javascript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("const x = 1;", config)
    }

    @Test
    fun testParsingPythonFunction() = runBlocking {
        // Parse a Python function definition and assert node type
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def hello(): pass", config)
    }

    @Test
    fun testParsingRustFunction() = runBlocking {
        // Parse a Rust function definition and assert node type
        val config = MAPPER.readValue("{\"language\":\"rust\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fn main() {}", config)
    }

    @Test
    fun testParsingRustStruct() = runBlocking {
        // Parse a Rust struct definition.
        val config = MAPPER.readValue("{\"language\":\"rust\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("struct Point { x: f64, y: f64 }", config)
    }

    @Test
    fun testParsingTypescriptFunction() = runBlocking {
        // Parse a TypeScript function with type annotations and template literal return
        val config = MAPPER.readValue("{\"language\":\"typescript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("function greet(name: string): string { return `hi \${name}`; }", config)
    }

}
