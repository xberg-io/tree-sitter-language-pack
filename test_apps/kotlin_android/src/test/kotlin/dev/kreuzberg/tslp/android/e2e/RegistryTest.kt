// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cbfce82405886ba16adac06e72a988e1089331253ea6c38cea8c42f34fc3987b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.tslp.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.tslp.android.TreeSitterLanguagePack

/** E2e tests for category: registry. */
class RegistryTest {

    companion object {
        init {
            try {
                System.loadLibrary("tree-sitter-language-pack_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load tree-sitter-language-pack_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
    }
    @Test
    fun testGetLanguagePython() = runBlocking {
        // get_language('python') returns a valid Language object
        val language = TreeSitterLanguagePack.getLanguage("python")
    }

    @Test
    fun testGetLanguageUnknown() = runBlocking {
        // get_language('nonexistent_xyz') returns an error for unknown language
        assertFailsWith<Exception> {
            TreeSitterLanguagePack.getLanguage("nonexistent_xyz")
        }
        Unit
    }

    @Test
    fun testGetParserPython() = runBlocking {
        // get_parser('python') returns a valid Parser object
        val parser = TreeSitterLanguagePack.getParser("python")
    }

    @Test
    fun testGetParserUnknown() = runBlocking {
        // get_parser('nonexistent_xyz') returns an error for unknown language
        assertFailsWith<Exception> {
            TreeSitterLanguagePack.getParser("nonexistent_xyz")
        }
        Unit
    }

    @Test
    fun testRegistryGetLanguageAlias() = runBlocking {
        // get_language('shell') resolves the alias to 'bash' and returns ok
        val language = TreeSitterLanguagePack.getLanguage("shell")
    }

    @Test
    fun testRegistryGetParserAlias() = runBlocking {
        // get_parser('shell') resolves the alias and returns a Parser
        val parser = TreeSitterLanguagePack.getParser("shell")
    }

    @Test
    fun testRegistryHasLanguageAlias() = runBlocking {
        // has_language('shell') returns true via alias resolution to 'bash'
        val result = TreeSitterLanguagePack.hasLanguage("shell")
        assertTrue(result, "expected true")
    }

    @Test
    fun testRegistryHasLanguageFalse() = runBlocking {
        // has_language('nonexistent') should return false
        val result = TreeSitterLanguagePack.hasLanguage("nonexistent")
        assertFalse(result, "expected false")
    }

    @Test
    fun testRegistryHasLanguageTrue() = runBlocking {
        // has_language('python') should return true
        val result = TreeSitterLanguagePack.hasLanguage("python")
        assertTrue(result, "expected true")
    }

    @Test
    fun testRegistryLanguageCount() = runBlocking {
        // language_count returns the number of languages available in the current build/cache
        val result = TreeSitterLanguagePack.languageCount()
        assertTrue(result >= 1, "expected >= 1")
    }

    @Test
    fun testRegistryListLanguages() = runBlocking {
        // available_languages should return a non-empty list
        val result = TreeSitterLanguagePack.availableLanguages()
        assertFalse(result.isEmpty(), "expected non-empty value")
    }

}
