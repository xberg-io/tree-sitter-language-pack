// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fb5d0b72d0cb65a5a7b8616e1d95bbae33bdfd375274e856b30a329132b8cf87
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.tslp.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.tslp.android.TreeSitterLanguagePack
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import dev.kreuzberg.tslp.android.ProcessConfig

/** E2e tests for category: process. */
class ProcessTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testCFunctionProcess() = runBlocking {
        // Intel: C function with include
        val config = MAPPER.readValue("{\"language\":\"c\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("#include <stdio.h>\n\nint main() {\n    printf(\"hello\");\n    return 0;\n}\n", config)
        assertEquals("c", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.metrics.totalLines >= 6, "expected >= 6")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testConfigAllPython() = runBlocking {
        // Intel: process with all features enabled
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("# A comment\ndef greet(name):\n    \"\"\"Say hello.\"\"\"\n    return f'Hi {name}'\n\nimport os\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.imports.size >= 1, "expected at least 1 elements")
        assertTrue(result.metrics.totalLines >= 6, "expected >= 6")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testConfigMinimalPython() = runBlocking {
        // Intel: process with minimal config - only metrics
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def hello():\n    pass\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.metrics.totalLines >= 2, "expected >= 2")
    }

    @Test
    fun testGoFunctionProcess() = runBlocking {
        // Intel: extract structure from Go function definition
        val config = MAPPER.readValue("{\"language\":\"go\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", config)
        assertEquals("go", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.imports.size >= 1, "expected at least 1 elements")
        assertTrue(result.metrics.totalLines >= 7, "expected >= 7")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testGoFunctionProcessDetail() = runBlocking {
        // Intel: extract structure from Go function definition
        val config = MAPPER.readValue("{\"language\":\"go\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", config)
        assertEquals("go", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.imports.size >= 1, "expected at least 1 elements")
        assertTrue(result.metrics.totalLines >= 7, "expected >= 7")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testJavaClassProcess() = runBlocking {
        // Intel: Java class with methods and imports
        val config = MAPPER.readValue("{\"language\":\"java\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return \"Hello \" + name;\n    }\n}\n", config)
        assertEquals("java", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Class"), "expected to contain: " + "Class")
        assertTrue(result.imports.size >= 1, "expected at least 1 elements")
        assertTrue(result.metrics.totalLines >= 7, "expected >= 7")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testJavaPackageDeclarationProcess() = runBlocking {
        // Intel: Java package declaration emitted as Module so callers can build FQNs (#112)
        val config = MAPPER.readValue("{\"language\":\"java\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("package com.example.widget;\n\npublic class Widget {\n    public String name() { return \"w\"; }\n}\n", config)
        assertEquals("java", result.language.trim())
        assertTrue(result.structure.size >= 2, "expected at least 2 elements")
        assertTrue((result.structure as List<String>).contains("Module"), "expected to contain: " + "Module")
        assertTrue((result.structure as List<String>).contains("Class"), "expected to contain: " + "Class")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testJavascriptMultiImportProcess() = runBlocking {
        // Intel: detect multiple imports and function in JavaScript
        val config = MAPPER.readValue("{\"language\":\"javascript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", config)
        assertEquals("javascript", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.imports.size >= 2, "expected at least 2 elements")
        assertTrue(result.metrics.totalLines >= 6, "expected >= 6")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testJavascriptMultiImportProcessDetail() = runBlocking {
        // Intel: detect multiple imports and function in JavaScript
        val config = MAPPER.readValue("{\"language\":\"javascript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", config)
        assertEquals("javascript", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.imports.size >= 2, "expected at least 2 elements")
        assertTrue(result.metrics.totalLines >= 6, "expected >= 6")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testKotlinPackageClassProcess() = runBlocking {
        // Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112)
        val config = MAPPER.readValue("{\"language\":\"kotlin\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("package foo.bar\n\nclass Widget {\n    fun greet(): String = \"hi\"\n}\n", config)
        assertEquals("kotlin", result.language.trim())
        assertTrue(result.structure.size >= 2, "expected at least 2 elements")
        assertTrue((result.structure as List<String>).contains("Module"), "expected to contain: " + "Module")
        assertTrue((result.structure as List<String>).contains("Class"), "expected to contain: " + "Class")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testProcessJavascriptExportsCount() = runBlocking {
        // JavaScript with multiple exports, verify export count
        val config = MAPPER.readValue("{\"language\":\"javascript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", config)
        assertEquals("javascript", result.language.trim())
        assertTrue(result.exports.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testProcessJavascriptExportsDetail() = runBlocking {
        // JavaScript with exports, verify export count
        val config = MAPPER.readValue("{\"language\":\"javascript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("export function greet(name) {\n  return `Hello \${name}`;\n}\n\nexport const VERSION = '1.0';\n", config)
        assertEquals("javascript", result.language.trim())
        assertTrue(result.exports.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testProcessPythonAllFeatures() = runBlocking {
        // Python comprehensive source with all feature extraction enabled
        val config = MAPPER.readValue("{\"comments\":true,\"docstrings\":true,\"imports\":true,\"language\":\"python\",\"structure\":true,\"symbols\":true}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.structure.size >= 2, "expected at least 2 elements")
        assertTrue(result.imports.size >= 1, "expected at least 1 elements")
        assertTrue(result.comments.size >= 1, "expected at least 1 elements")
        assertTrue(result.metrics.totalLines >= 10, "expected >= 10")
    }

    @Test
    fun testProcessPythonComments() = runBlocking {
        // Python with comments, verify comment count
        val config = MAPPER.readValue("{\"comments\":true,\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.comments.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testProcessPythonDocstrings() = runBlocking {
        // Python with function docstring, verify docstring count
        val config = MAPPER.readValue("{\"docstrings\":true,\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.metrics.totalLines >= 3, "expected >= 3")
    }

    @Test
    fun testProcessPythonImportsDetail() = runBlocking {
        // Python with multiple imports, verify imports contain specific source
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.imports.size >= 2, "expected at least 2 elements")
        assertTrue((result.imports as List<String>).contains("os"), "expected to contain: " + "os")
    }

    @Test
    fun testProcessPythonMetricsDetail() = runBlocking {
        // Python code with metrics assertions
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.metrics.codeLines >= 4, "expected >= 4")
        assertTrue(result.metrics.commentLines >= 1, "expected >= 1")
        assertTrue(result.metrics.maxDepth >= 1, "expected >= 1")
    }

    @Test
    fun testProcessPythonSymbols() = runBlocking {
        // Python with class and functions, verify symbol count
        val config = MAPPER.readValue("{\"language\":\"python\",\"symbols\":true}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.symbols.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testProcessRustStructureName() = runBlocking {
        // Rust struct with name, verify structure name contains value
        val config = MAPPER.readValue("{\"language\":\"rust\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", config)
        assertEquals("rust", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("MyConfig"), "expected to contain: " + "MyConfig")
    }

    @Test
    fun testPythonChunkingMedium() = runBlocking {
        // Intel: Python code with medium chunk size
        val config = MAPPER.readValue("{\"chunk_max_size\":50,\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.structure.size >= 3, "expected at least 3 elements")
        assertEquals(0, result.metrics.errorCount)
        assertTrue(result.chunks.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testPythonChunkingProcessDetail() = runBlocking {
        // Intel: chunk multi-function Python source into multiple pieces
        val config = MAPPER.readValue("{\"chunk_max_size\":30,\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.metrics.totalLines >= 8, "expected >= 8")
        assertTrue(result.chunks.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testPythonClassWithMethodsProcess() = runBlocking {
        // Intel: extract nested structure from Python class with methods
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Class"), "expected to contain: " + "Class")
        assertTrue(result.metrics.totalLines >= 6, "expected >= 6")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testPythonClassWithMethodsProcessDetail() = runBlocking {
        // Intel: extract nested structure from Python class with methods
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Class"), "expected to contain: " + "Class")
        assertTrue(result.metrics.totalLines >= 6, "expected >= 6")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testPythonErrorDiagnostics() = runBlocking {
        // Intel: Python code with syntax errors should report diagnostics
        val config = MAPPER.readValue("{\"diagnostics\":true,\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def broken(\n    pass\n", config)
        assertEquals("python", result.language.trim())
        assertEquals(1, result.metrics.errorCount)
        assertFalse(result.diagnostics.isEmpty(), "expected non-empty value")
    }

    @Test
    fun testPythonFunctionProcess() = runBlocking {
        // Intel: extract structure from Python function definition
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def greet(name):\n    return f'Hello, {name}!'\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.metrics.totalLines >= 2, "expected >= 2")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testPythonFunctionProcessDetail() = runBlocking {
        // Intel: extract structure from Python function definition
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def greet(name):\n    return f'Hello, {name}!'\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.metrics.totalLines >= 2, "expected >= 2")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testPythonMalformedCodeProcess() = runBlocking {
        // Intel: detect diagnostics in malformed Python code
        val config = MAPPER.readValue("{\"diagnostics\":true,\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def broken(\n    return\nclass", config)
        assertEquals("python", result.language.trim())
        assertFalse(result.diagnostics.isEmpty(), "expected non-empty value")
    }

    @Test
    fun testPythonMalformedCodeProcessDetail() = runBlocking {
        // Intel: detect diagnostics in malformed Python code
        val config = MAPPER.readValue("{\"diagnostics\":true,\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def broken(\n    return\nclass", config)
        assertEquals("python", result.language.trim())
        assertFalse(result.diagnostics.isEmpty(), "expected non-empty value")
    }

    @Test
    fun testPythonMultiImportProcess() = runBlocking {
        // Intel: detect multiple Python imports
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue(result.imports.size >= 3, "expected at least 3 elements")
        assertTrue(result.metrics.totalLines >= 5, "expected >= 5")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testPythonMultiImportProcessDetail() = runBlocking {
        // Intel: detect multiple Python imports
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", config)
        assertEquals("python", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue(result.imports.size >= 3, "expected at least 3 elements")
        assertTrue(result.metrics.totalLines >= 5, "expected >= 5")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testRubyClassProcess() = runBlocking {
        // Intel: Ruby class with method
        val config = MAPPER.readValue("{\"language\":\"ruby\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello #{name}\"\n  end\nend\n", config)
        assertEquals("ruby", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Class"), "expected to contain: " + "Class")
        assertTrue(result.metrics.totalLines >= 7, "expected >= 7")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testRustChunkingProcess() = runBlocking {
        // Intel: chunk multi-function Rust source into pieces
        val config = MAPPER.readValue("{\"chunk_max_size\":30,\"language\":\"rust\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", config)
        assertEquals("rust", result.language.trim())
        assertTrue(result.metrics.totalLines >= 7, "expected >= 7")
        assertTrue(result.chunks.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testRustChunkingProcessDetail() = runBlocking {
        // Intel: chunk multi-function Rust source into pieces
        val config = MAPPER.readValue("{\"chunk_max_size\":30,\"language\":\"rust\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", config)
        assertEquals("rust", result.language.trim())
        assertTrue(result.metrics.totalLines >= 7, "expected >= 7")
        assertTrue(result.chunks.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testRustFunctionProcess() = runBlocking {
        // Intel: extract structure from Rust function definition
        val config = MAPPER.readValue("{\"language\":\"rust\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", config)
        assertEquals("rust", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.metrics.totalLines >= 3, "expected >= 3")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testRustFunctionProcessDetail() = runBlocking {
        // Intel: extract structure from Rust function definition
        val config = MAPPER.readValue("{\"language\":\"rust\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", config)
        assertEquals("rust", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.metrics.totalLines >= 3, "expected >= 3")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testTypescriptFunctionProcess() = runBlocking {
        // Intel: extract structure from TypeScript function
        val config = MAPPER.readValue("{\"language\":\"typescript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, \${name}!`;\n}\n", config)
        assertEquals("typescript", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.imports.size >= 1, "expected at least 1 elements")
        assertTrue(result.metrics.totalLines >= 5, "expected >= 5")
        assertEquals(0, result.metrics.errorCount)
    }

    @Test
    fun testTypescriptFunctionProcessDetail() = runBlocking {
        // Intel: extract structure from TypeScript function
        val config = MAPPER.readValue("{\"language\":\"typescript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, \${name}!`;\n}\n", config)
        assertEquals("typescript", result.language.trim())
        assertTrue(result.structure.size >= 1, "expected at least 1 elements")
        assertTrue((result.structure as List<String>).contains("Function"), "expected to contain: " + "Function")
        assertTrue(result.imports.size >= 1, "expected at least 1 elements")
        assertTrue(result.metrics.totalLines >= 5, "expected >= 5")
        assertEquals(0, result.metrics.errorCount)
    }

}
