// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ab348ac414a5572696792da7db33006af0140da31c09d7b96c3063867aeaf9a0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { process, parseString } from '@kreuzberg/tree-sitter-language-pack-wasm';

describe('parsing', () => {
  it('parsing_go_function: Parse a Go function declaration and assert node type', () => {
    const sourceBytes = Buffer.from("package main\nfunc main() {}", "utf-8");
    parseString("go", sourceBytes);
  });

  it('parsing_html_element: Parse an HTML element and assert node type', () => {
    const sourceBytes = Buffer.from("<div>hello</div>", "utf-8");
    parseString("html", sourceBytes);
  });

  it('parsing_javascript_class: Parse a JavaScript class declaration.', () => {
    const sourceBytes = Buffer.from("class Foo { bar() {} }", "utf-8");
    parseString("javascript", sourceBytes);
  });

  it('parsing_javascript_variable: Parse a JavaScript variable declaration and assert node type', () => {
    const sourceBytes = Buffer.from("const x = 1;", "utf-8");
    parseString("javascript", sourceBytes);
  });

  it('parsing_python_function: Parse a Python function definition and assert node type', () => {
    const sourceBytes = Buffer.from("def hello(): pass", "utf-8");
    parseString("python", sourceBytes);
  });

  it('parsing_rust_function: Parse a Rust function definition and assert node type', () => {
    const sourceBytes = Buffer.from("fn main() {}", "utf-8");
    parseString("rust", sourceBytes);
  });

  it('parsing_rust_struct: Parse a Rust struct definition.', () => {
    const sourceBytes = Buffer.from("struct Point { x: f64, y: f64 }", "utf-8");
    parseString("rust", sourceBytes);
  });
});
