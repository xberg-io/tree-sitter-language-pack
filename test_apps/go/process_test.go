// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d45b5c782d275675afffd98e94fe4d5fdec1f181fe639106353272e290be62f3
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

// E2e tests for category: process
package e2e_test

import (
	"encoding/json"
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	tspack "github.com/kreuzberg-dev/tree-sitter-language-pack/packages/go"
)

func Test_CFunctionProcess(t *testing.T) {
	// Intel: C function with include
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"c"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`#include <stdio.h>

int main() {
    printf("hello");
    return 0;
}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `c` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	if result.Metrics.TotalLines < 6 {
		t.Errorf("expected >= 6, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_ConfigAllPython(t *testing.T) {
	// Intel: process with all features enabled
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`# A comment
def greet(name):
    """Say hello."""
    return f'Hi {name}'

import os
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	assert.GreaterOrEqual(t, len(result.Imports), 1, "expected at least 1 elements")
	if result.Metrics.TotalLines < 6 {
		t.Errorf("expected >= 6, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_ConfigMinimalPython(t *testing.T) {
	// Intel: process with minimal config - only metrics
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`def hello():
    pass
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	if result.Metrics.TotalLines < 2 {
		t.Errorf("expected >= 2, got %v", result.Metrics.TotalLines)
	}
}

func Test_GoFunctionProcess(t *testing.T) {
	// Intel: extract structure from Go function definition
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"go"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`package main

import "fmt"

func main() {
	fmt.Println("hello")
}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `go` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	assert.GreaterOrEqual(t, len(result.Imports), 1, "expected at least 1 elements")
	if result.Metrics.TotalLines < 7 {
		t.Errorf("expected >= 7, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_GoFunctionProcessDetail(t *testing.T) {
	// Intel: extract structure from Go function definition
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"go"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`package main

import "fmt"

func main() {
	fmt.Println("hello")
}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `go` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	assert.GreaterOrEqual(t, len(result.Imports), 1, "expected at least 1 elements")
	if result.Metrics.TotalLines < 7 {
		t.Errorf("expected >= 7, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_JavaClassProcess(t *testing.T) {
	// Intel: Java class with methods and imports
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"java"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`import java.util.List;

public class Greeter {
    public String greet(String name) {
        return "Hello " + name;
    }
}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `java` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Class`) {
		t.Errorf("expected to contain %s, got %v", `Class`, result.Structure)
	}
	assert.GreaterOrEqual(t, len(result.Imports), 1, "expected at least 1 elements")
	if result.Metrics.TotalLines < 7 {
		t.Errorf("expected >= 7, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_JavaPackageDeclarationProcess(t *testing.T) {
	// Intel: Java package declaration emitted as Module so callers can build FQNs (#112)
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"java"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`package com.example.widget;

public class Widget {
    public String name() { return "w"; }
}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `java` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 2, "expected at least 2 elements")
	if !strings.Contains(jsonString(result.Structure), `Module`) {
		t.Errorf("expected to contain %s, got %v", `Module`, result.Structure)
	}
	if !strings.Contains(jsonString(result.Structure), `Class`) {
		t.Errorf("expected to contain %s, got %v", `Class`, result.Structure)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_JavascriptMultiImportProcess(t *testing.T) {
	// Intel: detect multiple imports and function in JavaScript
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"javascript"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`import fs from 'fs';
import path from 'path';

function process(input) {
    return input.trim();
}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `javascript` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	assert.GreaterOrEqual(t, len(result.Imports), 2, "expected at least 2 elements")
	if result.Metrics.TotalLines < 6 {
		t.Errorf("expected >= 6, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_JavascriptMultiImportProcessDetail(t *testing.T) {
	// Intel: detect multiple imports and function in JavaScript
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"javascript"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`import fs from 'fs';
import path from 'path';

function process(input) {
    return input.trim();
}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `javascript` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	assert.GreaterOrEqual(t, len(result.Imports), 2, "expected at least 2 elements")
	if result.Metrics.TotalLines < 6 {
		t.Errorf("expected >= 6, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_KotlinPackageClassProcess(t *testing.T) {
	// Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112)
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"kotlin"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`package foo.bar

class Widget {
    fun greet(): String = "hi"
}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `kotlin` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 2, "expected at least 2 elements")
	if !strings.Contains(jsonString(result.Structure), `Module`) {
		t.Errorf("expected to contain %s, got %v", `Module`, result.Structure)
	}
	if !strings.Contains(jsonString(result.Structure), `Class`) {
		t.Errorf("expected to contain %s, got %v", `Class`, result.Structure)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_ProcessJavascriptExportsCount(t *testing.T) {
	// JavaScript with multiple exports, verify export count
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"javascript"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`export function greet() { return 'hi'; }
export const VERSION = '1.0';
export default class App {}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `javascript` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Exports), 2, "expected at least 2 elements")
}

func Test_ProcessJavascriptExportsDetail(t *testing.T) {
	// JavaScript with exports, verify export count
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"javascript"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process("export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n", config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `javascript` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Exports), 1, "expected at least 1 elements")
}

func Test_ProcessPythonAllFeatures(t *testing.T) {
	// Python comprehensive source with all feature extraction enabled
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"comments":true,"docstrings":true,"imports":true,"language":"python","structure":true,"symbols":true}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`import os
from pathlib import Path

# Configuration
MY_CONST = 42

def process_file(path):
    """Process a file and return contents."""
    with open(path) as f:
        return f.read()

class FileProcessor:
    def __init__(self, base_dir):
        self.base_dir = base_dir
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 2, "expected at least 2 elements")
	assert.GreaterOrEqual(t, len(result.Imports), 1, "expected at least 1 elements")
	assert.GreaterOrEqual(t, len(result.Comments), 1, "expected at least 1 elements")
	if result.Metrics.TotalLines < 10 {
		t.Errorf("expected >= 10, got %v", result.Metrics.TotalLines)
	}
}

func Test_ProcessPythonComments(t *testing.T) {
	// Python with comments, verify comment count
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"comments":true,"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`# This is a comment
# Another comment
def hello():
    # inline comment
    pass
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Comments), 1, "expected at least 1 elements")
}

func Test_ProcessPythonDocstrings(t *testing.T) {
	// Python with function docstring, verify docstring count
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"docstrings":true,"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`def greet(name):
    """Say hello to someone."""
    return f"Hello {name}"
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	if result.Metrics.TotalLines < 3 {
		t.Errorf("expected >= 3, got %v", result.Metrics.TotalLines)
	}
}

func Test_ProcessPythonImportsDetail(t *testing.T) {
	// Python with multiple imports, verify imports contain specific source
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`import os
import sys
from pathlib import Path

def main():
    pass
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Imports), 2, "expected at least 2 elements")
	if !strings.Contains(jsonString(result.Imports), `os`) {
		t.Errorf("expected to contain %s, got %v", `os`, result.Imports)
	}
}

func Test_ProcessPythonMetricsDetail(t *testing.T) {
	// Python code with metrics assertions
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`# module docstring
import os

def hello():
    # greeting
    print('hello')

def world():
    print('world')
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	if result.Metrics.CodeLines < 4 {
		t.Errorf("expected >= 4, got %v", result.Metrics.CodeLines)
	}
	if result.Metrics.CommentLines < 1 {
		t.Errorf("expected >= 1, got %v", result.Metrics.CommentLines)
	}
	if result.Metrics.MaxDepth < 1 {
		t.Errorf("expected >= 1, got %v", result.Metrics.MaxDepth)
	}
}

func Test_ProcessPythonSymbols(t *testing.T) {
	// Python with class and functions, verify symbol count
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"python","symbols":true}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`MY_CONST = 42
def helper(): pass
class Widget: pass
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Symbols), 1, "expected at least 1 elements")
}

func Test_ProcessRustStructureName(t *testing.T) {
	// Rust struct with name, verify structure name contains value
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"rust"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`pub struct MyConfig {
    pub name: String,
    pub value: i32,
}

impl MyConfig {
    pub fn new() -> Self {
        Self { name: String::new(), value: 0 }
    }
}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `rust` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `MyConfig`) {
		t.Errorf("expected to contain %s, got %v", `MyConfig`, result.Structure)
	}
}

func Test_PythonChunkingMedium(t *testing.T) {
	// Intel: Python code with medium chunk size
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"chunk_max_size":50,"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`def first():
    x = 1
    return x

def second():
    y = 2
    return y

def third():
    z = 3
    return z
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 3, "expected at least 3 elements")
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
	assert.GreaterOrEqual(t, len(result.Chunks), 2, "expected at least 2 elements")
}

func Test_PythonChunkingProcessDetail(t *testing.T) {
	// Intel: chunk multi-function Python source into multiple pieces
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"chunk_max_size":30,"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`def alpha():
    pass

def beta():
    pass

def gamma():
    pass

def delta():
    pass
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	if result.Metrics.TotalLines < 8 {
		t.Errorf("expected >= 8, got %v", result.Metrics.TotalLines)
	}
	assert.GreaterOrEqual(t, len(result.Chunks), 2, "expected at least 2 elements")
}

func Test_PythonClassWithMethodsProcess(t *testing.T) {
	// Intel: extract nested structure from Python class with methods
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`class Calculator:
    def add(self, a, b):
        return a + b

    def subtract(self, a, b):
        return a - b
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Class`) {
		t.Errorf("expected to contain %s, got %v", `Class`, result.Structure)
	}
	if result.Metrics.TotalLines < 6 {
		t.Errorf("expected >= 6, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_PythonClassWithMethodsProcessDetail(t *testing.T) {
	// Intel: extract nested structure from Python class with methods
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`class Calculator:
    def add(self, a, b):
        return a + b

    def subtract(self, a, b):
        return a - b
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Class`) {
		t.Errorf("expected to contain %s, got %v", `Class`, result.Structure)
	}
	if result.Metrics.TotalLines < 6 {
		t.Errorf("expected >= 6, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_PythonErrorDiagnostics(t *testing.T) {
	// Intel: Python code with syntax errors should report diagnostics
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"diagnostics":true,"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`def broken(
    pass
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	if result.Metrics.ErrorCount != 1 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
	if len(result.Diagnostics) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_PythonFunctionProcess(t *testing.T) {
	// Intel: extract structure from Python function definition
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`def greet(name):
    return f'Hello, {name}!'
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	if result.Metrics.TotalLines < 2 {
		t.Errorf("expected >= 2, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_PythonFunctionProcessDetail(t *testing.T) {
	// Intel: extract structure from Python function definition
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`def greet(name):
    return f'Hello, {name}!'
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	if result.Metrics.TotalLines < 2 {
		t.Errorf("expected >= 2, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_PythonMalformedCodeProcess(t *testing.T) {
	// Intel: detect diagnostics in malformed Python code
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"diagnostics":true,"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`def broken(
    return
class`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	if len(result.Diagnostics) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_PythonMalformedCodeProcessDetail(t *testing.T) {
	// Intel: detect diagnostics in malformed Python code
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"diagnostics":true,"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`def broken(
    return
class`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	if len(result.Diagnostics) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_PythonMultiImportProcess(t *testing.T) {
	// Intel: detect multiple Python imports
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`import os
import sys
from pathlib import Path

def main():
    pass
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	assert.GreaterOrEqual(t, len(result.Imports), 3, "expected at least 3 elements")
	if result.Metrics.TotalLines < 5 {
		t.Errorf("expected >= 5, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_PythonMultiImportProcessDetail(t *testing.T) {
	// Intel: detect multiple Python imports
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"python"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`import os
import sys
from pathlib import Path

def main():
    pass
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `python` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	assert.GreaterOrEqual(t, len(result.Imports), 3, "expected at least 3 elements")
	if result.Metrics.TotalLines < 5 {
		t.Errorf("expected >= 5, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_RubyClassProcess(t *testing.T) {
	// Intel: Ruby class with method
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"ruby"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`require 'json'

class Greeter
  def greet(name)
    "Hello #{name}"
  end
end
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `ruby` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Class`) {
		t.Errorf("expected to contain %s, got %v", `Class`, result.Structure)
	}
	if result.Metrics.TotalLines < 7 {
		t.Errorf("expected >= 7, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_RustChunkingProcess(t *testing.T) {
	// Intel: chunk multi-function Rust source into pieces
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"chunk_max_size":30,"language":"rust"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`fn alpha() {}

fn beta() {}

fn gamma() {}

fn delta() {}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `rust` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	if result.Metrics.TotalLines < 7 {
		t.Errorf("expected >= 7, got %v", result.Metrics.TotalLines)
	}
	assert.GreaterOrEqual(t, len(result.Chunks), 2, "expected at least 2 elements")
}

func Test_RustChunkingProcessDetail(t *testing.T) {
	// Intel: chunk multi-function Rust source into pieces
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"chunk_max_size":30,"language":"rust"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`fn alpha() {}

fn beta() {}

fn gamma() {}

fn delta() {}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `rust` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	if result.Metrics.TotalLines < 7 {
		t.Errorf("expected >= 7, got %v", result.Metrics.TotalLines)
	}
	assert.GreaterOrEqual(t, len(result.Chunks), 2, "expected at least 2 elements")
}

func Test_RustFunctionProcess(t *testing.T) {
	// Intel: extract structure from Rust function definition
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"rust"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`fn add(a: i32, b: i32) -> i32 {
    a + b
}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `rust` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	if result.Metrics.TotalLines < 3 {
		t.Errorf("expected >= 3, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_RustFunctionProcessDetail(t *testing.T) {
	// Intel: extract structure from Rust function definition
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"rust"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process(`fn add(a: i32, b: i32) -> i32 {
    a + b
}
`, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `rust` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	if result.Metrics.TotalLines < 3 {
		t.Errorf("expected >= 3, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_TypescriptFunctionProcess(t *testing.T) {
	// Intel: extract structure from TypeScript function
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"typescript"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `typescript` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	assert.GreaterOrEqual(t, len(result.Imports), 1, "expected at least 1 elements")
	if result.Metrics.TotalLines < 5 {
		t.Errorf("expected >= 5, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}

func Test_TypescriptFunctionProcessDetail(t *testing.T) {
	// Intel: extract structure from TypeScript function
	var config tspack.ProcessConfig
	if err := json.Unmarshal([]byte(`{"language":"typescript"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := tspack.Process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if strings.TrimSpace(string(result.Language)) != `typescript` {
		t.Errorf("equals mismatch: got %v", result.Language)
	}
	assert.GreaterOrEqual(t, len(result.Structure), 1, "expected at least 1 elements")
	if !strings.Contains(jsonString(result.Structure), `Function`) {
		t.Errorf("expected to contain %s, got %v", `Function`, result.Structure)
	}
	assert.GreaterOrEqual(t, len(result.Imports), 1, "expected at least 1 elements")
	if result.Metrics.TotalLines < 5 {
		t.Errorf("expected >= 5, got %v", result.Metrics.TotalLines)
	}
	if result.Metrics.ErrorCount != 0 {
		t.Errorf("equals mismatch: got %v", result.Metrics.ErrorCount)
	}
}
