// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:6602336840d457ae9a18238d9b6a51d6094503fa56d43bafa478ee0ec7092c40
// To regenerate: alef generate
// To verify freshness: alef verify

import { describe, expect, it } from "vitest";
import {
  process,
  getFoldsQuery,
  getHighlightsQuery,
  getIndentsQuery,
  getInjectionsQuery,
  getLocalsQuery,
  getTagsQuery,
} from "@xberg-io/tree-sitter-language-pack";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
  const contentEncoding = response.headers.get("content-encoding");
  let bytes = new Uint8Array(await response.arrayBuffer());

  // The runtime's own fetch (undici on Node, and the browser equivalent) decodes
  // `content-encoding` transparently and leaves the header in place, so by the time the
  // body reaches here it is usually already plain text. Decoding unconditionally on the
  // header alone therefore fails ("incorrect header check") against a server that really
  // did compress. Decode only when the payload still carries the encoding: gzip announces
  // itself with the 0x1f8b magic pair; brotli has no magic, so it is attempted and the
  // already-decoded case falls back to the bytes as received.
  if (contentEncoding === "gzip" && bytes.length >= 2 && bytes[0] === 0x1f && bytes[1] === 0x8b) {
    const { gunzipSync } = await import("zlib");
    bytes = new Uint8Array(gunzipSync(bytes));
  } else if (contentEncoding === "br") {
    const { brotliDecompressSync } = await import("zlib");
    try {
      bytes = new Uint8Array(brotliDecompressSync(bytes));
    } catch {
      // Already decoded by the runtime.
    }
  }

  const text = new TextDecoder().decode(bytes);
  return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}

describe("query", () => {
  it("folds_query_unknown_language: get_folds_query returns empty for unknown language", () => {
    const result = getFoldsQuery("nonexistent_xyz");
    expect((result ?? "").length).toBe(0);
  }, 30000);

  it("folds_query_zig: get_folds_query returns non-empty string for zig", () => {
    const result = getFoldsQuery("zig");
    {
      const _v = result;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);

  it("highlights_nonexistent_language: Get highlights query for nonexistent language returns null/empty", () => {
    const result = getHighlightsQuery("zzz_nonexistent_lang");
    expect((result ?? "").length).toBe(0);
  }, 30000);

  it("highlights_query_rust: get_highlights_query returns non-empty string for rust", () => {
    const result = getHighlightsQuery("rust");
    {
      const _v = result;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);

  it("highlights_query_unknown_language: get_highlights_query returns None for unknown language", () => {
    const result = getHighlightsQuery("nonexistent_language_xyz");
    expect((result ?? "").length).toBe(0);
  }, 30000);

  it("indents_query_cmake: get_indents_query returns non-empty string for cmake", () => {
    const result = getIndentsQuery("cmake");
    {
      const _v = result;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);

  it("indents_query_unknown_language: get_indents_query returns empty for unknown language", () => {
    const result = getIndentsQuery("nonexistent_xyz");
    expect((result ?? "").length).toBe(0);
  }, 30000);

  it("injections_query_rust: get_injections_query returns non-empty for rust", () => {
    const result = getInjectionsQuery("rust");
    {
      const _v = result;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);

  it("injections_query_unknown_language: get_injections_query returns empty for unknown language", () => {
    const result = getInjectionsQuery("nonexistent_xyz");
    expect((result ?? "").length).toBe(0);
  }, 30000);

  it("locals_query_cue: get_locals_query returns non-empty for cue", () => {
    const result = getLocalsQuery("cue");
    {
      const _v = result;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);

  it("locals_query_unknown_language: get_locals_query returns empty for unknown language", () => {
    const result = getLocalsQuery("nonexistent_xyz");
    expect((result ?? "").length).toBe(0);
  }, 30000);

  it("tags_query_kotlin: get_tags_query returns non-empty string for kotlin", () => {
    const result = getTagsQuery("kotlin");
    {
      const _v = result;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);

  it("tags_query_rust: get_tags_query returns non-empty string for rust", () => {
    const result = getTagsQuery("rust");
    {
      const _v = result;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);

  it("tags_query_unknown_language: get_tags_query returns empty for unknown language", () => {
    const result = getTagsQuery("nonexistent_xyz");
    expect((result ?? "").length).toBe(0);
  }, 30000);
});
