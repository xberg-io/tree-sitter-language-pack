// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f4784a53cf3064065b7cba7e8e4068fc6860803b09d6ba374e3b77b2fdfdff63
// To regenerate: alef generate
// To verify freshness: alef verify

import { describe, expect, it } from "vitest";
import { process, type ProcessConfig } from "@xberg-io/tree-sitter-language-pack";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
  const contentEncoding = response.headers.get("content-encoding");
  let bytes = new Uint8Array(await response.arrayBuffer());

  // The runtime's own fetch (undici on Node, and the browser equivalent) decodes
  // `content-encoding` transparently and leaves the header in place, so by the time the
  // body reaches here it is usually already plain text. Decoding unconditionally on the
  // header alone therefore fails ("incorrect header check") against a server that really
  // did compress. Decode only when the payload still carries the encoding: gzip announces
  // itself with the 0x1f8b magic pair; brotli has no magic, so it is attempted and the
  // already-decoded case falls back to the bytes as received.
  if (contentEncoding === "gzip" && bytes.length >= 2 && bytes[0] === 0x1f && bytes[1] === 0x8b) {
    const { gunzipSync } = await import("zlib");
    bytes = new Uint8Array(gunzipSync(bytes));
  } else if (contentEncoding === "br") {
    const { brotliDecompressSync } = await import("zlib");
    try {
      bytes = new Uint8Array(brotliDecompressSync(bytes));
    } catch {
      // Already decoded by the runtime.
    }
  }

  const text = new TextDecoder().decode(bytes);
  return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}

describe("smoke", () => {
  it("smoke_abl: Smoke test: load abl and parse a simple snippet", () => {
    const result = process("x", { language: "abl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_abnf: Smoke test: load abnf and parse a simple snippet", () => {
    const result = process('a = "b"\r\n', { language: "abnf" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_actionscript: Smoke test: load actionscript and parse a simple snippet", () => {
    const result = process("var x:int = 1;", { language: "actionscript" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ada: Smoke test: load ada and parse a simple snippet", () => {
    const result = process("procedure Main is begin null; end Main;", { language: "ada" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_agda: Smoke test: load agda and parse a simple snippet", () => {
    const result = process("module Main where", { language: "agda" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_aiken: Smoke test: load aiken and parse a simple snippet", () => {
    const result = process("fn main() {\n  1\n}\n", { language: "aiken" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_al: Smoke test: load al and parse a simple snippet", () => {
    const result = process("x", { language: "al" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_angular: Smoke test: load angular and parse a simple snippet", () => {
    const result = process("x", { language: "angular" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_apex: Smoke test: load apex and parse a simple snippet", () => {
    const result = process("public class Main {}", { language: "apex" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_applescript: Smoke test: load applescript and parse a simple snippet", () => {
    const result = process("set x to 1\n", { language: "applescript" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_arduino: Smoke test: load arduino and parse a simple snippet", () => {
    const result = process("void setup() {}", { language: "arduino" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_asciidoc: Smoke test: load asciidoc and parse a simple snippet", () => {
    const result = process("= Title\n\nParagraph.", { language: "asciidoc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_asm: Smoke test: load asm and parse a simple snippet", () => {
    const result = process("mov eax, 1", { language: "asm" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_astro: Smoke test: load astro and parse a simple snippet", () => {
    const result = process("---\n---\n<p>hello</p>", { language: "astro" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_avro: Smoke test: load avro and parse a simple snippet", () => {
    const result = process("protocol P {\n}\n", { language: "avro" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_awk: Smoke test: load awk and parse a simple snippet", () => {
    const result = process('BEGIN { print "hello" }', { language: "awk" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ballerina: Smoke test: load ballerina and parse a simple snippet", () => {
    const result = process("public function main() {\n}\n", { language: "ballerina" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_bash: Smoke test: load bash and parse a simple snippet", () => {
    const result = process("echo hello", { language: "bash" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_bass: Smoke test: load bass and parse a simple snippet", () => {
    const result = process("x", { language: "bass" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_batch: Smoke test: load batch and parse a simple snippet", () => {
    const result = process("@echo off\necho hello", { language: "batch" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_beancount: Smoke test: load beancount and parse a simple snippet", () => {
    const result = process("2024-01-01 open Assets:Bank USD", { language: "beancount" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_bibtex: Smoke test: load bibtex and parse a simple snippet", () => {
    const result = process("@article{key, title={A}}", { language: "bibtex" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_bicep: Smoke test: load bicep and parse a simple snippet", () => {
    const result = process("param name string", { language: "bicep" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_bitbake: Smoke test: load bitbake and parse a simple snippet", () => {
    const result = process('DESCRIPTION = "hello"', { language: "bitbake" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_blade: Smoke test: load blade and parse a simple snippet", () => {
    const result = process("x", { language: "blade" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_bpftrace: Smoke test: load bpftrace and parse a simple snippet", () => {
    const result = process("BEGIN { }\n", { language: "bpftrace" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_brightscript: Smoke test: load brightscript and parse a simple snippet", () => {
    const result = process("x", { language: "brightscript" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_bsl: Smoke test: load bsl and parse a simple snippet", () => {
    const result = process("Procedure Main() EndProcedure", { language: "bsl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_c: Smoke test: load c and parse a simple snippet", () => {
    const result = process("int main() { return 0; }", { language: "c" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_c3: Smoke test: load c3 and parse a simple snippet", () => {
    const result = process("x", { language: "c3" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_caddy: Smoke test: load caddy and parse a simple snippet", () => {
    const result = process(':8080 {\n\trespond "Hello"\n}', { language: "caddy" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cairo: Smoke test: load cairo and parse a simple snippet", () => {
    const result = process("fn main() {}", { language: "cairo" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_capnp: Smoke test: load capnp and parse a simple snippet", () => {
    const result = process("@0xabcdef1234567890;", { language: "capnp" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cedar: Smoke test: load cedar and parse a simple snippet", () => {
    const result = process("permit(principal, action, resource);", { language: "cedar" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cedarschema: Smoke test: load cedarschema and parse a simple snippet", () => {
    const result = process("entity User;", { language: "cedarschema" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cel: Smoke test: load cel and parse a simple snippet", () => {
    const result = process("x", { language: "cel" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cfml: Smoke test: load cfml and parse a simple snippet", () => {
    const result = process("x", { language: "cfml" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_chatito: Smoke test: load chatito and parse a simple snippet", () => {
    const result = process("%[greeting]\n    hello", { language: "chatito" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_chuck: Smoke test: load chuck and parse a simple snippet", () => {
    const result = process("x", { language: "chuck" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_circom: Smoke test: load circom and parse a simple snippet", () => {
    const result = process("x", { language: "circom" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_clarity: Smoke test: load clarity and parse a simple snippet", () => {
    const result = process("(define-public (hello) (ok true))", { language: "clarity" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_clojure: Smoke test: load clojure and parse a simple snippet", () => {
    const result = process("(def x 1)", { language: "clojure" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cmake: Smoke test: load cmake and parse a simple snippet", () => {
    const result = process("cmake_minimum_required(VERSION 3.0)", { language: "cmake" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cobol: Smoke test: load cobol and parse a simple snippet", () => {
    const result = process("       IDENTIFICATION DIVISION.\n       PROGRAM-ID. HELLO.", {
      language: "cobol",
    } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_comment: Smoke test: load comment and parse a simple snippet", () => {
    const result = process("Review: handle edge case", { language: "comment" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_commonlisp: Smoke test: load commonlisp and parse a simple snippet", () => {
    const result = process('(defun hello () (print "hello"))', { language: "commonlisp" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cooklang: Smoke test: load cooklang and parse a simple snippet", () => {
    const result = process("x", { language: "cooklang" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_corn: Smoke test: load corn and parse a simple snippet", () => {
    const result = process('{ key = "value" }', { language: "corn" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cpon: Smoke test: load cpon and parse a simple snippet", () => {
    const result = process('{"key": 1}', { language: "cpon" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cpp: Smoke test: load cpp and parse a simple snippet", () => {
    const result = process("int main() { return 0; }", { language: "cpp" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_crystal: Smoke test: load crystal and parse a simple snippet", () => {
    const result = process("x", { language: "crystal" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_csharp: Smoke test: load csharp and parse a simple snippet", () => {
    const result = process("class Main {}", { language: "csharp" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_css: Smoke test: load css and parse a simple snippet", () => {
    const result = process("body { color: red; }", { language: "css" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cst: Smoke test: load cst and parse a simple snippet", () => {
    const result = process("x", { language: "cst" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_csv: Smoke test: load csv and parse a simple snippet", () => {
    const result = process("a,b,c\n1,2,3", { language: "csv" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cuda: Smoke test: load cuda and parse a simple snippet", () => {
    const result = process("__global__ void kernel() {}", { language: "cuda" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cue: Smoke test: load cue and parse a simple snippet", () => {
    const result = process("x", { language: "cue" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cylc: Smoke test: load cylc and parse a simple snippet", () => {
    const result = process("x", { language: "cylc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cypher: Smoke test: load cypher and parse a simple snippet", () => {
    const result = process("MATCH (n) RETURN n\n", { language: "cypher" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_cython: Smoke test: load cython and parse a simple snippet", () => {
    const result = process("x = 1\n", { language: "cython" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_d: Smoke test: load d and parse a simple snippet", () => {
    const result = process("void main() {}", { language: "d" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_d2: Smoke test: load d2 and parse a simple snippet", () => {
    const result = process("a -> b\n", { language: "d2" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_dart: Smoke test: load dart and parse a simple snippet", () => {
    const result = process("void main() {}", { language: "dart" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_desktop: Smoke test: load desktop and parse a simple snippet", () => {
    const result = process("x", { language: "desktop" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_devicetree: Smoke test: load devicetree and parse a simple snippet", () => {
    const result = process("/dts-v1/;\n/ { };", { language: "devicetree" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_dhall: Smoke test: load dhall and parse a simple snippet", () => {
    const result = process("x", { language: "dhall" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_diff: Smoke test: load diff and parse a simple snippet", () => {
    const result = process("--- a/file\n+++ b/file\n@@ -1 +1 @@\n-old\n+new", { language: "diff" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_djot: Smoke test: load djot and parse a simple snippet", () => {
    const result = process("x", { language: "djot" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_dockerfile: Smoke test: load dockerfile and parse a simple snippet", () => {
    const result = process("FROM alpine", { language: "dockerfile" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_dot: Smoke test: load dot and parse a simple snippet", () => {
    const result = process("digraph G { A -> B; }", { language: "dot" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_dotenv: Smoke test: load dotenv and parse a simple snippet", () => {
    const result = process("KEY=value\n", { language: "dotenv" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_doxygen: Smoke test: load doxygen and parse a simple snippet", () => {
    const result = process("/** @brief A function */", { language: "doxygen" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_dtd: Smoke test: load dtd and parse a simple snippet", () => {
    const result = process("<!ELEMENT note (body)>", { language: "dtd" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_earthfile: Smoke test: load earthfile and parse a simple snippet", () => {
    const result = process("x", { language: "earthfile" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 90000);

  it("smoke_ebnf: Smoke test: load ebnf and parse a simple snippet", () => {
    const result = process("x", { language: "ebnf" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_editorconfig: Smoke test: load editorconfig and parse a simple snippet", () => {
    const result = process("x", { language: "editorconfig" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_edoc: Smoke test: load edoc and parse a simple snippet", () => {
    const result = process("@doc foo\n", { language: "edoc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_eds: Smoke test: load eds and parse a simple snippet", () => {
    const result = process("x", { language: "eds" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_eex: Smoke test: load eex and parse a simple snippet", () => {
    const result = process("x", { language: "eex" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_eiffel: Smoke test: load eiffel and parse a simple snippet", () => {
    const result = process("class FOO\nend\n", { language: "eiffel" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_elisp: Smoke test: load elisp and parse a simple snippet", () => {
    const result = process('(defun hello () (message "hello"))', { language: "elisp" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_elixir: Smoke test: load elixir and parse a simple snippet", () => {
    const result = process('IO.puts("hello")', { language: "elixir" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_elm: Smoke test: load elm and parse a simple snippet", () => {
    const result = process("module Main exposing (..)", { language: "elm" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_elsa: Smoke test: load elsa and parse a simple snippet", () => {
    const result = process("x", { language: "elsa" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_elvish: Smoke test: load elvish and parse a simple snippet", () => {
    const result = process("x", { language: "elvish" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_embeddedtemplate: Smoke test: load embeddedtemplate and parse a simple snippet", () => {
    const result = process("<%= value %>", { language: "embeddedtemplate" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_enforce: Smoke test: load enforce and parse a simple snippet", () => {
    const result = process("x", { language: "enforce" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_erlang: Smoke test: load erlang and parse a simple snippet", () => {
    const result = process("main() -> ok.", { language: "erlang" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_facility: Smoke test: load facility and parse a simple snippet", () => {
    const result = process("x", { language: "facility" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_faust: Smoke test: load faust and parse a simple snippet", () => {
    const result = process("x", { language: "faust" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_fennel: Smoke test: load fennel and parse a simple snippet", () => {
    const result = process("(fn hello [] (print :hello))", { language: "fennel" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_fidl: Smoke test: load fidl and parse a simple snippet", () => {
    const result = process("x", { language: "fidl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_firrtl: Smoke test: load firrtl and parse a simple snippet", () => {
    const result = process("circuit Main :", { language: "firrtl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_fish: Smoke test: load fish and parse a simple snippet", () => {
    const result = process("echo hello", { language: "fish" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_flatbuffers: Smoke test: load flatbuffers and parse a simple snippet", () => {
    const result = process("table Foo {}\n", { language: "flatbuffers" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_fluent: Smoke test: load fluent and parse a simple snippet", () => {
    const result = process("hello = Hello\n", { language: "fluent" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_foam: Smoke test: load foam and parse a simple snippet", () => {
    const result = process("x", { language: "foam" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_forth: Smoke test: load forth and parse a simple snippet", () => {
    const result = process("x", { language: "forth" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_fortran: Smoke test: load fortran and parse a simple snippet", () => {
    const result = process("program main\nend program main", { language: "fortran" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_fsharp: Smoke test: load fsharp and parse a simple snippet", () => {
    const result = process("let x = 1", { language: "fsharp" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_fsharp_signature: Smoke test: load fsharp_signature and parse a simple snippet", () => {
    const result = process("val x: int", { language: "fsharp_signature" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_func: Smoke test: load func and parse a simple snippet", () => {
    const result = process("() recv_internal() {}", { language: "func" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_fusion: Smoke test: load fusion and parse a simple snippet", () => {
    const result = process("foo = 1\n", { language: "fusion" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gap: Smoke test: load gap and parse a simple snippet", () => {
    const result = process("x", { language: "gap" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gcode: Smoke test: load gcode and parse a simple snippet", () => {
    const result = process("G0 X0\n", { language: "gcode" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gdscript: Smoke test: load gdscript and parse a simple snippet", () => {
    const result = process("extends Node\nfunc _ready():\n\tpass", { language: "gdscript" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gdshader: Smoke test: load gdshader and parse a simple snippet", () => {
    const result = process("x", { language: "gdshader" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gherkin: Smoke test: load gherkin and parse a simple feature", () => {
    const result = process(
      "Feature: Calculator\n  Scenario: Add numbers\n    Given I have entered 1\n    When I add 2\n    Then the result should be 3\n",
      { language: "gherkin" } as ProcessConfig,
    );
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_git_config: Smoke test: load git_config and parse a simple snippet", () => {
    const result = process("x", { language: "git_config" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_git_rebase: Smoke test: load git_rebase and parse a simple snippet", () => {
    const result = process("x", { language: "git_rebase" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gitattributes: Smoke test: load gitattributes and parse a simple snippet", () => {
    const result = process("*.txt text", { language: "gitattributes" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gitcommit: Smoke test: load gitcommit and parse a simple snippet", () => {
    const result = process("feat: add feature\n\nBody text", { language: "gitcommit" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gitignore: Smoke test: load gitignore and parse a simple snippet", () => {
    const result = process("*.o\n*.log", { language: "gitignore" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gleam: Smoke test: load gleam and parse a simple snippet", () => {
    const result = process("pub fn main() { }", { language: "gleam" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_glimmer: Smoke test: load glimmer and parse a simple snippet", () => {
    const result = process("x", { language: "glimmer" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_glsl: Smoke test: load glsl and parse a simple snippet", () => {
    const result = process("void main() { gl_Position = vec4(0.0); }", { language: "glsl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gn: Smoke test: load gn and parse a simple snippet", () => {
    const result = process('group("hello") {}', { language: "gn" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gnuplot: Smoke test: load gnuplot and parse a simple snippet", () => {
    const result = process("x", { language: "gnuplot" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_go: Smoke test: load go and parse a simple snippet", () => {
    const result = process("package main", { language: "go" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_godot_resource: Smoke test: load godot_resource and parse a simple snippet", () => {
    const result = process("x", { language: "godot_resource" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gomod: Smoke test: load gomod and parse a simple snippet", () => {
    const result = process("module example.com/hello\n\ngo 1.21", { language: "gomod" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gosum: Smoke test: load gosum and parse a simple snippet", () => {
    const result = process("example.com/pkg v1.0.0 h1:abc=", { language: "gosum" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gotmpl: Smoke test: load gotmpl and parse a simple snippet", () => {
    const result = process("x", { language: "gotmpl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gowork: Smoke test: load gowork and parse a simple snippet", () => {
    const result = process("x", { language: "gowork" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gpg: Smoke test: load gpg and parse a simple snippet", () => {
    const result = process("x", { language: "gpg" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_graphql: Smoke test: load graphql and parse a simple snippet", () => {
    const result = process("type Query { hello: String }", { language: "graphql" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gren: Smoke test: load gren and parse a simple snippet", () => {
    const result = process("module Main exposing (..)", { language: "gren" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_groovy: Smoke test: load groovy and parse a simple snippet", () => {
    const result = process("def x = 1", { language: "groovy" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_gstlaunch: Smoke test: load gstlaunch and parse a simple snippet", () => {
    const result = process("fakesrc ! fakesink", { language: "gstlaunch" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_hack: Smoke test: load hack and parse a simple snippet", () => {
    const result = process("<?hh\nfunction main(): void {}", { language: "hack" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_haml: Smoke test: load haml and parse a simple snippet", () => {
    const result = process("%p hello\n", { language: "haml" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_hare: Smoke test: load hare and parse a simple snippet", () => {
    const result = process("export fn main() void = void;", { language: "hare" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_haskell: Smoke test: load haskell and parse a simple snippet", () => {
    const result = process('main = putStrLn "hello"', { language: "haskell" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_haskell_persistent: Smoke test: load haskell_persistent and parse a simple snippet", () => {
    const result = process("Person\n  name String\n", { language: "haskell_persistent" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_haxe: Smoke test: load haxe and parse a simple snippet", () => {
    const result = process("class Main { static function main() {} }", { language: "haxe" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_hcl: Smoke test: load hcl and parse a simple snippet", () => {
    const result = process('variable "name" { type = string }', { language: "hcl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_heex: Smoke test: load heex and parse a simple snippet", () => {
    const result = process("<%= @greeting %>", { language: "heex" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_hjson: Smoke test: load hjson and parse a simple snippet", () => {
    const result = process("x", { language: "hjson" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_hlsl: Smoke test: load hlsl and parse a simple snippet", () => {
    const result = process("float4 main() : SV_Target { return 0; }", { language: "hlsl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_hocon: Smoke test: load hocon and parse a simple snippet", () => {
    const result = process("x", { language: "hocon" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_hoon: Smoke test: load hoon and parse a simple snippet", () => {
    const result = process("x", { language: "hoon" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_html: Smoke test: load html and parse a simple snippet", () => {
    const result = process("<p>hello</p>", { language: "html" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_htmldjango: Smoke test: load htmldjango and parse a simple snippet", () => {
    const result = process("x", { language: "htmldjango" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_http: Smoke test: load http and parse a simple snippet", () => {
    const result = process("x", { language: "http" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_hurl: Smoke test: load hurl and parse a simple snippet", () => {
    const result = process("x", { language: "hurl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_hyprlang: Smoke test: load hyprlang and parse a simple snippet", () => {
    const result = process("general { border_size = 1 }", { language: "hyprlang" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_idl: Smoke test: load idl and parse a simple snippet", () => {
    const result = process("module M {\n};\n", { language: "idl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_idris: Smoke test: load idris and parse a simple snippet", () => {
    const result = process("module Main", { language: "idris" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ini: Smoke test: load ini and parse a simple snippet", () => {
    const result = process("[section]\nkey = value", { language: "ini" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ispc: Smoke test: load ispc and parse a simple snippet", () => {
    const result = process("export void main() {}", { language: "ispc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_jai: Smoke test: load jai and parse a simple snippet", () => {
    const result = process("x", { language: "jai" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_janet: Smoke test: load janet and parse a simple snippet", () => {
    const result = process('(print "hello")', { language: "janet" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_java: Smoke test: load java and parse a simple snippet", () => {
    const result = process("class Main {}", { language: "java" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_javadoc: Smoke test: load javadoc and parse a simple snippet", () => {
    const result = process("x", { language: "javadoc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_javascript: Smoke test: load javascript and parse a simple snippet", () => {
    const result = process("console.log('hello');", { language: "javascript" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_jinja2: Smoke test: load jinja2 and parse a simple snippet", () => {
    const result = process("{{ variable }}", { language: "jinja2" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_jjdescription: Smoke test: load jjdescription and parse a simple snippet", () => {
    const result = process("commit message\n", { language: "jjdescription" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_jq: Smoke test: load jq and parse a simple snippet", () => {
    const result = process(".[] | select(.key)", { language: "jq" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_jsdoc: Smoke test: load jsdoc and parse a simple snippet", () => {
    const result = process("/** @param {string} name */", { language: "jsdoc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_json: Smoke test: load json and parse a simple snippet", () => {
    const result = process('{"key": "value"}', { language: "json" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_json5: Smoke test: load json5 and parse a simple snippet", () => {
    const result = process("x", { language: "json5" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_jsonnet: Smoke test: load jsonnet and parse a simple snippet", () => {
    const result = process("{ key: 'value' }", { language: "jsonnet" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_julia: Smoke test: load julia and parse a simple snippet", () => {
    const result = process("function main() end", { language: "julia" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_just: Smoke test: load just and parse a simple snippet", () => {
    const result = process("x", { language: "just" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_kcl: Smoke test: load kcl and parse a simple snippet", () => {
    const result = process("x", { language: "kcl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_kconfig: Smoke test: load kconfig and parse a simple snippet", () => {
    const result = process('config FOO\n\tbool "Enable foo"', { language: "kconfig" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_kdl: Smoke test: load kdl and parse a simple snippet", () => {
    const result = process('node "value"', { language: "kdl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_kitty: Smoke test: load kitty and parse a simple snippet", () => {
    const result = process("font_size 12\n", { language: "kitty" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_koka: Smoke test: load koka and parse a simple snippet", () => {
    const result = process("fun main()\n  1\n", { language: "koka" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_kotlin: Smoke test: load kotlin and parse a simple snippet", () => {
    const result = process("fun main() {}", { language: "kotlin" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_koto: Smoke test: load koto and parse a simple snippet", () => {
    const result = process("x = 1\n", { language: "koto" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_kusto: Smoke test: load kusto and parse a simple snippet", () => {
    const result = process("T | count\n", { language: "kusto" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_lalrpop: Smoke test: load lalrpop and parse a simple snippet", () => {
    const result = process("grammar;\n", { language: "lalrpop" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_latex: Smoke test: load latex and parse a simple snippet", () => {
    const result = process("\\documentclass{article}\n\\begin{document}\nHello\n\\end{document}", {
      language: "latex",
    } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_lean: Smoke test: load lean and parse a simple snippet", () => {
    const result = process("def main : IO Unit := pure ()", { language: "lean" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ledger: Smoke test: load ledger and parse a simple snippet", () => {
    const result = process("x", { language: "ledger" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_leo: Smoke test: load leo and parse a simple snippet", () => {
    const result = process("program test.aleo {\n}\n", { language: "leo" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_less: Smoke test: load less and parse a simple snippet", () => {
    const result = process("x", { language: "less" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_linkerscript: Smoke test: load linkerscript and parse a simple snippet", () => {
    const result = process("SECTIONS { .text : { *(.text) } }", { language: "linkerscript" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_liquid: Smoke test: load liquid and parse a simple snippet", () => {
    const result = process("x", { language: "liquid" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_llvm: Smoke test: load llvm and parse a simple snippet", () => {
    const result = process("define i32 @main() { ret i32 0 }", { language: "llvm" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_llvm_mir: Smoke test: load llvm_mir and parse a simple snippet", () => {
    const result = process("---\nname: foo\n...\n", { language: "llvm_mir" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_lua: Smoke test: load lua and parse a simple snippet", () => {
    const result = process("print('hello')", { language: "lua" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_luadoc: Smoke test: load luadoc and parse a simple snippet", () => {
    const result = process("---@param name string", { language: "luadoc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_luap: Smoke test: load luap and parse a simple snippet", () => {
    const result = process("[a-z]+", { language: "luap" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_luau: Smoke test: load luau and parse a simple snippet", () => {
    const result = process("local x: number = 1", { language: "luau" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_m68k: Smoke test: load m68k and parse a simple snippet", () => {
    const result = process(" move.l d0,d1\n", { language: "m68k" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_magik: Smoke test: load magik and parse a simple snippet", () => {
    const result = process("_method object.hello\n_endmethod", { language: "magik" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_make: Smoke test: load make and parse a simple snippet", () => {
    const result = process("all:\n\techo hello", { language: "make" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_markdown: Smoke test: load markdown and parse a simple snippet", () => {
    const result = process("# Hello\n\nWorld", { language: "markdown" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_markdown_inline: Smoke test: load markdown_inline and parse a simple snippet", () => {
    const result = process("**bold** and *italic*", { language: "markdown_inline" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_matlab: Smoke test: load matlab and parse a simple snippet", () => {
    const result = process("function y = hello(x)\ny = x;\nend", { language: "matlab" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_menhir: Smoke test: load menhir and parse a simple snippet", () => {
    const result = process("%token EOF\n%%\n", { language: "menhir" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_mermaid: Smoke test: load mermaid and parse a simple snippet", () => {
    const result = process("graph TD\nA --> B", { language: "mermaid" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_meson: Smoke test: load meson and parse a simple snippet", () => {
    const result = process("project('hello', 'c')", { language: "meson" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_mlir: Smoke test: load mlir and parse a simple snippet", () => {
    const result = process("x", { language: "mlir" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_mojo: Smoke test: load mojo and parse a simple snippet", () => {
    const result = process("x", { language: "mojo" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_moonbit: Smoke test: load moonbit and parse a simple snippet", () => {
    const result = process("fn main {\n}\n", { language: "moonbit" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_motoko: Smoke test: load motoko and parse a simple snippet", () => {
    const result = process("actor {\n}\n", { language: "motoko" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_move: Smoke test: load move and parse a simple snippet", () => {
    const result = process("x", { language: "move" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_nasm: Smoke test: load nasm and parse a simple snippet", () => {
    const result = process("x", { language: "nasm" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_netlinx: Smoke test: load netlinx and parse a simple snippet", () => {
    const result = process("PROGRAM_NAME='hello'", { language: "netlinx" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_nginx: Smoke test: load nginx and parse a simple snippet", () => {
    const result = process("x", { language: "nginx" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_nickel: Smoke test: load nickel and parse a simple snippet", () => {
    const result = process("x", { language: "nickel" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_nim: Smoke test: load nim and parse a simple snippet", () => {
    const result = process('echo "hello"', { language: "nim" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ninja: Smoke test: load ninja and parse a simple snippet", () => {
    const result = process("rule cc\n  command = cc $in -o $out", { language: "ninja" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_nix: Smoke test: load nix and parse a simple snippet", () => {
    const result = process("{ pkgs ? import <nixpkgs> {} }: pkgs.hello", { language: "nix" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_norg: Smoke test: load norg and parse a simple snippet", () => {
    const result = process("x", { language: "norg" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_norg_meta: Smoke test: load norg_meta and parse a simple snippet", () => {
    const result = process("x", { language: "norg_meta" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_nqc: Smoke test: load nqc and parse a simple snippet", () => {
    const result = process("task main() {}", { language: "nqc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_nushell: Smoke test: load nushell and parse a simple snippet", () => {
    const result = process("x", { language: "nushell" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_objc: Smoke test: load objc and parse a simple snippet", () => {
    const result = process("@interface Main @end", { language: "objc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ocaml: Smoke test: load ocaml and parse a simple snippet", () => {
    const result = process('let () = print_endline "hello"', { language: "ocaml" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ocaml_interface: Smoke test: load ocaml_interface and parse a simple snippet", () => {
    const result = process("val x : int", { language: "ocaml_interface" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ocamllex: Smoke test: load ocamllex and parse a simple snippet", () => {
    const result = process("x", { language: "ocamllex" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_odin: Smoke test: load odin and parse a simple snippet", () => {
    const result = process("package main", { language: "odin" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_openscad: Smoke test: load openscad and parse a simple snippet", () => {
    const result = process("x", { language: "openscad" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_org: Smoke test: load org and parse a simple snippet", () => {
    const result = process("* Hello\nWorld", { language: "org" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_pascal: Smoke test: load pascal and parse a simple snippet", () => {
    const result = process("program Hello; begin end.", { language: "pascal" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_pem: Smoke test: load pem and parse a simple snippet", () => {
    const result = process("-----BEGIN CERTIFICATE-----\ndata\n-----END CERTIFICATE-----", {
      language: "pem",
    } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_penrose: Smoke test: load penrose and parse a simple snippet", () => {
    const result = process("type Set\n", { language: "penrose" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_perl: Smoke test: load perl and parse a simple snippet", () => {
    const result = process("print 'hello';", { language: "perl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 90000);

  it("smoke_pgn: Smoke test: load pgn and parse a simple snippet", () => {
    const result = process("1. e4 e5 *", { language: "pgn" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_php: Smoke test: load php and parse a simple snippet", () => {
    const result = process("<?php echo 'hello'; ?>", { language: "php" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_phpdoc: Smoke test: load phpdoc and parse a simple snippet", () => {
    const result = process("x", { language: "phpdoc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_picat: Smoke test: load picat and parse a simple snippet", () => {
    const result = process("main => true.\n", { language: "picat" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_pkl: Smoke test: load pkl and parse a simple snippet", () => {
    const result = process('name = "hello"', { language: "pkl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_plantuml: Smoke test: load plantuml and parse a simple snippet", () => {
    const result = process("@startuml\n@enduml\n", { language: "plantuml" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_po: Smoke test: load po and parse a simple snippet", () => {
    const result = process('msgid "hello"\nmsgstr "world"', { language: "po" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_poe_filter: Smoke test: load poe_filter and parse a simple snippet", () => {
    const result = process("x", { language: "poe_filter" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_pony: Smoke test: load pony and parse a simple snippet", () => {
    const result = process("actor Main\n  new create(env: Env) => None", { language: "pony" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_postgres: Smoke test: load postgres and parse a simple snippet", () => {
    const result = process("SELECT 1;\n", { language: "postgres" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_postscript: Smoke test: load postscript and parse a simple snippet", () => {
    const result = process("/hello { (Hello) show } def", { language: "postscript" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_powershell: Smoke test: load powershell and parse a simple snippet", () => {
    const result = process("Write-Host 'hello'", { language: "powershell" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_printf: Smoke test: load printf and parse a simple snippet", () => {
    const result = process("%d %s", { language: "printf" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_prisma: Smoke test: load prisma and parse a simple snippet", () => {
    const result = process("model User { id Int @id }", { language: "prisma" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_prolog: Smoke test: load prolog and parse a simple snippet", () => {
    const result = process("hello :- write('hello'), nl.", { language: "prolog" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_promela: Smoke test: load promela and parse a simple snippet", () => {
    const result = process("init {\n}\n", { language: "promela" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_promql: Smoke test: load promql and parse a simple snippet", () => {
    const result = process("x", { language: "promql" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_properties: Smoke test: load properties and parse a simple snippet", () => {
    const result = process("key=value", { language: "properties" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_proto: Smoke test: load proto and parse a simple snippet", () => {
    const result = process('syntax = "proto3";', { language: "proto" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_prql: Smoke test: load prql and parse a simple snippet", () => {
    const result = process("x", { language: "prql" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_psv: Smoke test: load psv and parse a simple snippet", () => {
    const result = process("a|b|c\n1|2|3", { language: "psv" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_pug: Smoke test: load pug and parse a simple snippet", () => {
    const result = process("x", { language: "pug" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_puppet: Smoke test: load puppet and parse a simple snippet", () => {
    const result = process("notify { 'hello': }", { language: "puppet" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_purescript: Smoke test: load purescript and parse a simple snippet", () => {
    const result = process("module Main where", { language: "purescript" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_pymanifest: Smoke test: load pymanifest and parse a simple snippet", () => {
    const result = process("include *.txt", { language: "pymanifest" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_python: Smoke test: load python and parse a simple snippet", () => {
    const result = process("print('hello')", { language: "python" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ql: Smoke test: load ql and parse a simple snippet", () => {
    const result = process("x", { language: "ql" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_qmldir: Smoke test: load qmldir and parse a simple snippet", () => {
    const result = process("module Example", { language: "qmldir" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_qmljs: Smoke test: load qmljs and parse a simple snippet", () => {
    const result = process("import QtQuick 2.0\nItem {}", { language: "qmljs" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_query: Smoke test: load query and parse a simple snippet", () => {
    const result = process("(identifier) @name", { language: "query" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_r: Smoke test: load r and parse a simple snippet", () => {
    const result = process("print('hello')", { language: "r" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_racket: Smoke test: load racket and parse a simple snippet", () => {
    const result = process("#lang racket\n(define x 1)", { language: "racket" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_rasi: Smoke test: load rasi and parse a simple snippet", () => {
    const result = process("x", { language: "rasi" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_razor: Smoke test: load razor and parse a simple snippet", () => {
    const result = process("x", { language: "razor" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_rbs: Smoke test: load rbs and parse a simple snippet", () => {
    const result = process("x", { language: "rbs" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_re2c: Smoke test: load re2c and parse a simple snippet", () => {
    const result = process("/*!re2c\n  [a-z]+ { return; }\n*/", { language: "re2c" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_readline: Smoke test: load readline and parse a simple snippet", () => {
    const result = process("set editing-mode vi", { language: "readline" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_reason: Smoke test: load reason and parse a simple snippet", () => {
    const result = process("let x = 1;\n", { language: "reason" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_regex: Smoke test: load regex and parse a simple snippet", () => {
    const result = process("x", { language: "regex" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_rego: Smoke test: load rego and parse a simple snippet", () => {
    const result = process("package main\ndefault allow = false", { language: "rego" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_requirements: Smoke test: load requirements and parse a simple snippet", () => {
    const result = process("flask>=2.0", { language: "requirements" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_rescript: Smoke test: load rescript and parse a simple snippet", () => {
    const result = process("let x = 1", { language: "rescript" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_robot: Smoke test: load robot and parse a simple snippet", () => {
    const result = process("x", { language: "robot" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_roc: Smoke test: load roc and parse a simple snippet", () => {
    const result = process("x", { language: "roc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ron: Smoke test: load ron and parse a simple snippet", () => {
    const result = process('(key: "value")', { language: "ron" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_rshtml: Smoke test: load rshtml and parse a simple snippet", () => {
    const result = process("<p>hi</p>\n", { language: "rshtml" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_rst: Smoke test: load rst and parse a simple snippet", () => {
    const result = process("Hello\n=====\n\nWorld", { language: "rst" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_rtf: Smoke test: load rtf and parse a simple snippet", () => {
    const result = process("{\\rtf1 hello}", { language: "rtf" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ruby: Smoke test: load ruby and parse a simple snippet", () => {
    const result = process("puts 'hello'", { language: "ruby" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_rust: Smoke test: load rust and parse a simple snippet", () => {
    const result = process("fn main() {}", { language: "rust" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_sas: Smoke test: load sas and parse a simple snippet", () => {
    const result = process("data _null_;\nrun;\n", { language: "sas" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_scala: Smoke test: load scala and parse a simple snippet", () => {
    const result = process("object Main", { language: "scala" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_scfg: Smoke test: load scfg and parse a simple snippet", () => {
    const result = process("key value\n", { language: "scfg" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_scheme: Smoke test: load scheme and parse a simple snippet", () => {
    const result = process("(define x 1)", { language: "scheme" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_scss: Smoke test: load scss and parse a simple snippet", () => {
    const result = process("$color: red;\nbody { color: $color; }", { language: "scss" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_sflog: Smoke test: load sflog and parse a simple snippet", () => {
    const result = process("37.0 APEX_CODE,DEBUG\n16:06:58.18 (1)|EXECUTION_STARTED\n", {
      language: "sflog",
    } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_slang: Smoke test: load slang and parse a simple snippet", () => {
    const result = process("x", { language: "slang" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_slim: Smoke test: load slim and parse a simple snippet", () => {
    const result = process("p hello\n", { language: "slim" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_slint: Smoke test: load slint and parse a simple snippet", () => {
    const result = process("export component Foo {}\n", { language: "slint" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_smali: Smoke test: load smali and parse a simple snippet", () => {
    const result = process(".class public LMain;\n.super Ljava/lang/Object;", { language: "smali" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_smalltalk: Smoke test: load smalltalk and parse a simple snippet", () => {
    const result = process("x", { language: "smalltalk" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_smithy: Smoke test: load smithy and parse a simple snippet", () => {
    const result = process("namespace example\nstring MyString", { language: "smithy" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_sml: Smoke test: load sml and parse a simple snippet", () => {
    const result = process("x", { language: "sml" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_snakemake: Smoke test: load snakemake and parse a simple snippet", () => {
    const result = process("x", { language: "snakemake" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_snl: Smoke test: load snl and parse a simple snippet", () => {
    const result = process("program test\n", { language: "snl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_solidity: Smoke test: load solidity and parse a simple snippet", () => {
    const result = process("pragma solidity ^0.8.0;\ncontract Main {}", { language: "solidity" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_soql: Smoke test: load soql and parse a simple snippet", () => {
    const result = process("SELECT Id FROM Account\n", { language: "soql" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_sosl: Smoke test: load sosl and parse a simple snippet", () => {
    const result = process("FIND {test}\n", { language: "sosl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_souffle: Smoke test: load souffle and parse a simple snippet", () => {
    const result = process("x", { language: "souffle" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_sourcepawn: Smoke test: load sourcepawn and parse a simple snippet", () => {
    const result = process("x", { language: "sourcepawn" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_sparql: Smoke test: load sparql and parse a simple snippet", () => {
    const result = process("SELECT ?s WHERE { ?s ?p ?o }", { language: "sparql" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_spicedb: Smoke test: load spicedb and parse a simple snippet", () => {
    const result = process("definition user {}\n", { language: "spicedb" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_sql: Smoke test: load sql and parse a simple snippet", () => {
    const result = process("SELECT 1;", { language: "sql" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_sql_bigquery: Smoke test: load sql_bigquery and parse a simple snippet", () => {
    const result = process("x", { language: "sql_bigquery" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_squirrel: Smoke test: load squirrel and parse a simple snippet", () => {
    const result = process("function main() {}", { language: "squirrel" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ssh_config: Smoke test: load ssh_config and parse a simple snippet", () => {
    const result = process("Host example\n  HostName example.com", { language: "ssh_config" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_stan: Smoke test: load stan and parse a simple snippet", () => {
    const result = process("x", { language: "stan" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_starlark: Smoke test: load starlark and parse a simple snippet", () => {
    const result = process("def hello(): pass", { language: "starlark" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_strace: Smoke test: load strace and parse a simple snippet", () => {
    const result = process('open("/x", O_RDONLY) = 3\n', { language: "strace" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_styled: Smoke test: load styled and parse a simple snippet", () => {
    const result = process("color: red;\n", { language: "styled" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_superhtml: Smoke test: load superhtml and parse a simple snippet", () => {
    const result = process("x", { language: "superhtml" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_svelte: Smoke test: load svelte and parse a simple snippet", () => {
    const result = process("<script>let x = 1;</script>", { language: "svelte" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_sway: Smoke test: load sway and parse a simple snippet", () => {
    const result = process("x", { language: "sway" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_swift: Smoke test: load swift and parse a simple snippet", () => {
    const result = process('print("hello")', { language: "swift" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_sxhkdrc: Smoke test: load sxhkdrc and parse a simple snippet", () => {
    const result = process("super + a\n\techo hi\n", { language: "sxhkdrc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_sysml: Smoke test: load sysml and parse a simple snippet", () => {
    const result = process("package P {}\n", { language: "sysml" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_systemtap: Smoke test: load systemtap and parse a simple snippet", () => {
    const result = process("probe begin {}\n", { language: "systemtap" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_systemverilog: Smoke test: load systemverilog and parse a simple snippet", () => {
    const result = process("x", { language: "systemverilog" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_t32: Smoke test: load t32 and parse a simple snippet", () => {
    const result = process("PRINT 1\n", { language: "t32" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_tablegen: Smoke test: load tablegen and parse a simple snippet", () => {
    const result = process("def Hello : Base {}", { language: "tablegen" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_tact: Smoke test: load tact and parse a simple snippet", () => {
    const result = process("x", { language: "tact" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_task: Smoke test: load task and parse a simple snippet", () => {
    const result = process("todo item\n", { language: "task" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_tcl: Smoke test: load tcl and parse a simple snippet", () => {
    const result = process("puts hello", { language: "tcl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_teal: Smoke test: load teal and parse a simple snippet", () => {
    const result = process("x", { language: "teal" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_templ: Smoke test: load templ and parse a simple snippet", () => {
    const result = process("x", { language: "templ" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_tera: Smoke test: load tera and parse a simple snippet", () => {
    const result = process("x", { language: "tera" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_terraform: Smoke test: load terraform and parse a simple snippet", () => {
    const result = process('resource "null_resource" "main" {}', { language: "terraform" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_test: Smoke test: load test and parse a simple snippet", () => {
    const result = process("===========\nTest\n===========\n---\n(node)", { language: "test" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_textproto: Smoke test: load textproto and parse a simple snippet", () => {
    const result = process('key: "value"', { language: "textproto" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_thrift: Smoke test: load thrift and parse a simple snippet", () => {
    const result = process("service HelloService {}", { language: "thrift" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_tlaplus: Smoke test: load tlaplus and parse a simple snippet", () => {
    const result = process("---- MODULE Main ----\n====", { language: "tlaplus" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_tmux: Smoke test: load tmux and parse a simple snippet", () => {
    const result = process("x", { language: "tmux" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_todotxt: Smoke test: load todotxt and parse a simple snippet", () => {
    const result = process("x", { language: "todotxt" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_toml: Smoke test: load toml and parse a simple snippet", () => {
    const result = process('key = "value"', { language: "toml" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_tsql: Smoke test: load tsql and parse a simple snippet", () => {
    const result = process("SELECT 1;\n", { language: "tsql" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_tsv: Smoke test: load tsv and parse a simple snippet", () => {
    const result = process("a\tb\tc\n1\t2\t3", { language: "tsv" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_tsx: Smoke test: load tsx and parse a simple snippet", () => {
    const result = process("const App = () => <div />;", { language: "tsx" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_turtle: Smoke test: load turtle and parse a simple snippet", () => {
    const result = process("x", { language: "turtle" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_twig: Smoke test: load twig and parse a simple snippet", () => {
    const result = process("{{ variable }}", { language: "twig" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_typescript: Smoke test: load typescript and parse a simple snippet", () => {
    const result = process("const x: number = 42;", { language: "typescript" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_typespec: Smoke test: load typespec and parse a simple snippet", () => {
    const result = process("x", { language: "typespec" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_typoscript: Smoke test: load typoscript and parse a simple snippet", () => {
    const result = process("x", { language: "typoscript" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_typst: Smoke test: load typst and parse a simple snippet", () => {
    const result = process("#let x = 1", { language: "typst" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_udev: Smoke test: load udev and parse a simple snippet", () => {
    const result = process('ACTION=="add", KERNEL=="sd*"', { language: "udev" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ungrammar: Smoke test: load ungrammar and parse a simple snippet", () => {
    const result = process("Root = Item*\nItem = 'token'", { language: "ungrammar" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_unison: Smoke test: load unison and parse a simple snippet", () => {
    const result = process("x = 1\n", { language: "unison" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_uxntal: Smoke test: load uxntal and parse a simple snippet", () => {
    const result = process("|0100 LIT 01", { language: "uxntal" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_v: Smoke test: load v and parse a simple snippet", () => {
    const result = process("fn main() {}", { language: "v" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_vala: Smoke test: load vala and parse a simple snippet", () => {
    const result = process("class Foo {\n}\n", { language: "vala" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_vb: Smoke test: load vb and parse a simple snippet", () => {
    const result = process("Module Main\nEnd Module", { language: "vb" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 90000);

  it("smoke_vento: Smoke test: load vento and parse a simple snippet", () => {
    const result = process("hello\n", { language: "vento" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_verilog: Smoke test: load verilog and parse a simple snippet", () => {
    const result = process("module main; endmodule", { language: "verilog" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_vhdl: Smoke test: load vhdl and parse a simple snippet", () => {
    const result = process("entity main is end main;", { language: "vhdl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_vhs: Smoke test: load vhs and parse a simple snippet", () => {
    const result = process("x", { language: "vhs" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_vim: Smoke test: load vim and parse a simple snippet", () => {
    const result = process("echo 'hello'", { language: "vim" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_vimdoc: Smoke test: load vimdoc and parse a simple snippet", () => {
    const result = process("x", { language: "vimdoc" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_vrl: Smoke test: load vrl and parse a simple snippet", () => {
    const result = process("x", { language: "vrl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_vue: Smoke test: load vue and parse a simple snippet", () => {
    const result = process("<template><div>hello</div></template>", { language: "vue" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_wast: Smoke test: load wast and parse a simple snippet", () => {
    const result = process("(module)", { language: "wast" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_wat: Smoke test: load wat and parse a simple snippet", () => {
    const result = process("(module)", { language: "wat" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_wdl: Smoke test: load wdl and parse a simple snippet", () => {
    const result = process("version 1.0\n", { language: "wdl" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_wgsl: Smoke test: load wgsl and parse a simple snippet", () => {
    const result = process("@vertex fn main() -> @builtin(position) vec4f { return vec4f(); }", {
      language: "wgsl",
    } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_wgsl_bevy: Smoke test: load wgsl_bevy and parse a simple snippet", () => {
    const result = process("x", { language: "wgsl_bevy" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_wit: Smoke test: load wit and parse a simple snippet", () => {
    const result = process("package example:pkg;", { language: "wit" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_wolfram: Smoke test: load wolfram and parse a simple snippet", () => {
    const result = process("x = 1\n", { language: "wolfram" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_x86asm: Smoke test: load x86asm and parse a simple snippet", () => {
    const result = process("x", { language: "x86asm" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_xcompose: Smoke test: load xcompose and parse a simple snippet", () => {
    const result = process('<Multi_key> <a> : "a"', { language: "xcompose" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_xit: Smoke test: load xit and parse a simple snippet", () => {
    const result = process("[ ] todo\n", { language: "xit" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_xml: Smoke test: load xml and parse a simple snippet", () => {
    const result = process('<?xml version="1.0"?>\n<root>hello</root>', { language: "xml" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_xquery: Smoke test: load xquery and parse a simple snippet", () => {
    const result = process("1\n", { language: "xquery" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_xresources: Smoke test: load xresources and parse a simple snippet", () => {
    const result = process("*.foreground: #ffffff\n", { language: "xresources" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_yaml: Smoke test: load yaml and parse a simple snippet", () => {
    const result = process("key: value", { language: "yaml" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_yang: Smoke test: load yang and parse a simple snippet", () => {
    const result = process("module m {\n}\n", { language: "yang" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_yuck: Smoke test: load yuck and parse a simple snippet", () => {
    const result = process('(defwidget main [] (label :text "hi"))', { language: "yuck" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_yul: Smoke test: load yul and parse a simple snippet", () => {
    const result = process('object "C" {\n  code {\n  }\n}\n', { language: "yul" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_zig: Smoke test: load zig and parse a simple snippet", () => {
    const result = process("pub fn main() void {}", { language: "zig" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ziggy: Smoke test: load ziggy and parse a simple snippet", () => {
    const result = process("x", { language: "ziggy" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_ziggy_schema: Smoke test: load ziggy_schema and parse a simple snippet", () => {
    const result = process("x", { language: "ziggy_schema" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);

  it("smoke_zsh: Smoke test: load zsh and parse a simple snippet", () => {
    const result = process("echo hello", { language: "zsh" } as ProcessConfig);
    expect(result).toBeDefined();
  }, 30000);
});
