// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:779c12b9aaf1c8274a491cce899f4d9b648dc0c4112b2dc56a250959bc3804ef
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { process, parseString, treeContainsNodeType } from '@kreuzberg/tree-sitter-language-pack';

describe('parsing', () => {
  it('parsing_go_function: Parse a Go function declaration and assert node type', () => {
    const tree = parseString("go", Buffer.from("package main\nfunc main() {}", "base64"));
    expect(treeContainsNodeType(tree, "function_declaration")).toBe(true);
  });

  it('parsing_html_element: Parse an HTML element and assert node type', () => {
    const tree = parseString("html", Buffer.from("<div>hello</div>", "base64"));
    expect(treeContainsNodeType(tree, "element")).toBe(true);
  });

  it('parsing_javascript_class: Parse a JavaScript class declaration.', () => {
    const tree = parseString("javascript", Buffer.from("class Foo { bar() {} }", "base64"));
    expect(tree.rootChildCount).toBeGreaterThanOrEqual(1);
    expect(treeContainsNodeType(tree, "class_declaration")).toBe(true);
  });

  it('parsing_javascript_variable: Parse a JavaScript variable declaration and assert node type', () => {
    const tree = parseString("javascript", Buffer.from("const x = 1;", "base64"));
    expect(treeContainsNodeType(tree, "lexical_declaration")).toBe(true);
  });

  it('parsing_python_function: Parse a Python function definition and assert node type', () => {
    const tree = parseString("python", Buffer.from("def hello(): pass", "base64"));
    expect(treeContainsNodeType(tree, "function_definition")).toBe(true);
  });

  it('parsing_rust_function: Parse a Rust function definition and assert node type', () => {
    const tree = parseString("rust", Buffer.from("fn main() {}", "base64"));
    expect(treeContainsNodeType(tree, "function_item")).toBe(true);
  });

  it('parsing_rust_struct: Parse a Rust struct definition.', () => {
    const tree = parseString("rust", Buffer.from("struct Point { x: f64, y: f64 }", "base64"));
    expect(tree.rootChildCount).toBeGreaterThanOrEqual(1);
  });
});
