/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:77e026bd773d6d9f86385b7ed6896453a6577be5718b969522de9f9f0a69a0a2
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 */
/* E2e tests for category: language-detection */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "ts_pack.h"
#include "test_runner.h"

void test_detect_content_bash_shebang(void) {
    /* detect_language_from_content recognizes #!/bin/bash shebang */
    char* result = ts_pack_detect_language_from_content("#!/bin/bash\necho hi");
    assert(result != NULL && str_trim_eq(result, "bash") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_content_no_shebang(void) {
    /* detect_language_from_content returns None when no shebang present */
    char* result = ts_pack_detect_language_from_content("no shebang here");
    assert(result == NULL && "expected empty/null value");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_content_python_shebang(void) {
    /* detect_language_from_content recognizes #!/usr/bin/env python3 shebang */
    char* result = ts_pack_detect_language_from_content("#!/usr/bin/env python3\npass");
    assert(result != NULL && str_trim_eq(result, "python") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_ext_cpp(void) {
    /* detect_language_from_extension recognizes .cpp as cpp */
    char* result = ts_pack_detect_language_from_extension("cpp");
    assert(result != NULL && str_trim_eq(result, "cpp") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_ext_gherkin(void) {
    /* detect_language_from_extension recognizes .feature as gherkin */
    char* result = ts_pack_detect_language_from_extension("feature");
    assert(result != NULL && str_trim_eq(result, "gherkin") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_ext_go(void) {
    /* detect_language_from_extension recognizes .go as go */
    char* result = ts_pack_detect_language_from_extension("go");
    assert(result != NULL && str_trim_eq(result, "go") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_ext_java(void) {
    /* detect_language_from_extension recognizes .java as java */
    char* result = ts_pack_detect_language_from_extension("java");
    assert(result != NULL && str_trim_eq(result, "java") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_ext_javascript(void) {
    /* detect_language_from_extension recognizes .js as javascript */
    char* result = ts_pack_detect_language_from_extension("js");
    assert(result != NULL && str_trim_eq(result, "javascript") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_ext_php(void) {
    /* detect_language_from_extension recognizes .php as php */
    char* result = ts_pack_detect_language_from_extension("php");
    assert(result != NULL && str_trim_eq(result, "php") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_ext_python(void) {
    /* detect_language_from_extension recognizes .py as python */
    char* result = ts_pack_detect_language_from_extension("py");
    assert(result != NULL && str_trim_eq(result, "python") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_ext_ruby(void) {
    /* detect_language_from_extension recognizes .rb as ruby */
    char* result = ts_pack_detect_language_from_extension("rb");
    assert(result != NULL && str_trim_eq(result, "ruby") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_ext_rust(void) {
    /* detect_language_from_extension recognizes .rs as rust */
    char* result = ts_pack_detect_language_from_extension("rs");
    assert(result != NULL && str_trim_eq(result, "rust") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_ext_typescript(void) {
    /* detect_language_from_extension recognizes .ts as typescript */
    char* result = ts_pack_detect_language_from_extension("ts");
    assert(result != NULL && str_trim_eq(result, "typescript") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_ext_unknown(void) {
    /* detect_language_from_extension returns None for unknown extension .xyz */
    char* result = ts_pack_detect_language_from_extension("xyz");
    assert(result == NULL && "expected empty/null value");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_path_dotfile(void) {
    /* detect_language_from_path returns None for dotfile .gitignore (no standard extension) */
    char* result = ts_pack_detect_language_from_path(".gitignore");
    assert(result == NULL && "expected empty/null value");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_path_go_nested(void) {
    /* detect_language_from_path extracts extension from nested path lib/server.go */
    char* result = ts_pack_detect_language_from_path("lib/server.go");
    assert(result != NULL && str_trim_eq(result, "go") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_path_java_root(void) {
    /* detect_language_from_path recognizes Main.java in root directory */
    char* result = ts_pack_detect_language_from_path("Main.java");
    assert(result != NULL && str_trim_eq(result, "java") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_path_js_root(void) {
    /* detect_language_from_path recognizes app.js in root directory */
    char* result = ts_pack_detect_language_from_path("app.js");
    assert(result != NULL && str_trim_eq(result, "javascript") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_path_nested(void) {
    /* detect_language_from_path extracts extension from nested path src/main.py */
    char* result = ts_pack_detect_language_from_path("src/main.py");
    assert(result != NULL && str_trim_eq(result, "python") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_path_no_extension(void) {
    /* detect_language_from_path returns None for extensionless file Makefile */
    char* result = ts_pack_detect_language_from_path("Makefile");
    assert(result == NULL && "expected empty/null value");
    if (result != NULL) { ts_pack_free_string(result); }
}

void test_detect_path_rust_src(void) {
    /* detect_language_from_path extracts extension from nested path src/main.rs */
    char* result = ts_pack_detect_language_from_path("src/main.rs");
    assert(result != NULL && str_trim_eq(result, "rust") == 0 && "equals assertion failed");
    if (result != NULL) { ts_pack_free_string(result); }
}
