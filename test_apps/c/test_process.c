/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:4f5d66911897a4ed6b3f5d6561f57a6ac2320a8864265ea61ce567bcae0d72ba
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 */
/* E2e tests for category: process */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "ts_pack.h"
#include "test_runner.h"

void test_c_function_process(void) {
    /* Intel: C function with include */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"c\"}");
    TS_PACKProcessResult* result = ts_pack_process("#include <stdio.h>\n\nint main() {\n    printf(\"hello\");\n    return 0;\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "c") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    assert(metrics_total_lines >= 6 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_config_all_python(void) {
    /* Intel: process with all features enabled */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("# A comment\ndef greet(name):\n    \"\"\"Say hello.\"\"\"\n    return f'Hi {name}'\n\nimport os\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    char* imports = ts_pack_process_result_imports(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(metrics_total_lines >= 6 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(imports);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_config_minimal_python(void) {
    /* Intel: process with minimal config - only metrics */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("def hello():\n    pass\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    assert(metrics_total_lines >= 2 && "expected greater than or equal");
    ts_pack_free_string(language);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_go_function_process(void) {
    /* Intel: extract structure from Go function definition */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"go\"}");
    TS_PACKProcessResult* result = ts_pack_process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    char* imports = ts_pack_process_result_imports(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "go") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(metrics_total_lines >= 7 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(imports);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_go_function_process_detail(void) {
    /* Intel: extract structure from Go function definition */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"go\"}");
    TS_PACKProcessResult* result = ts_pack_process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    char* imports = ts_pack_process_result_imports(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "go") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(metrics_total_lines >= 7 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(imports);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_java_class_process(void) {
    /* Intel: Java class with methods and imports */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"java\"}");
    TS_PACKProcessResult* result = ts_pack_process("import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return \"Hello \" + name;\n    }\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    char* imports = ts_pack_process_result_imports(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "java") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Class") != NULL && "expected to contain substring");
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(metrics_total_lines >= 7 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(imports);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_java_package_declaration_process(void) {
    /* Intel: Java package declaration emitted as Module so callers can build FQNs (#112) */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"java\"}");
    TS_PACKProcessResult* result = ts_pack_process("package com.example.widget;\n\npublic class Widget {\n    public String name() { return \"w\"; }\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "java") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    assert(structure != NULL && strstr(structure, "Module") != NULL && "expected to contain substring");
    assert(structure != NULL && strstr(structure, "Class") != NULL && "expected to contain substring");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_javascript_multi_import_process(void) {
    /* Intel: detect multiple imports and function in JavaScript */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"javascript\"}");
    TS_PACKProcessResult* result = ts_pack_process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    char* imports = ts_pack_process_result_imports(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "javascript") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    assert(metrics_total_lines >= 6 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(imports);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_javascript_multi_import_process_detail(void) {
    /* Intel: detect multiple imports and function in JavaScript */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"javascript\"}");
    TS_PACKProcessResult* result = ts_pack_process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    char* imports = ts_pack_process_result_imports(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "javascript") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    assert(metrics_total_lines >= 6 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(imports);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_kotlin_package_class_process(void) {
    /* Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112) */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"kotlin\"}");
    TS_PACKProcessResult* result = ts_pack_process("package foo.bar\n\nclass Widget {\n    fun greet(): String = \"hi\"\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "kotlin") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    assert(structure != NULL && strstr(structure, "Module") != NULL && "expected to contain substring");
    assert(structure != NULL && strstr(structure, "Class") != NULL && "expected to contain substring");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_process_javascript_exports_count(void) {
    /* JavaScript with multiple exports, verify export count */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"javascript\"}");
    TS_PACKProcessResult* result = ts_pack_process("export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* exports = ts_pack_process_result_exports(result);
    assert(str_trim_eq(language, "javascript") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(exports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(exports);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_free_string(language);
    ts_pack_free_string(exports);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_process_javascript_exports_detail(void) {
    /* JavaScript with exports, verify export count */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"javascript\"}");
    TS_PACKProcessResult* result = ts_pack_process("export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* exports = ts_pack_process_result_exports(result);
    assert(str_trim_eq(language, "javascript") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(exports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(exports);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_free_string(language);
    ts_pack_free_string(exports);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_process_python_all_features(void) {
    /* Python comprehensive source with all feature extraction enabled */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"comments\":true,\"docstrings\":true,\"imports\":true,\"language\":\"python\",\"structure\":true,\"symbols\":true}");
    TS_PACKProcessResult* result = ts_pack_process("import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    char* imports = ts_pack_process_result_imports(result);
    char* comments = ts_pack_process_result_comments(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    {
        /* count_min: count top-level JSON array elements */
        assert(comments != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(comments);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(metrics_total_lines >= 10 && "expected greater than or equal");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(imports);
    ts_pack_free_string(comments);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_process_python_comments(void) {
    /* Python with comments, verify comment count */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"comments\":true,\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* comments = ts_pack_process_result_comments(result);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(comments != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(comments);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_free_string(language);
    ts_pack_free_string(comments);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_process_python_docstrings(void) {
    /* Python with function docstring, verify docstring count */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"docstrings\":true,\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    assert(metrics_total_lines >= 3 && "expected greater than or equal");
    ts_pack_free_string(language);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_process_python_imports_detail(void) {
    /* Python with multiple imports, verify imports contain specific source */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* imports = ts_pack_process_result_imports(result);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    assert(imports != NULL && strstr(imports, "os") != NULL && "expected to contain substring");
    ts_pack_free_string(language);
    ts_pack_free_string(imports);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_process_python_metrics_detail(void) {
    /* Python code with metrics assertions */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_code_lines = ts_pack_file_metrics_code_lines(metrics_handle);
    uintptr_t metrics_comment_lines = ts_pack_file_metrics_comment_lines(metrics_handle);
    uintptr_t metrics_max_depth = ts_pack_file_metrics_max_depth(metrics_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    assert(metrics_code_lines >= 4 && "expected greater than or equal");
    assert(metrics_comment_lines >= 1 && "expected greater than or equal");
    assert(metrics_max_depth >= 1 && "expected greater than or equal");
    ts_pack_free_string(language);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_process_python_symbols(void) {
    /* Python with class and functions, verify symbol count */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"python\",\"symbols\":true}");
    TS_PACKProcessResult* result = ts_pack_process("MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* symbols = ts_pack_process_result_symbols(result);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(symbols != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(symbols);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_free_string(language);
    ts_pack_free_string(symbols);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_process_rust_structure_name(void) {
    /* Rust struct with name, verify structure name contains value */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"rust\"}");
    TS_PACKProcessResult* result = ts_pack_process("pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    assert(str_trim_eq(language, "rust") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "MyConfig") != NULL && "expected to contain substring");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_python_chunking_medium(void) {
    /* Intel: Python code with medium chunk size */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"chunk_max_size\":50,\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    char* chunks = ts_pack_process_result_chunks(result);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 3 && "expected at least 3 elements");
    }
    assert(metrics_error_count == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(chunks != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(chunks);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(chunks);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_python_chunking_process_detail(void) {
    /* Intel: chunk multi-function Python source into multiple pieces */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"chunk_max_size\":30,\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    char* chunks = ts_pack_process_result_chunks(result);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    assert(metrics_total_lines >= 8 && "expected greater than or equal");
    {
        /* count_min: count top-level JSON array elements */
        assert(chunks != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(chunks);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_free_string(language);
    ts_pack_free_string(chunks);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_python_class_with_methods_process(void) {
    /* Intel: extract nested structure from Python class with methods */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Class") != NULL && "expected to contain substring");
    assert(metrics_total_lines >= 6 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_python_class_with_methods_process_detail(void) {
    /* Intel: extract nested structure from Python class with methods */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Class") != NULL && "expected to contain substring");
    assert(metrics_total_lines >= 6 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_python_error_diagnostics(void) {
    /* Intel: Python code with syntax errors should report diagnostics */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"diagnostics\":true,\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("def broken(\n    pass\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    char* diagnostics = ts_pack_process_result_diagnostics(result);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    assert(metrics_error_count == 1 && "equals assertion failed");
    assert(diagnostics != NULL && strlen(diagnostics) > 0 && "expected non-empty value");
    ts_pack_free_string(language);
    ts_pack_free_string(diagnostics);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_python_function_process(void) {
    /* Intel: extract structure from Python function definition */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("def greet(name):\n    return f'Hello, {name}!'\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    assert(metrics_total_lines >= 2 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_python_function_process_detail(void) {
    /* Intel: extract structure from Python function definition */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("def greet(name):\n    return f'Hello, {name}!'\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    assert(metrics_total_lines >= 2 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_python_malformed_code_process(void) {
    /* Intel: detect diagnostics in malformed Python code */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"diagnostics\":true,\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("def broken(\n    return\nclass", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* diagnostics = ts_pack_process_result_diagnostics(result);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    assert(diagnostics != NULL && strlen(diagnostics) > 0 && "expected non-empty value");
    ts_pack_free_string(language);
    ts_pack_free_string(diagnostics);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_python_malformed_code_process_detail(void) {
    /* Intel: detect diagnostics in malformed Python code */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"diagnostics\":true,\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("def broken(\n    return\nclass", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* diagnostics = ts_pack_process_result_diagnostics(result);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    assert(diagnostics != NULL && strlen(diagnostics) > 0 && "expected non-empty value");
    ts_pack_free_string(language);
    ts_pack_free_string(diagnostics);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_python_multi_import_process(void) {
    /* Intel: detect multiple Python imports */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    char* imports = ts_pack_process_result_imports(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 3 && "expected at least 3 elements");
    }
    assert(metrics_total_lines >= 5 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(imports);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_python_multi_import_process_detail(void) {
    /* Intel: detect multiple Python imports */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"python\"}");
    TS_PACKProcessResult* result = ts_pack_process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    char* imports = ts_pack_process_result_imports(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 3 && "expected at least 3 elements");
    }
    assert(metrics_total_lines >= 5 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(imports);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_ruby_class_process(void) {
    /* Intel: Ruby class with method */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"ruby\"}");
    TS_PACKProcessResult* result = ts_pack_process("require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello #{name}\"\n  end\nend\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "ruby") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Class") != NULL && "expected to contain substring");
    assert(metrics_total_lines >= 7 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_rust_chunking_process(void) {
    /* Intel: chunk multi-function Rust source into pieces */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"chunk_max_size\":30,\"language\":\"rust\"}");
    TS_PACKProcessResult* result = ts_pack_process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    char* chunks = ts_pack_process_result_chunks(result);
    assert(str_trim_eq(language, "rust") == 0 && "equals assertion failed");
    assert(metrics_total_lines >= 7 && "expected greater than or equal");
    {
        /* count_min: count top-level JSON array elements */
        assert(chunks != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(chunks);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_free_string(language);
    ts_pack_free_string(chunks);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_rust_chunking_process_detail(void) {
    /* Intel: chunk multi-function Rust source into pieces */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"chunk_max_size\":30,\"language\":\"rust\"}");
    TS_PACKProcessResult* result = ts_pack_process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    char* chunks = ts_pack_process_result_chunks(result);
    assert(str_trim_eq(language, "rust") == 0 && "equals assertion failed");
    assert(metrics_total_lines >= 7 && "expected greater than or equal");
    {
        /* count_min: count top-level JSON array elements */
        assert(chunks != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(chunks);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_free_string(language);
    ts_pack_free_string(chunks);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_rust_function_process(void) {
    /* Intel: extract structure from Rust function definition */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"rust\"}");
    TS_PACKProcessResult* result = ts_pack_process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "rust") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    assert(metrics_total_lines >= 3 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_rust_function_process_detail(void) {
    /* Intel: extract structure from Rust function definition */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"rust\"}");
    TS_PACKProcessResult* result = ts_pack_process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "rust") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    assert(metrics_total_lines >= 3 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_typescript_function_process(void) {
    /* Intel: extract structure from TypeScript function */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"typescript\"}");
    TS_PACKProcessResult* result = ts_pack_process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    char* imports = ts_pack_process_result_imports(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "typescript") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(metrics_total_lines >= 5 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(imports);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}

void test_typescript_function_process_detail(void) {
    /* Intel: extract structure from TypeScript function */
    TS_PACKProcessConfig* options_handle = ts_pack_process_config_from_json("{\"language\":\"typescript\"}");
    TS_PACKProcessResult* result = ts_pack_process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_process_result_language(result);
    char* structure = ts_pack_process_result_structure(result);
    char* imports = ts_pack_process_result_imports(result);
    TS_PACKFileMetrics* metrics_handle = ts_pack_process_result_metrics(result);
    assert(metrics_handle != NULL);
    uintptr_t metrics_total_lines = ts_pack_file_metrics_total_lines(metrics_handle);
    uintptr_t metrics_error_count = ts_pack_file_metrics_error_count(metrics_handle);
    assert(str_trim_eq(language, "typescript") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(structure != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(structure);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(structure != NULL && strstr(structure, "Function") != NULL && "expected to contain substring");
    {
        /* count_min: count top-level JSON array elements */
        assert(imports != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(imports);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    assert(metrics_total_lines >= 5 && "expected greater than or equal");
    assert(metrics_error_count == 0 && "equals assertion failed");
    ts_pack_free_string(language);
    ts_pack_free_string(structure);
    ts_pack_free_string(imports);
    ts_pack_file_metrics_free(metrics_handle);
    ts_pack_process_config_free(options_handle);
    ts_pack_process_result_free(result);
}
