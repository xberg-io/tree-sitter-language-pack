// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7c6bbf972501acdaf81d61e908d599fc0fff20fb888032cf8714a7f70418311f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
//! E2e tests for category: language-detection

use tree_sitter_language_pack::{
    detect_language_from_content, detect_language_from_extension, detect_language_from_path,
};

#[test]
fn test_detect_content_bash_shebang() {
    // detect_language_from_content recognizes #!/bin/bash shebang
    let content = r#"#!/bin/bash
echo hi"#;
    let result = detect_language_from_content(content);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"bash"#, "equals assertion failed");
}

#[test]
fn test_detect_content_no_shebang() {
    // detect_language_from_content returns None when no shebang present
    let content = r#"no shebang here"#;
    let result = detect_language_from_content(content);
    assert!(result.is_none(), "expected Option to be is_none");
}

#[test]
fn test_detect_content_python_shebang() {
    // detect_language_from_content recognizes #!/usr/bin/env python3 shebang
    let content = r#"#!/usr/bin/env python3
pass"#;
    let result = detect_language_from_content(content);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"python"#, "equals assertion failed");
}

#[test]
fn test_detect_ext_cpp() {
    // detect_language_from_extension recognizes .cpp as cpp
    let ext = r#"cpp"#;
    let result = detect_language_from_extension(ext);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"cpp"#, "equals assertion failed");
}

#[test]
fn test_detect_ext_gherkin() {
    // detect_language_from_extension recognizes .feature as gherkin
    let ext = r#"feature"#;
    let result = detect_language_from_extension(ext);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"gherkin"#, "equals assertion failed");
}

#[test]
fn test_detect_ext_go() {
    // detect_language_from_extension recognizes .go as go
    let ext = r#"go"#;
    let result = detect_language_from_extension(ext);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"go"#, "equals assertion failed");
}

#[test]
fn test_detect_ext_java() {
    // detect_language_from_extension recognizes .java as java
    let ext = r#"java"#;
    let result = detect_language_from_extension(ext);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"java"#, "equals assertion failed");
}

#[test]
fn test_detect_ext_javascript() {
    // detect_language_from_extension recognizes .js as javascript
    let ext = r#"js"#;
    let result = detect_language_from_extension(ext);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(
        r.to_string().as_str().trim(),
        r#"javascript"#,
        "equals assertion failed"
    );
}

#[test]
fn test_detect_ext_php() {
    // detect_language_from_extension recognizes .php as php
    let ext = r#"php"#;
    let result = detect_language_from_extension(ext);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"php"#, "equals assertion failed");
}

#[test]
fn test_detect_ext_python() {
    // detect_language_from_extension recognizes .py as python
    let ext = r#"py"#;
    let result = detect_language_from_extension(ext);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"python"#, "equals assertion failed");
}

#[test]
fn test_detect_ext_ruby() {
    // detect_language_from_extension recognizes .rb as ruby
    let ext = r#"rb"#;
    let result = detect_language_from_extension(ext);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"ruby"#, "equals assertion failed");
}

#[test]
fn test_detect_ext_rust() {
    // detect_language_from_extension recognizes .rs as rust
    let ext = r#"rs"#;
    let result = detect_language_from_extension(ext);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"rust"#, "equals assertion failed");
}

#[test]
fn test_detect_ext_typescript() {
    // detect_language_from_extension recognizes .ts as typescript
    let ext = r#"ts"#;
    let result = detect_language_from_extension(ext);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(
        r.to_string().as_str().trim(),
        r#"typescript"#,
        "equals assertion failed"
    );
}

#[test]
fn test_detect_ext_unknown() {
    // detect_language_from_extension returns None for unknown extension .xyz
    let ext = r#"xyz"#;
    let result = detect_language_from_extension(ext);
    assert!(result.is_none(), "expected Option to be is_none");
}

#[test]
fn test_detect_path_dotfile() {
    // detect_language_from_path returns None for dotfile .gitignore (no standard extension)
    let path = r#".gitignore"#;
    let result = detect_language_from_path(path);
    assert!(result.is_none(), "expected Option to be is_none");
}

#[test]
fn test_detect_path_go_nested() {
    // detect_language_from_path extracts extension from nested path lib/server.go
    let path = r#"lib/server.go"#;
    let result = detect_language_from_path(path);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"go"#, "equals assertion failed");
}

#[test]
fn test_detect_path_java_root() {
    // detect_language_from_path recognizes Main.java in root directory
    let path = r#"Main.java"#;
    let result = detect_language_from_path(path);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"java"#, "equals assertion failed");
}

#[test]
fn test_detect_path_js_root() {
    // detect_language_from_path recognizes app.js in root directory
    let path = r#"app.js"#;
    let result = detect_language_from_path(path);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(
        r.to_string().as_str().trim(),
        r#"javascript"#,
        "equals assertion failed"
    );
}

#[test]
fn test_detect_path_nested() {
    // detect_language_from_path extracts extension from nested path src/main.py
    let path = r#"src/main.py"#;
    let result = detect_language_from_path(path);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"python"#, "equals assertion failed");
}

#[test]
fn test_detect_path_no_extension() {
    // detect_language_from_path returns None for extensionless file Makefile
    let path = r#"Makefile"#;
    let result = detect_language_from_path(path);
    assert!(result.is_none(), "expected Option to be is_none");
}

#[test]
fn test_detect_path_rust_src() {
    // detect_language_from_path extracts extension from nested path src/main.rs
    let path = r#"src/main.rs"#;
    let result = detect_language_from_path(path);
    let r = result.as_ref().expect("Option<T> should be Some");
    assert_eq!(r.to_string().as_str().trim(), r#"rust"#, "equals assertion failed");
}
