<?php
// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e4ed6c34f1ccf13edcf2a95326b6a48f660b9502efea160580280da409d1761a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code


declare(strict_types=1);

namespace Tree\Sitter\Language\Pack\E2e;

use PHPUnit\Framework\TestCase;
use Tree\Sitter\Language\Pack\TreeSitterLanguagePack;
use Tree\Sitter\Language\Pack\ProcessConfig;

/** E2e tests for category: process. */
final class ProcessTest extends TestCase
{

    /** Intel: C function with include */
    public function test_c_function_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "c"]));
        $result = TreeSitterLanguagePack::process("#include <stdio.h>\n\nint main() {\n    printf(\"hello\");\n    return 0;\n}\n", $config);
        $structure = $result->getStructure();

            $this->assertEquals("c", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(6, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: process with all features enabled */
    public function test_config_all_python(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "python"]));
        $result = TreeSitterLanguagePack::process("# A comment\ndef greet(name):\n    \"\"\"Say hello.\"\"\"\n    return f'Hi {name}'\n\nimport os\n", $config);
        $imports = $result->getImports();
        $structure = $result->getStructure();

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(1, count($imports));
            $this->assertGreaterThanOrEqual(6, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: process with minimal config - only metrics */
    public function test_config_minimal_python(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "python"]));
        $result = TreeSitterLanguagePack::process("def hello():\n    pass\n", $config);

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(2, $result->getMetrics()->totalLines);


    }


    /** Intel: extract structure from Go function definition */
    public function test_go_function_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "go"]));
        $result = TreeSitterLanguagePack::process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", $config);
        $imports = $result->getImports();
        $structure = $result->getStructure();

            $this->assertEquals("go", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(1, count($imports));
            $this->assertGreaterThanOrEqual(7, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: extract structure from Go function definition */
    public function test_go_function_process_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "go"]));
        $result = TreeSitterLanguagePack::process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", $config);
        $imports = $result->getImports();
        $structure = $result->getStructure();

            $this->assertEquals("go", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(1, count($imports));
            $this->assertGreaterThanOrEqual(7, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: Java class with methods and imports */
    public function test_java_class_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "java"]));
        $result = TreeSitterLanguagePack::process("import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return \"Hello \" + name;\n    }\n}\n", $config);
        $imports = $result->getImports();
        $structure = $result->getStructure();

            $this->assertEquals("java", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Class") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(1, count($imports));
            $this->assertGreaterThanOrEqual(7, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: Java package declaration emitted as Module so callers can build FQNs (#112) */
    public function test_java_package_declaration_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "java"]));
        $result = TreeSitterLanguagePack::process("package com.example.widget;\n\npublic class Widget {\n    public String name() { return \"w\"; }\n}\n", $config);
        $structure = $result->getStructure();

            $this->assertEquals("java", trim($result->language));
            $this->assertGreaterThanOrEqual(2, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Module") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Class") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: detect multiple imports and function in JavaScript */
    public function test_javascript_multi_import_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "javascript"]));
        $result = TreeSitterLanguagePack::process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", $config);
        $imports = $result->getImports();
        $structure = $result->getStructure();

            $this->assertEquals("javascript", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(2, count($imports));
            $this->assertGreaterThanOrEqual(6, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: detect multiple imports and function in JavaScript */
    public function test_javascript_multi_import_process_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "javascript"]));
        $result = TreeSitterLanguagePack::process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", $config);
        $imports = $result->getImports();
        $structure = $result->getStructure();

            $this->assertEquals("javascript", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(2, count($imports));
            $this->assertGreaterThanOrEqual(6, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112) */
    public function test_kotlin_package_class_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "kotlin"]));
        $result = TreeSitterLanguagePack::process("package foo.bar\n\nclass Widget {\n    fun greet(): String = \"hi\"\n}\n", $config);
        $structure = $result->getStructure();

            $this->assertEquals("kotlin", trim($result->language));
            $this->assertGreaterThanOrEqual(2, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Module") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Class") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** JavaScript with multiple exports, verify export count */
    public function test_process_javascript_exports_count(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "javascript"]));
        $result = TreeSitterLanguagePack::process("export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", $config);

            $this->assertEquals("javascript", trim($result->language));
            $this->assertGreaterThanOrEqual(2, count($result->getExports()));


    }


    /** JavaScript with exports, verify export count */
    public function test_process_javascript_exports_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "javascript"]));
        $result = TreeSitterLanguagePack::process("export function greet(name) {\n  return `Hello \${name}`;\n}\n\nexport const VERSION = '1.0';\n", $config);

            $this->assertEquals("javascript", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($result->getExports()));


    }


    /** Python comprehensive source with all feature extraction enabled */
    public function test_process_python_all_features(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["comments" => true, "docstrings" => true, "imports" => true, "language" => "python", "structure" => true, "symbols" => true]));
        $result = TreeSitterLanguagePack::process("import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", $config);
        $imports = $result->getImports();
        $structure = $result->getStructure();

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(2, count($structure));
            $this->assertGreaterThanOrEqual(1, count($imports));
            $this->assertGreaterThanOrEqual(1, count($result->getComments()));
            $this->assertGreaterThanOrEqual(10, $result->getMetrics()->totalLines);


    }


    /** Python with comments, verify comment count */
    public function test_process_python_comments(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["comments" => true, "language" => "python"]));
        $result = TreeSitterLanguagePack::process("# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", $config);

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($result->getComments()));


    }


    /** Python with function docstring, verify docstring count */
    public function test_process_python_docstrings(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["docstrings" => true, "language" => "python"]));
        $result = TreeSitterLanguagePack::process("def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", $config);

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(3, $result->getMetrics()->totalLines);


    }


    /** Python with multiple imports, verify imports contain specific source */
    public function test_process_python_imports_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "python"]));
        $result = TreeSitterLanguagePack::process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", $config);
        $imports = $result->getImports();

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(2, count($imports));
            $found = false;
    foreach ($imports as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "os") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');


    }


    /** Python code with metrics assertions */
    public function test_process_python_metrics_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "python"]));
        $result = TreeSitterLanguagePack::process("# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n", $config);

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(4, $result->getMetrics()->codeLines);
            $this->assertGreaterThanOrEqual(1, $result->getMetrics()->commentLines);
            $this->assertGreaterThanOrEqual(1, $result->getMetrics()->maxDepth);


    }


    /** Python with class and functions, verify symbol count */
    public function test_process_python_symbols(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "python", "symbols" => true]));
        $result = TreeSitterLanguagePack::process("MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n", $config);

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($result->getSymbols()));


    }


    /** Rust struct with name, verify structure name contains value */
    public function test_process_rust_structure_name(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "rust"]));
        $result = TreeSitterLanguagePack::process("pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", $config);
        $structure = $result->getStructure();

            $this->assertEquals("rust", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "MyConfig") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');


    }


    /** Intel: Python code with medium chunk size */
    public function test_python_chunking_medium(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["chunkMaxSize" => 50, "language" => "python"]));
        $result = TreeSitterLanguagePack::process("def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n", $config);
        $chunks = $result->getChunks();
        $structure = $result->getStructure();

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(3, count($structure));
            $this->assertEquals(0, $result->getMetrics()->errorCount);
            $this->assertGreaterThanOrEqual(2, count($chunks));


    }


    /** Intel: chunk multi-function Python source into multiple pieces */
    public function test_python_chunking_process_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["chunkMaxSize" => 30, "language" => "python"]));
        $result = TreeSitterLanguagePack::process("def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n", $config);
        $chunks = $result->getChunks();

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(8, $result->getMetrics()->totalLines);
            $this->assertGreaterThanOrEqual(2, count($chunks));


    }


    /** Intel: extract nested structure from Python class with methods */
    public function test_python_class_with_methods_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "python"]));
        $result = TreeSitterLanguagePack::process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", $config);
        $structure = $result->getStructure();

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Class") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(6, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: extract nested structure from Python class with methods */
    public function test_python_class_with_methods_process_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "python"]));
        $result = TreeSitterLanguagePack::process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", $config);
        $structure = $result->getStructure();

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Class") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(6, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: Python code with syntax errors should report diagnostics */
    public function test_python_error_diagnostics(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["diagnostics" => true, "language" => "python"]));
        $result = TreeSitterLanguagePack::process("def broken(\n    pass\n", $config);

            $this->assertEquals("python", trim($result->language));
            $this->assertEquals(1, $result->getMetrics()->errorCount);
            $this->assertNotEmpty($result->getDiagnostics());


    }


    /** Intel: extract structure from Python function definition */
    public function test_python_function_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "python"]));
        $result = TreeSitterLanguagePack::process("def greet(name):\n    return f'Hello, {name}!'\n", $config);
        $structure = $result->getStructure();

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(2, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: extract structure from Python function definition */
    public function test_python_function_process_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "python"]));
        $result = TreeSitterLanguagePack::process("def greet(name):\n    return f'Hello, {name}!'\n", $config);
        $structure = $result->getStructure();

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(2, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: detect diagnostics in malformed Python code */
    public function test_python_malformed_code_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["diagnostics" => true, "language" => "python"]));
        $result = TreeSitterLanguagePack::process("def broken(\n    return\nclass", $config);

            $this->assertEquals("python", trim($result->language));
            $this->assertNotEmpty($result->getDiagnostics());


    }


    /** Intel: detect diagnostics in malformed Python code */
    public function test_python_malformed_code_process_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["diagnostics" => true, "language" => "python"]));
        $result = TreeSitterLanguagePack::process("def broken(\n    return\nclass", $config);

            $this->assertEquals("python", trim($result->language));
            $this->assertNotEmpty($result->getDiagnostics());


    }


    /** Intel: detect multiple Python imports */
    public function test_python_multi_import_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "python"]));
        $result = TreeSitterLanguagePack::process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", $config);
        $imports = $result->getImports();
        $structure = $result->getStructure();

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $this->assertGreaterThanOrEqual(3, count($imports));
            $this->assertGreaterThanOrEqual(5, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: detect multiple Python imports */
    public function test_python_multi_import_process_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "python"]));
        $result = TreeSitterLanguagePack::process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", $config);
        $imports = $result->getImports();
        $structure = $result->getStructure();

            $this->assertEquals("python", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $this->assertGreaterThanOrEqual(3, count($imports));
            $this->assertGreaterThanOrEqual(5, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: Ruby class with method */
    public function test_ruby_class_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "ruby"]));
        $result = TreeSitterLanguagePack::process("require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello #{name}\"\n  end\nend\n", $config);
        $structure = $result->getStructure();

            $this->assertEquals("ruby", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Class") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(7, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: chunk multi-function Rust source into pieces */
    public function test_rust_chunking_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["chunkMaxSize" => 30, "language" => "rust"]));
        $result = TreeSitterLanguagePack::process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", $config);
        $chunks = $result->getChunks();

            $this->assertEquals("rust", trim($result->language));
            $this->assertGreaterThanOrEqual(7, $result->getMetrics()->totalLines);
            $this->assertGreaterThanOrEqual(2, count($chunks));


    }


    /** Intel: chunk multi-function Rust source into pieces */
    public function test_rust_chunking_process_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["chunkMaxSize" => 30, "language" => "rust"]));
        $result = TreeSitterLanguagePack::process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", $config);
        $chunks = $result->getChunks();

            $this->assertEquals("rust", trim($result->language));
            $this->assertGreaterThanOrEqual(7, $result->getMetrics()->totalLines);
            $this->assertGreaterThanOrEqual(2, count($chunks));


    }


    /** Intel: extract structure from Rust function definition */
    public function test_rust_function_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "rust"]));
        $result = TreeSitterLanguagePack::process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", $config);
        $structure = $result->getStructure();

            $this->assertEquals("rust", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(3, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: extract structure from Rust function definition */
    public function test_rust_function_process_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "rust"]));
        $result = TreeSitterLanguagePack::process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", $config);
        $structure = $result->getStructure();

            $this->assertEquals("rust", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(3, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: extract structure from TypeScript function */
    public function test_typescript_function_process(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "typescript"]));
        $result = TreeSitterLanguagePack::process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, \${name}!`;\n}\n", $config);
        $imports = $result->getImports();
        $structure = $result->getStructure();

            $this->assertEquals("typescript", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(1, count($imports));
            $this->assertGreaterThanOrEqual(5, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }


    /** Intel: extract structure from TypeScript function */
    public function test_typescript_function_process_detail(): void
    {
        $config = \Tree\Sitter\Language\Pack\ProcessConfig::from_json(json_encode(["language" => "typescript"]));
        $result = TreeSitterLanguagePack::process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, \${name}!`;\n}\n", $config);
        $imports = $result->getImports();
        $structure = $result->getStructure();

            $this->assertEquals("typescript", trim($result->language));
            $this->assertGreaterThanOrEqual(1, count($structure));
            $found = false;
    foreach ($structure as $item) {
        $itemStr = is_object($item) ? json_encode($item) : (string)$item;
        if (stripos($itemStr, "Function") !== false) { $found = true; }
    }
    $this->assertTrue($found, 'expected array to contain string');
            $this->assertGreaterThanOrEqual(1, count($imports));
            $this->assertGreaterThanOrEqual(5, $result->getMetrics()->totalLines);
            $this->assertEquals(0, $result->getMetrics()->errorCount);


    }

}
