// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:adeee8a85e5bda7cd10c79d02c126e0dc5cb5391174cf393ee64a9e2ce961814
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
const std = @import("std");
const testing = std.testing;
const tree_sitter_language_pack = @import("tree_sitter_language_pack");

// Suppress C++ global destructor aborts that break zig's --listen=- IPC
extern "c" fn signal(sig: i32, handler: usize) usize;
var _abort_handler_installed: bool = false;
fn suppress_abort() void {
    if (!_abort_handler_installed) {
        // SIGABRT = 6 on POSIX; SIG_IGN = 1
        _ = signal(6, 1);
        _abort_handler_installed = true;
    }
}

// E2e tests for category: process

test "c_function_process" {
    // Intel: C function with include
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("#include <stdio.h>\n\nint main() {\n    printf(\"hello\");\n    return 0;\n}\n", "{\"language\":\"c\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("c", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 6));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "config_all_python" {
    // Intel: process with all features enabled
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("# A comment\ndef greet(name):\n    \"\"\"Say hello.\"\"\"\n    return f'Hi {name}'\n\nimport os\n", "{\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 1));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 6));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "config_minimal_python" {
    // Intel: process with minimal config - only metrics
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("def hello():\n    pass\n", "{\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 2));
}

test "go_function_process" {
    // Intel: extract structure from Go function definition
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", "{\"language\":\"go\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("go", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 1));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 7));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "go_function_process_detail" {
    // Intel: extract structure from Go function definition
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", "{\"language\":\"go\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("go", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 1));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 7));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "java_class_process" {
    // Intel: Java class with methods and imports
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return \"Hello \" + name;\n    }\n}\n", "{\"language\":\"java\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("java", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Class") != null);
    }
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 1));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 7));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "java_package_declaration_process" {
    // Intel: Java package declaration emitted as Module so callers can build FQNs (#112)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("package com.example.widget;\n\npublic class Widget {\n    public String name() { return \"w\"; }\n}\n", "{\"language\":\"java\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("java", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 2));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Module") != null);
    }
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Class") != null);
    }
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "javascript_multi_import_process" {
    // Intel: detect multiple imports and function in JavaScript
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", "{\"language\":\"javascript\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("javascript", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 2));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 6));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "javascript_multi_import_process_detail" {
    // Intel: detect multiple imports and function in JavaScript
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", "{\"language\":\"javascript\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("javascript", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 2));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 6));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "kotlin_package_class_process" {
    // Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("package foo.bar\n\nclass Widget {\n    fun greet(): String = \"hi\"\n}\n", "{\"language\":\"kotlin\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("kotlin", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 2));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Module") != null);
    }
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Class") != null);
    }
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "process_javascript_exports_count" {
    // JavaScript with multiple exports, verify export count
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", "{\"language\":\"javascript\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("javascript", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("exports").?.array.items.len >= @as(usize, 2));
}

test "process_javascript_exports_detail" {
    // JavaScript with exports, verify export count
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n", "{\"language\":\"javascript\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("javascript", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("exports").?.array.items.len >= @as(usize, 1));
}

test "process_python_all_features" {
    // Python comprehensive source with all feature extraction enabled
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", "{\"comments\":true,\"docstrings\":true,\"imports\":true,\"language\":\"python\",\"structure\":true,\"symbols\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 2));
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 1));
    try testing.expect(result.object.get("comments").?.array.items.len >= @as(usize, 1));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 10));
}

test "process_python_comments" {
    // Python with comments, verify comment count
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", "{\"comments\":true,\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("comments").?.array.items.len >= @as(usize, 1));
}

test "process_python_docstrings" {
    // Python with function docstring, verify docstring count
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", "{\"docstrings\":true,\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 3));
}

test "process_python_imports_detail" {
    // Python with multiple imports, verify imports contain specific source
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", "{\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 2));
    {
        const _jv = result.object.get("imports").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "os") != null);
    }
}

test "process_python_metrics_detail" {
    // Python code with metrics assertions
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n", "{\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("metrics").?.object.get("code_lines").?.integer >= @as(i64, 4));
    try testing.expect(result.object.get("metrics").?.object.get("comment_lines").?.integer >= @as(i64, 1));
    try testing.expect(result.object.get("metrics").?.object.get("max_depth").?.integer >= @as(i64, 1));
}

test "process_python_symbols" {
    // Python with class and functions, verify symbol count
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n", "{\"language\":\"python\",\"symbols\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("symbols").?.array.items.len >= @as(usize, 1));
}

test "process_rust_structure_name" {
    // Rust struct with name, verify structure name contains value
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", "{\"language\":\"rust\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("rust", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "MyConfig") != null);
    }
}

test "python_chunking_medium" {
    // Intel: Python code with medium chunk size
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n", "{\"chunk_max_size\":50,\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 3));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
    try testing.expect(result.object.get("chunks").?.array.items.len >= @as(usize, 2));
}

test "python_chunking_process_detail" {
    // Intel: chunk multi-function Python source into multiple pieces
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n", "{\"chunk_max_size\":30,\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 8));
    try testing.expect(result.object.get("chunks").?.array.items.len >= @as(usize, 2));
}

test "python_class_with_methods_process" {
    // Intel: extract nested structure from Python class with methods
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", "{\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Class") != null);
    }
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 6));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "python_class_with_methods_process_detail" {
    // Intel: extract nested structure from Python class with methods
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", "{\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Class") != null);
    }
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 6));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "python_error_diagnostics" {
    // Intel: Python code with syntax errors should report diagnostics
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("def broken(\n    pass\n", "{\"diagnostics\":true,\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expectEqual(@as(i64, 1), result.object.get("metrics").?.object.get("error_count").?.integer);
    try testing.expect(result.object.get("diagnostics").? != .null);
}

test "python_function_process" {
    // Intel: extract structure from Python function definition
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("def greet(name):\n    return f'Hello, {name}!'\n", "{\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 2));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "python_function_process_detail" {
    // Intel: extract structure from Python function definition
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("def greet(name):\n    return f'Hello, {name}!'\n", "{\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 2));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "python_malformed_code_process" {
    // Intel: detect diagnostics in malformed Python code
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("def broken(\n    return\nclass", "{\"diagnostics\":true,\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("diagnostics").? != .null);
}

test "python_malformed_code_process_detail" {
    // Intel: detect diagnostics in malformed Python code
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("def broken(\n    return\nclass", "{\"diagnostics\":true,\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("diagnostics").? != .null);
}

test "python_multi_import_process" {
    // Intel: detect multiple Python imports
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", "{\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 3));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 5));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "python_multi_import_process_detail" {
    // Intel: detect multiple Python imports
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", "{\"language\":\"python\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("python", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 3));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 5));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "ruby_class_process" {
    // Intel: Ruby class with method
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello #{name}\"\n  end\nend\n", "{\"language\":\"ruby\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("ruby", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Class") != null);
    }
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 7));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "rust_chunking_process" {
    // Intel: chunk multi-function Rust source into pieces
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", "{\"chunk_max_size\":30,\"language\":\"rust\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("rust", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 7));
    try testing.expect(result.object.get("chunks").?.array.items.len >= @as(usize, 2));
}

test "rust_chunking_process_detail" {
    // Intel: chunk multi-function Rust source into pieces
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", "{\"chunk_max_size\":30,\"language\":\"rust\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("rust", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 7));
    try testing.expect(result.object.get("chunks").?.array.items.len >= @as(usize, 2));
}

test "rust_function_process" {
    // Intel: extract structure from Rust function definition
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", "{\"language\":\"rust\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("rust", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 3));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "rust_function_process_detail" {
    // Intel: extract structure from Rust function definition
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", "{\"language\":\"rust\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("rust", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 3));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "typescript_function_process" {
    // Intel: extract structure from TypeScript function
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", "{\"language\":\"typescript\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("typescript", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 1));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 5));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}

test "typescript_function_process_detail" {
    // Intel: extract structure from TypeScript function
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try tree_sitter_language_pack.process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", "{\"language\":\"typescript\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("typescript", std.mem.trim(u8, result.object.get("language").?.string, " \n\r\t"));
    try testing.expect(result.object.get("structure").?.array.items.len >= @as(usize, 1));
    {
        const _jv = result.object.get("structure").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Function") != null);
    }
    try testing.expect(result.object.get("imports").?.array.items.len >= @as(usize, 1));
    try testing.expect(result.object.get("metrics").?.object.get("total_lines").?.integer >= @as(i64, 5));
    try testing.expectEqual(@as(i64, 0), result.object.get("metrics").?.object.get("error_count").?.integer);
}
