// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c4a80cf158d12cd198d149d1570f2761b6432b11febc4f0b5e92a7d90977d71e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

package dev.kreuzberg.treesitterlanguagepack.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.treesitterlanguagepack.TreeSitterLanguagePack;
import dev.kreuzberg.treesitterlanguagepack.ProcessConfig;
import java.util.Optional;
import dev.kreuzberg.treesitterlanguagepack.JsonUtil;

/** E2e tests for category: error-handling. */
public class ErrorHandlingTest {

    @Test
    void testErrorDetectContentEmpty() throws Exception {
        // Detect language from empty content returns null
        var result = TreeSitterLanguagePack.detectLanguageFromContent("");
        assertNull(result, "expected empty value");


    }


    @Test
    void testErrorDetectExtensionEmpty() throws Exception {
        // Detect language from empty extension returns null
        var result = TreeSitterLanguagePack.detectLanguageFromExtension("");
        assertNull(result, "expected empty value");


    }


    @Test
    void testErrorDetectPathEmpty() throws Exception {
        // Detect language from empty path returns null
        var result = TreeSitterLanguagePack.detectLanguageFromPath("");
        assertNull(result, "expected empty value");


    }


    @Test
    void testErrorEmptyLanguageName() throws Exception {
        // Parsing with empty language name should error
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var config = JsonUtil.fromJson("{\"language\":\"\"}", ProcessConfig.class);

            TreeSitterLanguagePack.process("hello", config);
        });

    }


    @Test
    void testErrorHandlingEmptySource() throws Exception {
        // Parsing an empty string should still produce a tree.
        var config = JsonUtil.fromJson("{\"language\":\"javascript\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("", config);


    }


    @Test
    void testErrorHandlingGetLanguageEmptyString() throws Exception {
        // get_language with empty string returns an error
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
            TreeSitterLanguagePack.getLanguage("");
        });

    }


    @Test
    void testErrorHandlingHaskellUnterminatedBlockComment() throws Exception {
        // Regression: unterminated nested Haskell block comment must not crash the process. On gcc/aarch64 at -O2 with a pre-0.26.4 vendored array.h and without -fno-strict-aliasing, this input triggers heap corruption (malloc: mismatching next->prev_size) via a strict-aliasing miscompilation in the scanner's array_push hot loop.
        var config = JsonUtil.fromJson("{\"language\":\"haskell\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("{-aaaaaaaaaaaaaa aaaa}\n    {-aaa (aaaaaaaaaa [aaaaaaaaaaaaa aaa", config);


    }


    @Test
    void testErrorHandlingInvalidSyntax() throws Exception {
        // Parsing invalid syntax should produce a tree with error nodes.
        var config = JsonUtil.fromJson("{\"language\":\"javascript\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("function function function @@@ %%%", config);


    }


    @Test
    void testErrorHandlingUnknownLanguage() throws Exception {
        // Loading a nonexistent language should produce an error.
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var config = JsonUtil.fromJson("{\"language\":\"nonexistent_xyz\"}", ProcessConfig.class);

            TreeSitterLanguagePack.process("", config);
        });

    }


    @Test
    void testErrorProcessEmptySource() throws Exception {
        // Process empty source code succeeds with zero metrics
        var config = JsonUtil.fromJson("{\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("", config);
assertEquals(0, result.metrics().totalLines());

    }


    @Test
    void testParseEmptyLanguage() throws Exception {
        // process() returns error with empty language name
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var config = JsonUtil.fromJson("{\"language\":\"\"}", ProcessConfig.class);

            TreeSitterLanguagePack.process("x = 1", config);
        });

    }


    @Test
    void testProcessUnknownLanguage() throws Exception {
        // process() returns error when config language is unknown
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var config = JsonUtil.fromJson("{\"language\":\"nonexistent_xyz\"}", ProcessConfig.class);

            TreeSitterLanguagePack.process("x = 1", config);
        });

    }

}
