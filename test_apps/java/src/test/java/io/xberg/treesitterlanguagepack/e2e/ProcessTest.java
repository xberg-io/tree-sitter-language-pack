// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ef492b9fed5c51f426bf570aead00ef7d3ce2a7f5a5e958558fb1d48e9c8a6a5
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

package io.xberg.treesitterlanguagepack.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.treesitterlanguagepack.TreeSitterLanguagePack;
import io.xberg.treesitterlanguagepack.ProcessConfig;
import java.util.Optional;
import io.xberg.treesitterlanguagepack.JsonUtil;

/** E2e tests for category: process. */
public class ProcessTest {

    @Test
    void testCFunctionProcess() throws Exception {
        // Intel: C function with include
        var config = JsonUtil.fromJson("{\"language\":\"c\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("#include <stdio.h>\n\nint main() {\n    printf(\"hello\");\n    return 0;\n}\n", config);
assertEquals("c", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.metrics().totalLines() >= 6, "expected >= 6");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testConfigAllPython() throws Exception {
        // Intel: process with all features enabled
        var config = JsonUtil.fromJson("{\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("# A comment\ndef greet(name):\n    \"\"\"Say hello.\"\"\"\n    return f'Hi {name}'\n\nimport os\n", config);
assertEquals("python", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.imports().size() >= 1, "expected at least 1 elements");assertTrue(result.metrics().totalLines() >= 6, "expected >= 6");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testConfigMinimalPython() throws Exception {
        // Intel: process with minimal config - only metrics
        var config = JsonUtil.fromJson("{\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("def hello():\n    pass\n", config);
assertEquals("python", result.language().trim());assertTrue(result.metrics().totalLines() >= 2, "expected >= 2");

    }


    @Test
    void testGoFunctionProcess() throws Exception {
        // Intel: extract structure from Go function definition
        var config = JsonUtil.fromJson("{\"language\":\"go\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", config);
assertEquals("go", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.imports().size() >= 1, "expected at least 1 elements");assertTrue(result.metrics().totalLines() >= 7, "expected >= 7");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testGoFunctionProcessDetail() throws Exception {
        // Intel: extract structure from Go function definition
        var config = JsonUtil.fromJson("{\"language\":\"go\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n", config);
assertEquals("go", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.imports().size() >= 1, "expected at least 1 elements");assertTrue(result.metrics().totalLines() >= 7, "expected >= 7");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testJavaClassProcess() throws Exception {
        // Intel: Java class with methods and imports
        var config = JsonUtil.fromJson("{\"language\":\"java\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return \"Hello \" + name;\n    }\n}\n", config);
assertEquals("java", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Class").toLowerCase()), "expected to contain: " + "Class");assertTrue(result.imports().size() >= 1, "expected at least 1 elements");assertTrue(result.metrics().totalLines() >= 7, "expected >= 7");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testJavaPackageDeclarationProcess() throws Exception {
        // Intel: Java package declaration emitted as Module so callers can build FQNs (#112)
        var config = JsonUtil.fromJson("{\"language\":\"java\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("package com.example.widget;\n\npublic class Widget {\n    public String name() { return \"w\"; }\n}\n", config);
assertEquals("java", result.language().trim());assertTrue(result.structure().size() >= 2, "expected at least 2 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Module").toLowerCase()), "expected to contain: " + "Module");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Class").toLowerCase()), "expected to contain: " + "Class");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testJavascriptMultiImportProcess() throws Exception {
        // Intel: detect multiple imports and function in JavaScript
        var config = JsonUtil.fromJson("{\"language\":\"javascript\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", config);
assertEquals("javascript", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.imports().size() >= 2, "expected at least 2 elements");assertTrue(result.metrics().totalLines() >= 6, "expected >= 6");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testJavascriptMultiImportProcessDetail() throws Exception {
        // Intel: detect multiple imports and function in JavaScript
        var config = JsonUtil.fromJson("{\"language\":\"javascript\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n", config);
assertEquals("javascript", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.imports().size() >= 2, "expected at least 2 elements");assertTrue(result.metrics().totalLines() >= 6, "expected >= 6");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testKotlinPackageClassProcess() throws Exception {
        // Intel: Kotlin package header emitted as Module and class name resolved via type_identifier fallback (#111, #112)
        var config = JsonUtil.fromJson("{\"language\":\"kotlin\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("package foo.bar\n\nclass Widget {\n    fun greet(): String = \"hi\"\n}\n", config);
assertEquals("kotlin", result.language().trim());assertTrue(result.structure().size() >= 2, "expected at least 2 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Module").toLowerCase()), "expected to contain: " + "Module");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Class").toLowerCase()), "expected to contain: " + "Class");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testProcessJavascriptExportsCount() throws Exception {
        // JavaScript with multiple exports, verify export count
        var config = JsonUtil.fromJson("{\"language\":\"javascript\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", config);
assertEquals("javascript", result.language().trim());assertTrue(result.exports().size() >= 2, "expected at least 2 elements");

    }


    @Test
    void testProcessJavascriptExportsDetail() throws Exception {
        // JavaScript with exports, verify export count
        var config = JsonUtil.fromJson("{\"language\":\"javascript\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n", config);
assertEquals("javascript", result.language().trim());assertTrue(result.exports().size() >= 1, "expected at least 1 elements");

    }


    @Test
    void testProcessPythonAllFeatures() throws Exception {
        // Python comprehensive source with all feature extraction enabled
        var config = JsonUtil.fromJson("{\"comments\":true,\"docstrings\":true,\"imports\":true,\"language\":\"python\",\"structure\":true,\"symbols\":true}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", config);
assertEquals("python", result.language().trim());assertTrue(result.structure().size() >= 2, "expected at least 2 elements");assertTrue(result.imports().size() >= 1, "expected at least 1 elements");assertTrue(result.comments().size() >= 1, "expected at least 1 elements");assertTrue(result.metrics().totalLines() >= 10, "expected >= 10");

    }


    @Test
    void testProcessPythonComments() throws Exception {
        // Python with comments, verify comment count
        var config = JsonUtil.fromJson("{\"comments\":true,\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", config);
assertEquals("python", result.language().trim());assertTrue(result.comments().size() >= 1, "expected at least 1 elements");

    }


    @Test
    void testProcessPythonDocstrings() throws Exception {
        // Python with function docstring, verify docstring count
        var config = JsonUtil.fromJson("{\"docstrings\":true,\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", config);
assertEquals("python", result.language().trim());assertTrue(result.metrics().totalLines() >= 3, "expected >= 3");

    }


    @Test
    void testProcessPythonImportsDetail() throws Exception {
        // Python with multiple imports, verify imports contain specific source
        var config = JsonUtil.fromJson("{\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", config);
assertEquals("python", result.language().trim());assertTrue(result.imports().size() >= 2, "expected at least 2 elements");assertTrue(result.imports().toString().toLowerCase().contains(String.valueOf("os").toLowerCase()), "expected to contain: " + "os");

    }


    @Test
    void testProcessPythonMetricsDetail() throws Exception {
        // Python code with metrics assertions
        var config = JsonUtil.fromJson("{\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n", config);
assertEquals("python", result.language().trim());assertTrue(result.metrics().codeLines() >= 4, "expected >= 4");assertTrue(result.metrics().commentLines() >= 1, "expected >= 1");assertTrue(result.metrics().maxDepth() >= 1, "expected >= 1");

    }


    @Test
    void testProcessPythonSymbols() throws Exception {
        // Python with class and functions, verify symbol count
        var config = JsonUtil.fromJson("{\"language\":\"python\",\"symbols\":true}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n", config);
assertEquals("python", result.language().trim());assertTrue(result.symbols().size() >= 1, "expected at least 1 elements");

    }


    @Test
    void testProcessRustStructureName() throws Exception {
        // Rust struct with name, verify structure name contains value
        var config = JsonUtil.fromJson("{\"language\":\"rust\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", config);
assertEquals("rust", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("MyConfig").toLowerCase()), "expected to contain: " + "MyConfig");

    }


    @Test
    void testPythonChunkingMedium() throws Exception {
        // Intel: Python code with medium chunk size
        var config = JsonUtil.fromJson("{\"chunk_max_size\":50,\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n", config);
assertEquals("python", result.language().trim());assertTrue(result.structure().size() >= 3, "expected at least 3 elements");assertEquals(0, result.metrics().errorCount());assertTrue(result.chunks().size() >= 2, "expected at least 2 elements");

    }


    @Test
    void testPythonChunkingProcessDetail() throws Exception {
        // Intel: chunk multi-function Python source into multiple pieces
        var config = JsonUtil.fromJson("{\"chunk_max_size\":30,\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n", config);
assertEquals("python", result.language().trim());assertTrue(result.metrics().totalLines() >= 8, "expected >= 8");assertTrue(result.chunks().size() >= 2, "expected at least 2 elements");

    }


    @Test
    void testPythonClassWithMethodsProcess() throws Exception {
        // Intel: extract nested structure from Python class with methods
        var config = JsonUtil.fromJson("{\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", config);
assertEquals("python", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Class").toLowerCase()), "expected to contain: " + "Class");assertTrue(result.metrics().totalLines() >= 6, "expected >= 6");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testPythonClassWithMethodsProcessDetail() throws Exception {
        // Intel: extract nested structure from Python class with methods
        var config = JsonUtil.fromJson("{\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n", config);
assertEquals("python", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Class").toLowerCase()), "expected to contain: " + "Class");assertTrue(result.metrics().totalLines() >= 6, "expected >= 6");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testPythonErrorDiagnostics() throws Exception {
        // Intel: Python code with syntax errors should report diagnostics
        var config = JsonUtil.fromJson("{\"diagnostics\":true,\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("def broken(\n    pass\n", config);
assertEquals("python", result.language().trim());assertEquals(1, result.metrics().errorCount());assertFalse(result.diagnostics().isEmpty(), "expected non-empty value");

    }


    @Test
    void testPythonFunctionProcess() throws Exception {
        // Intel: extract structure from Python function definition
        var config = JsonUtil.fromJson("{\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("def greet(name):\n    return f'Hello, {name}!'\n", config);
assertEquals("python", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.metrics().totalLines() >= 2, "expected >= 2");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testPythonFunctionProcessDetail() throws Exception {
        // Intel: extract structure from Python function definition
        var config = JsonUtil.fromJson("{\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("def greet(name):\n    return f'Hello, {name}!'\n", config);
assertEquals("python", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.metrics().totalLines() >= 2, "expected >= 2");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testPythonMalformedCodeProcess() throws Exception {
        // Intel: detect diagnostics in malformed Python code
        var config = JsonUtil.fromJson("{\"diagnostics\":true,\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("def broken(\n    return\nclass", config);
assertEquals("python", result.language().trim());assertFalse(result.diagnostics().isEmpty(), "expected non-empty value");

    }


    @Test
    void testPythonMalformedCodeProcessDetail() throws Exception {
        // Intel: detect diagnostics in malformed Python code
        var config = JsonUtil.fromJson("{\"diagnostics\":true,\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("def broken(\n    return\nclass", config);
assertEquals("python", result.language().trim());assertFalse(result.diagnostics().isEmpty(), "expected non-empty value");

    }


    @Test
    void testPythonMultiImportProcess() throws Exception {
        // Intel: detect multiple Python imports
        var config = JsonUtil.fromJson("{\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", config);
assertEquals("python", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.imports().size() >= 3, "expected at least 3 elements");assertTrue(result.metrics().totalLines() >= 5, "expected >= 5");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testPythonMultiImportProcessDetail() throws Exception {
        // Intel: detect multiple Python imports
        var config = JsonUtil.fromJson("{\"language\":\"python\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", config);
assertEquals("python", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.imports().size() >= 3, "expected at least 3 elements");assertTrue(result.metrics().totalLines() >= 5, "expected >= 5");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testRubyClassProcess() throws Exception {
        // Intel: Ruby class with method
        var config = JsonUtil.fromJson("{\"language\":\"ruby\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello #{name}\"\n  end\nend\n", config);
assertEquals("ruby", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Class").toLowerCase()), "expected to contain: " + "Class");assertTrue(result.metrics().totalLines() >= 7, "expected >= 7");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testRustChunkingProcess() throws Exception {
        // Intel: chunk multi-function Rust source into pieces
        var config = JsonUtil.fromJson("{\"chunk_max_size\":30,\"language\":\"rust\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", config);
assertEquals("rust", result.language().trim());assertTrue(result.metrics().totalLines() >= 7, "expected >= 7");assertTrue(result.chunks().size() >= 2, "expected at least 2 elements");

    }


    @Test
    void testRustChunkingProcessDetail() throws Exception {
        // Intel: chunk multi-function Rust source into pieces
        var config = JsonUtil.fromJson("{\"chunk_max_size\":30,\"language\":\"rust\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", config);
assertEquals("rust", result.language().trim());assertTrue(result.metrics().totalLines() >= 7, "expected >= 7");assertTrue(result.chunks().size() >= 2, "expected at least 2 elements");

    }


    @Test
    void testRustFunctionProcess() throws Exception {
        // Intel: extract structure from Rust function definition
        var config = JsonUtil.fromJson("{\"language\":\"rust\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", config);
assertEquals("rust", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.metrics().totalLines() >= 3, "expected >= 3");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testRustFunctionProcessDetail() throws Exception {
        // Intel: extract structure from Rust function definition
        var config = JsonUtil.fromJson("{\"language\":\"rust\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", config);
assertEquals("rust", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.metrics().totalLines() >= 3, "expected >= 3");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testTypescriptFunctionProcess() throws Exception {
        // Intel: extract structure from TypeScript function
        var config = JsonUtil.fromJson("{\"language\":\"typescript\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", config);
assertEquals("typescript", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.imports().size() >= 1, "expected at least 1 elements");assertTrue(result.metrics().totalLines() >= 5, "expected >= 5");assertEquals(0, result.metrics().errorCount());

    }


    @Test
    void testTypescriptFunctionProcessDetail() throws Exception {
        // Intel: extract structure from TypeScript function
        var config = JsonUtil.fromJson("{\"language\":\"typescript\"}", ProcessConfig.class);

        var result = TreeSitterLanguagePack.process("import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n", config);
assertEquals("typescript", result.language().trim());assertTrue(result.structure().size() >= 1, "expected at least 1 elements");assertTrue(result.structure().toString().toLowerCase().contains(String.valueOf("Function").toLowerCase()), "expected to contain: " + "Function");assertTrue(result.imports().size() >= 1, "expected at least 1 elements");assertTrue(result.metrics().totalLines() >= 5, "expected >= 5");assertEquals(0, result.metrics().errorCount());

    }

}
