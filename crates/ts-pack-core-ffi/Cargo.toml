# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:9d39918e5602cb509085dd5cfbd28ec2632fa0a3586adbe91513e4ca6a90759e
# To regenerate: alef generate
# To verify freshness: alef verify

[package]
name = "ts-pack-core-ffi"
version = "1.19.0"
edition = "2024"
license = "MIT"
description = "Pre-compiled tree-sitter grammars for 371 programming languages"
readme = false
keywords = ["language-pack", "parser", "syntax", "tree-sitter"]
categories = []
repository = "https://github.com/xberg-io/tree-sitter-language-pack"

# `serde`, `serde_json`, `ahash`, and `tokio` are emitted unconditionally above so the
# manifest is stable across regens (and so the C FFI codegen can pull them in
# when an async / Result-typed function appears in the API surface), but for
# umbrella crates with no async fns and no JSON-marshalled return types they
# are genuinely unused. The conditional `async-trait` / `futures-util` deps
# are similarly flagged when the umbrella has trait-bridge / streaming adapters
# configured but no actual async-trait / async-stream callsite in the generated
# FFI shim.
[package.metadata.cargo-machete]
ignored = ["ahash", "serde", "serde_json", "tokio"]

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[features]
default = ["config", "download"]
config = ["tree-sitter-language-pack/config"]
download = ["tree-sitter-language-pack/download"]

[dependencies]
ahash = "0.8"
serde = "1"
serde_json = "1"
tokio = { version = "1", features = ["full"] }
tree-sitter = "0.27"
tree-sitter-language-pack = { path = "../ts-pack-core", version = "1.19.0", features = ["serde", "config", "download"] }

[build-dependencies]
cbindgen = "0.29"

[dev-dependencies]
tempfile = "3"

# This crate deliberately does not use `[lints]` / `workspace = true`: its C-ABI /
# PyO3 / napi / ext-php-rs / NIF boundary requires `unsafe` code, and the workspace's
# `[workspace.lints.rust]` sets `unsafe_code = "deny"` -- an all-or-nothing table that
# would turn every such boundary into a compile error. The `[lints.clippy]` block below
# instead carries the subset of the workspace's deny-by-default lint policy this crate
# can actually satisfy. ~keep
[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
