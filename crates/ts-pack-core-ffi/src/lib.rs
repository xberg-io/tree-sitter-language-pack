// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:e4d14bb6c40c2b5eed7bd45728b16b65f6b93a37db4371c4220667b43c887184
// Re-generate with: alef generate
#![allow(dead_code, unused_imports, unused_variables, unused_mut)]
#![allow(
    clippy::too_many_arguments,
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::redundant_locals,
    dropping_references,
    clippy::unnecessary_cast,
    clippy::unused_unit,
    clippy::unwrap_or_default,
    clippy::derivable_impls,
    clippy::needless_borrows_for_generic_args,
    clippy::unnecessary_fallible_conversions,
    clippy::useless_conversion,
    clippy::type_complexity
)]

use std::cell::RefCell;
use std::ffi::{c_char, CStr, CString};

thread_local! {
    static LAST_ERROR_CODE: RefCell<i32> = const { RefCell::new(0) };
    static LAST_ERROR_CONTEXT: RefCell<Option<CString>> = const { RefCell::new(None) };
}

fn set_last_error(code: i32, message: &str) {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = code);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = CString::new(message).ok());
}

fn clear_last_error() {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = 0);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = None);
}

/// Return the last error code (0 means no error).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_last_error_code() -> i32 {
    LAST_ERROR_CODE.with_borrow(|c| *c)
}

/// Return the last error message. The pointer is valid until the next FFI call on this thread.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_last_error_context() -> *const c_char {
    LAST_ERROR_CONTEXT.with_borrow(|ctx| ctx.as_ref().map_or(std::ptr::null(), |c| c.as_ptr()))
}

/// Free a string previously returned by this library.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_free_string(ptr: *mut c_char) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by CString::into_raw; caller ensures no aliases.
        unsafe {
            drop(CString::from_raw(ptr));
        }
    }
}

/// Free a byte buffer previously returned by this library via out-params.
/// `ptr`, `len`, and `cap` must match the values written by the library function,
/// or the call must pass `ptr = null` (in which case it is a no-op).
/// # Safety
/// Pointer must have been returned by this library (via out_ptr / out_len / out_cap
/// out-params), or be null. The len and cap values must be unchanged since the call.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_free_bytes(ptr: *mut u8, len: usize, cap: usize) {
    if !ptr.is_null() {
        // SAFETY: ptr/len/cap were produced by Vec::into_raw_parts (or equivalent)
        // by this library; caller must not have mutated them.
        unsafe {
            drop(Vec::from_raw_parts(ptr, len, cap));
        }
    }
}

/// Return the library version string. The pointer is static and must NOT be freed.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_version() -> *const c_char {
    static VERSION: &str = concat!(env!("CARGO_PKG_VERSION"), "\0");
    VERSION.as_ptr() as *const c_char
}

/// Create a `Span` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_span_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_from_json(json: *const c_char) -> *mut tree_sitter_language_pack::Span {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::Span>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `Span` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_to_json(ptr: *const tree_sitter_language_pack::Span) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `Span` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_free(ptr: *mut tree_sitter_language_pack::Span) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `start_byte` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_start_byte(ptr: *const tree_sitter_language_pack::Span) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.start_byte
}

/// Get the `end_byte` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_end_byte(ptr: *const tree_sitter_language_pack::Span) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.end_byte
}

/// Get the `start_line` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_start_line(ptr: *const tree_sitter_language_pack::Span) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.start_line
}

/// Get the `start_column` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_start_column(ptr: *const tree_sitter_language_pack::Span) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.start_column
}

/// Get the `end_line` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_end_line(ptr: *const tree_sitter_language_pack::Span) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.end_line
}

/// Get the `end_column` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_end_column(ptr: *const tree_sitter_language_pack::Span) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.end_column
}

/// Create a `ProcessResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_process_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::ProcessResult {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::ProcessResult>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ProcessResult` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_to_json(
    ptr: *const tree_sitter_language_pack::ProcessResult,
) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ProcessResult` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_free(ptr: *mut tree_sitter_language_pack::ProcessResult) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `language` field from a `ProcessResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_language(
    ptr: *const tree_sitter_language_pack::ProcessResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.language.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `metrics` field from a `ProcessResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_metrics(
    ptr: *const tree_sitter_language_pack::ProcessResult,
) -> *mut tree_sitter_language_pack::FileMetrics {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.metrics.clone()))
}

/// Get the `structure` field from a `ProcessResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_structure(
    ptr: *const tree_sitter_language_pack::ProcessResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.structure) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `imports` field from a `ProcessResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_imports(
    ptr: *const tree_sitter_language_pack::ProcessResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.imports) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `exports` field from a `ProcessResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_exports(
    ptr: *const tree_sitter_language_pack::ProcessResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.exports) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `comments` field from a `ProcessResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_comments(
    ptr: *const tree_sitter_language_pack::ProcessResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.comments) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `docstrings` field from a `ProcessResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_docstrings(
    ptr: *const tree_sitter_language_pack::ProcessResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.docstrings) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `symbols` field from a `ProcessResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_symbols(
    ptr: *const tree_sitter_language_pack::ProcessResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.symbols) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `diagnostics` field from a `ProcessResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_diagnostics(
    ptr: *const tree_sitter_language_pack::ProcessResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.diagnostics) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `chunks` field from a `ProcessResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_chunks(
    ptr: *const tree_sitter_language_pack::ProcessResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.chunks) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Create a `FileMetrics` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_file_metrics_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::FileMetrics {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::FileMetrics>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `FileMetrics` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_to_json(
    ptr: *const tree_sitter_language_pack::FileMetrics,
) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `FileMetrics` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_free(ptr: *mut tree_sitter_language_pack::FileMetrics) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `total_lines` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_total_lines(ptr: *const tree_sitter_language_pack::FileMetrics) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.total_lines
}

/// Get the `code_lines` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_code_lines(ptr: *const tree_sitter_language_pack::FileMetrics) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.code_lines
}

/// Get the `comment_lines` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_comment_lines(
    ptr: *const tree_sitter_language_pack::FileMetrics,
) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.comment_lines
}

/// Get the `blank_lines` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_blank_lines(ptr: *const tree_sitter_language_pack::FileMetrics) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.blank_lines
}

/// Get the `total_bytes` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_total_bytes(ptr: *const tree_sitter_language_pack::FileMetrics) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.total_bytes
}

/// Get the `node_count` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_node_count(ptr: *const tree_sitter_language_pack::FileMetrics) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.node_count
}

/// Get the `error_count` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_error_count(ptr: *const tree_sitter_language_pack::FileMetrics) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.error_count
}

/// Get the `max_depth` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_max_depth(ptr: *const tree_sitter_language_pack::FileMetrics) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.max_depth
}

/// Create a `StructureItem` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_structure_item_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::StructureItem {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::StructureItem>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `StructureItem` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_to_json(
    ptr: *const tree_sitter_language_pack::StructureItem,
) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `StructureItem` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_free(ptr: *mut tree_sitter_language_pack::StructureItem) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `kind` field from a `StructureItem`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_kind(
    ptr: *const tree_sitter_language_pack::StructureItem,
) -> *mut tree_sitter_language_pack::StructureKind {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.kind.clone()))
}

/// Get the `name` field from a `StructureItem`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_name(
    ptr: *const tree_sitter_language_pack::StructureItem,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.name {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `visibility` field from a `StructureItem`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_visibility(
    ptr: *const tree_sitter_language_pack::StructureItem,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.visibility {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `span` field from a `StructureItem`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_span(
    ptr: *const tree_sitter_language_pack::StructureItem,
) -> *mut tree_sitter_language_pack::Span {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.span.clone()))
}

/// Get the `children` field from a `StructureItem`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_children(
    ptr: *const tree_sitter_language_pack::StructureItem,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.children) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `decorators` field from a `StructureItem`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_decorators(
    ptr: *const tree_sitter_language_pack::StructureItem,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.decorators) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `doc_comment` field from a `StructureItem`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_doc_comment(
    ptr: *const tree_sitter_language_pack::StructureItem,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.doc_comment {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `signature` field from a `StructureItem`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_signature(
    ptr: *const tree_sitter_language_pack::StructureItem,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.signature {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `body_span` field from a `StructureItem`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_body_span(
    ptr: *const tree_sitter_language_pack::StructureItem,
) -> *mut tree_sitter_language_pack::Span {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.body_span {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Create a `CommentInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_comment_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::CommentInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::CommentInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `CommentInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_to_json(
    ptr: *const tree_sitter_language_pack::CommentInfo,
) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `CommentInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_free(ptr: *mut tree_sitter_language_pack::CommentInfo) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `text` field from a `CommentInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_text(
    ptr: *const tree_sitter_language_pack::CommentInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.text.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `kind` field from a `CommentInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_kind(
    ptr: *const tree_sitter_language_pack::CommentInfo,
) -> *mut tree_sitter_language_pack::CommentKind {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.kind.clone()))
}

/// Get the `span` field from a `CommentInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_span(
    ptr: *const tree_sitter_language_pack::CommentInfo,
) -> *mut tree_sitter_language_pack::Span {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.span.clone()))
}

/// Get the `associated_node` field from a `CommentInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_associated_node(
    ptr: *const tree_sitter_language_pack::CommentInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.associated_node {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `DocstringInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_docstring_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::DocstringInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::DocstringInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `DocstringInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_to_json(
    ptr: *const tree_sitter_language_pack::DocstringInfo,
) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `DocstringInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_free(ptr: *mut tree_sitter_language_pack::DocstringInfo) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `text` field from a `DocstringInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_text(
    ptr: *const tree_sitter_language_pack::DocstringInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.text.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `format` field from a `DocstringInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_format(
    ptr: *const tree_sitter_language_pack::DocstringInfo,
) -> *mut tree_sitter_language_pack::DocstringFormat {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.format.clone()))
}

/// Get the `span` field from a `DocstringInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_span(
    ptr: *const tree_sitter_language_pack::DocstringInfo,
) -> *mut tree_sitter_language_pack::Span {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.span.clone()))
}

/// Get the `associated_item` field from a `DocstringInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_associated_item(
    ptr: *const tree_sitter_language_pack::DocstringInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.associated_item {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `parsed_sections` field from a `DocstringInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_parsed_sections(
    ptr: *const tree_sitter_language_pack::DocstringInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.parsed_sections) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Create a `DocSection` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_doc_section_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::DocSection {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::DocSection>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `DocSection` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_to_json(ptr: *const tree_sitter_language_pack::DocSection) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `DocSection` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_free(ptr: *mut tree_sitter_language_pack::DocSection) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `kind` field from a `DocSection`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_kind(
    ptr: *const tree_sitter_language_pack::DocSection,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.kind.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `name` field from a `DocSection`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_name(
    ptr: *const tree_sitter_language_pack::DocSection,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.name {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `description` field from a `DocSection`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_description(
    ptr: *const tree_sitter_language_pack::DocSection,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.description.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Create a `ImportInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_import_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::ImportInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::ImportInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ImportInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_to_json(ptr: *const tree_sitter_language_pack::ImportInfo) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ImportInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_free(ptr: *mut tree_sitter_language_pack::ImportInfo) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `source` field from a `ImportInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_source(
    ptr: *const tree_sitter_language_pack::ImportInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.source.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `items` field from a `ImportInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_items(
    ptr: *const tree_sitter_language_pack::ImportInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.items) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `alias` field from a `ImportInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_alias(
    ptr: *const tree_sitter_language_pack::ImportInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.alias {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `is_wildcard` field from a `ImportInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_is_wildcard(ptr: *const tree_sitter_language_pack::ImportInfo) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.is_wildcard as i32
}

/// Get the `span` field from a `ImportInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_span(
    ptr: *const tree_sitter_language_pack::ImportInfo,
) -> *mut tree_sitter_language_pack::Span {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.span.clone()))
}

/// Create a `ExportInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_export_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::ExportInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::ExportInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ExportInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_to_json(ptr: *const tree_sitter_language_pack::ExportInfo) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ExportInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_free(ptr: *mut tree_sitter_language_pack::ExportInfo) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `name` field from a `ExportInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_name(
    ptr: *const tree_sitter_language_pack::ExportInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.name.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `kind` field from a `ExportInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_kind(
    ptr: *const tree_sitter_language_pack::ExportInfo,
) -> *mut tree_sitter_language_pack::ExportKind {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.kind.clone()))
}

/// Get the `span` field from a `ExportInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_span(
    ptr: *const tree_sitter_language_pack::ExportInfo,
) -> *mut tree_sitter_language_pack::Span {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.span.clone()))
}

/// Create a `SymbolInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_symbol_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::SymbolInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::SymbolInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `SymbolInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_to_json(ptr: *const tree_sitter_language_pack::SymbolInfo) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `SymbolInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_free(ptr: *mut tree_sitter_language_pack::SymbolInfo) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `name` field from a `SymbolInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_name(
    ptr: *const tree_sitter_language_pack::SymbolInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.name.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `kind` field from a `SymbolInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_kind(
    ptr: *const tree_sitter_language_pack::SymbolInfo,
) -> *mut tree_sitter_language_pack::SymbolKind {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.kind.clone()))
}

/// Get the `span` field from a `SymbolInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_span(
    ptr: *const tree_sitter_language_pack::SymbolInfo,
) -> *mut tree_sitter_language_pack::Span {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.span.clone()))
}

/// Get the `type_annotation` field from a `SymbolInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_type_annotation(
    ptr: *const tree_sitter_language_pack::SymbolInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.type_annotation {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `doc` field from a `SymbolInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_doc(
    ptr: *const tree_sitter_language_pack::SymbolInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.doc {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `Diagnostic` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_diagnostic_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::Diagnostic {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::Diagnostic>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `Diagnostic` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_to_json(ptr: *const tree_sitter_language_pack::Diagnostic) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `Diagnostic` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_free(ptr: *mut tree_sitter_language_pack::Diagnostic) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `message` field from a `Diagnostic`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_message(
    ptr: *const tree_sitter_language_pack::Diagnostic,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.message.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `severity` field from a `Diagnostic`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity(
    ptr: *const tree_sitter_language_pack::Diagnostic,
) -> *mut tree_sitter_language_pack::DiagnosticSeverity {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.severity.clone()))
}

/// Get the `span` field from a `Diagnostic`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_span(
    ptr: *const tree_sitter_language_pack::Diagnostic,
) -> *mut tree_sitter_language_pack::Span {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.span.clone()))
}

/// Create a `CodeChunk` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_code_chunk_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::CodeChunk {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::CodeChunk>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `CodeChunk` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_to_json(ptr: *const tree_sitter_language_pack::CodeChunk) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `CodeChunk` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_free(ptr: *mut tree_sitter_language_pack::CodeChunk) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `content` field from a `CodeChunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_content(
    ptr: *const tree_sitter_language_pack::CodeChunk,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.content.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `start_byte` field from a `CodeChunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_start_byte(ptr: *const tree_sitter_language_pack::CodeChunk) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.start_byte
}

/// Get the `end_byte` field from a `CodeChunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_end_byte(ptr: *const tree_sitter_language_pack::CodeChunk) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.end_byte
}

/// Get the `start_line` field from a `CodeChunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_start_line(ptr: *const tree_sitter_language_pack::CodeChunk) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.start_line
}

/// Get the `end_line` field from a `CodeChunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_end_line(ptr: *const tree_sitter_language_pack::CodeChunk) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.end_line
}

/// Get the `metadata` field from a `CodeChunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_metadata(
    ptr: *const tree_sitter_language_pack::CodeChunk,
) -> *mut tree_sitter_language_pack::ChunkContext {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.metadata.clone()))
}

/// Create a `ChunkContext` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_chunk_context_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::ChunkContext {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::ChunkContext>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ChunkContext` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_to_json(
    ptr: *const tree_sitter_language_pack::ChunkContext,
) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ChunkContext` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_free(ptr: *mut tree_sitter_language_pack::ChunkContext) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `language` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_language(
    ptr: *const tree_sitter_language_pack::ChunkContext,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.language.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `chunk_index` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_chunk_index(
    ptr: *const tree_sitter_language_pack::ChunkContext,
) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.chunk_index
}

/// Get the `total_chunks` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_total_chunks(
    ptr: *const tree_sitter_language_pack::ChunkContext,
) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.total_chunks
}

/// Get the `node_types` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_node_types(
    ptr: *const tree_sitter_language_pack::ChunkContext,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.node_types) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `context_path` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_context_path(
    ptr: *const tree_sitter_language_pack::ChunkContext,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.context_path) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `symbols_defined` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_symbols_defined(
    ptr: *const tree_sitter_language_pack::ChunkContext,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.symbols_defined) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `comments` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_comments(
    ptr: *const tree_sitter_language_pack::ChunkContext,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.comments) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `docstrings` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_docstrings(
    ptr: *const tree_sitter_language_pack::ChunkContext,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.docstrings) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `has_error_nodes` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_has_error_nodes(
    ptr: *const tree_sitter_language_pack::ChunkContext,
) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.has_error_nodes as i32
}

/// Create a `PackConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_pack_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::PackConfig {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::PackConfig>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `PackConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_to_json(ptr: *const tree_sitter_language_pack::PackConfig) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `PackConfig` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_free(ptr: *mut tree_sitter_language_pack::PackConfig) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `cache_dir` field from a `PackConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_cache_dir(
    ptr: *const tree_sitter_language_pack::PackConfig,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.cache_dir {
        Some(val) => match CString::new(val.to_string_lossy().to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `languages` field from a `PackConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_languages(
    ptr: *const tree_sitter_language_pack::PackConfig,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.languages {
        Some(val) => match serde_json::to_string(&val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `groups` field from a `PackConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_groups(
    ptr: *const tree_sitter_language_pack::PackConfig,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.groups {
        Some(val) => match serde_json::to_string(&val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Load configuration from a TOML file.
///
/// # Errors
///
/// Returns an error if the file cannot be read or the TOML is invalid.
///
/// # Example
///
/// ```no_run
/// use std::path::Path;
/// use tree_sitter_language_pack::PackConfig;
///
/// let config = PackConfig::from_toml_file(Path::new("language-pack.toml")).unwrap();
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_from_toml_file(
    path: *const std::ffi::c_char,
) -> *mut tree_sitter_language_pack::PackConfig {
    clear_last_error();
    if path.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'path'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees path is a valid pointer; string is valid UTF-8 from caller.
    let path_rs = match unsafe { CStr::from_ptr(path) }.to_str() {
        Ok(s) => std::path::PathBuf::from(s),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'path'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::PackConfig::from_toml_file(path_rs.as_path());
    match result {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Discover configuration by searching for `language-pack.toml` in:
///
/// 1. Current directory and up to 10 parent directories
/// 2. `$XDG_CONFIG_HOME/tree-sitter-language-pack/config.toml`
/// 3. `~/.config/tree-sitter-language-pack/config.toml`
///
/// Returns `None` if no configuration file is found.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::PackConfig;
///
/// if let Some(config) = PackConfig::discover() {
///     println!("Found config with {:?} languages", config.languages);
/// }
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_discover() -> *mut tree_sitter_language_pack::PackConfig {
    clear_last_error();
    let result = tree_sitter_language_pack::PackConfig::discover();
    match result {
        Some(val) => Box::into_raw(Box::new(val)),
        None => std::ptr::null_mut(),
    }
}

/// Create a `Point` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_point_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_point_from_json(json: *const c_char) -> *mut tree_sitter_language_pack::Point {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::Point>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `Point` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_point_to_json(ptr: *const tree_sitter_language_pack::Point) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `Point` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_point_free(ptr: *mut tree_sitter_language_pack::Point) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `row` field from a `Point`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_point_row(ptr: *const tree_sitter_language_pack::Point) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.row
}

/// Get the `column` field from a `Point`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_point_column(ptr: *const tree_sitter_language_pack::Point) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.column
}

/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_point_from(
    p: *const tree_sitter_language_pack::Point,
) -> *mut tree_sitter_language_pack::Point {
    clear_last_error();
    if p.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'p'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees p is a valid pointer.
    let p_rs = *unsafe { &*p };
    let result = tree_sitter_language_pack::Point::from(p_rs);
    Box::into_raw(Box::new(result))
}

/// Create a `ByteRange` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_byte_range_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_byte_range_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::ByteRange {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::ByteRange>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ByteRange` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_byte_range_to_json(ptr: *const tree_sitter_language_pack::ByteRange) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ByteRange` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_byte_range_free(ptr: *mut tree_sitter_language_pack::ByteRange) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `start` field from a `ByteRange`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_byte_range_start(ptr: *const tree_sitter_language_pack::ByteRange) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.start
}

/// Get the `end` field from a `ByteRange`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_byte_range_end(ptr: *const tree_sitter_language_pack::ByteRange) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.end
}

/// Free a `Parser` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_free(ptr: *mut tree_sitter_language_pack::Parser) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Configure the parser to use the language identified by name (e.g. `"python"`).
///
/// Resolves the language through the global registry — auto-downloading
/// if necessary, when the `download` feature is enabled.
///
/// # Errors
///
/// Returns [`Error::LanguageNotFound`] if the language is not recognized,
/// or [`Error::ParserSetup`] if the language ABI is incompatible.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_set_language(
    this: *mut tree_sitter_language_pack::Parser,
    name: *const std::ffi::c_char,
) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return -1;
    }
    // SAFETY: null check above guarantees this is a valid pointer; caller ensures exclusive access.
    let obj = unsafe { &mut *this };

    if name.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'name'");
        return -1;
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'name'");
            return -1;
        }
    };
    let result = obj.set_language(&name_rs);
    match result {
        Ok(()) => 0,
        Err(e) => {
            set_last_error(2, &e.to_string());
            -1
        }
    }
}

/// Parse a UTF-8 source string. Returns `None` if parsing was cancelled
/// or no language is set.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_parse(
    this: *mut tree_sitter_language_pack::Parser,
    source: *const std::ffi::c_char,
) -> *mut tree_sitter_language_pack::Tree {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer; caller ensures exclusive access.
    let obj = unsafe { &mut *this };

    if source.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'source'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees source is a valid pointer; string is valid UTF-8 from caller.
    let source_rs = match unsafe { CStr::from_ptr(source) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'source'");
            return std::ptr::null_mut();
        }
    };
    let result = obj.parse(&source_rs);
    match result {
        Some(val) => Box::into_raw(Box::new(val)),
        None => std::ptr::null_mut(),
    }
}

/// Parse a raw byte slice. Returns `None` if parsing was cancelled or
/// no language is set.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_parse_bytes(
    this: *mut tree_sitter_language_pack::Parser,
    source: *const u8,
    source_len: usize,
) -> *mut tree_sitter_language_pack::Tree {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer; caller ensures exclusive access.
    let obj = unsafe { &mut *this };

    if source.is_null() && source_len > 0 {
        set_last_error(1, "Null pointer passed for parameter 'source'");
        return std::ptr::null_mut();
    }
    // SAFETY: when source is null, source_len is 0 (checked above), so we use an empty slice; otherwise data is valid for len elements.
    let source_rs = if source.is_null() {
        Vec::new()
    } else {
        unsafe { std::slice::from_raw_parts(source, source_len) }.to_vec()
    };
    let result = obj.parse_bytes(&source_rs);
    match result {
        Some(val) => Box::into_raw(Box::new(val)),
        None => std::ptr::null_mut(),
    }
}

/// Reset internal state. The next call to [`parse`](Self::parse) will
/// not be incremental.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_reset(this: *mut tree_sitter_language_pack::Parser) {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return;
    }
    // SAFETY: null check above guarantees this is a valid pointer; caller ensures exclusive access.
    let obj = unsafe { &mut *this };

    let result = obj.reset();
}

/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_default() -> *mut tree_sitter_language_pack::Parser {
    clear_last_error();
    let result = tree_sitter_language_pack::Parser::default();
    Box::into_raw(Box::new(result))
}

/// Free a `Tree` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_free(ptr: *mut tree_sitter_language_pack::Tree) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Return the root [`Node`] of this tree.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_root_node(
    this: *const tree_sitter_language_pack::Tree,
) -> *mut tree_sitter_language_pack::Node {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.root_node();
    Box::into_raw(Box::new(result))
}

/// Return a [`TreeCursor`] positioned at the root.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_walk(
    this: *const tree_sitter_language_pack::Tree,
) -> *mut tree_sitter_language_pack::TreeCursor {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.walk();
    Box::into_raw(Box::new(result))
}

/// Free a `Node` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_free(ptr: *mut tree_sitter_language_pack::Node) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_clone(
    this: *const tree_sitter_language_pack::Node,
) -> *mut tree_sitter_language_pack::Node {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.clone();
    Box::into_raw(Box::new(result))
}

/// Return the node's kind name (e.g. `"function_definition"`).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_kind(this: *const tree_sitter_language_pack::Node) -> *mut std::ffi::c_char {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.kind();
    match CString::new(result.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Return the node's numeric kind ID.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_kind_id(this: *const tree_sitter_language_pack::Node) -> u16 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    obj.kind_id()
}

/// Return the inclusive start byte offset of this node.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_start_byte(this: *const tree_sitter_language_pack::Node) -> usize {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    obj.start_byte()
}

/// Return the exclusive end byte offset of this node.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_end_byte(this: *const tree_sitter_language_pack::Node) -> usize {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    obj.end_byte()
}

/// Return the node's byte range as a [`ByteRange`].
///
/// Callers should slice their own source bytes — this is a zero-copy
/// text accessor.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_byte_range(
    this: *const tree_sitter_language_pack::Node,
) -> *mut tree_sitter_language_pack::ByteRange {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.byte_range();
    Box::into_raw(Box::new(result))
}

/// Return the start [`Point`] (row, column).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_start_position(
    this: *const tree_sitter_language_pack::Node,
) -> *mut tree_sitter_language_pack::Point {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.start_position();
    Box::into_raw(Box::new(result))
}

/// Return the end [`Point`] (row, column).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_end_position(
    this: *const tree_sitter_language_pack::Node,
) -> *mut tree_sitter_language_pack::Point {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.end_position();
    Box::into_raw(Box::new(result))
}

/// True when this node is named (not punctuation/whitespace).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_is_named(this: *const tree_sitter_language_pack::Node) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.is_named();
    if result {
        1
    } else {
        0
    }
}

/// True when this is an error node.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_is_error(this: *const tree_sitter_language_pack::Node) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.is_error();
    if result {
        1
    } else {
        0
    }
}

/// True when this is a missing-token node.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_is_missing(this: *const tree_sitter_language_pack::Node) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.is_missing();
    if result {
        1
    } else {
        0
    }
}

/// True when this is an "extra" node (e.g. a comment).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_is_extra(this: *const tree_sitter_language_pack::Node) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.is_extra();
    if result {
        1
    } else {
        0
    }
}

/// True when this node or any descendant is an error.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_has_error(this: *const tree_sitter_language_pack::Node) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.has_error();
    if result {
        1
    } else {
        0
    }
}

/// Return this node's parent, if any.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_parent(
    this: *const tree_sitter_language_pack::Node,
) -> *mut tree_sitter_language_pack::Node {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.parent();
    match result {
        Some(val) => Box::into_raw(Box::new(val)),
        None => std::ptr::null_mut(),
    }
}

/// Return the i-th child of this node, if any.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_child(
    this: *const tree_sitter_language_pack::Node,
    index: u32,
) -> *mut tree_sitter_language_pack::Node {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let index_rs = index;
    let result = obj.child(index_rs);
    match result {
        Some(val) => Box::into_raw(Box::new(val)),
        None => std::ptr::null_mut(),
    }
}

/// Total number of children (including unnamed).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_child_count(this: *const tree_sitter_language_pack::Node) -> usize {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    obj.child_count()
}

/// Return the i-th named child of this node, if any.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_named_child(
    this: *const tree_sitter_language_pack::Node,
    index: u32,
) -> *mut tree_sitter_language_pack::Node {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let index_rs = index;
    let result = obj.named_child(index_rs);
    match result {
        Some(val) => Box::into_raw(Box::new(val)),
        None => std::ptr::null_mut(),
    }
}

/// Number of named children of this node.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_named_child_count(this: *const tree_sitter_language_pack::Node) -> usize {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    obj.named_child_count()
}

/// Look up a child by its grammar-defined field name.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_child_by_field_name(
    this: *const tree_sitter_language_pack::Node,
    name: *const std::ffi::c_char,
) -> *mut tree_sitter_language_pack::Node {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    if name.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'name'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'name'");
            return std::ptr::null_mut();
        }
    };
    let result = obj.child_by_field_name(&name_rs);
    match result {
        Some(val) => Box::into_raw(Box::new(val)),
        None => std::ptr::null_mut(),
    }
}

/// Return the S-expression form of this node's subtree.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_to_sexp(this: *const tree_sitter_language_pack::Node) -> *mut std::ffi::c_char {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.to_sexp();
    match CString::new(result.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Return a [`TreeCursor`] positioned at this node.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_walk(
    this: *const tree_sitter_language_pack::Node,
) -> *mut tree_sitter_language_pack::TreeCursor {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.walk();
    Box::into_raw(Box::new(result))
}

/// Free a `TreeCursor` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_free(ptr: *mut tree_sitter_language_pack::TreeCursor) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Return the [`Node`] at the cursor's current position.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_node(
    this: *const tree_sitter_language_pack::TreeCursor,
) -> *mut tree_sitter_language_pack::Node {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.node();
    Box::into_raw(Box::new(result))
}

/// Move the cursor to the first child of the current node.
/// Returns `true` if a child existed.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_goto_first_child(this: *mut tree_sitter_language_pack::TreeCursor) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer; caller ensures exclusive access.
    let obj = unsafe { &mut *this };

    let result = obj.goto_first_child();
    if result {
        1
    } else {
        0
    }
}

/// Move the cursor to the parent of the current node.
/// Returns `true` if a parent existed.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_goto_parent(this: *mut tree_sitter_language_pack::TreeCursor) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer; caller ensures exclusive access.
    let obj = unsafe { &mut *this };

    let result = obj.goto_parent();
    if result {
        1
    } else {
        0
    }
}

/// Move the cursor to the next sibling of the current node.
/// Returns `true` if a sibling existed.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_goto_next_sibling(
    this: *mut tree_sitter_language_pack::TreeCursor,
) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer; caller ensures exclusive access.
    let obj = unsafe { &mut *this };

    let result = obj.goto_next_sibling();
    if result {
        1
    } else {
        0
    }
}

/// Return the field name for the current node, if any.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_field_name(
    this: *const tree_sitter_language_pack::TreeCursor,
) -> *mut std::ffi::c_char {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.field_name();
    match result {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `ProcessConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_process_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::ProcessConfig {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::ProcessConfig>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ProcessConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_to_json(
    ptr: *const tree_sitter_language_pack::ProcessConfig,
) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ProcessConfig` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_free(ptr: *mut tree_sitter_language_pack::ProcessConfig) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `structure` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_structure(ptr: *const tree_sitter_language_pack::ProcessConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.structure as i32
}

/// Get the `imports` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_imports(ptr: *const tree_sitter_language_pack::ProcessConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.imports as i32
}

/// Get the `exports` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_exports(ptr: *const tree_sitter_language_pack::ProcessConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.exports as i32
}

/// Get the `comments` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_comments(ptr: *const tree_sitter_language_pack::ProcessConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.comments as i32
}

/// Get the `docstrings` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_docstrings(
    ptr: *const tree_sitter_language_pack::ProcessConfig,
) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.docstrings as i32
}

/// Get the `symbols` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_symbols(ptr: *const tree_sitter_language_pack::ProcessConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.symbols as i32
}

/// Get the `diagnostics` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_diagnostics(
    ptr: *const tree_sitter_language_pack::ProcessConfig,
) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.diagnostics as i32
}

/// Get the `chunk_max_size` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_chunk_max_size(
    ptr: *const tree_sitter_language_pack::ProcessConfig,
) -> usize {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match &obj.chunk_max_size {
        Some(val) => *val,
        None => 0,
    }
}

/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_default() -> *mut tree_sitter_language_pack::ProcessConfig {
    clear_last_error();
    let result = tree_sitter_language_pack::ProcessConfig::default();
    Box::into_raw(Box::new(result))
}

/// Enable chunking with the given maximum chunk size in bytes.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_with_chunking(
    this: *mut tree_sitter_language_pack::ProcessConfig,
    max_size: usize,
) -> *mut tree_sitter_language_pack::ProcessConfig {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer originally from Box::into_raw.
    let obj = unsafe { *Box::from_raw(this) };

    let max_size_rs = max_size;
    let result = obj.with_chunking(max_size_rs);
    Box::into_raw(Box::new(result))
}

/// Enable all analysis features.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_all(
    this: *mut tree_sitter_language_pack::ProcessConfig,
) -> *mut tree_sitter_language_pack::ProcessConfig {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer originally from Box::into_raw.
    let obj = unsafe { *Box::from_raw(this) };

    let result = obj.all();
    Box::into_raw(Box::new(result))
}

/// Disable all analysis features (only metrics computed).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_minimal(
    this: *mut tree_sitter_language_pack::ProcessConfig,
) -> *mut tree_sitter_language_pack::ProcessConfig {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer originally from Box::into_raw.
    let obj = unsafe { *Box::from_raw(this) };

    let result = obj.minimal();
    Box::into_raw(Box::new(result))
}

/// Free a `LanguageRegistry` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_free(ptr: *mut tree_sitter_language_pack::LanguageRegistry) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Create a registry with a custom directory for dynamic libraries.
///
/// Overrides the default build-time library directory. Useful when
/// dynamic grammar shared libraries are stored in a non-standard location.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_with_libs_dir(
    libs_dir: *const std::ffi::c_char,
) -> *mut tree_sitter_language_pack::LanguageRegistry {
    clear_last_error();
    if libs_dir.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'libs_dir'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees libs_dir is a valid pointer; string is valid UTF-8 from caller.
    let libs_dir_rs = match unsafe { CStr::from_ptr(libs_dir) }.to_str() {
        Ok(s) => std::path::PathBuf::from(s),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'libs_dir'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::LanguageRegistry::with_libs_dir(libs_dir_rs);
    Box::into_raw(Box::new(result))
}

/// Add an additional directory to search for dynamic libraries.
///
/// When [`get_language`](Self::get_language) cannot find a grammar in the
/// primary library directory, it searches these extra directories in order.
/// Typically used by the download system to register its cache directory.
///
/// Takes `&self` (not `&mut self`) because `extra_lib_dirs` uses interior
/// mutability via an `Arc<RwLock<...>>`, so the outer registry can remain
/// immutable while the directory list is updated.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_add_extra_libs_dir(
    this: *const tree_sitter_language_pack::LanguageRegistry,
    dir: *const std::ffi::c_char,
) {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    if dir.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'dir'");
        return;
    }
    // SAFETY: null check above guarantees dir is a valid pointer; string is valid UTF-8 from caller.
    let dir_rs = match unsafe { CStr::from_ptr(dir) }.to_str() {
        Ok(s) => std::path::PathBuf::from(s),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'dir'");
            return;
        }
    };
    let result = obj.add_extra_libs_dir(dir_rs);
}

/// Get a tree-sitter [`Language`] by name.
///
/// Resolves aliases (e.g., `"shell"` -> `"bash"`, `"makefile"` -> `"make"`),
/// then looks up the language in the static table. When the `dynamic-loading`
/// feature is enabled, falls back to loading a shared library on demand.
///
/// # Errors
///
/// Returns [`Error::LanguageNotFound`] if the name (after alias resolution)
/// does not match any known grammar.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_get_language(
    this: *const tree_sitter_language_pack::LanguageRegistry,
    name: *const std::ffi::c_char,
) -> *mut tree_sitter_language_pack::Language {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    if name.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'name'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'name'");
            return std::ptr::null_mut();
        }
    };
    let result = obj.get_language(&name_rs);
    match result {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// List all available language names, sorted and deduplicated.
///
/// Includes statically compiled languages, dynamically loadable languages
/// (if the `dynamic-loading` feature is enabled), and all configured aliases.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_available_languages(
    this: *const tree_sitter_language_pack::LanguageRegistry,
) -> *mut std::ffi::c_char {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.available_languages();
    match serde_json::to_string(&result) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Check whether a language is available by name or alias.
///
/// Returns `true` if the language can be loaded, either from the static
/// table or from a dynamic library on disk.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_has_language(
    this: *const tree_sitter_language_pack::LanguageRegistry,
    name: *const std::ffi::c_char,
) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    if name.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'name'");
        return 0;
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'name'");
            return 0;
        }
    };
    let result = obj.has_language(&name_rs);
    if result {
        1
    } else {
        0
    }
}

/// Return the total number of available languages (including aliases).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_language_count(
    this: *const tree_sitter_language_pack::LanguageRegistry,
) -> usize {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    obj.language_count()
}

/// Parse source code and extract file intelligence based on config in a single pass.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_process(
    this: *const tree_sitter_language_pack::LanguageRegistry,
    source: *const std::ffi::c_char,
    config: *const tree_sitter_language_pack::ProcessConfig,
) -> *mut tree_sitter_language_pack::ProcessResult {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    if source.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'source'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees source is a valid pointer; string is valid UTF-8 from caller.
    let source_rs = match unsafe { CStr::from_ptr(source) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'source'");
            return std::ptr::null_mut();
        }
    };
    if config.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'config'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees config is a valid pointer.
    let config_rs = unsafe { &*config };
    let result = obj.process(&source_rs, &config_rs);
    match result {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_default() -> *mut tree_sitter_language_pack::LanguageRegistry {
    clear_last_error();
    let result = tree_sitter_language_pack::LanguageRegistry::default();
    Box::into_raw(Box::new(result))
}

/// Create a `ParserManifest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_parser_manifest_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_manifest_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::download::ParserManifest {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::download::ParserManifest>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ParserManifest` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_manifest_to_json(
    ptr: *const tree_sitter_language_pack::download::ParserManifest,
) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ParserManifest` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_manifest_free(ptr: *mut tree_sitter_language_pack::download::ParserManifest) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `version` field from a `ParserManifest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_manifest_version(
    ptr: *const tree_sitter_language_pack::download::ParserManifest,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.version.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `platforms` field from a `ParserManifest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_manifest_platforms(
    ptr: *const tree_sitter_language_pack::download::ParserManifest,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.platforms) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `languages` field from a `ParserManifest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_manifest_languages(
    ptr: *const tree_sitter_language_pack::download::ParserManifest,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.languages) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `groups` field from a `ParserManifest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_manifest_groups(
    ptr: *const tree_sitter_language_pack::download::ParserManifest,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.groups) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Create a `PlatformBundle` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_platform_bundle_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_platform_bundle_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::download::PlatformBundle {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::download::PlatformBundle>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `PlatformBundle` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_platform_bundle_to_json(
    ptr: *const tree_sitter_language_pack::download::PlatformBundle,
) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `PlatformBundle` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_platform_bundle_free(ptr: *mut tree_sitter_language_pack::download::PlatformBundle) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `PlatformBundle`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_platform_bundle_url(
    ptr: *const tree_sitter_language_pack::download::PlatformBundle,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `sha256` field from a `PlatformBundle`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_platform_bundle_sha256(
    ptr: *const tree_sitter_language_pack::download::PlatformBundle,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.sha256.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `size` field from a `PlatformBundle`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_platform_bundle_size(
    ptr: *const tree_sitter_language_pack::download::PlatformBundle,
) -> u64 {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.size
}

/// Create a `LanguageInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_language_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_info_from_json(
    json: *const c_char,
) -> *mut tree_sitter_language_pack::download::LanguageInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<tree_sitter_language_pack::download::LanguageInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `LanguageInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_info_to_json(
    ptr: *const tree_sitter_language_pack::download::LanguageInfo,
) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `LanguageInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_info_free(ptr: *mut tree_sitter_language_pack::download::LanguageInfo) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `group` field from a `LanguageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_info_group(
    ptr: *const tree_sitter_language_pack::download::LanguageInfo,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    match CString::new(obj.group.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `size` field from a `LanguageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_info_size(
    ptr: *const tree_sitter_language_pack::download::LanguageInfo,
) -> u64 {
    if ptr.is_null() {
        return 0;
    }
    // SAFETY: null check above guarantees ptr is a valid pointer.
    let obj = unsafe { &*ptr };
    obj.size
}

/// Free a `DownloadManager` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_free(ptr: *mut tree_sitter_language_pack::DownloadManager) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Create a new download manager for the given version.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_new(
    version: *const std::ffi::c_char,
) -> *mut tree_sitter_language_pack::DownloadManager {
    clear_last_error();
    if version.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'version'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees version is a valid pointer; string is valid UTF-8 from caller.
    let version_rs = match unsafe { CStr::from_ptr(version) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'version'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::DownloadManager::new(&version_rs);
    match result {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Create a download manager with a custom cache directory.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_with_cache_dir(
    version: *const std::ffi::c_char,
    cache_dir: *const std::ffi::c_char,
) -> *mut tree_sitter_language_pack::DownloadManager {
    clear_last_error();
    if version.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'version'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees version is a valid pointer; string is valid UTF-8 from caller.
    let version_rs = match unsafe { CStr::from_ptr(version) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'version'");
            return std::ptr::null_mut();
        }
    };
    if cache_dir.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'cache_dir'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees cache_dir is a valid pointer; string is valid UTF-8 from caller.
    let cache_dir_rs = match unsafe { CStr::from_ptr(cache_dir) }.to_str() {
        Ok(s) => std::path::PathBuf::from(s),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'cache_dir'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::DownloadManager::with_cache_dir(&version_rs, cache_dir_rs);
    Box::into_raw(Box::new(result))
}

/// Default cache directory: `~/.cache/tree-sitter-language-pack/v{version}/libs/`
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_default_cache_dir(
    version: *const std::ffi::c_char,
) -> *mut std::ffi::c_char {
    clear_last_error();
    if version.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'version'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees version is a valid pointer; string is valid UTF-8 from caller.
    let version_rs = match unsafe { CStr::from_ptr(version) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'version'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::DownloadManager::default_cache_dir(&version_rs);
    match result {
        Ok(val) => match CString::new(val.to_string_lossy().to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Return the path to the libs cache directory.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_cache_dir(
    this: *const tree_sitter_language_pack::DownloadManager,
) -> *mut std::ffi::c_char {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.cache_dir();
    match CString::new(result.to_string_lossy().to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// List languages that are already downloaded and cached.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_installed_languages(
    this: *const tree_sitter_language_pack::DownloadManager,
) -> *mut std::ffi::c_char {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.installed_languages();
    match serde_json::to_string(&result) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Ensure the specified languages are available in the cache.
/// Downloads the platform bundle if any requested languages are missing.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_ensure_languages(
    this: *const tree_sitter_language_pack::DownloadManager,
    names: *const std::ffi::c_char,
) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return -1;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    if names.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'names'");
        return -1;
    }
    // SAFETY: null check above guarantees names is a valid pointer; string is valid UTF-8 from caller.
    let names_rs_str = match unsafe { CStr::from_ptr(names) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'names'");
            return -1;
        }
    };
    let names_rs = match serde_json::from_str::<Vec<String>>(names_rs_str) {
        Ok(v) => v,
        Err(e) => {
            set_last_error(2, &e.to_string());
            return -1;
        }
    };
    let names_rs_refs: Vec<&str> = names_rs.iter().map(|s| s.as_str()).collect();
    let result = obj.ensure_languages(&names_rs_refs);
    match result {
        Ok(()) => 0,
        Err(e) => {
            set_last_error(2, &e.to_string());
            -1
        }
    }
}

/// Ensure all languages in a named group are available.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_ensure_group(
    this: *const tree_sitter_language_pack::DownloadManager,
    group: *const std::ffi::c_char,
) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return -1;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    if group.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'group'");
        return -1;
    }
    // SAFETY: null check above guarantees group is a valid pointer; string is valid UTF-8 from caller.
    let group_rs = match unsafe { CStr::from_ptr(group) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'group'");
            return -1;
        }
    };
    let result = obj.ensure_group(&group_rs);
    match result {
        Ok(()) => 0,
        Err(e) => {
            set_last_error(2, &e.to_string());
            -1
        }
    }
}

/// Get the expected path for a language's shared library in the cache.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_lib_path(
    this: *const tree_sitter_language_pack::DownloadManager,
    name: *const std::ffi::c_char,
) -> *mut std::ffi::c_char {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    if name.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'name'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'name'");
            return std::ptr::null_mut();
        }
    };
    let result = obj.lib_path(&name_rs);
    match CString::new(result.to_string_lossy().to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Fetch the parser manifest from GitHub Releases.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_fetch_manifest(
    this: *const tree_sitter_language_pack::DownloadManager,
) -> *mut tree_sitter_language_pack::download::ParserManifest {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.fetch_manifest();
    match result {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Download the platform bundle and extract every library file it contains.
///
/// Unlike [`ensure_languages`], this does not check the manifest language list
/// against archive contents — it simply extracts all `.so`/`.dylib`/`.dll` files
/// from the bundle. Languages in the manifest that are missing from the archive
/// are silently ignored rather than returning an error.
///
/// Returns the number of library files extracted (including those already cached).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_download_all_best_effort(
    this: *const tree_sitter_language_pack::DownloadManager,
) -> usize {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.download_all_best_effort();
    match result {
        Ok(val) => val,
        Err(e) => {
            set_last_error(2, &e.to_string());
            0
        }
    }
}

/// Remove all cached parser libraries.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_clean_cache(
    this: *const tree_sitter_language_pack::DownloadManager,
) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return -1;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    let result = obj.clean_cache();
    match result {
        Ok(()) => 0,
        Err(e) => {
            set_last_error(2, &e.to_string());
            -1
        }
    }
}

/// Free a `Language` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_free(ptr: *mut tree_sitter_language_pack::Language) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Convert an integer to a `StructureKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_kind_from_i32(value: i32) -> i32 {
    match value {
        0 => 0,   // Function
        1 => 1,   // Method
        2 => 2,   // Class
        3 => 3,   // Struct
        4 => 4,   // Interface
        5 => 5,   // Enum
        6 => 6,   // Module
        7 => 7,   // Trait
        8 => 8,   // Impl
        9 => 9,   // Namespace
        10 => 10, // Other
        _ => {
            set_last_error(1, "Invalid StructureKind variant");
            -1
        }
    }
}

/// Convert a `StructureKind` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_kind_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Function" => 0,
        "Method" => 1,
        "Class" => 2,
        "Struct" => 3,
        "Interface" => 4,
        "Enum" => 5,
        "Module" => 6,
        "Trait" => 7,
        "Impl" => 8,
        "Namespace" => 9,
        "Other" => 10,
        _ => {
            set_last_error(1, "Unknown StructureKind variant");
            -1
        }
    }
}

/// Convert an integer to a `CommentKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_kind_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // Line
        1 => 1, // Block
        2 => 2, // Doc
        _ => {
            set_last_error(1, "Invalid CommentKind variant");
            -1
        }
    }
}

/// Convert a `CommentKind` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_kind_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Line" => 0,
        "Block" => 1,
        "Doc" => 2,
        _ => {
            set_last_error(1, "Unknown CommentKind variant");
            -1
        }
    }
}

/// Convert an integer to a `DocstringFormat` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_format_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // PythonTripleQuote
        1 => 1, // JSDoc
        2 => 2, // Rustdoc
        3 => 3, // GoDoc
        4 => 4, // JavaDoc
        5 => 5, // Other
        _ => {
            set_last_error(1, "Invalid DocstringFormat variant");
            -1
        }
    }
}

/// Convert a `DocstringFormat` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_format_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "PythonTripleQuote" => 0,
        "JSDoc" => 1,
        "Rustdoc" => 2,
        "GoDoc" => 3,
        "JavaDoc" => 4,
        "Other" => 5,
        _ => {
            set_last_error(1, "Unknown DocstringFormat variant");
            -1
        }
    }
}

/// Convert an integer to a `ExportKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_kind_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // Named
        1 => 1, // Default
        2 => 2, // ReExport
        _ => {
            set_last_error(1, "Invalid ExportKind variant");
            -1
        }
    }
}

/// Convert a `ExportKind` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_kind_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Named" => 0,
        "Default" => 1,
        "ReExport" => 2,
        _ => {
            set_last_error(1, "Unknown ExportKind variant");
            -1
        }
    }
}

/// Convert an integer to a `SymbolKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_kind_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // Variable
        1 => 1, // Constant
        2 => 2, // Function
        3 => 3, // Class
        4 => 4, // Type
        5 => 5, // Interface
        6 => 6, // Enum
        7 => 7, // Module
        8 => 8, // Other
        _ => {
            set_last_error(1, "Invalid SymbolKind variant");
            -1
        }
    }
}

/// Convert a `SymbolKind` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_kind_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Variable" => 0,
        "Constant" => 1,
        "Function" => 2,
        "Class" => 3,
        "Type" => 4,
        "Interface" => 5,
        "Enum" => 6,
        "Module" => 7,
        "Other" => 8,
        _ => {
            set_last_error(1, "Unknown SymbolKind variant");
            -1
        }
    }
}

/// Convert an integer to a `DiagnosticSeverity` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // Error
        1 => 1, // Warning
        2 => 2, // Info
        _ => {
            set_last_error(1, "Invalid DiagnosticSeverity variant");
            -1
        }
    }
}

/// Convert a `DiagnosticSeverity` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Error" => 0,
        "Warning" => 1,
        "Info" => 2,
        _ => {
            set_last_error(1, "Unknown DiagnosticSeverity variant");
            -1
        }
    }
}

/// Free a heap-allocated `StructureKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_kind_free(ptr: *mut tree_sitter_language_pack::StructureKind) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Serialize a heap-allocated `StructureKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_kind_to_json(
    ptr: *const tree_sitter_language_pack::StructureKind,
) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_structure_kind_to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Render a heap-allocated `StructureKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_kind_to_string(
    ptr: *const tree_sitter_language_pack::StructureKind,
) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_structure_kind_to_string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    let s: String = serde_json::to_value(val)
        .ok()
        .and_then(|v| v.as_str().map(str::to_owned))
        .unwrap_or_default();
    match CString::new(s) {
        Ok(cs) => cs.into_raw(),
        Err(_) => {
            set_last_error(1, "StructureKind variant contained interior NUL byte");
            std::ptr::null_mut()
        }
    }
}

/// Free a heap-allocated `CommentKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_kind_free(ptr: *mut tree_sitter_language_pack::CommentKind) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Serialize a heap-allocated `CommentKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_kind_to_json(
    ptr: *const tree_sitter_language_pack::CommentKind,
) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_comment_kind_to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Render a heap-allocated `CommentKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_kind_to_string(
    ptr: *const tree_sitter_language_pack::CommentKind,
) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_comment_kind_to_string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    let s: String = serde_json::to_value(val)
        .ok()
        .and_then(|v| v.as_str().map(str::to_owned))
        .unwrap_or_default();
    match CString::new(s) {
        Ok(cs) => cs.into_raw(),
        Err(_) => {
            set_last_error(1, "CommentKind variant contained interior NUL byte");
            std::ptr::null_mut()
        }
    }
}

/// Free a heap-allocated `DocstringFormat` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_format_free(ptr: *mut tree_sitter_language_pack::DocstringFormat) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Serialize a heap-allocated `DocstringFormat` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_format_to_json(
    ptr: *const tree_sitter_language_pack::DocstringFormat,
) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_docstring_format_to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Render a heap-allocated `DocstringFormat` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_format_to_string(
    ptr: *const tree_sitter_language_pack::DocstringFormat,
) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_docstring_format_to_string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    let s: String = serde_json::to_value(val)
        .ok()
        .and_then(|v| v.as_str().map(str::to_owned))
        .unwrap_or_default();
    match CString::new(s) {
        Ok(cs) => cs.into_raw(),
        Err(_) => {
            set_last_error(1, "DocstringFormat variant contained interior NUL byte");
            std::ptr::null_mut()
        }
    }
}

/// Free a heap-allocated `ExportKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_kind_free(ptr: *mut tree_sitter_language_pack::ExportKind) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Serialize a heap-allocated `ExportKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_kind_to_json(ptr: *const tree_sitter_language_pack::ExportKind) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_export_kind_to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Render a heap-allocated `ExportKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_kind_to_string(
    ptr: *const tree_sitter_language_pack::ExportKind,
) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_export_kind_to_string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    let s: String = serde_json::to_value(val)
        .ok()
        .and_then(|v| v.as_str().map(str::to_owned))
        .unwrap_or_default();
    match CString::new(s) {
        Ok(cs) => cs.into_raw(),
        Err(_) => {
            set_last_error(1, "ExportKind variant contained interior NUL byte");
            std::ptr::null_mut()
        }
    }
}

/// Free a heap-allocated `SymbolKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_kind_free(ptr: *mut tree_sitter_language_pack::SymbolKind) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Serialize a heap-allocated `SymbolKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_kind_to_json(ptr: *const tree_sitter_language_pack::SymbolKind) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_symbol_kind_to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Render a heap-allocated `SymbolKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_kind_to_string(
    ptr: *const tree_sitter_language_pack::SymbolKind,
) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_symbol_kind_to_string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    let s: String = serde_json::to_value(val)
        .ok()
        .and_then(|v| v.as_str().map(str::to_owned))
        .unwrap_or_default();
    match CString::new(s) {
        Ok(cs) => cs.into_raw(),
        Err(_) => {
            set_last_error(1, "SymbolKind variant contained interior NUL byte");
            std::ptr::null_mut()
        }
    }
}

/// Free a heap-allocated `DiagnosticSeverity` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity_free(ptr: *mut tree_sitter_language_pack::DiagnosticSeverity) {
    if !ptr.is_null() {
        // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Serialize a heap-allocated `DiagnosticSeverity` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity_to_json(
    ptr: *const tree_sitter_language_pack::DiagnosticSeverity,
) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_diagnostic_severity_to_json");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Render a heap-allocated `DiagnosticSeverity` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity_to_string(
    ptr: *const tree_sitter_language_pack::DiagnosticSeverity,
) -> *mut c_char {
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to ts_pack_diagnostic_severity_to_string");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
    let val = unsafe { &*ptr };
    let s: String = serde_json::to_value(val)
        .ok()
        .and_then(|v| v.as_str().map(str::to_owned))
        .unwrap_or_default();
    match CString::new(s) {
        Ok(cs) => cs.into_raw(),
        Err(_) => {
            set_last_error(1, "DiagnosticSeverity variant contained interior NUL byte");
            std::ptr::null_mut()
        }
    }
}

/// Detect language name from a file extension (without leading dot).
///
/// Returns `None` for unrecognized extensions. The match is case-insensitive.
///
/// ```
/// use tree_sitter_language_pack::detect_language_from_extension;
/// assert_eq!(detect_language_from_extension("py"), Some("python"));
/// assert_eq!(detect_language_from_extension("RS"), Some("rust"));
/// assert_eq!(detect_language_from_extension("xyz"), None);
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language_from_extension(ext: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    if ext.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'ext'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees ext is a valid pointer; string is valid UTF-8 from caller.
    let ext_rs = match unsafe { CStr::from_ptr(ext) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'ext'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::detect_language_from_extension(&ext_rs);
    match result {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Detect language name from a file path.
///
/// Extracts the file extension and looks it up. Returns `None` if the
/// path has no extension or the extension is not recognized.
///
/// ```
/// use tree_sitter_language_pack::detect_language_from_path;
/// assert_eq!(detect_language_from_path("src/main.rs"), Some("rust"));
/// assert_eq!(detect_language_from_path("README.md"), Some("markdown"));
/// assert_eq!(detect_language_from_path("Makefile"), None);
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language_from_path(path: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    if path.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'path'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees path is a valid pointer; string is valid UTF-8 from caller.
    let path_rs = match unsafe { CStr::from_ptr(path) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'path'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::detect_language_from_path(&path_rs);
    match result {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Detect language name from file content using the shebang line (`#!`).
///
/// Inspects only the first line of `content`. If it begins with `#!`, the
/// interpreter name is extracted and mapped to a language name.
///
/// Handles common patterns:
/// - `#!/usr/bin/env python3` → `"python"`
/// - `#!/bin/bash` → `"bash"`
/// - `#!/usr/bin/env node` → `"javascript"`
///
/// The `-S` flag accepted by some `env` implementations is skipped automatically.
/// Version suffixes (e.g. `python3.11`, `ruby3.2`) are stripped before matching.
///
/// Returns `None` when content does not start with `#!`, the shebang is
/// malformed, or the interpreter is not recognised.
///
/// ```
/// use tree_sitter_language_pack::detect_language_from_content;
/// assert_eq!(detect_language_from_content("#!/usr/bin/env python3\npass"), Some("python"));
/// assert_eq!(detect_language_from_content("#!/bin/bash\necho hi"), Some("bash"));
/// assert_eq!(detect_language_from_content("no shebang here"), None);
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language_from_content(
    content: *const std::ffi::c_char,
) -> *mut std::ffi::c_char {
    clear_last_error();
    if content.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'content'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees content is a valid pointer; string is valid UTF-8 from caller.
    let content_rs = match unsafe { CStr::from_ptr(content) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'content'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::detect_language_from_content(&content_rs);
    match result {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the highlights query for a language, if bundled.
///
/// Returns the contents of `highlights.scm` as a static string, or `None`
/// if no highlights query is bundled for this language.
///
/// # Example
///
/// ```
/// use tree_sitter_language_pack::get_highlights_query;
///
/// // Returns Some(...) for languages with bundled queries
/// let query = get_highlights_query("python");
/// // Returns None for languages without bundled highlights queries
/// let missing = get_highlights_query("nonexistent_lang");
/// assert!(missing.is_none());
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_highlights_query(language: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    if language.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'language'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees language is a valid pointer; string is valid UTF-8 from caller.
    let language_rs = match unsafe { CStr::from_ptr(language) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'language'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::get_highlights_query(&language_rs);
    match result {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the injections query for a language, if bundled.
///
/// Returns the contents of `injections.scm` as a static string, or `None`
/// if no injections query is bundled for this language.
///
/// # Example
///
/// ```
/// use tree_sitter_language_pack::get_injections_query;
///
/// let query = get_injections_query("markdown");
/// // Returns None for languages without bundled injections queries
/// let missing = get_injections_query("nonexistent_lang");
/// assert!(missing.is_none());
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_injections_query(language: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    if language.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'language'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees language is a valid pointer; string is valid UTF-8 from caller.
    let language_rs = match unsafe { CStr::from_ptr(language) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'language'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::get_injections_query(&language_rs);
    match result {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the locals query for a language, if bundled.
///
/// Returns the contents of `locals.scm` as a static string, or `None`
/// if no locals query is bundled for this language.
///
/// # Example
///
/// ```
/// use tree_sitter_language_pack::get_locals_query;
///
/// let query = get_locals_query("python");
/// // Returns None for languages without bundled locals queries
/// let missing = get_locals_query("nonexistent_lang");
/// assert!(missing.is_none());
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_locals_query(language: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    if language.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'language'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees language is a valid pointer; string is valid UTF-8 from caller.
    let language_rs = match unsafe { CStr::from_ptr(language) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'language'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::get_locals_query(&language_rs);
    match result {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get a tree-sitter [`Language`] by name using the global registry.
///
/// Resolves language aliases (e.g., `"shell"` maps to `"bash"`).
/// When the `download` feature is enabled (default), automatically downloads
/// the parser from GitHub releases if not found locally.
///
/// # Errors
///
/// Returns [`Error::LanguageNotFound`] if the language is not recognized,
/// or [`Error::Download`] if auto-download fails.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::{get_language, Parser};
///
/// let _lang = get_language("python")?;
/// let mut parser = Parser::new();
/// parser.set_language("python")?;
/// let tree = parser.parse("x = 1").expect("parse failed");
/// assert_eq!(tree.root_node().kind(), "module");
/// # Ok::<(), tree_sitter_language_pack::Error>(())
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_language(
    name: *const std::ffi::c_char,
) -> *mut tree_sitter_language_pack::Language {
    clear_last_error();
    if name.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'name'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'name'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::get_language(&name_rs);
    match result {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Get a [`Parser`] pre-configured for the given language.
///
/// This is a convenience function that calls [`get_language`] and configures
/// a new parser in one step.
///
/// # Errors
///
/// Returns [`Error::LanguageNotFound`] if the language is not recognized, or
/// [`Error::ParserSetup`] if the language cannot be applied to the parser.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::get_parser;
///
/// let mut parser = get_parser("rust")?;
/// let tree = parser.parse("fn main() {}").expect("parse failed");
/// assert!(!tree.root_node().has_error());
/// # Ok::<(), tree_sitter_language_pack::Error>(())
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_parser(name: *const std::ffi::c_char) -> *mut tree_sitter_language_pack::Parser {
    clear_last_error();
    if name.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'name'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'name'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::get_parser(&name_rs);
    match result {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Detect language name from a file path or extension.
///
/// This compatibility alias matches the pre-Alef Python binding API.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language(path: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    if path.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'path'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees path is a valid pointer; string is valid UTF-8 from caller.
    let path_rs = match unsafe { CStr::from_ptr(path) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'path'");
            return std::ptr::null_mut();
        }
    };
    let result = tree_sitter_language_pack::detect_language(&path_rs);
    match result {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// List all available language names (sorted, deduplicated, includes aliases).
///
/// Returns names of both statically compiled and dynamically loadable languages,
/// plus any configured aliases.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::available_languages;
///
/// let langs = available_languages();
/// for name in &langs {
///     println!("{}", name);
/// }
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_available_languages() -> *mut std::ffi::c_char {
    clear_last_error();
    let result = tree_sitter_language_pack::available_languages();
    match serde_json::to_string(&result) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Check if a language is available by name or alias.
///
/// Returns `true` if the language can be loaded (statically compiled,
/// dynamically available, or a known alias for one of these).
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::has_language;
///
/// assert!(has_language("python"));
/// assert!(has_language("shell")); // alias for "bash"
/// assert!(!has_language("nonexistent_language"));
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_has_language(name: *const std::ffi::c_char) -> i32 {
    clear_last_error();
    if name.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'name'");
        return 0;
    }
    // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
    let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'name'");
            return 0;
        }
    };
    let result = tree_sitter_language_pack::has_language(&name_rs);
    if result {
        1
    } else {
        0
    }
}

/// Return the number of available languages.
///
/// Includes statically compiled languages, dynamically loadable languages,
/// and aliases.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::language_count;
///
/// let count = language_count();
/// println!("{} languages available", count);
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_count() -> usize {
    clear_last_error();
    tree_sitter_language_pack::language_count()
}

/// Process source code and extract file intelligence using the global registry.
///
/// Parses the source with tree-sitter and extracts metrics, structure, imports,
/// exports, comments, docstrings, symbols, diagnostics, and/or chunks based on
/// the flags set in [`ProcessConfig`].
///
/// # Errors
///
/// Returns an error if the language is not found or parsing fails.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::{ProcessConfig, process};
///
/// let config = ProcessConfig::new("python").all();
/// let result = process("def hello(): pass", &config).unwrap();
/// println!("Language: {}", result.language);
/// println!("Lines: {}", result.metrics.total_lines);
/// println!("Structures: {}", result.structure.len());
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process(
    source: *const std::ffi::c_char,
    config: *const tree_sitter_language_pack::ProcessConfig,
) -> *mut tree_sitter_language_pack::ProcessResult {
    clear_last_error();
    if source.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'source'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees source is a valid pointer; string is valid UTF-8 from caller.
    let source_rs = match unsafe { CStr::from_ptr(source) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'source'");
            return std::ptr::null_mut();
        }
    };
    if config.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'config'");
        return std::ptr::null_mut();
    }
    // SAFETY: null check above guarantees config is a valid pointer.
    let config_rs = unsafe { &*config };
    let result = tree_sitter_language_pack::process(&source_rs, &config_rs);
    match result {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Initialize the language pack with the given configuration.
///
/// Applies any custom cache directory, then downloads all languages and groups
/// specified in the config. This is the recommended entry point when you want
/// to pre-warm the cache before use.
///
/// # Errors
///
/// Returns an error if configuration cannot be applied or if downloads fail.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::{PackConfig, init};
///
/// let config = PackConfig {
///     cache_dir: None,
///     languages: Some(vec!["python".to_string(), "rust".to_string()]),
///     groups: None,
/// };
/// init(&config).unwrap();
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_init(config: *const tree_sitter_language_pack::PackConfig) -> i32 {
    clear_last_error();
    if config.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'config'");
        return -1;
    }
    // SAFETY: null check above guarantees config is a valid pointer.
    let config_rs = unsafe { &*config };
    let result = tree_sitter_language_pack::init(&config_rs);
    match result {
        Ok(()) => 0,
        Err(e) => {
            set_last_error(2, &e.to_string());
            -1
        }
    }
}

/// Apply download configuration without downloading anything.
///
/// Use this to set a custom cache directory before the first call to
/// [`get_language`] or any download function. Changing the cache dir
/// after languages have been registered has no effect on already-loaded
/// languages.
///
/// # Errors
///
/// Returns an error if the lock cannot be acquired.
///
/// # Example
///
/// ```no_run
/// use std::path::PathBuf;
/// use tree_sitter_language_pack::{PackConfig, configure};
///
/// let config = PackConfig {
///     cache_dir: Some(PathBuf::from("/tmp/my-parsers")),
///     languages: None,
///     groups: None,
/// };
/// configure(&config).unwrap();
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_configure(config: *const tree_sitter_language_pack::PackConfig) -> i32 {
    clear_last_error();
    if config.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'config'");
        return -1;
    }
    // SAFETY: null check above guarantees config is a valid pointer.
    let config_rs = unsafe { &*config };
    let result = tree_sitter_language_pack::configure(&config_rs);
    match result {
        Ok(()) => 0,
        Err(e) => {
            set_last_error(2, &e.to_string());
            -1
        }
    }
}

/// Download specific languages to the local cache.
///
/// Returns the number of requested languages available after the call. Already
/// compiled or cached languages are included in the count.
///
/// # Errors
///
/// Returns an error if any language is not available in the manifest or if
/// the download fails.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::download;
///
/// let count = download(&["python", "rust", "typescript"]).unwrap();
/// println!("Ensured {} languages", count);
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download(names: *const std::ffi::c_char) -> usize {
    clear_last_error();
    if names.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'names'");
        return 0;
    }
    // SAFETY: null check above guarantees names is a valid pointer; string is valid UTF-8 from caller.
    let names_rs_str = match unsafe { CStr::from_ptr(names) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'names'");
            return 0;
        }
    };
    let names_rs = match serde_json::from_str::<Vec<String>>(names_rs_str) {
        Ok(v) => v,
        Err(e) => {
            set_last_error(2, &e.to_string());
            return 0;
        }
    };
    let names_rs_refs: Vec<&str> = names_rs.iter().map(|s| s.as_str()).collect();
    let result = tree_sitter_language_pack::download(&names_rs_refs);
    match result {
        Ok(val) => val,
        Err(e) => {
            set_last_error(2, &e.to_string());
            0
        }
    }
}

/// Download all available languages from the remote manifest.
///
/// Downloads the platform bundle and extracts every library it contains.
/// Languages that appear in the manifest but are absent from the bundle
/// (e.g. grammars that failed to compile at release time) are silently
/// skipped — they are not treated as an error.
///
/// Returns the total number of languages now available (statically compiled
/// plus downloaded and cached).
///
/// # Errors
///
/// Returns an error if the manifest cannot be fetched or the bundle download fails.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::download_all;
///
/// let count = download_all().unwrap();
/// println!("{} languages available", count);
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_all() -> usize {
    clear_last_error();
    let result = tree_sitter_language_pack::download_all();
    match result {
        Ok(val) => val,
        Err(e) => {
            set_last_error(2, &e.to_string());
            0
        }
    }
}

/// Return all language names available in the remote manifest (305).
///
/// Fetches (and caches) the remote manifest to discover the full list of
/// downloadable languages. Use [`downloaded_languages`] to list what is
/// already cached locally.
///
/// # Errors
///
/// Returns an error if the manifest cannot be fetched.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::manifest_languages;
///
/// let langs = manifest_languages().unwrap();
/// println!("{} languages available for download", langs.len());
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_manifest_languages() -> *mut std::ffi::c_char {
    clear_last_error();
    let result = tree_sitter_language_pack::manifest_languages();
    match result {
        Ok(val) => match serde_json::to_string(&val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(_) => std::ptr::null_mut(),
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Return languages that are already downloaded and cached locally.
///
/// Does not perform any network requests. Returns an empty list if the
/// cache directory does not exist or cannot be read.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::downloaded_languages;
///
/// let langs = downloaded_languages();
/// println!("{} languages already cached", langs.len());
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_downloaded_languages() -> *mut std::ffi::c_char {
    clear_last_error();
    let result = tree_sitter_language_pack::downloaded_languages();
    match serde_json::to_string(&result) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Delete all cached parser shared libraries.
///
/// Resets the cache registration so the next call to [`get_language`] or
/// a download function will re-register the (now empty) cache directory.
///
/// # Errors
///
/// Returns an error if the cache directory cannot be removed.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::clean_cache;
///
/// clean_cache().unwrap();
/// println!("Cache cleared");
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_clean_cache() -> i32 {
    clear_last_error();
    let result = tree_sitter_language_pack::clean_cache();
    match result {
        Ok(()) => 0,
        Err(e) => {
            set_last_error(2, &e.to_string());
            -1
        }
    }
}

/// Return the effective cache directory path.
///
/// This is either the custom path set via [`configure`] / [`init`] or the
/// default: `~/.cache/tree-sitter-language-pack/v{version}/libs/`.
///
/// # Errors
///
/// Returns an error if the system cache directory cannot be determined.
///
/// # Example
///
/// ```no_run
/// use tree_sitter_language_pack::cache_dir;
///
/// let dir = cache_dir().unwrap();
/// println!("Cache directory: {dir}");
/// ```
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_cache_dir() -> *mut std::ffi::c_char {
    clear_last_error();
    let result = tree_sitter_language_pack::cache_dir();
    match result {
        Ok(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}
