<?php

// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:96878690dc026e722c2d058bb69f75828a438dff7e51cf0f48415bda807d48ee
// To regenerate: alef generate
// To verify freshness: alef verify
// Type stubs for the native PHP extension — declares classes
// provided at runtime by the compiled Rust extension (.so/.dll).
// Include this in phpstan.neon scanFiles for static analysis.

declare(strict_types=1);

namespace Tree\Sitter\Language\Pack {
class TreeSitterLanguagePackException extends \RuntimeException
{
    public function getErrorCode(): int { throw new \RuntimeException('Not implemented.'); }
}

/**
 * A byte range — start (inclusive) to end (exclusive).
 */
final class ByteRange
{
    public function __construct(
        /** @var int Inclusive start byte offset. */
        public readonly int $start,
        /** @var int Exclusive end byte offset. */
        public readonly int $end
    ) { }
    public function getStart(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getEnd(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * Metadata for a single chunk of source code.
 */
final class ChunkContext
{
    public function __construct(
        /** @var string Language name used to parse this chunk. */
        public readonly string $language,
        /** @var int Zero-indexed position of this chunk within the file's chunk list. */
        public readonly int $chunkIndex,
        /** @var int Total number of chunks the file was split into. */
        public readonly int $totalChunks,
        /** @var array<string> Tree-sitter node kinds that appear at the top level of this chunk. */
        public readonly array $nodeTypes,
        /** @var array<string> Hierarchical path of enclosing structural items (e.g., `["MyClass", "my_method"]`). */
        public readonly array $contextPath,
        /** @var array<string> Names of symbols defined within this chunk. */
        public readonly array $symbolsDefined,
        array $comments,
        array $docstrings,
        /** @var bool Whether this chunk contains any tree-sitter error nodes. */
        public readonly bool $hasErrorNodes
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getLanguage(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getChunkIndex(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getTotalChunks(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<string> */
    public function getNodeTypes(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<string> */
    public function getContextPath(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<string> */
    public function getSymbolsDefined(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\CommentInfo> */
    public function getComments(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\DocstringInfo> */
    public function getDocstrings(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getHasErrorNodes(): bool
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * A chunk of source code with rich metadata.
 */
final class CodeChunk
{
    public function __construct(
        /** @var string The raw source text of this chunk. */
        public readonly string $content,
        /** @var int Inclusive start byte offset of this chunk in the original source. */
        public readonly int $startByte,
        /** @var int Exclusive end byte offset of this chunk in the original source. */
        public readonly int $endByte,
        /** @var int Zero-indexed start line of this chunk. */
        public readonly int $startLine,
        /**
         * Zero-indexed row of the last byte actually included in this chunk
         * (inclusive — the same row a `Span::end_line` would report for a node
         * ending on that byte). Computed the same way regardless of whether the
         * source has a trailing newline: it is always the row of `end_byte - 1`,
         * never a phantom row past the file's real content.
         *
         * @var int
         */
        public readonly int $endLine,
        ChunkContext $metadata
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getContent(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getStartByte(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getEndByte(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getStartLine(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getEndLine(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getMetadata(): ChunkContext
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * A comment extracted from source code.
 */
final class CommentInfo
{
    public function __construct(
        /** @var string The raw text content of the comment. */
        public readonly string $text,
        /** @var string The kind of comment (line, block, or doc). */
        public readonly string $kind,
        Span $span,
        /** @var ?string Name of the syntax node this comment is directly associated with. */
        public readonly ?string $associatedNode = null
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getText(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getKind(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSpan(): Span
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getAssociatedNode(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * An XML-style attribute attached to an `Element` node.
 *
 * Populated only for `DataNodeKind.Element`; always empty for `KeyValue` and
 * `Sequence` nodes.
 */
final class DataAttribute
{
    public function __construct(
        /** @var string Attribute name (e.g. `"class"`, `"href"`). */
        public readonly string $name,
        /** @var string Attribute value as a raw string (quotes stripped). */
        public readonly string $value,
        Span $span
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getName(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getValue(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSpan(): Span
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * A node in the hierarchical data tree produced by data-format extraction.
 *
 * When `ProcessConfig.data_extraction` is
 * `true`, `ProcessResult.data` is populated with a root `DataNode` whose
 * `children` mirror the structure of the parsed file.
 *
 * The `kind` field determines which other fields are meaningful:
 *
 * | `kind`     | `key`                    | `value`       | `attributes` | `children` |
 * |------------|--------------------------|---------------|--------------|------------|
 * | `KeyValue` | key / mapping key / index | leaf value   | empty        | nested map |
 * | `Element`  | XML tag name             | text content  | XML attrs    | child elements |
 * | `Sequence` | positional index (`"0"`) | leaf value   | empty        | sub-items  |
 *
 * # Example
 */
final class DataNode
{
    public function __construct(
        /** @var string Whether this node is a key/value pair, XML element, or sequence item. */
        public readonly string $kind,
        array $attributes,
        array $children,
        Span $span,
        /**
         * Key, attribute name, tag name, or positional index (`"0"`, `"1"`, …).
         * `None` at the document root.
         *
         * @var ?string
         */
        public readonly ?string $key = null,
        /**
         * Leaf scalar value, if any. `None` for containers (objects, arrays, XML elements
         * with child elements).
         *
         * @var ?string
         */
        public readonly ?string $value = null
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getKind(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getKey(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getValue(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\DataAttribute> */
    public function getAttributes(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\DataNode> */
    public function getChildren(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSpan(): Span
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * A diagnostic (syntax error, missing node, etc.) from parsing.
 */
final class Diagnostic
{
    public function __construct(
        /** @var string Human-readable description of the diagnostic. */
        public readonly string $message,
        /** @var string Severity of the diagnostic. */
        public readonly string $severity,
        Span $span
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getMessage(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSeverity(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSpan(): Span
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * A section within a docstring (e.g., Args, Returns, Raises).
 */
final class DocSection
{
    public function __construct(
        /** @var string Section kind (e.g., `"args"`, `"returns"`, `"raises"`). */
        public readonly string $kind,
        /** @var string Description text for this section. */
        public readonly string $description,
        /** @var ?string Parameter or return value name, if applicable. */
        public readonly ?string $name = null
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getKind(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getName(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getDescription(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * A docstring extracted from source code.
 */
final class DocstringInfo
{
    public function __construct(
        /** @var string The raw text of the docstring. */
        public readonly string $text,
        DocstringFormat $format,
        Span $span,
        array $parsedSections,
        /** @var ?string Name of the item this docstring documents. */
        public readonly ?string $associatedItem = null
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getText(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getFormat(): DocstringFormat
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSpan(): Span
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getAssociatedItem(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\DocSection> */
    public function getParsedSections(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * An export statement extracted from source code.
 */
final class ExportInfo
{
    public function __construct(
        /** @var string The exported name. */
        public readonly string $name,
        /** @var string The kind of export (named, default, or re-export). */
        public readonly string $kind,
        Span $span
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getName(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getKind(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSpan(): Span
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * Aggregate metrics for a source file.
 */
final class FileMetrics
{
    public function __construct(
        /** @var int Total number of lines (including blank and comment lines). */
        public readonly int $totalLines,
        /** @var int Number of lines containing non-blank, non-comment source code. */
        public readonly int $codeLines,
        /** @var int Number of lines that are entirely comments. */
        public readonly int $commentLines,
        /** @var int Number of blank (whitespace-only) lines. */
        public readonly int $blankLines,
        /** @var int Total byte length of the source file. */
        public readonly int $totalBytes,
        /** @var int Total number of nodes in the syntax tree. */
        public readonly int $nodeCount,
        /** @var int Number of error nodes in the syntax tree (parse errors). */
        public readonly int $errorCount,
        /** @var int Maximum nesting depth reached in the syntax tree. */
        public readonly int $maxDepth
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getTotalLines(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getCodeLines(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getCommentLines(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getBlankLines(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getTotalBytes(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getNodeCount(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getErrorCount(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getMaxDepth(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * An import statement extracted from source code.
 */
final class ImportInfo
{
    public function __construct(
        /** @var string The module or path being imported from. */
        public readonly string $source,
        /**
         * Specific names imported from the source module.
         *
         * For `import a, b` / `from m import a, b`, every entry's base name
         * (never the alias). For a JavaScript/TypeScript named-imports clause
         * (`import { a, b as c } from 'm'`), every specifier's original name.
         *
         * Populated for Python and JavaScript/TypeScript only. Always empty for
         * every other language this crate recognises import statements for —
         * Rust, Go, Java, Kotlin, and Elixir (`import`/`alias`/`require`/`use`)
         * — and for a JS/TS namespace import (`import * as ns`) or default
         * import (`import x from 'm'`), neither of which names individual items.
         *
         * @var array<string>
         */
        public readonly array $items,
        /**
         * Whether this is a wildcard import (e.g., `import *` or `use foo::*`).
         *
         * Detected from the syntax tree — a dedicated wildcard node
         * (`wildcard_import` in Python, `use_wildcard` in Rust) or a bare `*`
         * token outside of string-literal content — not by searching the
         * import's source text for a `*` character, so a glob in an import path
         * string (`import a from './glob*.js'`) is not mistaken for one.
         *
         * @var bool
         */
        public readonly bool $isWildcard,
        Span $span,
        /**
         * Alias assigned to the import (e.g., `import numpy as np`).
         *
         * Populated for Python's single-name form (`import numpy as np`,
         * `from m import a as b`) and JavaScript/TypeScript's namespace form
         * (`import * as ns from 'm'`) or a named-imports clause naming exactly
         * one specifier (`import { a as b } from 'm'`). `None` for Rust, Go,
         * Java, Kotlin, and Elixir (which has its own `as:` alias option on
         * `alias` directives, not yet extracted here) — and for a Python
         * statement that aliases several names at once (`import os, sys as s`),
         * where there is no single alias to report for the statement as a
         * whole, so only `items` is populated for it.
         *
         * @var ?string
         */
        public readonly ?string $alias = null
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSource(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<string> */
    public function getItems(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getAlias(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getIsWildcard(): bool
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSpan(): Span
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * Configuration for the tree-sitter language pack.
 *
 * Controls cache directory and which languages to pre-download.
 * Can be loaded from a TOML file, constructed programmatically,
 * or passed as a dict/object from language bindings.
 *
 * # Example
 */
final class PackConfig
{
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getCacheDir(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<string> */
    public function getLanguages(): ?array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<string> */
    public function getGroups(): ?array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * A source position — row + column, zero-indexed.
 */
final class Point
{
    public function __construct(
        /** @var int Zero-indexed row number. */
        public readonly int $row,
        /**
         * Zero-indexed column, counted in **bytes** from the start of the row.
         *
         * This is `tree_sitter::Point::column` verbatim, and tree-sitter defines it
         * as a byte offset — not characters, and not UTF-16 code units. Measured on
         * a row of the form `x = '<c>'` where `<c>` is a single 4-byte character
         * (an emoji), the string node reports columns `4..10`, identical to its
         * byte range; the character columns would be `4..7` and the UTF-16 columns
         * `4..8`.
         *
         * Anything that builds LSP positions (UTF-16) or editor caret columns
         * (characters) from this field is silently wrong on every row containing a
         * non-ASCII byte, and must re-measure the row against the source text
         * instead. Earlier releases documented this field as UTF-16 code units;
         * that was never what the value contained.
         *
         * @var int
         */
        public readonly int $column
    ) { }
    public function getRow(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getColumn(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * Configuration for the `process()` function.
 *
 * Controls which analysis features are enabled and whether chunking is performed.
 *
 * # Examples
 */
final class ProcessConfig
{
    public function __construct(
        /** @var string Language name (required). */
        public readonly string $language,
        /** @var bool Extract structural items (functions, classes, etc.). Default: true. */
        public readonly bool $structure,
        /** @var bool Extract import statements. Default: true. */
        public readonly bool $imports,
        /** @var bool Extract export statements. Default: true. */
        public readonly bool $exports,
        /** @var bool Extract comments. Default: false. */
        public readonly bool $comments,
        /** @var bool Extract docstrings. Default: false. */
        public readonly bool $docstrings,
        /** @var bool Extract symbol definitions. Default: false. */
        public readonly bool $symbols,
        /** @var bool Include parse diagnostics. Default: false. */
        public readonly bool $diagnostics,
        /**
         * Extract hierarchical key/value data tree from data-format files. Default: false.
         *
         * When `true`, [`ProcessResult::data`](crate::ProcessResult::data) is populated
         * with a [`DataNode`](crate::DataNode) tree for supported languages: JSON, YAML,
         * TOML, `.properties`, HCL/HOCON, INI, editorconfig, KDL, CUE, CSV, PSV, PO,
         * nginx config, Caddy config, XML, and DTD.
         *
         * For languages outside this set the field is left as `None`.
         *
         * # Example
         *
         * ```no_run
         * use tree_sitter_language_pack::{ProcessConfig, process};
         *
         * let config = ProcessConfig::new("json").with_data_extraction(true);
         * let result = process(r#"{"host": "localhost"}"#, &config).unwrap();
         * assert!(result.data.is_some());
         * ```
         *
         * @var bool
         */
        public readonly bool $dataExtraction,
        /**
         * Maximum chunk size in bytes. `None` disables chunking.
         *
         * `Some(0)` is rejected by [`ProcessConfig::validate`] with
         * [`Error::InvalidRange`](crate::Error::InvalidRange). A zero-sized chunk
         * limit previously produced an empty chunk list and silently discarded the
         * whole source; use `None` to mean "do not chunk".
         *
         * @var ?int
         */
        public readonly ?int $chunkMaxSize = null,
        /**
         * Reject source longer than this many bytes instead of parsing it.
         * Default: `None` (unbounded).
         *
         * Tree-sitter allocates and walks proportionally to input size, so an
         * unbounded parse of attacker-supplied input is a denial-of-service vector.
         * The default stays unbounded for backward compatibility; services handling
         * untrusted input should opt in, e.g. with
         * [`RECOMMENDED_MAX_SOURCE_BYTES`].
         *
         * Exceeding the limit fails the call with
         * [`Error::InvalidRange`](crate::Error::InvalidRange) — the source is never
         * silently truncated.
         *
         * @var ?int
         */
        public readonly ?int $maxSourceBytes = null,
        /**
         * Wall-clock budget for the parse step, in milliseconds.
         * Default: `None` (no timeout).
         *
         * Enforced through tree-sitter's parse progress callback, which the parser
         * invokes periodically; cancellation is therefore granular to that callback
         * interval rather than exact. A parse that exceeds the budget fails with
         * [`Error::ParseTimeout`](crate::Error::ParseTimeout).
         *
         * @var ?int
         */
        public readonly ?int $parseTimeoutMs = null
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getLanguage(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getStructure(): bool
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getImports(): bool
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getExports(): bool
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getComments(): bool
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getDocstrings(): bool
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSymbols(): bool
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getDiagnostics(): bool
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getChunkMaxSize(): ?int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getDataExtraction(): bool
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getMaxSourceBytes(): ?int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getParseTimeoutMs(): ?int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function default(): ProcessConfig
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function withChunking(int $max_size): ProcessConfig
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function all(): ProcessConfig
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function minimal(): ProcessConfig
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function withDataExtraction(bool $enabled): ProcessConfig
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function withMaxSourceBytes(?int $max_bytes = null): ProcessConfig
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function withParseTimeoutMs(?int $timeout_ms = null): ProcessConfig
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function validate(): void
    { }
}

/**
 * Complete analysis result from processing a source file.
 *
 * Contains metrics, structural analysis, imports/exports, comments,
 * docstrings, symbols, diagnostics, and optionally chunked code segments.
 * Fields are populated based on the `ProcessConfig` flags.
 *
 * # Fields
 *
 * - `language` - The language used for parsing
 * - `metrics` - Always computed: line counts, byte sizes, error counts
 * - `structure` - Functions, classes, structs (when `config.structure = true`)
 * - `imports` - Import statements (when `config.imports = true`)
 * - `exports` - Export statements (when `config.exports = true`)
 * - `comments` - Comments (when `config.comments = true`)
 * - `docstrings` - Docstrings (when `config.docstrings = true`)
 * - `symbols` - Symbol definitions (when `config.symbols = true`)
 * - `diagnostics` - Parse errors (when `config.diagnostics = true`)
 * - `chunks` - Chunked code segments (when `config.chunk_max_size` is set)
 */
final class ProcessResult
{
    public function __construct(
        /** @var string The language name used to parse the source file. */
        public readonly string $language,
        FileMetrics $metrics,
        array $structure,
        array $imports,
        array $exports,
        array $comments,
        array $docstrings,
        array $symbols,
        array $diagnostics,
        array $chunks,
        ?DataNode $data = null
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getLanguage(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getMetrics(): FileMetrics
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\StructureItem> */
    public function getStructure(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\ImportInfo> */
    public function getImports(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\ExportInfo> */
    public function getExports(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\CommentInfo> */
    public function getComments(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\DocstringInfo> */
    public function getDocstrings(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\SymbolInfo> */
    public function getSymbols(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\Diagnostic> */
    public function getDiagnostics(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\CodeChunk> */
    public function getChunks(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getData(): ?DataNode
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * Byte and line/column range in source code.
 *
 * Represents both byte offsets (for slicing) and human-readable line/column
 * positions (for display and diagnostics).
 */
final class Span
{
    public function __construct(
        /** @var int Inclusive start byte offset in the source. */
        public readonly int $startByte,
        /** @var int Exclusive end byte offset in the source. */
        public readonly int $endByte,
        /** @var int Zero-indexed line number of the span's start. */
        public readonly int $startLine,
        /**
         * Zero-indexed column of the span's start, counted in **bytes** from the
         * start of the line — not characters, not UTF-16 code units.
         *
         * @var int
         */
        public readonly int $startColumn,
        /** @var int Zero-indexed line number of the span's end. */
        public readonly int $endLine,
        /**
         * Zero-indexed column of the span's end, counted in **bytes** from the
         * start of the line — not characters, not UTF-16 code units.
         *
         * @var int
         */
        public readonly int $endColumn
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getStartByte(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getEndByte(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getStartLine(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getStartColumn(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getEndLine(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getEndColumn(): int
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * A structural item (function, class, struct, etc.) in source code.
 */
final class StructureItem
{
    public function __construct(
        StructureKind $kind,
        Span $span,
        array $children,
        /**
         * Decorator or attribute names applied to the item.
         *
         * **Reserved: not yet populated.** Always empty, for every language.
         * Recognising a decorator requires per-language grammar knowledge (a
         * Python `decorated_definition` wrapper, Java/C# annotations, Rust
         * attributes each have a different shape), which no extractor here
         * implements yet. The field is kept rather than removed so a consumer's
         * deserializer does not need updating once it is.
         *
         * @var array<string>
         */
        public readonly array $decorators,
        /** @var ?string The declared name of the item, if present. */
        public readonly ?string $name = null,
        /** @var ?string Visibility modifier (e.g., `"pub"`, `"public"`, `"private"`). */
        public readonly ?string $visibility = null,
        /**
         * Documentation comment attached to the item, if any.
         *
         * The text of the comment (or run of comments) immediately preceding
         * the item, with no blank line in between, when that comment is
         * classified as a doc comment. Multiple adjacent single-line doc
         * comments (Rust `///`) are joined with `\n` in source order.
         *
         * Populated for Rust (`///`/`//!`), Java (`/** */`), and JavaScript/
         * TypeScript (`/** */`). `None` for every other language, and for a
         * preceding comment that is not in doc-comment form (a plain `#`
         * comment in Python, Ruby, or Elixir) — Python's docstring convention is
         * captured separately, as [`DocstringInfo`], not through this field.
         *
         * @var ?string
         */
        public readonly ?string $docComment = null,
        /**
         * Full signature text of the item (e.g., function parameters and return type).
         *
         * The item's own source text from its start up to the start of its body
         * (see [`StructureItem::body_span`]), trimmed of trailing whitespace —
         * for example `fn add(a: i32, b: i32) -> i32` for a Rust function whose
         * body is `{ a + b }`. `None` only when that text is empty, which does
         * not happen for any item [`StructureKind`] currently reports. Populated
         * for every language and kind this crate extracts structure for, since
         * it is derived from `body_span`'s boundary rather than per-language
         * syntax.
         *
         * @var ?string
         */
        public readonly ?string $signature = null,
        ?Span $bodySpan = null
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getKind(): StructureKind
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getName(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getVisibility(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSpan(): Span
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<\Tree\Sitter\Language\Pack\StructureItem> */
    public function getChildren(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    /** @return array<string> */
    public function getDecorators(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getDocComment(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSignature(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getBodySpan(): ?Span
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * A symbol (variable, function, type, etc.) extracted from source code.
 */
final class SymbolInfo
{
    public function __construct(
        /** @var string The name of the symbol. */
        public readonly string $name,
        SymbolKind $kind,
        Span $span,
        /** @var ?string Explicit type annotation, if present in the source. */
        public readonly ?string $typeAnnotation = null,
        /**
         * Documentation comment immediately preceding this symbol, resolved by
         * the same walk [`StructureItem::doc_comment`] uses (see
         * `doc_comment_at`) — never hard-coded `None`.
         *
         * Populated for Rust (`///`/`//!`), Java (`/** */`), and JavaScript/
         * TypeScript (`/** */`) — the languages whose comment classification
         * recognizes a doc-kind comment. `None` for every other language (e.g.
         * Python, Go, Ruby, which have no doc-kind comment classification), and
         * also `None` when a symbol in a supported language simply has no doc
         * comment immediately above it.
         *
         * @var ?string
         */
        public readonly ?string $doc = null
    ) { }
    /**
     * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getName(): string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getKind(): SymbolKind
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getSpan(): Span
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getTypeAnnotation(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function getDoc(): ?string
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

final class CommentKind
{
    public const LINE = 'Line';
    public const BLOCK = 'Block';
    public const DOC = 'Doc';
}

final class DataNodeKind
{
    public const KEYVALUE = 'KeyValue';
    public const ELEMENT = 'Element';
    public const SEQUENCE = 'Sequence';
}

final class DiagnosticSeverity
{
    public const ERROR = 'Error';
    public const WARNING = 'Warning';
    public const INFO = 'Info';
}

/**
 * The format of a docstring extracted from source code.
 *
 * Identifies the docstring convention used, which varies by language
 * (e.g., Python triple-quoted strings, JSDoc, Rustdoc `///` comments).
 *
 * # Wire format (public JSON contract)
 *
 * Unit variants serialize as a bare string (`"JSDoc"`); the `Other`
 * variant serializes as a single-keyed object (`{"Other": "rst"}`). DO
 * NOT add `#[serde(tag = "...")]`. Covered by `tests/wire_format.rs`.
 */
final class DocstringFormat
{
    /** @var string The variant discriminator carried by the `type` JSON field. */
    public readonly string $type_tag;
    /** @var ?string */
    public readonly ?string $other;
    /**
     * Construct from a JSON string — the flat class's only whole-value constructor. The payload's tag field selects the variant; an unrecognised tag throws.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function pythonTripleQuote(): DocstringFormat { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function jsDoc(): DocstringFormat { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function rustdoc(): DocstringFormat { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function goDoc(): DocstringFormat { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function javaDoc(): DocstringFormat { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function other(string $other): DocstringFormat { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

final class ExportKind
{
    public const NAMED = 'Named';
    public const DEFAULT_ = 'Default';
    public const REEXPORT = 'ReExport';
}

/**
 * The kind of structural item found in source code.
 *
 * Categorizes top-level and nested declarations such as functions, classes,
 * structs, enums, traits, and more. Use `Other` for
 * language-specific constructs that do not fit a standard category.
 *
 * # Wire format (public JSON contract)
 *
 * Unit variants serialize as a bare string (`"Function"`); the `Other`
 * variant serializes as a single-keyed object (`{"Other": "macro"}`). DO
 * NOT add `#[serde(tag = "...")]` or rename variants — every language
 * binding has a hand-written deserializer matching this exact shape, and
 * any change breaks all bindings' `process()` tests simultaneously.
 * Covered by `tests/wire_format.rs`.
 */
final class StructureKind
{
    /** @var string The variant discriminator carried by the `type` JSON field. */
    public readonly string $type_tag;
    /** @var ?string */
    public readonly ?string $other;
    /**
     * Construct from a JSON string — the flat class's only whole-value constructor. The payload's tag field selects the variant; an unrecognised tag throws.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function function(): StructureKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function method(): StructureKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function class(): StructureKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function struct(): StructureKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function interface(): StructureKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function enum(): StructureKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function module(): StructureKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function trait(): StructureKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function impl(): StructureKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function namespace(): StructureKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function other(string $other): StructureKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * The kind of a symbol definition found in source code.
 *
 * Categorizes symbol definitions such as variables, constants, functions,
 * classes, types, interfaces, enums, and modules.
 *
 * # Wire format (public JSON contract)
 *
 * Unit variants serialize as a bare string (`"Function"`); the `Other`
 * variant serializes as a single-keyed object (`{"Other": "macro"}`). DO
 * NOT add `#[serde(tag = "...")]`. Covered by `tests/wire_format.rs`.
 */
final class SymbolKind
{
    /** @var string The variant discriminator carried by the `type` JSON field. */
    public readonly string $type_tag;
    /** @var ?string */
    public readonly ?string $other;
    /**
     * Construct from a JSON string — the flat class's only whole-value constructor. The payload's tag field selects the variant; an unrecognised tag throws.
     */
    public static function from_json(string $json): self
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function variable(): SymbolKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function constant(): SymbolKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function function(): SymbolKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function class(): SymbolKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function type(): SymbolKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function interface(): SymbolKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function enum(): SymbolKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function module(): SymbolKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function other(string $other): SymbolKind { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

class TreeSitterLanguagePackApi
{
    /**
     * @return array<string>
     */
    public static function availableLanguages(): array { throw new \RuntimeException('Not implemented.'); }
    public static function cacheDir(): string { throw new \RuntimeException('Not implemented.'); }
    public static function cleanCache(): void { }
    public static function configure(\Tree\Sitter\Language\Pack\PackConfig $config): void { }
    public static function detectLanguage(string $path): ?string { throw new \RuntimeException('Not implemented.'); }
    public static function detectLanguageFromContent(string $content): ?string { throw new \RuntimeException('Not implemented.'); }
    public static function detectLanguageFromExtension(string $ext): ?string { throw new \RuntimeException('Not implemented.'); }
    public static function detectLanguageFromPath(string $path): ?string { throw new \RuntimeException('Not implemented.'); }
    /**
     * @param array<string> $names
     * @return int
     */
    public static function download(array $names): int { throw new \RuntimeException('Not implemented.'); }
    public static function downloadAll(): int { throw new \RuntimeException('Not implemented.'); }
    public static function downloadGroup(string $name): int { throw new \RuntimeException('Not implemented.'); }
    /**
     * @return array<string>
     */
    public static function downloadedLanguages(): array { throw new \RuntimeException('Not implemented.'); }
    public static function getFoldsQuery(string $language): ?string { throw new \RuntimeException('Not implemented.'); }
    public static function getHighlightsQuery(string $language): ?string { throw new \RuntimeException('Not implemented.'); }
    public static function getIndentsQuery(string $language): ?string { throw new \RuntimeException('Not implemented.'); }
    public static function getInjectionsQuery(string $language): ?string { throw new \RuntimeException('Not implemented.'); }
    public static function getLanguage(string $name): \Tree\Sitter\Language\Pack\Language { throw new \RuntimeException('Not implemented.'); }
    public static function getLocalsQuery(string $language): ?string { throw new \RuntimeException('Not implemented.'); }
    public static function getParser(string $name): \Tree\Sitter\Language\Pack\Parser { throw new \RuntimeException('Not implemented.'); }
    public static function getTagsQuery(string $language): ?string { throw new \RuntimeException('Not implemented.'); }
    public static function hasLanguage(string $name): bool { throw new \RuntimeException('Not implemented.'); }
    public static function init(\Tree\Sitter\Language\Pack\PackConfig $config): void { }
    public static function languageCount(): int { throw new \RuntimeException('Not implemented.'); }
    /**
     * @return array<string>
     */
    public static function manifestGroups(): array { throw new \RuntimeException('Not implemented.'); }
    /**
     * @return array<string>
     */
    public static function manifestLanguages(): array { throw new \RuntimeException('Not implemented.'); }
    /**
     * @param array<string> $languages
     * @return void
     */
    public static function prefetch(array $languages): void { }
    public static function process(string $source, \Tree\Sitter\Language\Pack\ProcessConfig $config): \Tree\Sitter\Language\Pack\ProcessResult { throw new \RuntimeException('Not implemented.'); }
}

} // end namespace
