# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:a5d3540db798e7a255dc3aa87653c4cce759b100d30b45474f29ba15d6c8598d
# To regenerate: alef generate
# To verify freshness: alef verify

[package]
name = "ts-pack-core-node"
version = "1.18.0"
edition = "2024"
license = "MIT"
description = "Pre-compiled tree-sitter grammars for 371 programming languages"
readme = false
keywords = ["language-pack", "parser", "syntax", "tree-sitter"]
categories = []

# `serde_json` is emitted unconditionally above so the manifest is stable
# across regens, but for umbrella crates with no JSON-marshalled return types
# it is genuinely unused. The conditional `async-trait` / `futures-util` deps
# are similarly flagged when the umbrella has trait-bridge / streaming
# adapters configured but no actual async-trait callsite in this binding.
[package.metadata.cargo-machete]
ignored = ["serde_json"]

[lib]
crate-type = ["cdylib"]

[features]
default = ["download"]
download = ["tree-sitter-language-pack/download"]

[dependencies]
napi = { version = "3", features = ["async"] }
napi-derive = "3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tree-sitter-language-pack = { version = "1.18.0", path = "../ts-pack-core", features = ["serde", "config", "download"] }

[build-dependencies]
napi-build = "2"

# This crate deliberately does not use `[lints]` / `workspace = true`: its C-ABI /
# PyO3 / napi / ext-php-rs / NIF boundary requires `unsafe` code, and the workspace's
# `[workspace.lints.rust]` sets `unsafe_code = "deny"` -- an all-or-nothing table that
# would turn every such boundary into a compile error. The `[lints.clippy]` block below
# instead carries the subset of the workspace's deny-by-default lint policy this crate
# can actually satisfy. ~keep
[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
