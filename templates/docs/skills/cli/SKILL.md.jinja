# {{ krate.name }} CLI Reference

Use this skill when invoking, documenting, or changing the {{ krate.name }} command-line interface.

{% for command in cli.commands %}
- `{{ command.path }}`{{ ": " ~ command.about if command.about else "" }}
{% for subcommand in command.subcommands %}
- `{{ subcommand.path }}`{{ ": " ~ subcommand.about if subcommand.about else "" }}
{% endfor %}
{% else %}
No CLI commands were discovered from the configured source paths.
{% endfor %}
