# ts-pack-cli

CLI proxy for [`ts-pack`](https://github.com/kreuzberg-dev/tree-sitter-language-pack). Installing
this package provides a `ts-pack` command that downloads the matching native
binary from GitHub releases and runs it.

Pin a specific release tag with the `TS_PACK_CLI_VERSION` environment variable.
