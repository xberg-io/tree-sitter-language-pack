name: Validate Issues

on:
    issues:
        types: [opened, edited]

concurrency:
    group: validate-issues-${{ github.event.issue.number }}
    cancel-in-progress: true

jobs:
    validate:
        uses: xberg-io/actions/.github/workflows/reusable-validate-issues.yml@v1
        secrets: inherit
